import { a as __toCommonJS$3, i as __require, n as __esm, o as __toDynamicImportESM, r as __export$3, s as __toESM, t as __commonJS } from "./chunk-btbCw1m3.mjs";
import { i as init_strip_ansi, o as strip_ansi_exports, t as stringWidth } from "./string-width-D78SVDLD.mjs";
import { a as _classPrivateFieldInitSpec, i as _assertClassBrand, n as _classPrivateFieldGet2, o as _classPrivateMethodInitSpec, r as _classPrivateFieldSet2, t as cli_cursor_default } from "./cli-cursor-Dab4mDU2.mjs";
import { n as require_auth_errors, r as require_token_error, t as require_token_util } from "./token-util-DACSFqcB.mjs";
import process$1 from "node:process";
import os, { tmpdir } from "node:os";
import tty, { isatty } from "node:tty";
import createDebugger from "debug";
import { promisify, stripVTControlCharacters } from "node:util";
import { Buffer as Buffer$1 } from "node:buffer";
import Path from "node:path";
import { fileURLToPath } from "node:url";
import childProcess, { execFile } from "node:child_process";
import fs, { constants, writeFile } from "node:fs/promises";
import fs$1 from "node:fs";
import * as Auth from "@vercel/sandbox/dist/auth/index.js";
import { NotOk, OAuth, getAuth, inferScope, pollForToken, updateAuthConfig } from "@vercel/sandbox/dist/auth/index.js";
import { EOL } from "os";
import { z } from "zod/v4";
import { z as z$1 } from "zod";
import retry from "async-retry";
import { APIError, Sandbox, Snapshot, StreamError } from "@vercel/sandbox";
import { WebSocket } from "ws";
import { setTimeout as setTimeout$1 } from "node:timers/promises";
import * as tty$1 from "tty";
import { format } from "util";
import { StringDecoder } from "string_decoder";
import { Writable } from "stream";
import { randomUUID } from "crypto";
import consume from "node:stream/consumers";

//#region ../../node_modules/.pnpm/chalk@5.6.0/node_modules/chalk/source/vendor/ansi-styles/index.js
function assembleStyles() {
	const codes = /* @__PURE__ */ new Map();
	for (const [groupName, group] of Object.entries(styles$1)) {
		for (const [styleName, style] of Object.entries(group)) {
			styles$1[styleName] = {
				open: `\u001B[${style[0]}m`,
				close: `\u001B[${style[1]}m`
			};
			group[styleName] = styles$1[styleName];
			codes.set(style[0], style[1]);
		}
		Object.defineProperty(styles$1, groupName, {
			value: group,
			enumerable: false
		});
	}
	Object.defineProperty(styles$1, "codes", {
		value: codes,
		enumerable: false
	});
	styles$1.color.close = "\x1B[39m";
	styles$1.bgColor.close = "\x1B[49m";
	styles$1.color.ansi = wrapAnsi16();
	styles$1.color.ansi256 = wrapAnsi256();
	styles$1.color.ansi16m = wrapAnsi16m();
	styles$1.bgColor.ansi = wrapAnsi16(ANSI_BACKGROUND_OFFSET);
	styles$1.bgColor.ansi256 = wrapAnsi256(ANSI_BACKGROUND_OFFSET);
	styles$1.bgColor.ansi16m = wrapAnsi16m(ANSI_BACKGROUND_OFFSET);
	Object.defineProperties(styles$1, {
		rgbToAnsi256: {
			value(red$1, green$1, blue$1) {
				if (red$1 === green$1 && green$1 === blue$1) {
					if (red$1 < 8) return 16;
					if (red$1 > 248) return 231;
					return Math.round((red$1 - 8) / 247 * 24) + 232;
				}
				return 16 + 36 * Math.round(red$1 / 255 * 5) + 6 * Math.round(green$1 / 255 * 5) + Math.round(blue$1 / 255 * 5);
			},
			enumerable: false
		},
		hexToRgb: {
			value(hex) {
				const matches = /[a-f\d]{6}|[a-f\d]{3}/i.exec(hex.toString(16));
				if (!matches) return [
					0,
					0,
					0
				];
				let [colorString] = matches;
				if (colorString.length === 3) colorString = [...colorString].map((character) => character + character).join("");
				const integer = Number.parseInt(colorString, 16);
				return [
					integer >> 16 & 255,
					integer >> 8 & 255,
					integer & 255
				];
			},
			enumerable: false
		},
		hexToAnsi256: {
			value: (hex) => styles$1.rgbToAnsi256(...styles$1.hexToRgb(hex)),
			enumerable: false
		},
		ansi256ToAnsi: {
			value(code) {
				if (code < 8) return 30 + code;
				if (code < 16) return 90 + (code - 8);
				let red$1;
				let green$1;
				let blue$1;
				if (code >= 232) {
					red$1 = ((code - 232) * 10 + 8) / 255;
					green$1 = red$1;
					blue$1 = red$1;
				} else {
					code -= 16;
					const remainder = code % 36;
					red$1 = Math.floor(code / 36) / 5;
					green$1 = Math.floor(remainder / 6) / 5;
					blue$1 = remainder % 6 / 5;
				}
				const value = Math.max(red$1, green$1, blue$1) * 2;
				if (value === 0) return 30;
				let result = 30 + (Math.round(blue$1) << 2 | Math.round(green$1) << 1 | Math.round(red$1));
				if (value === 2) result += 60;
				return result;
			},
			enumerable: false
		},
		rgbToAnsi: {
			value: (red$1, green$1, blue$1) => styles$1.ansi256ToAnsi(styles$1.rgbToAnsi256(red$1, green$1, blue$1)),
			enumerable: false
		},
		hexToAnsi: {
			value: (hex) => styles$1.ansi256ToAnsi(styles$1.hexToAnsi256(hex)),
			enumerable: false
		}
	});
	return styles$1;
}
var ANSI_BACKGROUND_OFFSET, wrapAnsi16, wrapAnsi256, wrapAnsi16m, styles$1, modifierNames, foregroundColorNames, backgroundColorNames, colorNames, ansiStyles, ansi_styles_default;
var init_ansi_styles = __esm({ "../../node_modules/.pnpm/chalk@5.6.0/node_modules/chalk/source/vendor/ansi-styles/index.js": (() => {
	ANSI_BACKGROUND_OFFSET = 10;
	wrapAnsi16 = (offset = 0) => (code) => `\u001B[${code + offset}m`;
	wrapAnsi256 = (offset = 0) => (code) => `\u001B[${38 + offset};5;${code}m`;
	wrapAnsi16m = (offset = 0) => (red$1, green$1, blue$1) => `\u001B[${38 + offset};2;${red$1};${green$1};${blue$1}m`;
	styles$1 = {
		modifier: {
			reset: [0, 0],
			bold: [1, 22],
			dim: [2, 22],
			italic: [3, 23],
			underline: [4, 24],
			overline: [53, 55],
			inverse: [7, 27],
			hidden: [8, 28],
			strikethrough: [9, 29]
		},
		color: {
			black: [30, 39],
			red: [31, 39],
			green: [32, 39],
			yellow: [33, 39],
			blue: [34, 39],
			magenta: [35, 39],
			cyan: [36, 39],
			white: [37, 39],
			blackBright: [90, 39],
			gray: [90, 39],
			grey: [90, 39],
			redBright: [91, 39],
			greenBright: [92, 39],
			yellowBright: [93, 39],
			blueBright: [94, 39],
			magentaBright: [95, 39],
			cyanBright: [96, 39],
			whiteBright: [97, 39]
		},
		bgColor: {
			bgBlack: [40, 49],
			bgRed: [41, 49],
			bgGreen: [42, 49],
			bgYellow: [43, 49],
			bgBlue: [44, 49],
			bgMagenta: [45, 49],
			bgCyan: [46, 49],
			bgWhite: [47, 49],
			bgBlackBright: [100, 49],
			bgGray: [100, 49],
			bgGrey: [100, 49],
			bgRedBright: [101, 49],
			bgGreenBright: [102, 49],
			bgYellowBright: [103, 49],
			bgBlueBright: [104, 49],
			bgMagentaBright: [105, 49],
			bgCyanBright: [106, 49],
			bgWhiteBright: [107, 49]
		}
	};
	modifierNames = Object.keys(styles$1.modifier);
	foregroundColorNames = Object.keys(styles$1.color);
	backgroundColorNames = Object.keys(styles$1.bgColor);
	colorNames = [...foregroundColorNames, ...backgroundColorNames];
	ansiStyles = assembleStyles();
	ansi_styles_default = ansiStyles;
}) });

//#endregion
//#region ../../node_modules/.pnpm/chalk@5.6.0/node_modules/chalk/source/vendor/supports-color/index.js
function hasFlag(flag$1, argv$1 = globalThis.Deno ? globalThis.Deno.args : process$1.argv) {
	const prefix$1 = flag$1.startsWith("-") ? "" : flag$1.length === 1 ? "-" : "--";
	const position = argv$1.indexOf(prefix$1 + flag$1);
	const terminatorPosition = argv$1.indexOf("--");
	return position !== -1 && (terminatorPosition === -1 || position < terminatorPosition);
}
function envForceColor() {
	if ("FORCE_COLOR" in env$1) {
		if (env$1.FORCE_COLOR === "true") return 1;
		if (env$1.FORCE_COLOR === "false") return 0;
		return env$1.FORCE_COLOR.length === 0 ? 1 : Math.min(Number.parseInt(env$1.FORCE_COLOR, 10), 3);
	}
}
function translateLevel(level) {
	if (level === 0) return false;
	return {
		level,
		hasBasic: true,
		has256: level >= 2,
		has16m: level >= 3
	};
}
function _supportsColor(haveStream, { streamIsTTY, sniffFlags = true } = {}) {
	const noFlagForceColor = envForceColor();
	if (noFlagForceColor !== void 0) flagForceColor = noFlagForceColor;
	const forceColor = sniffFlags ? flagForceColor : noFlagForceColor;
	if (forceColor === 0) return 0;
	if (sniffFlags) {
		if (hasFlag("color=16m") || hasFlag("color=full") || hasFlag("color=truecolor")) return 3;
		if (hasFlag("color=256")) return 2;
	}
	if ("TF_BUILD" in env$1 && "AGENT_NAME" in env$1) return 1;
	if (haveStream && !streamIsTTY && forceColor === void 0) return 0;
	const min = forceColor || 0;
	if (env$1.TERM === "dumb") return min;
	if (process$1.platform === "win32") {
		const osRelease = os.release().split(".");
		if (Number(osRelease[0]) >= 10 && Number(osRelease[2]) >= 10586) return Number(osRelease[2]) >= 14931 ? 3 : 2;
		return 1;
	}
	if ("CI" in env$1) {
		if ([
			"GITHUB_ACTIONS",
			"GITEA_ACTIONS",
			"CIRCLECI"
		].some((key) => key in env$1)) return 3;
		if ([
			"TRAVIS",
			"APPVEYOR",
			"GITLAB_CI",
			"BUILDKITE",
			"DRONE"
		].some((sign) => sign in env$1) || env$1.CI_NAME === "codeship") return 1;
		return min;
	}
	if ("TEAMCITY_VERSION" in env$1) return /^(9\.(0*[1-9]\d*)\.|\d{2,}\.)/.test(env$1.TEAMCITY_VERSION) ? 1 : 0;
	if (env$1.COLORTERM === "truecolor") return 3;
	if (env$1.TERM === "xterm-kitty") return 3;
	if (env$1.TERM === "xterm-ghostty") return 3;
	if (env$1.TERM === "wezterm") return 3;
	if ("TERM_PROGRAM" in env$1) {
		const version$1 = Number.parseInt((env$1.TERM_PROGRAM_VERSION || "").split(".")[0], 10);
		switch (env$1.TERM_PROGRAM) {
			case "iTerm.app": return version$1 >= 3 ? 3 : 2;
			case "Apple_Terminal": return 2;
		}
	}
	if (/-256(color)?$/i.test(env$1.TERM)) return 2;
	if (/^screen|^xterm|^vt100|^vt220|^rxvt|color|ansi|cygwin|linux/i.test(env$1.TERM)) return 1;
	if ("COLORTERM" in env$1) return 1;
	return min;
}
function createSupportsColor(stream, options = {}) {
	return translateLevel(_supportsColor(stream, {
		streamIsTTY: stream && stream.isTTY,
		...options
	}));
}
var env$1, flagForceColor, supportsColor, supports_color_default;
var init_supports_color = __esm({ "../../node_modules/.pnpm/chalk@5.6.0/node_modules/chalk/source/vendor/supports-color/index.js": (() => {
	({env: env$1} = process$1);
	;
	if (hasFlag("no-color") || hasFlag("no-colors") || hasFlag("color=false") || hasFlag("color=never")) flagForceColor = 0;
	else if (hasFlag("color") || hasFlag("colors") || hasFlag("color=true") || hasFlag("color=always")) flagForceColor = 1;
	supportsColor = {
		stdout: createSupportsColor({ isTTY: tty.isatty(1) }),
		stderr: createSupportsColor({ isTTY: tty.isatty(2) })
	};
	supports_color_default = supportsColor;
}) });

//#endregion
//#region ../../node_modules/.pnpm/chalk@5.6.0/node_modules/chalk/source/utilities.js
function stringReplaceAll(string$2, substring, replacer) {
	let index = string$2.indexOf(substring);
	if (index === -1) return string$2;
	const substringLength = substring.length;
	let endIndex = 0;
	let returnValue = "";
	do {
		returnValue += string$2.slice(endIndex, index) + substring + replacer;
		endIndex = index + substringLength;
		index = string$2.indexOf(substring, endIndex);
	} while (index !== -1);
	returnValue += string$2.slice(endIndex);
	return returnValue;
}
function stringEncaseCRLFWithFirstIndex(string$2, prefix$1, postfix, index) {
	let endIndex = 0;
	let returnValue = "";
	do {
		const gotCR = string$2[index - 1] === "\r";
		returnValue += string$2.slice(endIndex, gotCR ? index - 1 : index) + prefix$1 + (gotCR ? "\r\n" : "\n") + postfix;
		endIndex = index + 1;
		index = string$2.indexOf("\n", endIndex);
	} while (index !== -1);
	returnValue += string$2.slice(endIndex);
	return returnValue;
}
var init_utilities = __esm({ "../../node_modules/.pnpm/chalk@5.6.0/node_modules/chalk/source/utilities.js": (() => {}) });

//#endregion
//#region ../../node_modules/.pnpm/chalk@5.6.0/node_modules/chalk/source/index.js
var source_exports = /* @__PURE__ */ __export$3({
	Chalk: () => Chalk,
	backgroundColorNames: () => backgroundColorNames,
	backgroundColors: () => backgroundColorNames,
	chalkStderr: () => chalkStderr,
	colorNames: () => colorNames,
	colors: () => colorNames,
	default: () => source_default,
	foregroundColorNames: () => foregroundColorNames,
	foregroundColors: () => foregroundColorNames,
	modifierNames: () => modifierNames,
	modifiers: () => modifierNames,
	supportsColor: () => stdoutColor,
	supportsColorStderr: () => stderrColor
});
function createChalk(options) {
	return chalkFactory(options);
}
var stdoutColor, stderrColor, GENERATOR, STYLER, IS_EMPTY, levelMapping, styles, applyOptions, Chalk, chalkFactory, getModelAnsi, proto, createStyler, createBuilder, applyStyle, chalk, chalkStderr, source_default;
var init_source = __esm({ "../../node_modules/.pnpm/chalk@5.6.0/node_modules/chalk/source/index.js": (() => {
	init_ansi_styles();
	init_supports_color();
	init_utilities();
	({stdout: stdoutColor, stderr: stderrColor} = supports_color_default);
	GENERATOR = Symbol("GENERATOR");
	STYLER = Symbol("STYLER");
	IS_EMPTY = Symbol("IS_EMPTY");
	levelMapping = [
		"ansi",
		"ansi",
		"ansi256",
		"ansi16m"
	];
	styles = Object.create(null);
	applyOptions = (object, options = {}) => {
		if (options.level && !(Number.isInteger(options.level) && options.level >= 0 && options.level <= 3)) throw new Error("The `level` option should be an integer from 0 to 3");
		const colorLevel = stdoutColor ? stdoutColor.level : 0;
		object.level = options.level === void 0 ? colorLevel : options.level;
	};
	Chalk = class {
		constructor(options) {
			return chalkFactory(options);
		}
	};
	chalkFactory = (options) => {
		const chalk$1 = (...strings) => strings.join(" ");
		applyOptions(chalk$1, options);
		Object.setPrototypeOf(chalk$1, createChalk.prototype);
		return chalk$1;
	};
	Object.setPrototypeOf(createChalk.prototype, Function.prototype);
	for (const [styleName, style] of Object.entries(ansi_styles_default)) styles[styleName] = { get() {
		const builder = createBuilder(this, createStyler(style.open, style.close, this[STYLER]), this[IS_EMPTY]);
		Object.defineProperty(this, styleName, { value: builder });
		return builder;
	} };
	styles.visible = { get() {
		const builder = createBuilder(this, this[STYLER], true);
		Object.defineProperty(this, "visible", { value: builder });
		return builder;
	} };
	getModelAnsi = (model, level, type, ...arguments_) => {
		if (model === "rgb") {
			if (level === "ansi16m") return ansi_styles_default[type].ansi16m(...arguments_);
			if (level === "ansi256") return ansi_styles_default[type].ansi256(ansi_styles_default.rgbToAnsi256(...arguments_));
			return ansi_styles_default[type].ansi(ansi_styles_default.rgbToAnsi(...arguments_));
		}
		if (model === "hex") return getModelAnsi("rgb", level, type, ...ansi_styles_default.hexToRgb(...arguments_));
		return ansi_styles_default[type][model](...arguments_);
	};
	for (const model of [
		"rgb",
		"hex",
		"ansi256"
	]) {
		styles[model] = { get() {
			const { level } = this;
			return function(...arguments_) {
				const styler = createStyler(getModelAnsi(model, levelMapping[level], "color", ...arguments_), ansi_styles_default.color.close, this[STYLER]);
				return createBuilder(this, styler, this[IS_EMPTY]);
			};
		} };
		const bgModel = "bg" + model[0].toUpperCase() + model.slice(1);
		styles[bgModel] = { get() {
			const { level } = this;
			return function(...arguments_) {
				const styler = createStyler(getModelAnsi(model, levelMapping[level], "bgColor", ...arguments_), ansi_styles_default.bgColor.close, this[STYLER]);
				return createBuilder(this, styler, this[IS_EMPTY]);
			};
		} };
	}
	proto = Object.defineProperties(() => {}, {
		...styles,
		level: {
			enumerable: true,
			get() {
				return this[GENERATOR].level;
			},
			set(level) {
				this[GENERATOR].level = level;
			}
		}
	});
	createStyler = (open$1, close, parent) => {
		let openAll;
		let closeAll;
		if (parent === void 0) {
			openAll = open$1;
			closeAll = close;
		} else {
			openAll = parent.openAll + open$1;
			closeAll = close + parent.closeAll;
		}
		return {
			open: open$1,
			close,
			openAll,
			closeAll,
			parent
		};
	};
	createBuilder = (self, _styler, _isEmpty) => {
		const builder = (...arguments_) => applyStyle(builder, arguments_.length === 1 ? "" + arguments_[0] : arguments_.join(" "));
		Object.setPrototypeOf(builder, proto);
		builder[GENERATOR] = self;
		builder[STYLER] = _styler;
		builder[IS_EMPTY] = _isEmpty;
		return builder;
	};
	applyStyle = (self, string$2) => {
		if (self.level <= 0 || !string$2) return self[IS_EMPTY] ? "" : string$2;
		let styler = self[STYLER];
		if (styler === void 0) return string$2;
		const { openAll, closeAll } = styler;
		if (string$2.includes("\x1B")) while (styler !== void 0) {
			string$2 = stringReplaceAll(string$2, styler.close, styler.open);
			styler = styler.parent;
		}
		const lfIndex = string$2.indexOf("\n");
		if (lfIndex !== -1) string$2 = stringEncaseCRLFWithFirstIndex(string$2, closeAll, openAll, lfIndex);
		return openAll + string$2 + closeAll;
	};
	Object.defineProperties(createChalk.prototype, styles);
	chalk = createChalk();
	chalkStderr = createChalk({ level: stderrColor ? stderrColor.level : 0 });
	source_default = chalk;
}) });

//#endregion
//#region ../../node_modules/.pnpm/didyoumean@1.2.2/node_modules/didyoumean/didYouMean-1.2.1.js
var require_didYouMean_1_2_1 = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/didyoumean@1.2.2/node_modules/didyoumean/didYouMean-1.2.1.js": ((exports, module) => {
	(function() {
		"use strict";
		function didYouMean(str, list$3, key) {
			if (!str) return null;
			if (!didYouMean.caseSensitive) str = str.toLowerCase();
			var thresholdRelative = didYouMean.threshold === null ? null : didYouMean.threshold * str.length, thresholdAbsolute = didYouMean.thresholdAbsolute, winningVal;
			if (thresholdRelative !== null && thresholdAbsolute !== null) winningVal = Math.min(thresholdRelative, thresholdAbsolute);
			else if (thresholdRelative !== null) winningVal = thresholdRelative;
			else if (thresholdAbsolute !== null) winningVal = thresholdAbsolute;
			else winningVal = null;
			var winner, candidate, testCandidate, val, i, len = list$3.length;
			for (i = 0; i < len; i++) {
				candidate = list$3[i];
				if (key) candidate = candidate[key];
				if (!candidate) continue;
				if (!didYouMean.caseSensitive) testCandidate = candidate.toLowerCase();
				else testCandidate = candidate;
				val = getEditDistance(str, testCandidate, winningVal);
				if (winningVal === null || val < winningVal) {
					winningVal = val;
					if (key && didYouMean.returnWinningObject) winner = list$3[i];
					else winner = candidate;
					if (didYouMean.returnFirstMatch) return winner;
				}
			}
			return winner || didYouMean.nullResultValue;
		}
		didYouMean.threshold = .4;
		didYouMean.thresholdAbsolute = 20;
		didYouMean.caseSensitive = false;
		didYouMean.nullResultValue = null;
		didYouMean.returnWinningObject = null;
		didYouMean.returnFirstMatch = false;
		if (typeof module !== "undefined" && module.exports) module.exports = didYouMean;
		else window.didYouMean = didYouMean;
		var MAX_INT = Math.pow(2, 32) - 1;
		function getEditDistance(a, b, max) {
			max = max || max === 0 ? max : MAX_INT;
			var lena = a.length;
			var lenb = b.length;
			if (lena === 0) return Math.min(max + 1, lenb);
			if (lenb === 0) return Math.min(max + 1, lena);
			if (Math.abs(lena - lenb) > max) return max + 1;
			var matrix = [], i, j, colMin, minJ, maxJ;
			for (i = 0; i <= lenb; i++) matrix[i] = [i];
			for (j = 0; j <= lena; j++) matrix[0][j] = j;
			for (i = 1; i <= lenb; i++) {
				colMin = MAX_INT;
				minJ = 1;
				if (i > max) minJ = i - max;
				maxJ = lenb + 1;
				if (maxJ > max + i) maxJ = max + i;
				for (j = 1; j <= lena; j++) {
					if (j < minJ || j > maxJ) matrix[i][j] = max + 1;
					else if (b.charAt(i - 1) === a.charAt(j - 1)) matrix[i][j] = matrix[i - 1][j - 1];
					else matrix[i][j] = Math.min(matrix[i - 1][j - 1] + 1, Math.min(matrix[i][j - 1] + 1, matrix[i - 1][j] + 1));
					if (matrix[i][j] < colMin) colMin = matrix[i][j];
				}
				if (colMin > max) return max + 1;
			}
			return matrix[lenb][lena];
		}
	})();
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/Result.js
var require_Result = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/Result.js": ((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.ok = ok;
	exports.err = err;
	exports.isOk = isOk;
	exports.isErr = isErr;
	exports.safeAsync = safeAsync;
	function ok(value) {
		return {
			_tag: "ok",
			value
		};
	}
	function err(error) {
		return {
			_tag: "error",
			error
		};
	}
	/**
	* Checks whether a value is an `Ok`.
	* Handy with TypeScript guards
	*/
	function isOk(result) {
		return result._tag === "ok";
	}
	/**
	* Checks whether a value is an `Err`.
	* Handy with TypeScript guards
	*/
	function isErr(either) {
		return either._tag === "error";
	}
	/**
	* Convert a `Promise<T>` into a `Promise<Result<Error, T>>`,
	* therefore catching the errors and being able to handle them explicitly
	*/
	async function safeAsync(promise) {
		try {
			return ok(await promise);
		} catch (e) {
			return err(e);
		}
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/effects.js
var require_effects = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/effects.js": ((exports) => {
	/**
	* "Effects" are custom exceptions that can do stuff.
	* The concept comes from React, where they throw a `Promise` to provide the ability to write
	* async code with synchronous syntax.
	*
	* These effects _should stay an implementation detail_ and not leak out of the library.
	*
	* @packageDocumentation
	*/
	var __importDefault$9 = exports && exports.__importDefault || function(mod) {
		return mod && mod.__esModule ? mod : { "default": mod };
	};
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.Exit = void 0;
	const chalk_1$7 = __importDefault$9((init_source(), __toCommonJS$3(source_exports)));
	/**
	* An effect to exit the program with a message
	*
	* **Why this is an effect?**
	*
	* Using `process.exit` in a program is both a problem:
	* * in tests, because it needs to be mocked somehow
	* * in browser usage, because it does not have `process` at all
	*
	* Also, using `console.log` is something we'd rather avoid and return strings, and if returning strings
	* would be harmful for performance we might ask for a stream to write to:
	* Printing to stdout and stderr means that we don't control the values and it ties us to only use `cmd-ts`
	* with a command line, and again, to mock `stdout` and `stderr` it if we want to test it.
	*/
	var Exit = class {
		constructor(config$1) {
			this.config = config$1;
		}
		run() {
			this.output()(this.config.message);
			process.exit(this.config.exitCode);
		}
		dryRun() {
			const { into, message, exitCode } = this.config;
			return `${message}\n\n${chalk_1$7.default.dim(`process exited with status ${exitCode} (${into})`)}`;
		}
		output() {
			if (this.config.into === "stderr") return console.error;
			return console.log;
		}
	};
	exports.Exit = Exit;
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/newparser/findOption.js
var require_findOption = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/newparser/findOption.js": ((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.findOption = findOption;
	/**
	* A utility function to find an option in the AST
	*
	* @param nodes AST node list
	* @param opts Long and short names to look up
	*/
	function findOption(nodes, opts) {
		const result = [];
		for (const node of nodes) {
			if (node.type === "longOption" && opts.longNames.includes(node.key)) {
				result.push(node);
				continue;
			}
			if (node.type === "shortOptions" && opts.shortNames.length) {
				for (const option$1 of node.options) if (opts.shortNames.includes(option$1.key)) result.push(option$1);
			}
		}
		return result;
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/from.js
var require_from = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/from.js": ((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.identity = identity;
	/**
	* A type "conversion" from any type to itself
	*/
	function identity() {
		return { async from(a) {
			return a;
		} };
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/type.js
var require_type = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/type.js": ((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.identity = void 0;
	exports.typeDef = typeDef;
	exports.fromFn = fromFn;
	exports.extendType = extendType$2;
	var from_1 = require_from();
	Object.defineProperty(exports, "identity", {
		enumerable: true,
		get: function() {
			return from_1.identity;
		}
	});
	/**
	* Get the type definitions or an empty object from a type or a decoding function
	*/
	function typeDef(from) {
		if (typeof from === "function") return {};
		return from;
	}
	/**
	* Get the decoding function from a type or a function
	*/
	function fromFn(t) {
		if (typeof t === "function") return t;
		return t.from;
	}
	/**
	* Extend a type: take a type and use it as a base for another type. Much like using the spread operator:
	* ```
	* const newType = { ...oldType }
	* ```
	* but composes the `from` arguments
	*
	* @param base A base type from `InputA` to `OutputA`
	* @param nextTypeOrDecodingFunction Either an entire `Type<OutputA, AnyOutput>` or just a decoding function from `OutputA` to any type
	*/
	function extendType$2(base, nextTypeOrDecodingFunction) {
		const { defaultValue: _defaultValue, onMissing: _onMissing, from: _from, ...t1WithoutDefault } = base;
		const t2Object = typeDef(nextTypeOrDecodingFunction);
		const t2From = fromFn(nextTypeOrDecodingFunction);
		return {
			...t1WithoutDefault,
			...t2Object,
			async from(a) {
				return await t2From(await base.from(a));
			}
		};
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/types.js
var require_types = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/types.js": ((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.boolean = exports.string = exports.number = void 0;
	exports.optional = optional;
	exports.array = array;
	const type_1$3 = require_type();
	/**
	* A number type to be used with `option`
	*
	* Throws an error when the provided string is not a number
	*/
	exports.number = {
		async from(str) {
			const decoded = Number.parseFloat(str);
			if (Number.isNaN(decoded)) throw new Error("Not a number");
			return decoded;
		},
		displayName: "number",
		description: "a number"
	};
	/**
	* A string type to be used with `option`.
	*/
	exports.string = {
		...(0, type_1$3.identity)(),
		description: "a string",
		displayName: "str"
	};
	/**
	* A boolean type to be used with `flag`.
	*/
	exports.boolean = {
		...(0, type_1$3.identity)(),
		description: "a boolean",
		displayName: "true/false",
		defaultValue() {
			return false;
		}
	};
	/**
	* Makes any type optional, by defaulting to `undefined`.
	*/
	function optional(t) {
		return {
			...t,
			defaultValue() {}
		};
	}
	/**
	* Transforms any type into an array, useful for `multioption` and `multiflag`.
	*/
	function array(t) {
		return {
			...t,
			async from(inputs) {
				return Promise.all(inputs.map((input) => t.from(input)));
			}
		};
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/flag.js
var require_flag = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/flag.js": ((exports) => {
	var __createBinding$11 = exports && exports.__createBinding || (Object.create ? (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		var desc = Object.getOwnPropertyDescriptor(m$1, k);
		if (!desc || ("get" in desc ? !m$1.__esModule : desc.writable || desc.configurable)) desc = {
			enumerable: true,
			get: function() {
				return m$1[k];
			}
		};
		Object.defineProperty(o, k2, desc);
	}) : (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		o[k2] = m$1[k];
	}));
	var __setModuleDefault$10 = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
		Object.defineProperty(o, "default", {
			enumerable: true,
			value: v
		});
	}) : function(o, v) {
		o["default"] = v;
	});
	var __importStar$10 = exports && exports.__importStar || (function() {
		var ownKeys = function(o) {
			ownKeys = Object.getOwnPropertyNames || function(o$1) {
				var ar = [];
				for (var k in o$1) if (Object.prototype.hasOwnProperty.call(o$1, k)) ar[ar.length] = k;
				return ar;
			};
			return ownKeys(o);
		};
		return function(mod) {
			if (mod && mod.__esModule) return mod;
			var result = {};
			if (mod != null) {
				for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding$11(result, mod, k[i]);
			}
			__setModuleDefault$10(result, mod);
			return result;
		};
	})();
	var __importDefault$8 = exports && exports.__importDefault || function(mod) {
		return mod && mod.__esModule ? mod : { "default": mod };
	};
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.boolean = void 0;
	exports.fullFlag = fullFlag;
	exports.flag = flag;
	const chalk_1$6 = __importDefault$8((init_source(), __toCommonJS$3(source_exports)));
	const Result$10 = __importStar$10(require_Result());
	const findOption_1$3 = require_findOption();
	const type_1$2 = require_type();
	const types_1$4 = require_types();
	/**
	* A decoder from `string` to `boolean`
	* works for `true` and `false` only.
	*/
	exports.boolean = {
		async from(str) {
			if (str === "true") return true;
			if (str === "false") return false;
			throw new Error(`expected value to be either "true" or "false". got: "${str}"`);
		},
		displayName: "true/false",
		defaultValue: () => false
	};
	function fullFlag(config$1) {
		var _a;
		const decoder = (0, type_1$2.extendType)(exports.boolean, config$1.type);
		return {
			description: (_a = config$1.description) !== null && _a !== void 0 ? _a : config$1.type.description,
			helpTopics() {
				var _a$1, _b;
				let usage = `--${config$1.long}`;
				if (config$1.short) usage += `, -${config$1.short}`;
				const defaults = [];
				if (config$1.env) {
					const env$2 = process.env[config$1.env] === void 0 ? "" : `=${chalk_1$6.default.italic(process.env[config$1.env])}`;
					defaults.push(`env: ${config$1.env}${env$2}`);
				}
				if (config$1.defaultValue) try {
					const defaultValue = config$1.defaultValue();
					if (config$1.defaultValueIsSerializable) defaults.push(`default: ${chalk_1$6.default.italic(defaultValue)}`);
					else defaults.push("optional");
				} catch (e) {}
				else if (config$1.type.defaultValue) try {
					const defaultValue = config$1.type.defaultValue();
					if (config$1.type.defaultValueIsSerializable) defaults.push(`default: ${chalk_1$6.default.italic(defaultValue)}`);
					else defaults.push("optional");
				} catch (e) {}
				else if (config$1.onMissing || config$1.type.onMissing) defaults.push("optional");
				return [{
					category: "flags",
					usage,
					defaults,
					description: (_b = (_a$1 = config$1.description) !== null && _a$1 !== void 0 ? _a$1 : config$1.type.description) !== null && _b !== void 0 ? _b : "self explanatory"
				}];
			},
			register(opts) {
				opts.forceFlagLongNames.add(config$1.long);
				if (config$1.short) opts.forceFlagShortNames.add(config$1.short);
			},
			async parse({ nodes, visitedNodes }) {
				var _a$1, _b;
				const options = (0, findOption_1$3.findOption)(nodes, {
					longNames: [config$1.long],
					shortNames: config$1.short ? [config$1.short] : []
				}).filter((x) => !visitedNodes.has(x));
				options.forEach((opt) => visitedNodes.add(opt));
				if (options.length > 1) return Result$10.err({ errors: [{
					nodes: options,
					message: `Expected 1 occurence, got ${options.length}`
				}] });
				const valueFromEnv = config$1.env ? process.env[config$1.env] : void 0;
				const onMissingFn = config$1.onMissing || config$1.type.onMissing;
				let rawValue;
				let envPrefix = "";
				if (options.length === 0 && valueFromEnv !== void 0) {
					rawValue = valueFromEnv;
					envPrefix = `env[${chalk_1$6.default.italic(config$1.env)}]: `;
				} else if (options.length === 0 && config$1.defaultValue) try {
					const defaultValue = config$1.defaultValue();
					return Result$10.ok(defaultValue);
				} catch (e) {
					const message = `Default value not found for '--${config$1.long}': ${e.message}`;
					return Result$10.err({ errors: [{
						message,
						nodes: []
					}] });
				}
				else if (options.length === 0 && onMissingFn) try {
					const missingValue = await onMissingFn();
					return Result$10.ok(missingValue);
				} catch (e) {
					const message = `Failed to get missing value for '--${config$1.long}': ${e.message}`;
					return Result$10.err({ errors: [{
						message,
						nodes: []
					}] });
				}
				else if (options.length === 0 && config$1.type.defaultValue) try {
					const defaultValue = config$1.type.defaultValue();
					return Result$10.ok(defaultValue);
				} catch (e) {
					const message = `Default value not found for '--${config$1.long}': ${e.message}`;
					return Result$10.err({ errors: [{
						message,
						nodes: []
					}] });
				}
				else if (options.length === 1) rawValue = (_b = (_a$1 = options[0].value) === null || _a$1 === void 0 ? void 0 : _a$1.node.raw) !== null && _b !== void 0 ? _b : "true";
				else return Result$10.err({ errors: [{
					nodes: [],
					message: `No value provided for --${config$1.long}`
				}] });
				const decoded = await Result$10.safeAsync(decoder.from(rawValue));
				if (Result$10.isErr(decoded)) return Result$10.err({ errors: [{
					nodes: options,
					message: envPrefix + decoded.error.message
				}] });
				return decoded;
			}
		};
	}
	function flag(config$1) {
		return fullFlag({
			type: types_1$4.boolean,
			...config$1
		});
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/circuitbreaker.js
var require_circuitbreaker = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/circuitbreaker.js": ((exports) => {
	var __createBinding$10 = exports && exports.__createBinding || (Object.create ? (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		var desc = Object.getOwnPropertyDescriptor(m$1, k);
		if (!desc || ("get" in desc ? !m$1.__esModule : desc.writable || desc.configurable)) desc = {
			enumerable: true,
			get: function() {
				return m$1[k];
			}
		};
		Object.defineProperty(o, k2, desc);
	}) : (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		o[k2] = m$1[k];
	}));
	var __setModuleDefault$9 = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
		Object.defineProperty(o, "default", {
			enumerable: true,
			value: v
		});
	}) : function(o, v) {
		o["default"] = v;
	});
	var __importStar$9 = exports && exports.__importStar || (function() {
		var ownKeys = function(o) {
			ownKeys = Object.getOwnPropertyNames || function(o$1) {
				var ar = [];
				for (var k in o$1) if (Object.prototype.hasOwnProperty.call(o$1, k)) ar[ar.length] = k;
				return ar;
			};
			return ownKeys(o);
		};
		return function(mod) {
			if (mod && mod.__esModule) return mod;
			var result = {};
			if (mod != null) {
				for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding$10(result, mod, k[i]);
			}
			__setModuleDefault$9(result, mod);
			return result;
		};
	})();
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.versionFlag = exports.helpFlag = void 0;
	exports.handleCircuitBreaker = handleCircuitBreaker;
	exports.createCircuitBreaker = createCircuitBreaker;
	const Result$9 = __importStar$9(require_Result());
	const effects_1$1 = require_effects();
	const flag_1$2 = require_flag();
	const types_1$3 = require_types();
	exports.helpFlag = (0, flag_1$2.flag)({
		long: "help",
		short: "h",
		type: types_1$3.boolean,
		description: "show help"
	});
	exports.versionFlag = (0, flag_1$2.flag)({
		long: "version",
		short: "v",
		type: types_1$3.boolean,
		description: "print the version"
	});
	function handleCircuitBreaker(context, value, breaker) {
		if (Result$9.isErr(breaker)) return;
		if (breaker.value === "help") {
			const message = value.printHelp(context);
			throw new effects_1$1.Exit({
				exitCode: 0,
				message,
				into: "stdout"
			});
		}
		if (breaker.value === "version") {
			const message = value.version || "0.0.0";
			throw new effects_1$1.Exit({
				exitCode: 0,
				message,
				into: "stdout"
			});
		}
	}
	/**
	* Helper flags that are being used in `command` and `subcommands`:
	* `--help, -h` to show help
	* `--version, -v` to show the current version
	*
	* It is called circuitbreaker because if you have `--help` or `--version`
	* anywhere in your argument list, you'll see the version and the help for the closest command
	*/
	function createCircuitBreaker(withVersion) {
		return {
			register(opts) {
				exports.helpFlag.register(opts);
				if (withVersion) exports.versionFlag.register(opts);
			},
			helpTopics() {
				const helpTopics = exports.helpFlag.helpTopics();
				if (withVersion) helpTopics.push(...exports.versionFlag.helpTopics());
				return helpTopics;
			},
			async parse(context) {
				const help = await exports.helpFlag.parse(context);
				const version$1 = withVersion ? await exports.versionFlag.parse(context) : void 0;
				if (Result$9.isErr(help) || version$1 && Result$9.isErr(version$1)) {
					const helpErrors = Result$9.isErr(help) ? help.error.errors : [];
					const versionErrors = version$1 && Result$9.isErr(version$1) ? version$1.error.errors : [];
					return Result$9.err({ errors: [...helpErrors, ...versionErrors] });
				}
				if (help.value) return Result$9.ok("help");
				if (version$1 === null || version$1 === void 0 ? void 0 : version$1.value) return Result$9.ok("version");
				return Result$9.err({ errors: [{
					nodes: [],
					message: "Neither help nor version"
				}] });
			}
		};
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/utils.js
var require_utils = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/utils.js": ((exports) => {
	var __importDefault$7 = exports && exports.__importDefault || function(mod) {
		return mod && mod.__esModule ? mod : { "default": mod };
	};
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.padNoAnsi = padNoAnsi;
	exports.groupBy = groupBy;
	exports.entries = entries;
	exports.enumerate = enumerate;
	exports.flatMap = flatMap$1;
	exports.flatten = flatten;
	const strip_ansi_1$1 = __importDefault$7((init_strip_ansi(), __toCommonJS$3(strip_ansi_exports)));
	/**
	* @ignore
	*/
	function padNoAnsi(str, length, place) {
		const noAnsiStr = (0, strip_ansi_1$1.default)(str);
		if (length < noAnsiStr.length) return str;
		const pad = Array(length - noAnsiStr.length + 1).join(" ");
		if (place === "end") return str + pad;
		return pad + str;
	}
	/**
	* Group an array by a function that returns the key
	*
	* @ignore
	*/
	function groupBy(objs, f) {
		var _a;
		const result = {};
		for (const obj of objs) {
			const key = f(obj);
			result[key] = (_a = result[key]) !== null && _a !== void 0 ? _a : [];
			result[key].push(obj);
		}
		return result;
	}
	/**
	* A better typed version of `Object.entries`
	*
	* @ignore
	*/
	function entries(obj) {
		return Object.entries(obj);
	}
	/**
	* Enumerate over a list, to get a pair of [index, value]
	*
	* @ignore
	*/
	function* enumerate(arr) {
		for (let i = 0; i < arr.length; i++) yield [i, arr[i]];
	}
	/**
	* Array#flatMap polyfill
	*
	* @ignore
	*/
	function flatMap$1(xs, fn) {
		const results = [];
		for (const x of xs) results.push(...fn(x));
		return results;
	}
	/**
	* Flatten an array
	*
	* @ignore
	*/
	function flatten(xs) {
		const results = [];
		for (const x of xs) results.push(...x);
		return results;
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/helpFormatter.js
var require_helpFormatter = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/helpFormatter.js": ((exports) => {
	var __importDefault$6 = exports && exports.__importDefault || function(mod) {
		return mod && mod.__esModule ? mod : { "default": mod };
	};
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.defaultHelpFormatter = void 0;
	exports.setDefaultHelpFormatter = setDefaultHelpFormatter;
	exports.resetHelpFormatter = resetHelpFormatter;
	exports.getHelpFormatter = getHelpFormatter;
	const chalk_1$5 = __importDefault$6((init_source(), __toCommonJS$3(source_exports)));
	const utils_1$3 = require_utils();
	let currentFormatter = null;
	/**
	* Set a custom help formatter to be used for all commands.
	*
	* @example
	* ```ts
	* import { setDefaultHelpFormatter } from "cmd-ts";
	*
	* setDefaultHelpFormatter({
	*   formatCommand(data, context) {
	*     return `My CLI ${data.version}\n${data.description}`;
	*   },
	*   formatSubcommands(data, context) {
	*     return `Commands: ${data.commands.map(c => c.name).join(", ")}`;
	*   },
	* });
	* ```
	*/
	function setDefaultHelpFormatter(formatter) {
		currentFormatter = formatter;
	}
	/**
	* Reset the help formatter to the default.
	* Useful for testing.
	*/
	function resetHelpFormatter() {
		currentFormatter = null;
	}
	/**
	* Get the current help formatter (custom or default).
	* @internal
	*/
	function getHelpFormatter() {
		return currentFormatter !== null && currentFormatter !== void 0 ? currentFormatter : exports.defaultHelpFormatter;
	}
	/**
	* The default help formatter that matches cmd-ts's original output style.
	*/
	exports.defaultHelpFormatter = {
		formatCommand(data, _context) {
			const lines = [];
			let name = data.path.length > 0 ? data.path.join(" ") : data.name;
			name = chalk_1$5.default.bold(name);
			if (data.version) name += ` ${chalk_1$5.default.dim(data.version)}`;
			lines.push(name);
			if (data.description) lines.push(chalk_1$5.default.dim("> ") + data.description);
			const usageBreakdown = (0, utils_1$3.groupBy)(data.helpTopics, (x) => x.category);
			for (const [category, helpTopics] of (0, utils_1$3.entries)(usageBreakdown)) {
				lines.push("");
				lines.push(`${category.toUpperCase()}:`);
				const widestUsage = helpTopics.reduce((len, curr) => {
					return Math.max(len, curr.usage.length);
				}, 0);
				for (const helpTopic of helpTopics) {
					let line = "";
					line += `  ${(0, utils_1$3.padNoAnsi)(helpTopic.usage, widestUsage, "end")}`;
					line += " - ";
					line += helpTopic.description;
					for (const defaultValue of helpTopic.defaults) line += chalk_1$5.default.dim(` [${defaultValue}]`);
					lines.push(line);
				}
			}
			if (data.examples && data.examples.length > 0) {
				lines.push("");
				lines.push("EXAMPLES:");
				for (const example of data.examples) {
					lines.push("");
					lines.push(`  ${example.description}`);
					lines.push(chalk_1$5.default.dim(`  $ ${example.command}`));
				}
			}
			return lines.join("\n");
		},
		formatSubcommands(data, _context) {
			var _a, _b;
			const lines = [];
			const argsSoFar = data.path.length > 0 ? data.path.join(" ") : data.name;
			lines.push(chalk_1$5.default.bold(argsSoFar + chalk_1$5.default.italic(" <subcommand>")));
			if (data.description) lines.push(chalk_1$5.default.dim("> ") + data.description);
			lines.push("");
			lines.push(`where ${chalk_1$5.default.italic("<subcommand>")} can be one of:`);
			lines.push("");
			for (const cmd of data.commands) {
				let description = (_a = cmd.description) !== null && _a !== void 0 ? _a : "";
				description = description && ` - ${description} `;
				if ((_b = cmd.aliases) === null || _b === void 0 ? void 0 : _b.length) {
					const aliasTxt = cmd.aliases.length === 1 ? "alias" : "aliases";
					const aliases = cmd.aliases.join(", ");
					description += chalk_1$5.default.dim(`[${aliasTxt}: ${aliases}]`);
				}
				const row = chalk_1$5.default.dim("- ") + cmd.name + description;
				lines.push(row.trim());
			}
			const helpCommand = chalk_1$5.default.yellow(`${argsSoFar} <subcommand> --help`);
			lines.push("");
			lines.push(chalk_1$5.default.dim(`For more help, try running \`${helpCommand}\``));
			if (data.examples && data.examples.length > 0) {
				lines.push("");
				lines.push("EXAMPLES:");
				for (const example of data.examples) {
					lines.push("");
					lines.push(`  ${example.description}`);
					lines.push(chalk_1$5.default.dim(`  $ ${example.command}`));
				}
			}
			return lines.join("\n");
		}
	};
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/positional.js
var require_positional = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/positional.js": ((exports) => {
	var __createBinding$9 = exports && exports.__createBinding || (Object.create ? (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		var desc = Object.getOwnPropertyDescriptor(m$1, k);
		if (!desc || ("get" in desc ? !m$1.__esModule : desc.writable || desc.configurable)) desc = {
			enumerable: true,
			get: function() {
				return m$1[k];
			}
		};
		Object.defineProperty(o, k2, desc);
	}) : (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		o[k2] = m$1[k];
	}));
	var __setModuleDefault$8 = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
		Object.defineProperty(o, "default", {
			enumerable: true,
			value: v
		});
	}) : function(o, v) {
		o["default"] = v;
	});
	var __importStar$8 = exports && exports.__importStar || (function() {
		var ownKeys = function(o) {
			ownKeys = Object.getOwnPropertyNames || function(o$1) {
				var ar = [];
				for (var k in o$1) if (Object.prototype.hasOwnProperty.call(o$1, k)) ar[ar.length] = k;
				return ar;
			};
			return ownKeys(o);
		};
		return function(mod) {
			if (mod && mod.__esModule) return mod;
			var result = {};
			if (mod != null) {
				for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding$9(result, mod, k[i]);
			}
			__setModuleDefault$8(result, mod);
			return result;
		};
	})();
	var __importDefault$5 = exports && exports.__importDefault || function(mod) {
		return mod && mod.__esModule ? mod : { "default": mod };
	};
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.positional = positional;
	const chalk_1$4 = __importDefault$5((init_source(), __toCommonJS$3(source_exports)));
	const Result$8 = __importStar$8(require_Result());
	const types_1$2 = require_types();
	function fullPositional(config$1) {
		var _a, _b, _c;
		const displayName = (_b = (_a = config$1.displayName) !== null && _a !== void 0 ? _a : config$1.type.displayName) !== null && _b !== void 0 ? _b : "arg";
		return {
			description: (_c = config$1.description) !== null && _c !== void 0 ? _c : config$1.type.description,
			helpTopics() {
				var _a$1, _b$1, _c$1, _d;
				const defaults = [];
				const defaultValueFn = (_a$1 = config$1.defaultValue) !== null && _a$1 !== void 0 ? _a$1 : config$1.type.defaultValue;
				if (defaultValueFn) try {
					const defaultValue = defaultValueFn();
					if ((_b$1 = config$1.defaultValueIsSerializable) !== null && _b$1 !== void 0 ? _b$1 : config$1.type.defaultValueIsSerializable) defaults.push(`default: ${chalk_1$4.default.italic(defaultValue)}`);
					else defaults.push("optional");
				} catch (e) {}
				return [{
					category: "arguments",
					usage: defaults.length > 0 ? `[${displayName}]` : `<${displayName}>`,
					description: (_d = (_c$1 = config$1.description) !== null && _c$1 !== void 0 ? _c$1 : config$1.type.description) !== null && _d !== void 0 ? _d : "self explanatory",
					defaults
				}];
			},
			register(_opts) {},
			async parse({ nodes, visitedNodes }) {
				var _a$1;
				const positionals = nodes.filter((node) => node.type === "positionalArgument" && !visitedNodes.has(node));
				const defaultValueFn = (_a$1 = config$1.defaultValue) !== null && _a$1 !== void 0 ? _a$1 : config$1.type.defaultValue;
				const positional$1 = positionals[0];
				if (!positional$1) {
					if (defaultValueFn) return Result$8.ok(defaultValueFn());
					return Result$8.err({ errors: [{
						nodes: [],
						message: `No value provided for ${displayName}`
					}] });
				}
				visitedNodes.add(positional$1);
				const decoded = await Result$8.safeAsync(config$1.type.from(positional$1.raw));
				if (Result$8.isErr(decoded)) return Result$8.err({ errors: [{
					nodes: [positional$1],
					message: decoded.error.message
				}] });
				return Result$8.ok(decoded.value);
			}
		};
	}
	function positional(config$1) {
		return fullPositional({
			type: types_1$2.string,
			...config$1
		});
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/subcommands.js
var require_subcommands = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/subcommands.js": ((exports) => {
	var __createBinding$8 = exports && exports.__createBinding || (Object.create ? (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		var desc = Object.getOwnPropertyDescriptor(m$1, k);
		if (!desc || ("get" in desc ? !m$1.__esModule : desc.writable || desc.configurable)) desc = {
			enumerable: true,
			get: function() {
				return m$1[k];
			}
		};
		Object.defineProperty(o, k2, desc);
	}) : (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		o[k2] = m$1[k];
	}));
	var __setModuleDefault$7 = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
		Object.defineProperty(o, "default", {
			enumerable: true,
			value: v
		});
	}) : function(o, v) {
		o["default"] = v;
	});
	var __importStar$7 = exports && exports.__importStar || (function() {
		var ownKeys = function(o) {
			ownKeys = Object.getOwnPropertyNames || function(o$1) {
				var ar = [];
				for (var k in o$1) if (Object.prototype.hasOwnProperty.call(o$1, k)) ar[ar.length] = k;
				return ar;
			};
			return ownKeys(o);
		};
		return function(mod) {
			if (mod && mod.__esModule) return mod;
			var result = {};
			if (mod != null) {
				for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding$8(result, mod, k[i]);
			}
			__setModuleDefault$7(result, mod);
			return result;
		};
	})();
	var __importDefault$4 = exports && exports.__importDefault || function(mod) {
		return mod && mod.__esModule ? mod : { "default": mod };
	};
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.subcommands = subcommands$3;
	const chalk_1$3 = __importDefault$4((init_source(), __toCommonJS$3(source_exports)));
	const didyoumean_1 = __importDefault$4(require_didYouMean_1_2_1());
	const Result$7 = __importStar$7(require_Result());
	const circuitbreaker_1$1 = require_circuitbreaker();
	const helpFormatter_1$2 = require_helpFormatter();
	const positional_1$1 = require_positional();
	/**
	* Combine multiple `command`s into one
	*/
	function subcommands$3(config$1) {
		const circuitbreaker = (0, circuitbreaker_1$1.createCircuitBreaker)(!!config$1.version);
		const subcommand = (0, positional_1$1.positional)({
			displayName: "subcommand",
			description: `one of ${Object.keys(config$1.cmds).join(", ")}`,
			type: { async from(str) {
				const commands = Object.entries(config$1.cmds).map(([name, cmd$1]) => {
					var _a;
					return {
						cmdName: name,
						names: [name, ...(_a = cmd$1.aliases) !== null && _a !== void 0 ? _a : []]
					};
				});
				const cmd = commands.find((x) => x.names.includes(str));
				if (cmd) return cmd.cmdName;
				let errorMessage = "Not a valid subcommand name";
				const closeOptions = (0, didyoumean_1.default)(str, flatMap(commands, (x) => x.names));
				if (closeOptions) {
					const option$1 = Array.isArray(closeOptions) ? closeOptions[0] : closeOptions;
					errorMessage += `\nDid you mean ${chalk_1$3.default.italic(option$1)}?`;
				}
				throw new Error(errorMessage);
			} }
		});
		function normalizeContext(context) {
			var _a;
			if (((_a = context.hotPath) === null || _a === void 0 ? void 0 : _a.length) === 0) context.hotPath.push(config$1.name);
			if (!context.nodes.some((n) => !context.visitedNodes.has(n))) context.nodes.push({
				type: "longOption",
				index: 0,
				key: "help",
				raw: "--help"
			});
		}
		return {
			version: config$1.version,
			description: config$1.description,
			name: config$1.name,
			handler: (value) => {
				return config$1.cmds[value.command].handler(value.args);
			},
			register(opts) {
				for (const cmd of Object.values(config$1.cmds)) cmd.register(opts);
				circuitbreaker.register(opts);
			},
			printHelp(context) {
				var _a;
				const data = {
					name: config$1.name,
					path: (_a = context.hotPath) !== null && _a !== void 0 ? _a : [config$1.name],
					version: config$1.version,
					description: config$1.description,
					commands: Object.entries(config$1.cmds).map(([name, cmd]) => {
						var _a$1, _b;
						return {
							name,
							description: cmd.description,
							aliases: cmd.aliases,
							helpTopics: (_b = (_a$1 = cmd.helpTopics) === null || _a$1 === void 0 ? void 0 : _a$1.call(cmd)) !== null && _b !== void 0 ? _b : []
						};
					}),
					examples: config$1.examples
				};
				return (0, helpFormatter_1$2.getHelpFormatter)().formatSubcommands(data, context);
			},
			async parse(context) {
				var _a;
				normalizeContext(context);
				const parsed = await subcommand.parse(context);
				if (Result$7.isErr(parsed)) return Result$7.err({
					errors: parsed.error.errors,
					partialValue: {}
				});
				(_a = context.hotPath) === null || _a === void 0 || _a.push(parsed.value);
				const parsedCommand = await config$1.cmds[parsed.value].parse(context);
				if (Result$7.isErr(parsedCommand)) return Result$7.err({
					errors: parsedCommand.error.errors,
					partialValue: {
						command: parsed.value,
						args: parsedCommand.error.partialValue
					}
				});
				return Result$7.ok({
					args: parsedCommand.value,
					command: parsed.value
				});
			},
			async run(context) {
				var _a;
				normalizeContext(context);
				const parsedSubcommand = await subcommand.parse(context);
				if (Result$7.isErr(parsedSubcommand)) {
					const breaker = await circuitbreaker.parse(context);
					(0, circuitbreaker_1$1.handleCircuitBreaker)(context, this, breaker);
					return Result$7.err({
						...parsedSubcommand.error,
						partialValue: {}
					});
				}
				(_a = context.hotPath) === null || _a === void 0 || _a.push(parsedSubcommand.value);
				const commandRun = await config$1.cmds[parsedSubcommand.value].run(context);
				if (Result$7.isOk(commandRun)) return Result$7.ok({
					command: parsedSubcommand.value,
					value: commandRun.value
				});
				return Result$7.err({
					...commandRun.error,
					partialValue: {
						command: parsedSubcommand.value,
						value: commandRun.error.partialValue
					}
				});
			}
		};
	}
	function flatMap(array$1, f) {
		const rs = [];
		for (const item of array$1) rs.push(...f(item));
		return rs;
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/binary.js
var require_binary = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/binary.js": ((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.binary = binary;
	/**
	* A small helper to easily use `process.argv` without dropping context
	*
	* @param cmd a command line parser
	*/
	function binary(cmd) {
		return {
			...cmd,
			run(context) {
				var _a;
				const name = cmd.name || context.nodes[1].raw;
				(_a = context.hotPath) === null || _a === void 0 || _a.push(name);
				context.nodes.splice(0, 1);
				context.nodes[0].raw = name;
				context.visitedNodes.add(context.nodes[0]);
				return cmd.run(context);
			}
		};
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/command.js
var require_command = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/command.js": ((exports) => {
	var __createBinding$7 = exports && exports.__createBinding || (Object.create ? (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		var desc = Object.getOwnPropertyDescriptor(m$1, k);
		if (!desc || ("get" in desc ? !m$1.__esModule : desc.writable || desc.configurable)) desc = {
			enumerable: true,
			get: function() {
				return m$1[k];
			}
		};
		Object.defineProperty(o, k2, desc);
	}) : (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		o[k2] = m$1[k];
	}));
	var __setModuleDefault$6 = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
		Object.defineProperty(o, "default", {
			enumerable: true,
			value: v
		});
	}) : function(o, v) {
		o["default"] = v;
	});
	var __importStar$6 = exports && exports.__importStar || (function() {
		var ownKeys = function(o) {
			ownKeys = Object.getOwnPropertyNames || function(o$1) {
				var ar = [];
				for (var k in o$1) if (Object.prototype.hasOwnProperty.call(o$1, k)) ar[ar.length] = k;
				return ar;
			};
			return ownKeys(o);
		};
		return function(mod) {
			if (mod && mod.__esModule) return mod;
			var result = {};
			if (mod != null) {
				for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding$7(result, mod, k[i]);
			}
			__setModuleDefault$6(result, mod);
			return result;
		};
	})();
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.command = command;
	const Result$6 = __importStar$6(require_Result());
	const circuitbreaker_1 = require_circuitbreaker();
	const helpFormatter_1$1 = require_helpFormatter();
	const utils_1$2 = require_utils();
	/**
	* A command line utility.
	*
	* A combination of multiple flags, options and arguments
	* with a common name and a handler that expects them as input.
	*/
	function command(config$1) {
		const argEntries = (0, utils_1$2.entries)(config$1.args);
		const circuitbreaker = (0, circuitbreaker_1.createCircuitBreaker)(!!config$1.version);
		return {
			name: config$1.name,
			aliases: config$1.aliases,
			handler: config$1.handler,
			description: config$1.description,
			version: config$1.version,
			helpTopics() {
				return (0, utils_1$2.flatMap)(Object.values(config$1.args).concat([circuitbreaker]), (x) => {
					var _a, _b;
					return (_b = (_a = x.helpTopics) === null || _a === void 0 ? void 0 : _a.call(x)) !== null && _b !== void 0 ? _b : [];
				});
			},
			printHelp(context) {
				var _a;
				const data = {
					name: config$1.name,
					path: (_a = context.hotPath) !== null && _a !== void 0 ? _a : [config$1.name],
					version: config$1.version,
					description: config$1.description,
					aliases: config$1.aliases,
					helpTopics: this.helpTopics(),
					examples: config$1.examples
				};
				return (0, helpFormatter_1$1.getHelpFormatter)().formatCommand(data, context);
			},
			register(opts) {
				var _a;
				for (const [, arg] of argEntries) (_a = arg.register) === null || _a === void 0 || _a.call(arg, opts);
			},
			async parse(context) {
				var _a;
				if (((_a = context.hotPath) === null || _a === void 0 ? void 0 : _a.length) === 0) context.hotPath.push(config$1.name);
				const resultObject = {};
				const errors = [];
				for (const [argName, arg] of argEntries) {
					const result = await arg.parse(context);
					if (Result$6.isErr(result)) errors.push(...result.error.errors);
					else resultObject[argName] = result.value;
				}
				const unknownArguments = [];
				for (const node of context.nodes) {
					if (context.visitedNodes.has(node)) continue;
					if (node.type === "forcePositional") {} else if (node.type === "shortOptions") for (const option$1 of node.options) {
						if (context.visitedNodes.has(option$1)) continue;
						unknownArguments.push(option$1);
					}
					else unknownArguments.push(node);
				}
				if (unknownArguments.length > 0) errors.push({
					message: "Unknown arguments",
					nodes: unknownArguments
				});
				if (errors.length > 0) return Result$6.err({
					errors,
					partialValue: resultObject
				});
				return Result$6.ok(resultObject);
			},
			async run(context) {
				const breaker = await circuitbreaker.parse(context);
				(0, circuitbreaker_1.handleCircuitBreaker)(context, this, breaker);
				const parsed = await this.parse(context);
				if (Result$6.isErr(parsed)) return Result$6.err(parsed.error);
				return Result$6.ok(await this.handler(parsed.value));
			}
		};
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/option.js
var require_option = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/option.js": ((exports) => {
	var __createBinding$6 = exports && exports.__createBinding || (Object.create ? (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		var desc = Object.getOwnPropertyDescriptor(m$1, k);
		if (!desc || ("get" in desc ? !m$1.__esModule : desc.writable || desc.configurable)) desc = {
			enumerable: true,
			get: function() {
				return m$1[k];
			}
		};
		Object.defineProperty(o, k2, desc);
	}) : (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		o[k2] = m$1[k];
	}));
	var __setModuleDefault$5 = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
		Object.defineProperty(o, "default", {
			enumerable: true,
			value: v
		});
	}) : function(o, v) {
		o["default"] = v;
	});
	var __importStar$5 = exports && exports.__importStar || (function() {
		var ownKeys = function(o) {
			ownKeys = Object.getOwnPropertyNames || function(o$1) {
				var ar = [];
				for (var k in o$1) if (Object.prototype.hasOwnProperty.call(o$1, k)) ar[ar.length] = k;
				return ar;
			};
			return ownKeys(o);
		};
		return function(mod) {
			if (mod && mod.__esModule) return mod;
			var result = {};
			if (mod != null) {
				for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding$6(result, mod, k[i]);
			}
			__setModuleDefault$5(result, mod);
			return result;
		};
	})();
	var __importDefault$3 = exports && exports.__importDefault || function(mod) {
		return mod && mod.__esModule ? mod : { "default": mod };
	};
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.option = option;
	const chalk_1$2 = __importDefault$3((init_source(), __toCommonJS$3(source_exports)));
	const Result$5 = __importStar$5(require_Result());
	const findOption_1$2 = require_findOption();
	const types_1$1 = require_types();
	function fullOption(config$1) {
		var _a;
		return {
			description: (_a = config$1.description) !== null && _a !== void 0 ? _a : config$1.type.description,
			helpTopics() {
				var _a$1, _b, _c;
				const displayName = (_a$1 = config$1.type.displayName) !== null && _a$1 !== void 0 ? _a$1 : "value";
				let usage = `--${config$1.long}`;
				if (config$1.short) usage += `, -${config$1.short}`;
				usage += ` <${displayName}>`;
				const defaults = [];
				if (config$1.env) {
					const env$2 = process.env[config$1.env] === void 0 ? "" : `=${chalk_1$2.default.italic(process.env[config$1.env])}`;
					defaults.push(`env: ${config$1.env}${env$2}`);
				}
				if (config$1.defaultValue) try {
					const defaultValue = config$1.defaultValue();
					if (config$1.defaultValueIsSerializable) defaults.push(`default: ${chalk_1$2.default.italic(defaultValue)}`);
					else defaults.push("optional");
				} catch (e) {}
				else if (config$1.type.defaultValue) try {
					const defaultValue = config$1.type.defaultValue();
					if (config$1.type.defaultValueIsSerializable) defaults.push(`default: ${chalk_1$2.default.italic(defaultValue)}`);
					else defaults.push("optional");
				} catch (e) {}
				else if (config$1.onMissing || config$1.type.onMissing) defaults.push("optional");
				return [{
					category: "options",
					usage,
					defaults,
					description: (_c = (_b = config$1.description) !== null && _b !== void 0 ? _b : config$1.type.description) !== null && _c !== void 0 ? _c : "self explanatory"
				}];
			},
			register(opts) {
				opts.forceOptionLongNames.add(config$1.long);
				if (config$1.short) opts.forceOptionShortNames.add(config$1.short);
			},
			async parse({ nodes, visitedNodes }) {
				const options = (0, findOption_1$2.findOption)(nodes, {
					longNames: [config$1.long],
					shortNames: config$1.short ? [config$1.short] : []
				}).filter((x) => !visitedNodes.has(x));
				options.forEach((opt) => visitedNodes.add(opt));
				if (options.length > 1) {
					const error = {
						message: `Too many times provided. Expected 1, got: ${options.length}`,
						nodes: options
					};
					return Result$5.err({ errors: [error] });
				}
				const valueFromEnv = config$1.env ? process.env[config$1.env] : void 0;
				const defaultValueFn = config$1.defaultValue || config$1.type.defaultValue;
				const onMissingFn = config$1.onMissing || config$1.type.onMissing;
				const option$1 = options[0];
				let rawValue;
				let envPrefix = "";
				if (option$1 === null || option$1 === void 0 ? void 0 : option$1.value) rawValue = option$1.value.node.raw;
				else if (valueFromEnv !== void 0) {
					rawValue = valueFromEnv;
					envPrefix = `env[${chalk_1$2.default.italic(config$1.env)}]: `;
				} else if (defaultValueFn) try {
					const defaultValue = defaultValueFn();
					return Result$5.ok(defaultValue);
				} catch (e) {
					const message = `Default value not found for '--${config$1.long}': ${e.message}`;
					return Result$5.err({ errors: [{
						nodes: [],
						message
					}] });
				}
				else if (onMissingFn) try {
					const missingValue = await onMissingFn();
					return Result$5.ok(missingValue);
				} catch (e) {
					const message = `Failed to get missing value for '--${config$1.long}': ${e.message}`;
					return Result$5.err({ errors: [{
						nodes: [],
						message
					}] });
				}
				else {
					const raw = (option$1 === null || option$1 === void 0 ? void 0 : option$1.type) === "shortOption" ? `-${option$1 === null || option$1 === void 0 ? void 0 : option$1.key}` : `--${config$1.long}`;
					return Result$5.err({ errors: [{
						nodes: options,
						message: `No value provided for ${raw}`
					}] });
				}
				const decoded = await Result$5.safeAsync(config$1.type.from(rawValue));
				if (Result$5.isErr(decoded)) return Result$5.err({ errors: [{
					nodes: options,
					message: envPrefix + decoded.error.message
				}] });
				return Result$5.ok(decoded.value);
			}
		};
	}
	function option(config$1) {
		return fullOption({
			type: types_1$1.string,
			...config$1
		});
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/errorBox.js
var require_errorBox = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/errorBox.js": ((exports) => {
	var __importDefault$2 = exports && exports.__importDefault || function(mod) {
		return mod && mod.__esModule ? mod : { "default": mod };
	};
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.errorBox = errorBox;
	const chalk_1$1 = __importDefault$2((init_source(), __toCommonJS$3(source_exports)));
	const strip_ansi_1 = __importDefault$2((init_strip_ansi(), __toCommonJS$3(strip_ansi_exports)));
	const utils_1$1 = require_utils();
	/**
	* Get the input as highlighted keywords to show to the user
	* with the error that was generated from parsing the input.
	*
	* @param nodes AST nodes
	* @param error A parsing error
	*/
	function highlight(nodes, error) {
		const strings = [];
		let errorIndex = void 0;
		function foundError() {
			if (errorIndex !== void 0) return;
			errorIndex = (0, strip_ansi_1.default)(strings.join(" ")).length;
		}
		if (error.nodes.length === 0) return;
		nodes.forEach((node) => {
			if (error.nodes.includes(node)) {
				foundError();
				return strings.push(chalk_1$1.default.red(node.raw));
			}
			if (node.type === "shortOptions") {
				let failed = false;
				let s$1 = "";
				for (const option$1 of node.options) if (error.nodes.includes(option$1)) {
					s$1 += chalk_1$1.default.red(option$1.raw);
					failed = true;
				} else s$1 += chalk_1$1.default.dim(option$1.raw);
				const prefix$1 = failed ? chalk_1$1.default.red("-") : chalk_1$1.default.dim("-");
				if (failed) foundError();
				return strings.push(prefix$1 + s$1);
			}
			return strings.push(chalk_1$1.default.dim(node.raw));
		});
		return {
			colorized: strings.join(" "),
			errorIndex: errorIndex !== null && errorIndex !== void 0 ? errorIndex : 0
		};
	}
	/**
	* An error UI
	*
	* @param breadcrumbs The command breadcrumbs to print with the error
	*/
	function errorBox(nodes, errors, breadcrumbs) {
		const withHighlight = [];
		const errorMessages = [];
		for (const error of errors) {
			const highlighted = highlight(nodes, error);
			withHighlight.push({
				message: error.message,
				highlighted
			});
		}
		let number = 1;
		const maxNumberWidth = String(withHighlight.length).length;
		errorMessages.push(`${chalk_1$1.default.red.bold("error: ")}found ${chalk_1$1.default.yellow(withHighlight.length)} error${withHighlight.length > 1 ? "s" : ""}`);
		errorMessages.push("");
		withHighlight.filter((x) => x.highlighted).forEach((x) => {
			if (!x.highlighted) throw new Error("WELP");
			const pad = "".padStart(x.highlighted.errorIndex);
			errorMessages.push(`  ${x.highlighted.colorized}`);
			for (const [index, line] of (0, utils_1$1.enumerate)(x.message.split("\n"))) {
				const prefix$1 = index === 0 ? chalk_1$1.default.bold("^") : " ";
				const msg = chalk_1$1.default.red(`  ${pad} ${prefix$1} ${line}`);
				errorMessages.push(msg);
			}
			errorMessages.push("");
			number++;
		});
		const withNoHighlight = withHighlight.filter((x) => !x.highlighted);
		if (number > 1) {
			if (withNoHighlight.length === 1) errorMessages.push("Along with the following error:");
			else if (withNoHighlight.length > 1) errorMessages.push("Along with the following errors:");
		}
		withNoHighlight.forEach(({ message }) => {
			var _a;
			const num = chalk_1$1.default.red.bold(`${(0, utils_1$1.padNoAnsi)(number.toString(), maxNumberWidth, "start")}.`);
			const lines = message.split("\n");
			errorMessages.push(`  ${num} ${chalk_1$1.default.red((_a = lines[0]) !== null && _a !== void 0 ? _a : "")}`);
			for (const line of lines.slice(1)) errorMessages.push(` ${"".padStart(maxNumberWidth + 1)}  ${chalk_1$1.default.red(line)}`);
			number++;
		});
		const helpCmd = chalk_1$1.default.yellow(`${breadcrumbs.join(" ")} --help`);
		errorMessages.push("");
		errorMessages.push(`${chalk_1$1.default.red.bold("hint: ")}for more information, try '${helpCmd}'`);
		return errorMessages.join("\n");
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/newparser/parser.js
var require_parser = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/newparser/parser.js": ((exports) => {
	var __importDefault$1 = exports && exports.__importDefault || function(mod) {
		return mod && mod.__esModule ? mod : { "default": mod };
	};
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.parse = parse$2;
	const debug$9 = (0, __importDefault$1(__require("debug")).default)("cmd-ts:parser");
	/**
	* Create an AST from a token list
	*
	* @param tokens A token list, coming from `tokenizer.ts`
	* @param forceFlag Keys to force as flag. {@see ForceFlag} to read more about it.
	*/
	function parse$2(tokens, forceFlag) {
		if (debug$9.enabled) {
			const registered = {
				shortFlags: [...forceFlag.forceFlagShortNames],
				longFlags: [...forceFlag.forceFlagLongNames],
				shortOptions: [...forceFlag.forceOptionShortNames],
				longOptions: [...forceFlag.forceOptionLongNames]
			};
			debug$9("Registered:", JSON.stringify(registered));
		}
		const nodes = [];
		let index = 0;
		let forcedPositional = false;
		function getToken() {
			return tokens[index++];
		}
		function peekToken() {
			return tokens[index];
		}
		while (index < tokens.length) {
			const currentToken = getToken();
			if (!currentToken) break;
			if (currentToken.type === "argumentDivider") continue;
			if (forcedPositional) {
				let str = currentToken.raw;
				let nextToken = getToken();
				while (nextToken && (nextToken === null || nextToken === void 0 ? void 0 : nextToken.type) !== "argumentDivider") {
					str += nextToken.raw;
					nextToken = getToken();
				}
				nodes.push({
					type: "positionalArgument",
					index: currentToken.index,
					raw: str
				});
				continue;
			}
			if (currentToken.type === "char") {
				let str = currentToken.raw;
				let nextToken = getToken();
				while (nextToken && (nextToken === null || nextToken === void 0 ? void 0 : nextToken.type) !== "argumentDivider") {
					str += nextToken.raw;
					nextToken = getToken();
				}
				nodes.push({
					type: "positionalArgument",
					index: currentToken.index,
					raw: str
				});
				continue;
			}
			if (currentToken.type === "longPrefix") {
				let nextToken = getToken();
				if ((nextToken === null || nextToken === void 0 ? void 0 : nextToken.type) === "argumentDivider" || !nextToken) {
					nodes.push({
						type: "forcePositional",
						index: currentToken.index,
						raw: "--"
					});
					forcedPositional = true;
					continue;
				}
				let key = "";
				while (nextToken && (nextToken === null || nextToken === void 0 ? void 0 : nextToken.raw) !== "=" && (nextToken === null || nextToken === void 0 ? void 0 : nextToken.type) !== "argumentDivider") {
					key += nextToken.raw;
					nextToken = getToken();
				}
				const parsedValue = parseOptionValue({
					key,
					delimiterToken: nextToken,
					forceFlag: forceFlag.forceFlagLongNames,
					getToken,
					peekToken,
					forceOption: forceFlag.forceOptionLongNames
				});
				let raw = `--${key}`;
				if (parsedValue) raw += parsedValue.raw;
				nodes.push({
					type: "longOption",
					key,
					index: currentToken.index,
					raw,
					value: parsedValue
				});
				continue;
			}
			if (currentToken.type === "shortPrefix") {
				const keys = [];
				let nextToken = getToken();
				if ((nextToken === null || nextToken === void 0 ? void 0 : nextToken.type) === "argumentDivider" || !nextToken) {
					nodes.push({
						type: "positionalArgument",
						index: currentToken.index,
						raw: "-"
					});
					continue;
				}
				while (nextToken && (nextToken === null || nextToken === void 0 ? void 0 : nextToken.type) !== "argumentDivider" && (nextToken === null || nextToken === void 0 ? void 0 : nextToken.raw) !== "=") {
					keys.push(nextToken);
					nextToken = getToken();
				}
				const lastKey = keys.pop();
				const parsedValue = parseOptionValue({
					key: lastKey.raw,
					delimiterToken: nextToken,
					forceFlag: forceFlag.forceFlagShortNames,
					forceOption: forceFlag.forceOptionShortNames,
					getToken,
					peekToken
				});
				const options = [];
				for (const key of keys) options.push({
					type: "shortOption",
					index: key.index,
					raw: key.raw,
					key: key.raw
				});
				let lastKeyRaw = lastKey.raw;
				if (parsedValue) lastKeyRaw += parsedValue.raw;
				options.push({
					type: "shortOption",
					index: lastKey.index,
					raw: lastKeyRaw,
					value: parsedValue,
					key: lastKey.raw
				});
				let optionsRaw = `-${keys.map((x) => x.raw).join("")}${lastKey.raw}`;
				if (parsedValue) optionsRaw += parsedValue.raw;
				const shortOptions = {
					type: "shortOptions",
					index: currentToken.index,
					raw: optionsRaw,
					options
				};
				nodes.push(shortOptions);
				continue;
			}
			index++;
		}
		if (debug$9.enabled) {
			const objectNodes = nodes.map((node) => ({ [node.type]: node.raw }));
			debug$9("Parsed items:", JSON.stringify(objectNodes));
		}
		return nodes;
	}
	function parseOptionValue(opts) {
		var _a;
		const { getToken, delimiterToken, forceFlag, key, forceOption } = opts;
		const shouldReadKeyAsFlag = !forceOption.has(key) && (forceFlag.has(key) || ((_a = opts.peekToken()) === null || _a === void 0 ? void 0 : _a.type) !== "char");
		if (!delimiterToken || delimiterToken.raw !== "=" && shouldReadKeyAsFlag) return;
		const delimiter = delimiterToken.raw === "=" ? "=" : " ";
		const delimiterIndex = delimiterToken.index;
		let nextToken = getToken();
		if (!nextToken) return;
		let value = "";
		const valueIndex = nextToken.index;
		while (nextToken && (nextToken === null || nextToken === void 0 ? void 0 : nextToken.type) !== "argumentDivider") {
			value += nextToken.raw;
			nextToken = getToken();
		}
		return {
			type: "optionValue",
			index: delimiterToken.index,
			delimiter: {
				type: "delimiter",
				raw: delimiter,
				index: delimiterIndex
			},
			node: {
				type: "value",
				raw: value,
				index: valueIndex
			},
			raw: `${delimiter}${value}`
		};
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/newparser/tokenizer.js
var require_tokenizer = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/newparser/tokenizer.js": ((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.tokenize = tokenize;
	const utils_1 = require_utils();
	/**
	* Tokenize a list of arguments
	*
	* @param strings arguments, based on `process.argv`
	*/
	function tokenize(strings) {
		const tokens = [];
		let overallIndex = 0;
		const push = (token$1) => {
			tokens.push(token$1);
			overallIndex += token$1.raw.length;
		};
		for (const [stringIndex, string$2] of (0, utils_1.enumerate)(strings)) {
			const chars = [...string$2];
			for (let i = 0; i < chars.length; i++) if (chars[i] === "-" && chars[i + 1] === "-") {
				push({
					type: "longPrefix",
					raw: "--",
					index: overallIndex
				});
				i++;
			} else if (chars[i] === "-") push({
				type: "shortPrefix",
				raw: "-",
				index: overallIndex
			});
			else push({
				type: "char",
				raw: chars[i],
				index: overallIndex
			});
			if (stringIndex + 1 !== strings.length) push({
				type: "argumentDivider",
				raw: " ",
				index: overallIndex
			});
		}
		return tokens;
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/runner.js
var require_runner = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/runner.js": ((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.run = run$1;
	exports.runSafely = runSafely;
	exports.dryRun = dryRun;
	exports.parse = parse$1;
	const Result_1 = require_Result();
	const effects_1 = require_effects();
	const errorBox_1 = require_errorBox();
	const parser_1 = require_parser();
	const tokenizer_1 = require_tokenizer();
	async function run$1(ap, strings) {
		const result = await runSafely(ap, strings);
		if ((0, Result_1.isErr)(result)) return result.error.run();
		return result.value;
	}
	/**
	* Runs a command but does not apply any effect
	*/
	async function runSafely(ap, strings) {
		const hotPath = [];
		const nodes = parseCommon(ap, strings);
		try {
			const result = await ap.run({
				nodes,
				visitedNodes: /* @__PURE__ */ new Set(),
				hotPath
			});
			if ((0, Result_1.isErr)(result)) throw new effects_1.Exit({
				message: (0, errorBox_1.errorBox)(nodes, result.error.errors, hotPath),
				exitCode: 1,
				into: "stderr"
			});
			return (0, Result_1.ok)(result.value);
		} catch (e) {
			if (e instanceof effects_1.Exit) return (0, Result_1.err)(e);
			throw e;
		}
	}
	/**
	* Run a command but don't quit. Returns an `Result` instead.
	*/
	async function dryRun(ap, strings) {
		const result = await runSafely(ap, strings);
		if ((0, Result_1.isErr)(result)) return (0, Result_1.err)(result.error.dryRun());
		return result;
	}
	/**
	* Parse the command as if to run it, but only return the parse result and don't run the command.
	*/
	function parse$1(ap, strings) {
		const hotPath = [];
		const nodes = parseCommon(ap, strings);
		return ap.parse({
			nodes,
			visitedNodes: /* @__PURE__ */ new Set(),
			hotPath
		});
	}
	function parseCommon(ap, strings) {
		const longFlagKeys = /* @__PURE__ */ new Set();
		const shortFlagKeys = /* @__PURE__ */ new Set();
		const longOptionKeys = /* @__PURE__ */ new Set();
		const registerContext = {
			forceFlagShortNames: shortFlagKeys,
			forceFlagLongNames: longFlagKeys,
			forceOptionShortNames: /* @__PURE__ */ new Set(),
			forceOptionLongNames: longOptionKeys
		};
		ap.register(registerContext);
		const tokens = (0, tokenizer_1.tokenize)(strings);
		return (0, parser_1.parse)(tokens, registerContext);
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/restPositionals.js
var require_restPositionals = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/restPositionals.js": ((exports) => {
	var __createBinding$5 = exports && exports.__createBinding || (Object.create ? (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		var desc = Object.getOwnPropertyDescriptor(m$1, k);
		if (!desc || ("get" in desc ? !m$1.__esModule : desc.writable || desc.configurable)) desc = {
			enumerable: true,
			get: function() {
				return m$1[k];
			}
		};
		Object.defineProperty(o, k2, desc);
	}) : (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		o[k2] = m$1[k];
	}));
	var __setModuleDefault$4 = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
		Object.defineProperty(o, "default", {
			enumerable: true,
			value: v
		});
	}) : function(o, v) {
		o["default"] = v;
	});
	var __importStar$4 = exports && exports.__importStar || (function() {
		var ownKeys = function(o) {
			ownKeys = Object.getOwnPropertyNames || function(o$1) {
				var ar = [];
				for (var k in o$1) if (Object.prototype.hasOwnProperty.call(o$1, k)) ar[ar.length] = k;
				return ar;
			};
			return ownKeys(o);
		};
		return function(mod) {
			if (mod && mod.__esModule) return mod;
			var result = {};
			if (mod != null) {
				for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding$5(result, mod, k[i]);
			}
			__setModuleDefault$4(result, mod);
			return result;
		};
	})();
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.restPositionals = restPositionals;
	const Result$4 = __importStar$4(require_Result());
	const types_1 = require_types();
	/**
	* Read all the positionals and decode them using the type provided.
	* Works best when it is the last item on the `command` construct, to be
	* used like the `...rest` operator in JS and TypeScript.
	*/
	function fullRestPositionals(config$1) {
		return {
			helpTopics() {
				var _a, _b, _c, _d;
				return [{
					usage: `[...${(_b = (_a = config$1.displayName) !== null && _a !== void 0 ? _a : config$1.type.displayName) !== null && _b !== void 0 ? _b : "arg"}]`,
					category: "arguments",
					defaults: [],
					description: (_d = (_c = config$1.description) !== null && _c !== void 0 ? _c : config$1.type.description) !== null && _d !== void 0 ? _d : ""
				}];
			},
			register(_opts) {},
			async parse({ nodes, visitedNodes }) {
				const positionals = nodes.filter((node) => node.type === "positionalArgument" && !visitedNodes.has(node));
				const results = [];
				const errors = [];
				for (const positional$1 of positionals) {
					visitedNodes.add(positional$1);
					const decoded = await Result$4.safeAsync(config$1.type.from(positional$1.raw));
					if (Result$4.isOk(decoded)) results.push(decoded.value);
					else errors.push({
						nodes: [positional$1],
						message: decoded.error.message
					});
				}
				if (errors.length > 0) return Result$4.err({ errors });
				return Result$4.ok(results);
			}
		};
	}
	function restPositionals(config$1) {
		return fullRestPositionals({
			type: types_1.string,
			...config$1
		});
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/multiflag.js
var require_multiflag = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/multiflag.js": ((exports) => {
	var __createBinding$4 = exports && exports.__createBinding || (Object.create ? (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		var desc = Object.getOwnPropertyDescriptor(m$1, k);
		if (!desc || ("get" in desc ? !m$1.__esModule : desc.writable || desc.configurable)) desc = {
			enumerable: true,
			get: function() {
				return m$1[k];
			}
		};
		Object.defineProperty(o, k2, desc);
	}) : (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		o[k2] = m$1[k];
	}));
	var __setModuleDefault$3 = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
		Object.defineProperty(o, "default", {
			enumerable: true,
			value: v
		});
	}) : function(o, v) {
		o["default"] = v;
	});
	var __importStar$3 = exports && exports.__importStar || (function() {
		var ownKeys = function(o) {
			ownKeys = Object.getOwnPropertyNames || function(o$1) {
				var ar = [];
				for (var k in o$1) if (Object.prototype.hasOwnProperty.call(o$1, k)) ar[ar.length] = k;
				return ar;
			};
			return ownKeys(o);
		};
		return function(mod) {
			if (mod && mod.__esModule) return mod;
			var result = {};
			if (mod != null) {
				for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding$4(result, mod, k[i]);
			}
			__setModuleDefault$3(result, mod);
			return result;
		};
	})();
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.multiflag = multiflag;
	const Result$3 = __importStar$3(require_Result());
	const flag_1$1 = require_flag();
	const findOption_1$1 = require_findOption();
	/**
	* Like `option`, but can accept multiple options, and expects a decoder from a list of strings.
	* An error will highlight all option occurences.
	*/
	function multiflag(config$1) {
		return {
			helpTopics() {
				var _a;
				let usage = `--${config$1.long}`;
				if (config$1.short) usage += `, -${config$1.short}`;
				return [{
					category: "flags",
					usage,
					defaults: [],
					description: (_a = config$1.description) !== null && _a !== void 0 ? _a : "self explanatory"
				}];
			},
			register(opts) {
				opts.forceFlagLongNames.add(config$1.long);
				if (config$1.short) opts.forceFlagShortNames.add(config$1.short);
			},
			async parse({ nodes, visitedNodes }) {
				var _a, _b;
				const options = (0, findOption_1$1.findOption)(nodes, {
					longNames: [config$1.long],
					shortNames: config$1.short ? [config$1.short] : []
				}).filter((x) => !visitedNodes.has(x));
				for (const option$1 of options) visitedNodes.add(option$1);
				const optionValues = [];
				const errors = [];
				for (const option$1 of options) {
					const decoded = await Result$3.safeAsync(flag_1$1.boolean.from((_b = (_a = option$1.value) === null || _a === void 0 ? void 0 : _a.node.raw) !== null && _b !== void 0 ? _b : "true"));
					if (Result$3.isErr(decoded)) errors.push({
						nodes: [option$1],
						message: decoded.error.message
					});
					else optionValues.push(decoded.value);
				}
				if (errors.length > 0) return Result$3.err({ errors });
				const multiDecoded = await Result$3.safeAsync(config$1.type.from(optionValues));
				if (Result$3.isErr(multiDecoded)) return Result$3.err({ errors: [{
					nodes: options,
					message: multiDecoded.error.message
				}] });
				return multiDecoded;
			}
		};
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/multioption.js
var require_multioption = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/multioption.js": ((exports) => {
	var __createBinding$3 = exports && exports.__createBinding || (Object.create ? (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		var desc = Object.getOwnPropertyDescriptor(m$1, k);
		if (!desc || ("get" in desc ? !m$1.__esModule : desc.writable || desc.configurable)) desc = {
			enumerable: true,
			get: function() {
				return m$1[k];
			}
		};
		Object.defineProperty(o, k2, desc);
	}) : (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		o[k2] = m$1[k];
	}));
	var __setModuleDefault$2 = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
		Object.defineProperty(o, "default", {
			enumerable: true,
			value: v
		});
	}) : function(o, v) {
		o["default"] = v;
	});
	var __importStar$2 = exports && exports.__importStar || (function() {
		var ownKeys = function(o) {
			ownKeys = Object.getOwnPropertyNames || function(o$1) {
				var ar = [];
				for (var k in o$1) if (Object.prototype.hasOwnProperty.call(o$1, k)) ar[ar.length] = k;
				return ar;
			};
			return ownKeys(o);
		};
		return function(mod) {
			if (mod && mod.__esModule) return mod;
			var result = {};
			if (mod != null) {
				for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding$3(result, mod, k[i]);
			}
			__setModuleDefault$2(result, mod);
			return result;
		};
	})();
	var __importDefault = exports && exports.__importDefault || function(mod) {
		return mod && mod.__esModule ? mod : { "default": mod };
	};
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.multioption = multioption;
	const chalk_1 = __importDefault((init_source(), __toCommonJS$3(source_exports)));
	const Result$2 = __importStar$2(require_Result());
	const findOption_1 = require_findOption();
	/**
	* Like `option`, but can accept multiple options, and expects a decoder from a list of strings.
	* An error will highlight all option occurences.
	*/
	function multioption(config$1) {
		return {
			helpTopics() {
				var _a, _b;
				const displayName = (_a = config$1.type.displayName) !== null && _a !== void 0 ? _a : "value";
				let usage = `--${config$1.long} <${displayName}>`;
				if (config$1.short) usage += `, -${config$1.short}=<${displayName}>`;
				const defaults = [];
				if (config$1.defaultValue) try {
					const defaultValue = config$1.defaultValue();
					if (config$1.defaultValueIsSerializable) defaults.push(`default: ${chalk_1.default.italic(defaultValue)}`);
					else defaults.push("[...optional]");
				} catch (e) {}
				else if (config$1.type.defaultValue) try {
					const defaultValue = config$1.type.defaultValue();
					if (config$1.type.defaultValueIsSerializable) defaults.push(`default: ${chalk_1.default.italic(defaultValue)}`);
					else defaults.push("[...optional]");
				} catch (e) {}
				else if (config$1.onMissing || config$1.type.onMissing) defaults.push("[...optional]");
				return [{
					category: "options",
					usage,
					defaults,
					description: (_b = config$1.description) !== null && _b !== void 0 ? _b : "self explanatory"
				}];
			},
			register(opts) {
				opts.forceOptionLongNames.add(config$1.long);
				if (config$1.short) opts.forceOptionShortNames.add(config$1.short);
			},
			async parse({ nodes, visitedNodes }) {
				var _a;
				const options = (0, findOption_1.findOption)(nodes, {
					longNames: [config$1.long],
					shortNames: config$1.short ? [config$1.short] : []
				}).filter((x) => !visitedNodes.has(x));
				const defaultValueFn = config$1.defaultValue || config$1.type.defaultValue;
				const onMissingFn = config$1.onMissing || config$1.type.onMissing;
				if (options.length === 0 && defaultValueFn) try {
					const defaultValue = defaultValueFn();
					return Result$2.ok(defaultValue);
				} catch (e) {
					const message = `Failed to resolve default value for '--${config$1.long}': ${e.message}`;
					return Result$2.err({ errors: [{
						nodes: [],
						message
					}] });
				}
				else if (options.length === 0 && onMissingFn) try {
					const missingValue = await onMissingFn();
					return Result$2.ok(missingValue);
				} catch (e) {
					const message = `Failed to get missing value for '--${config$1.long}': ${e.message}`;
					return Result$2.err({ errors: [{
						nodes: [],
						message
					}] });
				}
				for (const option$1 of options) visitedNodes.add(option$1);
				const optionValues = [];
				const errors = [];
				const flagNodes = [];
				for (const option$1 of options) {
					const providedValue = (_a = option$1.value) === null || _a === void 0 ? void 0 : _a.node.raw;
					if (providedValue === void 0) {
						flagNodes.push(option$1);
						continue;
					}
					optionValues.push(providedValue);
				}
				if (flagNodes.length > 0) errors.push({
					nodes: flagNodes,
					message: "Expected to get a value, found a flag"
				});
				if (errors.length > 0) return Result$2.err({ errors });
				const multiDecoded = await Result$2.safeAsync(config$1.type.from(optionValues));
				if (Result$2.isErr(multiDecoded)) return Result$2.err({ errors: [{
					nodes: options,
					message: multiDecoded.error.message
				}] });
				return multiDecoded;
			}
		};
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/union.js
var require_union = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/union.js": ((exports) => {
	var __createBinding$2 = exports && exports.__createBinding || (Object.create ? (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		var desc = Object.getOwnPropertyDescriptor(m$1, k);
		if (!desc || ("get" in desc ? !m$1.__esModule : desc.writable || desc.configurable)) desc = {
			enumerable: true,
			get: function() {
				return m$1[k];
			}
		};
		Object.defineProperty(o, k2, desc);
	}) : (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		o[k2] = m$1[k];
	}));
	var __setModuleDefault$1 = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
		Object.defineProperty(o, "default", {
			enumerable: true,
			value: v
		});
	}) : function(o, v) {
		o["default"] = v;
	});
	var __importStar$1 = exports && exports.__importStar || (function() {
		var ownKeys = function(o) {
			ownKeys = Object.getOwnPropertyNames || function(o$1) {
				var ar = [];
				for (var k in o$1) if (Object.prototype.hasOwnProperty.call(o$1, k)) ar[ar.length] = k;
				return ar;
			};
			return ownKeys(o);
		};
		return function(mod) {
			if (mod && mod.__esModule) return mod;
			var result = {};
			if (mod != null) {
				for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding$2(result, mod, k[i]);
			}
			__setModuleDefault$1(result, mod);
			return result;
		};
	})();
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.union = union;
	const Result$1 = __importStar$1(require_Result());
	const type_1$1 = require_type();
	/**
	* Take one of the types. Merge the metadata from left to right.
	* If nothing matches, prints all the errors.
	*/
	function union(ts, { combineErrors = (errors) => errors.join("\n") } = {}) {
		return {
			...Object.assign({}, ...ts.map((x) => (0, type_1$1.typeDef)(x))),
			async from(input) {
				const errors = [];
				for (const t of ts) {
					const decoded = await Result$1.safeAsync((0, type_1$1.fromFn)(t)(input));
					if (Result$1.isOk(decoded)) return decoded.value;
					errors.push(decoded.error.message);
				}
				throw new Error(combineErrors(errors));
			}
		};
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/oneOf.js
var require_oneOf = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/oneOf.js": ((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.oneOf = oneOf;
	const node_util_1 = __require("node:util");
	/**
	* A union of literals. When you want to take an exact enum value.
	*/
	function oneOf(literals) {
		const examples = literals.map((x) => (0, node_util_1.inspect)(x)).join(", ");
		return {
			async from(str) {
				const value = literals.find((x) => x === str);
				if (!value) throw new Error(`Invalid value '${str}'. Expected one of: ${examples}`);
				return value;
			},
			description: `One of ${examples}`
		};
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/rest.js
var require_rest = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/rest.js": ((exports) => {
	var __createBinding$1 = exports && exports.__createBinding || (Object.create ? (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		var desc = Object.getOwnPropertyDescriptor(m$1, k);
		if (!desc || ("get" in desc ? !m$1.__esModule : desc.writable || desc.configurable)) desc = {
			enumerable: true,
			get: function() {
				return m$1[k];
			}
		};
		Object.defineProperty(o, k2, desc);
	}) : (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		o[k2] = m$1[k];
	}));
	var __setModuleDefault = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
		Object.defineProperty(o, "default", {
			enumerable: true,
			value: v
		});
	}) : function(o, v) {
		o["default"] = v;
	});
	var __importStar = exports && exports.__importStar || (function() {
		var ownKeys = function(o) {
			ownKeys = Object.getOwnPropertyNames || function(o$1) {
				var ar = [];
				for (var k in o$1) if (Object.prototype.hasOwnProperty.call(o$1, k)) ar[ar.length] = k;
				return ar;
			};
			return ownKeys(o);
		};
		return function(mod) {
			if (mod && mod.__esModule) return mod;
			var result = {};
			if (mod != null) {
				for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding$1(result, mod, k[i]);
			}
			__setModuleDefault(result, mod);
			return result;
		};
	})();
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.rest = rest;
	const Result = __importStar(require_Result());
	function rest(config$1) {
		return {
			helpTopics() {
				var _a, _b;
				return [{
					usage: `[...${(_a = config$1 === null || config$1 === void 0 ? void 0 : config$1.displayName) !== null && _a !== void 0 ? _a : "arg"}]`,
					category: "arguments",
					defaults: [],
					description: (_b = config$1 === null || config$1 === void 0 ? void 0 : config$1.description) !== null && _b !== void 0 ? _b : "catches the rest of the values"
				}];
			},
			register() {},
			async parse(context) {
				const visitedNodeIndices = [...context.visitedNodes].map((x) => context.nodes.indexOf(x)).filter((x) => x > -1);
				const strings = [];
				const maxIndex = Math.max(-1, ...visitedNodeIndices);
				const restItems = context.nodes.slice(maxIndex + 1);
				for (const node of restItems) switch (node.type) {
					case "positionalArgument":
						strings.push(node.raw);
						context.visitedNodes.add(node);
						break;
					case "longOption":
						strings.push(...getOriginal(node));
						context.visitedNodes.add(node);
						break;
					case "shortOption":
						strings.push(...getOriginal(node));
						context.visitedNodes.add(node);
						break;
					case "forcePositional":
						strings.push(node.raw);
						context.visitedNodes.add(node);
						break;
					case "shortOptions": {
						const last = node.options.at(-1);
						context.visitedNodes.add(node);
						strings.push(...getOriginal({
							...node,
							value: last === null || last === void 0 ? void 0 : last.value
						}));
						break;
					}
				}
				return Result.ok(strings);
			}
		};
	}
	function getOriginal(node) {
		if (!node.value) return [node.raw];
		if (node.value.delimiter.raw === " ") return [node.raw.slice(0, node.value.index - node.index), node.value.node.raw];
		return [node.raw];
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/index.js
var require_cjs = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cmd-ts@0.15.0/node_modules/cmd-ts/dist/cjs/index.js": ((exports) => {
	/**
	* The index module: the entrance to the world of cmd-ts 😎
	*
	* @packageDocumentation
	*/
	var __createBinding = exports && exports.__createBinding || (Object.create ? (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		var desc = Object.getOwnPropertyDescriptor(m$1, k);
		if (!desc || ("get" in desc ? !m$1.__esModule : desc.writable || desc.configurable)) desc = {
			enumerable: true,
			get: function() {
				return m$1[k];
			}
		};
		Object.defineProperty(o, k2, desc);
	}) : (function(o, m$1, k, k2) {
		if (k2 === void 0) k2 = k;
		o[k2] = m$1[k];
	}));
	var __exportStar = exports && exports.__exportStar || function(m$1, exports$1) {
		for (var p in m$1) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports$1, p)) __createBinding(exports$1, m$1, p);
	};
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.rest = exports.oneOf = exports.union = exports.multioption = exports.multiflag = exports.restPositionals = exports.parse = exports.run = exports.runSafely = exports.dryRun = exports.positional = exports.option = exports.defaultHelpFormatter = exports.resetHelpFormatter = exports.setDefaultHelpFormatter = exports.flag = exports.command = exports.binary = exports.extendType = exports.subcommands = void 0;
	var subcommands_1 = require_subcommands();
	Object.defineProperty(exports, "subcommands", {
		enumerable: true,
		get: function() {
			return subcommands_1.subcommands;
		}
	});
	var type_1 = require_type();
	Object.defineProperty(exports, "extendType", {
		enumerable: true,
		get: function() {
			return type_1.extendType;
		}
	});
	__exportStar(require_types(), exports);
	var binary_1 = require_binary();
	Object.defineProperty(exports, "binary", {
		enumerable: true,
		get: function() {
			return binary_1.binary;
		}
	});
	var command_1 = require_command();
	Object.defineProperty(exports, "command", {
		enumerable: true,
		get: function() {
			return command_1.command;
		}
	});
	var flag_1 = require_flag();
	Object.defineProperty(exports, "flag", {
		enumerable: true,
		get: function() {
			return flag_1.flag;
		}
	});
	var helpFormatter_1 = require_helpFormatter();
	Object.defineProperty(exports, "setDefaultHelpFormatter", {
		enumerable: true,
		get: function() {
			return helpFormatter_1.setDefaultHelpFormatter;
		}
	});
	Object.defineProperty(exports, "resetHelpFormatter", {
		enumerable: true,
		get: function() {
			return helpFormatter_1.resetHelpFormatter;
		}
	});
	Object.defineProperty(exports, "defaultHelpFormatter", {
		enumerable: true,
		get: function() {
			return helpFormatter_1.defaultHelpFormatter;
		}
	});
	var option_1 = require_option();
	Object.defineProperty(exports, "option", {
		enumerable: true,
		get: function() {
			return option_1.option;
		}
	});
	var positional_1 = require_positional();
	Object.defineProperty(exports, "positional", {
		enumerable: true,
		get: function() {
			return positional_1.positional;
		}
	});
	var runner_1 = require_runner();
	Object.defineProperty(exports, "dryRun", {
		enumerable: true,
		get: function() {
			return runner_1.dryRun;
		}
	});
	Object.defineProperty(exports, "runSafely", {
		enumerable: true,
		get: function() {
			return runner_1.runSafely;
		}
	});
	Object.defineProperty(exports, "run", {
		enumerable: true,
		get: function() {
			return runner_1.run;
		}
	});
	Object.defineProperty(exports, "parse", {
		enumerable: true,
		get: function() {
			return runner_1.parse;
		}
	});
	var restPositionals_1 = require_restPositionals();
	Object.defineProperty(exports, "restPositionals", {
		enumerable: true,
		get: function() {
			return restPositionals_1.restPositionals;
		}
	});
	var multiflag_1 = require_multiflag();
	Object.defineProperty(exports, "multiflag", {
		enumerable: true,
		get: function() {
			return multiflag_1.multiflag;
		}
	});
	var multioption_1 = require_multioption();
	Object.defineProperty(exports, "multioption", {
		enumerable: true,
		get: function() {
			return multioption_1.multioption;
		}
	});
	var union_1 = require_union();
	Object.defineProperty(exports, "union", {
		enumerable: true,
		get: function() {
			return union_1.union;
		}
	});
	var oneOf_1 = require_oneOf();
	Object.defineProperty(exports, "oneOf", {
		enumerable: true,
		get: function() {
			return oneOf_1.oneOf;
		}
	});
	var rest_1 = require_rest();
	Object.defineProperty(exports, "rest", {
		enumerable: true,
		get: function() {
			return rest_1.rest;
		}
	});
}) });

//#endregion
//#region src/args/runtime.ts
var import_cjs$30 = /* @__PURE__ */ __toESM(require_cjs());
const runtimeType = {
	...import_cjs$30.oneOf([
		"node22",
		"node24",
		"node26",
		"python3.13"
	]),
	displayName: "runtime"
};
const runtime = import_cjs$30.option({
	long: "runtime",
	type: import_cjs$30.optional(runtimeType)
});

//#endregion
//#region ../../node_modules/.pnpm/ms@2.1.3/node_modules/ms/index.js
var require_ms = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/ms@2.1.3/node_modules/ms/index.js": ((exports, module) => {
	/**
	* Helpers.
	*/
	var s = 1e3;
	var m = s * 60;
	var h = m * 60;
	var d = h * 24;
	var w = d * 7;
	var y = d * 365.25;
	/**
	* Parse or format the given `val`.
	*
	* Options:
	*
	*  - `long` verbose formatting [false]
	*
	* @param {String|Number} val
	* @param {Object} [options]
	* @throws {Error} throw an error if val is not a non-empty string or a number
	* @return {String|Number}
	* @api public
	*/
	module.exports = function(val, options) {
		options = options || {};
		var type = typeof val;
		if (type === "string" && val.length > 0) return parse(val);
		else if (type === "number" && isFinite(val)) return options.long ? fmtLong(val) : fmtShort(val);
		throw new Error("val is not a non-empty string or a valid number. val=" + JSON.stringify(val));
	};
	/**
	* Parse the given `str` and return milliseconds.
	*
	* @param {String} str
	* @return {Number}
	* @api private
	*/
	function parse(str) {
		str = String(str);
		if (str.length > 100) return;
		var match$1 = /^(-?(?:\d+)?\.?\d+) *(milliseconds?|msecs?|ms|seconds?|secs?|s|minutes?|mins?|m|hours?|hrs?|h|days?|d|weeks?|w|years?|yrs?|y)?$/i.exec(str);
		if (!match$1) return;
		var n = parseFloat(match$1[1]);
		switch ((match$1[2] || "ms").toLowerCase()) {
			case "years":
			case "year":
			case "yrs":
			case "yr":
			case "y": return n * y;
			case "weeks":
			case "week":
			case "w": return n * w;
			case "days":
			case "day":
			case "d": return n * d;
			case "hours":
			case "hour":
			case "hrs":
			case "hr":
			case "h": return n * h;
			case "minutes":
			case "minute":
			case "mins":
			case "min":
			case "m": return n * m;
			case "seconds":
			case "second":
			case "secs":
			case "sec":
			case "s": return n * s;
			case "milliseconds":
			case "millisecond":
			case "msecs":
			case "msec":
			case "ms": return n;
			default: return;
		}
	}
	/**
	* Short format for `ms`.
	*
	* @param {Number} ms
	* @return {String}
	* @api private
	*/
	function fmtShort(ms$7) {
		var msAbs = Math.abs(ms$7);
		if (msAbs >= d) return Math.round(ms$7 / d) + "d";
		if (msAbs >= h) return Math.round(ms$7 / h) + "h";
		if (msAbs >= m) return Math.round(ms$7 / m) + "m";
		if (msAbs >= s) return Math.round(ms$7 / s) + "s";
		return ms$7 + "ms";
	}
	/**
	* Long format for `ms`.
	*
	* @param {Number} ms
	* @return {String}
	* @api private
	*/
	function fmtLong(ms$7) {
		var msAbs = Math.abs(ms$7);
		if (msAbs >= d) return plural(ms$7, msAbs, d, "day");
		if (msAbs >= h) return plural(ms$7, msAbs, h, "hour");
		if (msAbs >= m) return plural(ms$7, msAbs, m, "minute");
		if (msAbs >= s) return plural(ms$7, msAbs, s, "second");
		return ms$7 + " ms";
	}
	/**
	* Pluralization helper.
	*/
	function plural(ms$7, msAbs, n, name) {
		var isPlural = msAbs >= n * 1.5;
		return Math.round(ms$7 / n) + " " + name + (isPlural ? "s" : "");
	}
}) });

//#endregion
//#region src/types/duration.ts
var import_cjs$29 = require_cjs();
init_source();
const Duration = (0, import_cjs$29.extendType)(import_cjs$29.string, {
	displayName: "num UNIT",
	description: "A duration, e.g. 5m, 10s, 1h",
	async from(string$2) {
		const match$1 = string$2.match(/^(\d+) ?(ms|milliseconds?|msecs?|s(?:econds?)?|m(?:inutes?)?|h(?:ours?)?|d(?:ays?)?)?$/);
		if (!match$1) throw new Error([
			`Malformed duration: "${string$2}".`,
			`${source_default.bold("hint:")} Use a number followed by a unit: s (seconds), m (minutes), h (hours), d (days).`,
			"╰▶ Examples: 30s, 5m, 2h, 1d"
		].join("\n"));
		return match$1[0];
	}
});

//#endregion
//#region src/args/timeout.ts
var import_cjs$28 = /* @__PURE__ */ __toESM(require_cjs());
const timeout = import_cjs$28.option({
	long: "timeout",
	type: Duration,
	description: "The maximum duration a sandbox can run for. Example: 5m, 30m",
	defaultValue: () => "5 minutes",
	defaultValueIsSerializable: true
});

//#endregion
//#region src/args/vcpus.ts
var import_cjs$27 = /* @__PURE__ */ __toESM(require_cjs());
const vcpusType = import_cjs$27.extendType(import_cjs$27.number, {
	displayName: "COUNT",
	async from(n) {
		if (!Number.isInteger(n) || n < 1) throw new Error(`Invalid vCPU count: ${n}. Must be a positive integer.`);
		return n;
	}
});
const vcpus = import_cjs$27.option({
	long: "vcpus",
	type: import_cjs$27.optional(vcpusType),
	description: "Number of vCPUs to allocate (each vCPU includes 2048 MB of memory)"
});

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/locale/en-US/_lib/formatDistance.js
const formatDistanceLocale = {
	lessThanXSeconds: {
		one: "less than a second",
		other: "less than {{count}} seconds"
	},
	xSeconds: {
		one: "1 second",
		other: "{{count}} seconds"
	},
	halfAMinute: "half a minute",
	lessThanXMinutes: {
		one: "less than a minute",
		other: "less than {{count}} minutes"
	},
	xMinutes: {
		one: "1 minute",
		other: "{{count}} minutes"
	},
	aboutXHours: {
		one: "about 1 hour",
		other: "about {{count}} hours"
	},
	xHours: {
		one: "1 hour",
		other: "{{count}} hours"
	},
	xDays: {
		one: "1 day",
		other: "{{count}} days"
	},
	aboutXWeeks: {
		one: "about 1 week",
		other: "about {{count}} weeks"
	},
	xWeeks: {
		one: "1 week",
		other: "{{count}} weeks"
	},
	aboutXMonths: {
		one: "about 1 month",
		other: "about {{count}} months"
	},
	xMonths: {
		one: "1 month",
		other: "{{count}} months"
	},
	aboutXYears: {
		one: "about 1 year",
		other: "about {{count}} years"
	},
	xYears: {
		one: "1 year",
		other: "{{count}} years"
	},
	overXYears: {
		one: "over 1 year",
		other: "over {{count}} years"
	},
	almostXYears: {
		one: "almost 1 year",
		other: "almost {{count}} years"
	}
};
const formatDistance = (token$1, count, options) => {
	let result;
	const tokenValue = formatDistanceLocale[token$1];
	if (typeof tokenValue === "string") result = tokenValue;
	else if (count === 1) result = tokenValue.one;
	else result = tokenValue.other.replace("{{count}}", count.toString());
	if (options?.addSuffix) if (options.comparison && options.comparison > 0) return "in " + result;
	else return result + " ago";
	return result;
};

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/locale/_lib/buildFormatLongFn.js
function buildFormatLongFn(args$5) {
	return (options = {}) => {
		const width = options.width ? String(options.width) : args$5.defaultWidth;
		return args$5.formats[width] || args$5.formats[args$5.defaultWidth];
	};
}

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/locale/en-US/_lib/formatLong.js
const dateFormats = {
	full: "EEEE, MMMM do, y",
	long: "MMMM do, y",
	medium: "MMM d, y",
	short: "MM/dd/yyyy"
};
const timeFormats = {
	full: "h:mm:ss a zzzz",
	long: "h:mm:ss a z",
	medium: "h:mm:ss a",
	short: "h:mm a"
};
const dateTimeFormats = {
	full: "{{date}} 'at' {{time}}",
	long: "{{date}} 'at' {{time}}",
	medium: "{{date}}, {{time}}",
	short: "{{date}}, {{time}}"
};
const formatLong = {
	date: buildFormatLongFn({
		formats: dateFormats,
		defaultWidth: "full"
	}),
	time: buildFormatLongFn({
		formats: timeFormats,
		defaultWidth: "full"
	}),
	dateTime: buildFormatLongFn({
		formats: dateTimeFormats,
		defaultWidth: "full"
	})
};

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/locale/en-US/_lib/formatRelative.js
const formatRelativeLocale = {
	lastWeek: "'last' eeee 'at' p",
	yesterday: "'yesterday at' p",
	today: "'today at' p",
	tomorrow: "'tomorrow at' p",
	nextWeek: "eeee 'at' p",
	other: "P"
};
const formatRelative = (token$1, _date, _baseDate, _options$1) => formatRelativeLocale[token$1];

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/locale/_lib/buildLocalizeFn.js
/**
* The localize function argument callback which allows to convert raw value to
* the actual type.
*
* @param value - The value to convert
*
* @returns The converted value
*/
/**
* The map of localized values for each width.
*/
/**
* The index type of the locale unit value. It types conversion of units of
* values that don't start at 0 (i.e. quarters).
*/
/**
* Converts the unit value to the tuple of values.
*/
/**
* The tuple of localized era values. The first element represents BC,
* the second element represents AD.
*/
/**
* The tuple of localized quarter values. The first element represents Q1.
*/
/**
* The tuple of localized day values. The first element represents Sunday.
*/
/**
* The tuple of localized month values. The first element represents January.
*/
function buildLocalizeFn(args$5) {
	return (value, options) => {
		const context = options?.context ? String(options.context) : "standalone";
		let valuesArray;
		if (context === "formatting" && args$5.formattingValues) {
			const defaultWidth = args$5.defaultFormattingWidth || args$5.defaultWidth;
			const width = options?.width ? String(options.width) : defaultWidth;
			valuesArray = args$5.formattingValues[width] || args$5.formattingValues[defaultWidth];
		} else {
			const defaultWidth = args$5.defaultWidth;
			const width = options?.width ? String(options.width) : args$5.defaultWidth;
			valuesArray = args$5.values[width] || args$5.values[defaultWidth];
		}
		const index = args$5.argumentCallback ? args$5.argumentCallback(value) : value;
		return valuesArray[index];
	};
}

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/locale/en-US/_lib/localize.js
const eraValues = {
	narrow: ["B", "A"],
	abbreviated: ["BC", "AD"],
	wide: ["Before Christ", "Anno Domini"]
};
const quarterValues = {
	narrow: [
		"1",
		"2",
		"3",
		"4"
	],
	abbreviated: [
		"Q1",
		"Q2",
		"Q3",
		"Q4"
	],
	wide: [
		"1st quarter",
		"2nd quarter",
		"3rd quarter",
		"4th quarter"
	]
};
const monthValues = {
	narrow: [
		"J",
		"F",
		"M",
		"A",
		"M",
		"J",
		"J",
		"A",
		"S",
		"O",
		"N",
		"D"
	],
	abbreviated: [
		"Jan",
		"Feb",
		"Mar",
		"Apr",
		"May",
		"Jun",
		"Jul",
		"Aug",
		"Sep",
		"Oct",
		"Nov",
		"Dec"
	],
	wide: [
		"January",
		"February",
		"March",
		"April",
		"May",
		"June",
		"July",
		"August",
		"September",
		"October",
		"November",
		"December"
	]
};
const dayValues = {
	narrow: [
		"S",
		"M",
		"T",
		"W",
		"T",
		"F",
		"S"
	],
	short: [
		"Su",
		"Mo",
		"Tu",
		"We",
		"Th",
		"Fr",
		"Sa"
	],
	abbreviated: [
		"Sun",
		"Mon",
		"Tue",
		"Wed",
		"Thu",
		"Fri",
		"Sat"
	],
	wide: [
		"Sunday",
		"Monday",
		"Tuesday",
		"Wednesday",
		"Thursday",
		"Friday",
		"Saturday"
	]
};
const dayPeriodValues = {
	narrow: {
		am: "a",
		pm: "p",
		midnight: "mi",
		noon: "n",
		morning: "morning",
		afternoon: "afternoon",
		evening: "evening",
		night: "night"
	},
	abbreviated: {
		am: "AM",
		pm: "PM",
		midnight: "midnight",
		noon: "noon",
		morning: "morning",
		afternoon: "afternoon",
		evening: "evening",
		night: "night"
	},
	wide: {
		am: "a.m.",
		pm: "p.m.",
		midnight: "midnight",
		noon: "noon",
		morning: "morning",
		afternoon: "afternoon",
		evening: "evening",
		night: "night"
	}
};
const formattingDayPeriodValues = {
	narrow: {
		am: "a",
		pm: "p",
		midnight: "mi",
		noon: "n",
		morning: "in the morning",
		afternoon: "in the afternoon",
		evening: "in the evening",
		night: "at night"
	},
	abbreviated: {
		am: "AM",
		pm: "PM",
		midnight: "midnight",
		noon: "noon",
		morning: "in the morning",
		afternoon: "in the afternoon",
		evening: "in the evening",
		night: "at night"
	},
	wide: {
		am: "a.m.",
		pm: "p.m.",
		midnight: "midnight",
		noon: "noon",
		morning: "in the morning",
		afternoon: "in the afternoon",
		evening: "in the evening",
		night: "at night"
	}
};
const ordinalNumber = (dirtyNumber, _options$1) => {
	const number = Number(dirtyNumber);
	const rem100 = number % 100;
	if (rem100 > 20 || rem100 < 10) switch (rem100 % 10) {
		case 1: return number + "st";
		case 2: return number + "nd";
		case 3: return number + "rd";
	}
	return number + "th";
};
const localize = {
	ordinalNumber,
	era: buildLocalizeFn({
		values: eraValues,
		defaultWidth: "wide"
	}),
	quarter: buildLocalizeFn({
		values: quarterValues,
		defaultWidth: "wide",
		argumentCallback: (quarter) => quarter - 1
	}),
	month: buildLocalizeFn({
		values: monthValues,
		defaultWidth: "wide"
	}),
	day: buildLocalizeFn({
		values: dayValues,
		defaultWidth: "wide"
	}),
	dayPeriod: buildLocalizeFn({
		values: dayPeriodValues,
		defaultWidth: "wide",
		formattingValues: formattingDayPeriodValues,
		defaultFormattingWidth: "wide"
	})
};

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/locale/_lib/buildMatchFn.js
function buildMatchFn(args$5) {
	return (string$2, options = {}) => {
		const width = options.width;
		const matchPattern = width && args$5.matchPatterns[width] || args$5.matchPatterns[args$5.defaultMatchWidth];
		const matchResult = string$2.match(matchPattern);
		if (!matchResult) return null;
		const matchedString = matchResult[0];
		const parsePatterns = width && args$5.parsePatterns[width] || args$5.parsePatterns[args$5.defaultParseWidth];
		const key = Array.isArray(parsePatterns) ? findIndex(parsePatterns, (pattern) => pattern.test(matchedString)) : findKey(parsePatterns, (pattern) => pattern.test(matchedString));
		let value;
		value = args$5.valueCallback ? args$5.valueCallback(key) : key;
		value = options.valueCallback ? options.valueCallback(value) : value;
		const rest$1 = string$2.slice(matchedString.length);
		return {
			value,
			rest: rest$1
		};
	};
}
function findKey(object, predicate) {
	for (const key in object) if (Object.prototype.hasOwnProperty.call(object, key) && predicate(object[key])) return key;
}
function findIndex(array$1, predicate) {
	for (let key = 0; key < array$1.length; key++) if (predicate(array$1[key])) return key;
}

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/locale/_lib/buildMatchPatternFn.js
function buildMatchPatternFn(args$5) {
	return (string$2, options = {}) => {
		const matchResult = string$2.match(args$5.matchPattern);
		if (!matchResult) return null;
		const matchedString = matchResult[0];
		const parseResult = string$2.match(args$5.parsePattern);
		if (!parseResult) return null;
		let value = args$5.valueCallback ? args$5.valueCallback(parseResult[0]) : parseResult[0];
		value = options.valueCallback ? options.valueCallback(value) : value;
		const rest$1 = string$2.slice(matchedString.length);
		return {
			value,
			rest: rest$1
		};
	};
}

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/locale/en-US/_lib/match.js
const matchOrdinalNumberPattern = /^(\d+)(th|st|nd|rd)?/i;
const parseOrdinalNumberPattern = /\d+/i;
const matchEraPatterns = {
	narrow: /^(b|a)/i,
	abbreviated: /^(b\.?\s?c\.?|b\.?\s?c\.?\s?e\.?|a\.?\s?d\.?|c\.?\s?e\.?)/i,
	wide: /^(before christ|before common era|anno domini|common era)/i
};
const parseEraPatterns = { any: [/^b/i, /^(a|c)/i] };
const matchQuarterPatterns = {
	narrow: /^[1234]/i,
	abbreviated: /^q[1234]/i,
	wide: /^[1234](th|st|nd|rd)? quarter/i
};
const parseQuarterPatterns = { any: [
	/1/i,
	/2/i,
	/3/i,
	/4/i
] };
const matchMonthPatterns = {
	narrow: /^[jfmasond]/i,
	abbreviated: /^(jan|feb|mar|apr|may|jun|jul|aug|sep|oct|nov|dec)/i,
	wide: /^(january|february|march|april|may|june|july|august|september|october|november|december)/i
};
const parseMonthPatterns = {
	narrow: [
		/^j/i,
		/^f/i,
		/^m/i,
		/^a/i,
		/^m/i,
		/^j/i,
		/^j/i,
		/^a/i,
		/^s/i,
		/^o/i,
		/^n/i,
		/^d/i
	],
	any: [
		/^ja/i,
		/^f/i,
		/^mar/i,
		/^ap/i,
		/^may/i,
		/^jun/i,
		/^jul/i,
		/^au/i,
		/^s/i,
		/^o/i,
		/^n/i,
		/^d/i
	]
};
const matchDayPatterns = {
	narrow: /^[smtwf]/i,
	short: /^(su|mo|tu|we|th|fr|sa)/i,
	abbreviated: /^(sun|mon|tue|wed|thu|fri|sat)/i,
	wide: /^(sunday|monday|tuesday|wednesday|thursday|friday|saturday)/i
};
const parseDayPatterns = {
	narrow: [
		/^s/i,
		/^m/i,
		/^t/i,
		/^w/i,
		/^t/i,
		/^f/i,
		/^s/i
	],
	any: [
		/^su/i,
		/^m/i,
		/^tu/i,
		/^w/i,
		/^th/i,
		/^f/i,
		/^sa/i
	]
};
const matchDayPeriodPatterns = {
	narrow: /^(a|p|mi|n|(in the|at) (morning|afternoon|evening|night))/i,
	any: /^([ap]\.?\s?m\.?|midnight|noon|(in the|at) (morning|afternoon|evening|night))/i
};
const parseDayPeriodPatterns = { any: {
	am: /^a/i,
	pm: /^p/i,
	midnight: /^mi/i,
	noon: /^no/i,
	morning: /morning/i,
	afternoon: /afternoon/i,
	evening: /evening/i,
	night: /night/i
} };
const match = {
	ordinalNumber: buildMatchPatternFn({
		matchPattern: matchOrdinalNumberPattern,
		parsePattern: parseOrdinalNumberPattern,
		valueCallback: (value) => parseInt(value, 10)
	}),
	era: buildMatchFn({
		matchPatterns: matchEraPatterns,
		defaultMatchWidth: "wide",
		parsePatterns: parseEraPatterns,
		defaultParseWidth: "any"
	}),
	quarter: buildMatchFn({
		matchPatterns: matchQuarterPatterns,
		defaultMatchWidth: "wide",
		parsePatterns: parseQuarterPatterns,
		defaultParseWidth: "any",
		valueCallback: (index) => index + 1
	}),
	month: buildMatchFn({
		matchPatterns: matchMonthPatterns,
		defaultMatchWidth: "wide",
		parsePatterns: parseMonthPatterns,
		defaultParseWidth: "any"
	}),
	day: buildMatchFn({
		matchPatterns: matchDayPatterns,
		defaultMatchWidth: "wide",
		parsePatterns: parseDayPatterns,
		defaultParseWidth: "any"
	}),
	dayPeriod: buildMatchFn({
		matchPatterns: matchDayPeriodPatterns,
		defaultMatchWidth: "any",
		parsePatterns: parseDayPeriodPatterns,
		defaultParseWidth: "any"
	})
};

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/locale/en-US.js
/**
* @category Locales
* @summary English locale (United States).
* @language English
* @iso-639-2 eng
* @author Sasha Koss [@kossnocorp](https://github.com/kossnocorp)
* @author Lesha Koss [@leshakoss](https://github.com/leshakoss)
*/
const enUS = {
	code: "en-US",
	formatDistance,
	formatLong,
	formatRelative,
	localize,
	match,
	options: {
		weekStartsOn: 0,
		firstWeekContainsDate: 1
	}
};

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/_lib/defaultOptions.js
let defaultOptions = {};
function getDefaultOptions() {
	return defaultOptions;
}

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/_lib/getRoundingMethod.js
function getRoundingMethod(method) {
	return (number) => {
		const result = (method ? Math[method] : Math.trunc)(number);
		return result === 0 ? 0 : result;
	};
}

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/constants.js
/**
* @constant
* @name daysInYear
* @summary Days in 1 year.
*
* @description
* How many days in a year.
*
* One years equals 365.2425 days according to the formula:
*
* > Leap year occurs every 4 years, except for years that are divisible by 100 and not divisible by 400.
* > 1 mean year = (365+1/4-1/100+1/400) days = 365.2425 days
*/
const daysInYear = 365.2425;
/**
* @constant
* @name maxTime
* @summary Maximum allowed time.
*
* @example
* import { maxTime } from "./constants/date-fns/constants";
*
* const isValid = 8640000000000001 <= maxTime;
* //=> false
*
* new Date(8640000000000001);
* //=> Invalid Date
*/
const maxTime = Math.pow(10, 8) * 24 * 60 * 60 * 1e3;
/**
* @constant
* @name millisecondsInMinute
* @summary Milliseconds in 1 minute
*/
const millisecondsInMinute = 6e4;
/**
* @constant
* @name minutesInYear
* @summary Minutes in 1 year.
*/
const minutesInYear = 525600;
/**
* @constant
* @name minutesInMonth
* @summary Minutes in 1 month.
*/
const minutesInMonth = 43200;
/**
* @constant
* @name minutesInDay
* @summary Minutes in 1 day.
*/
const minutesInDay = 1440;
/**
* @constant
* @name secondsInHour
* @summary Seconds in 1 hour.
*/
const secondsInHour = 3600;
/**
* @constant
* @name secondsInDay
* @summary Seconds in 1 day.
*/
const secondsInDay = secondsInHour * 24;
/**
* @constant
* @name secondsInWeek
* @summary Seconds in 1 week.
*/
const secondsInWeek = secondsInDay * 7;
/**
* @constant
* @name secondsInYear
* @summary Seconds in 1 year.
*/
const secondsInYear = secondsInDay * daysInYear;
/**
* @constant
* @name secondsInMonth
* @summary Seconds in 1 month
*/
const secondsInMonth = secondsInYear / 12;
/**
* @constant
* @name secondsInQuarter
* @summary Seconds in 1 quarter.
*/
const secondsInQuarter = secondsInMonth * 3;
/**
* @constant
* @name constructFromSymbol
* @summary Symbol enabling Date extensions to inherit properties from the reference date.
*
* The symbol is used to enable the `constructFrom` function to construct a date
* using a reference date and a value. It allows to transfer extra properties
* from the reference date to the new date. It's useful for extensions like
* [`TZDate`](https://github.com/date-fns/tz) that accept a time zone as
* a constructor argument.
*/
const constructFromSymbol = Symbol.for("constructDateFrom");

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/constructFrom.js
/**
* @name constructFrom
* @category Generic Helpers
* @summary Constructs a date using the reference date and the value
*
* @description
* The function constructs a new date using the constructor from the reference
* date and the given value. It helps to build generic functions that accept
* date extensions.
*
* It defaults to `Date` if the passed reference date is a number or a string.
*
* Starting from v3.7.0, it allows to construct a date using `[Symbol.for("constructDateFrom")]`
* enabling to transfer extra properties from the reference date to the new date.
* It's useful for extensions like [`TZDate`](https://github.com/date-fns/tz)
* that accept a time zone as a constructor argument.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
*
* @param date - The reference date to take constructor from
* @param value - The value to create the date
*
* @returns Date initialized using the given date and value
*
* @example
* import { constructFrom } from "./constructFrom/date-fns";
*
* // A function that clones a date preserving the original type
* function cloneDate<DateType extends Date>(date: DateType): DateType {
*   return constructFrom(
*     date, // Use constructor from the given date
*     date.getTime() // Use the date value to create a new date
*   );
* }
*/
function constructFrom(date, value) {
	if (typeof date === "function") return date(value);
	if (date && typeof date === "object" && constructFromSymbol in date) return date[constructFromSymbol](value);
	if (date instanceof Date) return new date.constructor(value);
	return new Date(value);
}

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/toDate.js
/**
* @name toDate
* @category Common Helpers
* @summary Convert the given argument to an instance of Date.
*
* @description
* Convert the given argument to an instance of Date.
*
* If the argument is an instance of Date, the function returns its clone.
*
* If the argument is a number, it is treated as a timestamp.
*
* If the argument is none of the above, the function returns Invalid Date.
*
* Starting from v3.7.0, it clones a date using `[Symbol.for("constructDateFrom")]`
* enabling to transfer extra properties from the reference date to the new date.
* It's useful for extensions like [`TZDate`](https://github.com/date-fns/tz)
* that accept a time zone as a constructor argument.
*
* **Note**: *all* Date arguments passed to any *date-fns* function is processed by `toDate`.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param argument - The value to convert
*
* @returns The parsed date in the local time zone
*
* @example
* // Clone the date:
* const result = toDate(new Date(2014, 1, 11, 11, 30, 30))
* //=> Tue Feb 11 2014 11:30:30
*
* @example
* // Convert the timestamp to date:
* const result = toDate(1392098430000)
* //=> Tue Feb 11 2014 11:30:30
*/
function toDate(argument, context) {
	return constructFrom(context || argument, argument);
}

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/_lib/getTimezoneOffsetInMilliseconds.js
/**
* Google Chrome as of 67.0.3396.87 introduced timezones with offset that includes seconds.
* They usually appear for dates that denote time before the timezones were introduced
* (e.g. for 'Europe/Prague' timezone the offset is GMT+00:57:44 before 1 October 1891
* and GMT+01:00:00 after that date)
*
* Date#getTimezoneOffset returns the offset in minutes and would return 57 for the example above,
* which would lead to incorrect calculations.
*
* This function returns the timezone offset in milliseconds that takes seconds in account.
*/
function getTimezoneOffsetInMilliseconds(date) {
	const _date = toDate(date);
	const utcDate = new Date(Date.UTC(_date.getFullYear(), _date.getMonth(), _date.getDate(), _date.getHours(), _date.getMinutes(), _date.getSeconds(), _date.getMilliseconds()));
	utcDate.setUTCFullYear(_date.getFullYear());
	return +date - +utcDate;
}

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/_lib/normalizeDates.js
function normalizeDates(context, ...dates) {
	const normalize = constructFrom.bind(null, context || dates.find((date) => typeof date === "object"));
	return dates.map(normalize);
}

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/compareAsc.js
/**
* @name compareAsc
* @category Common Helpers
* @summary Compare the two dates and return -1, 0 or 1.
*
* @description
* Compare the two dates and return 1 if the first date is after the second,
* -1 if the first date is before the second or 0 if dates are equal.
*
* @param dateLeft - The first date to compare
* @param dateRight - The second date to compare
*
* @returns The result of the comparison
*
* @example
* // Compare 11 February 1987 and 10 July 1989:
* const result = compareAsc(new Date(1987, 1, 11), new Date(1989, 6, 10))
* //=> -1
*
* @example
* // Sort the array of dates:
* const result = [
*   new Date(1995, 6, 2),
*   new Date(1987, 1, 11),
*   new Date(1989, 6, 10)
* ].sort(compareAsc)
* //=> [
* //   Wed Feb 11 1987 00:00:00,
* //   Mon Jul 10 1989 00:00:00,
* //   Sun Jul 02 1995 00:00:00
* // ]
*/
function compareAsc(dateLeft, dateRight) {
	const diff = +toDate(dateLeft) - +toDate(dateRight);
	if (diff < 0) return -1;
	else if (diff > 0) return 1;
	return diff;
}

//#endregion
//#region ../../node_modules/.pnpm/date-fns@4.1.0/node_modules/date-fns/formatDistanceStrict.js
/**
* The {@link formatDistanceStrict} function options.
*/
/**
* The unit used to format the distance in {@link formatDistanceStrict}.
*/
/**
* @name formatDistanceStrict
* @category Common Helpers
* @summary Return the distance between the given dates in words.
*
* @description
* Return the distance between the given dates in words, using strict units.
* This is like `formatDistance`, but does not use helpers like 'almost', 'over',
* 'less than' and the like.
*
* | Distance between dates | Result              |
* |------------------------|---------------------|
* | 0 ... 59 secs          | [0..59] seconds     |
* | 1 ... 59 mins          | [1..59] minutes     |
* | 1 ... 23 hrs           | [1..23] hours       |
* | 1 ... 29 days          | [1..29] days        |
* | 1 ... 11 months        | [1..11] months      |
* | 1 ... N years          | [1..N]  years       |
*
* @param laterDate - The date
* @param earlierDate - The date to compare with
* @param options - An object with options
*
* @returns The distance in words
*
* @throws `date` must not be Invalid Date
* @throws `baseDate` must not be Invalid Date
* @throws `options.unit` must be 'second', 'minute', 'hour', 'day', 'month' or 'year'
* @throws `options.locale` must contain `formatDistance` property
*
* @example
* // What is the distance between 2 July 2014 and 1 January 2015?
* const result = formatDistanceStrict(new Date(2014, 6, 2), new Date(2015, 0, 2))
* //=> '6 months'
*
* @example
* // What is the distance between 1 January 2015 00:00:15
* // and 1 January 2015 00:00:00?
* const result = formatDistanceStrict(
*   new Date(2015, 0, 1, 0, 0, 15),
*   new Date(2015, 0, 1, 0, 0, 0)
* )
* //=> '15 seconds'
*
* @example
* // What is the distance from 1 January 2016
* // to 1 January 2015, with a suffix?
* const result = formatDistanceStrict(new Date(2015, 0, 1), new Date(2016, 0, 1), {
*   addSuffix: true
* })
* //=> '1 year ago'
*
* @example
* // What is the distance from 1 January 2016
* // to 1 January 2015, in minutes?
* const result = formatDistanceStrict(new Date(2016, 0, 1), new Date(2015, 0, 1), {
*   unit: 'minute'
* })
* //=> '525600 minutes'
*
* @example
* // What is the distance from 1 January 2015
* // to 28 January 2015, in months, rounded up?
* const result = formatDistanceStrict(new Date(2015, 0, 28), new Date(2015, 0, 1), {
*   unit: 'month',
*   roundingMethod: 'ceil'
* })
* //=> '1 month'
*
* @example
* // What is the distance between 1 August 2016 and 1 January 2015 in Esperanto?
* import { eoLocale } from 'date-fns/locale/eo'
* const result = formatDistanceStrict(new Date(2016, 7, 1), new Date(2015, 0, 1), {
*   locale: eoLocale
* })
* //=> '1 jaro'
*/
function formatDistanceStrict(laterDate, earlierDate, options) {
	const defaultOptions$1 = getDefaultOptions();
	const locale = options?.locale ?? defaultOptions$1.locale ?? enUS;
	const comparison = compareAsc(laterDate, earlierDate);
	if (isNaN(comparison)) throw new RangeError("Invalid time value");
	const localizeOptions = Object.assign({}, options, {
		addSuffix: options?.addSuffix,
		comparison
	});
	const [laterDate_, earlierDate_] = normalizeDates(options?.in, ...comparison > 0 ? [earlierDate, laterDate] : [laterDate, earlierDate]);
	const roundingMethod = getRoundingMethod(options?.roundingMethod ?? "round");
	const milliseconds = earlierDate_.getTime() - laterDate_.getTime();
	const minutes = milliseconds / millisecondsInMinute;
	const dstNormalizedMinutes = (milliseconds - (getTimezoneOffsetInMilliseconds(earlierDate_) - getTimezoneOffsetInMilliseconds(laterDate_))) / millisecondsInMinute;
	const defaultUnit = options?.unit;
	let unit;
	if (!defaultUnit) if (minutes < 1) unit = "second";
	else if (minutes < 60) unit = "minute";
	else if (minutes < minutesInDay) unit = "hour";
	else if (dstNormalizedMinutes < minutesInMonth) unit = "day";
	else if (dstNormalizedMinutes < minutesInYear) unit = "month";
	else unit = "year";
	else unit = defaultUnit;
	if (unit === "second") {
		const seconds = roundingMethod(milliseconds / 1e3);
		return locale.formatDistance("xSeconds", seconds, localizeOptions);
	} else if (unit === "minute") {
		const roundedMinutes = roundingMethod(minutes);
		return locale.formatDistance("xMinutes", roundedMinutes, localizeOptions);
	} else if (unit === "hour") {
		const hours = roundingMethod(minutes / 60);
		return locale.formatDistance("xHours", hours, localizeOptions);
	} else if (unit === "day") {
		const days = roundingMethod(dstNormalizedMinutes / minutesInDay);
		return locale.formatDistance("xDays", days, localizeOptions);
	} else if (unit === "month") {
		const months = roundingMethod(dstNormalizedMinutes / minutesInMonth);
		return months === 12 && defaultUnit !== "month" ? locale.formatDistance("xYears", 1, localizeOptions) : locale.formatDistance("xMonths", months, localizeOptions);
	} else {
		const years = roundingMethod(dstNormalizedMinutes / minutesInYear);
		return locale.formatDistance("xYears", years, localizeOptions);
	}
}

//#endregion
//#region src/util/output.ts
init_source();
const output = {
	print: console.log,
	error: console.error,
	link(text, href) {
		return `\u001B]8;;${href}\u001B\\${text}\u001B]8;;\u001B\\`;
	}
};
function formatBytes(bytes) {
	const units = [
		"B",
		"KB",
		"MB",
		"GB",
		"TB"
	];
	let value = bytes;
	let unitIndex = 0;
	while (value >= 1024 && unitIndex < units.length - 1) {
		value /= 1024;
		unitIndex += 1;
	}
	return `${new Intl.NumberFormat(void 0, { maximumFractionDigits: value < 10 && unitIndex > 0 ? 1 : 0 }).format(value)} ${units[unitIndex]}`;
}
function timeAgo(date) {
	if (date === void 0) return "-";
	return formatDistanceStrict(date, /* @__PURE__ */ new Date(), { addSuffix: true });
}
function table(opts) {
	const titles = Object.keys(opts.columns);
	const visibleLength = (value) => stripVTControlCharacters(value).length;
	const maxWidths = titles.map((title) => visibleLength(title));
	const data = opts.rows.map((row) => {
		return titles.map((title, i) => {
			let value = String(opts.columns[title].value(row));
			const width = visibleLength(value);
			if (width > maxWidths[i]) maxWidths[i] = width;
			if (opts.columns[title].color) value = opts.columns[title].color(row)(value);
			return value;
		});
	});
	const padded = (value, i) => {
		const padding = maxWidths[i] - visibleLength(value);
		return padding > 0 ? `${value}${" ".repeat(padding)}` : value;
	};
	const space = "   ";
	return [source_default.bold(titles.map(padded).join(space)), ...data.map((row) => row.map(padded).join(space))].join("\n");
}
function formatRunDuration(d$1) {
	if (d$1 < 1e3) return `${d$1}ms`;
	return `${d$1 / 1e3}s`;
}
function formatNextCursorHint(cursor) {
	const args$5 = process.argv.slice(2);
	const filtered = [];
	for (let i = 0; i < args$5.length; i++) {
		if (args$5[i] === "--cursor") {
			i++;
			continue;
		}
		if (args$5[i].startsWith("--cursor=")) continue;
		filtered.push(args$5[i]);
	}
	return `\nMore results: sandbox ${filtered.join(" ")} --cursor ${cursor}`;
}

//#endregion
//#region ../../node_modules/.pnpm/is-docker@3.0.0/node_modules/is-docker/index.js
let isDockerCached;
function hasDockerEnv() {
	try {
		fs$1.statSync("/.dockerenv");
		return true;
	} catch {
		return false;
	}
}
function hasDockerCGroup() {
	try {
		return fs$1.readFileSync("/proc/self/cgroup", "utf8").includes("docker");
	} catch {
		return false;
	}
}
function isDocker() {
	if (isDockerCached === void 0) isDockerCached = hasDockerEnv() || hasDockerCGroup();
	return isDockerCached;
}

//#endregion
//#region ../../node_modules/.pnpm/is-inside-container@1.0.0/node_modules/is-inside-container/index.js
let cachedResult;
const hasContainerEnv = () => {
	try {
		fs$1.statSync("/run/.containerenv");
		return true;
	} catch {
		return false;
	}
};
function isInsideContainer() {
	if (cachedResult === void 0) cachedResult = hasContainerEnv() || isDocker();
	return cachedResult;
}

//#endregion
//#region ../../node_modules/.pnpm/is-wsl@3.1.0/node_modules/is-wsl/index.js
const isWsl = () => {
	if (process$1.platform !== "linux") return false;
	if (os.release().toLowerCase().includes("microsoft")) {
		if (isInsideContainer()) return false;
		return true;
	}
	try {
		return fs$1.readFileSync("/proc/version", "utf8").toLowerCase().includes("microsoft") ? !isInsideContainer() : false;
	} catch {
		return false;
	}
};
var is_wsl_default = process$1.env.__IS_WSL_TEST__ ? isWsl : isWsl();

//#endregion
//#region ../../node_modules/.pnpm/wsl-utils@0.1.0/node_modules/wsl-utils/index.js
const wslDrivesMountPoint = (() => {
	const defaultMountPoint = "/mnt/";
	let mountPoint;
	return async function() {
		if (mountPoint) return mountPoint;
		const configFilePath = "/etc/wsl.conf";
		let isConfigFileExists = false;
		try {
			await fs.access(configFilePath, constants.F_OK);
			isConfigFileExists = true;
		} catch {}
		if (!isConfigFileExists) return defaultMountPoint;
		const configContent = await fs.readFile(configFilePath, { encoding: "utf8" });
		const configMountPoint = /(?<!#.*)root\s*=\s*(?<mountPoint>.*)/g.exec(configContent);
		if (!configMountPoint) return defaultMountPoint;
		mountPoint = configMountPoint.groups.mountPoint.trim();
		mountPoint = mountPoint.endsWith("/") ? mountPoint : `${mountPoint}/`;
		return mountPoint;
	};
})();
const powerShellPathFromWsl = async () => {
	return `${await wslDrivesMountPoint()}c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe`;
};
const powerShellPath = async () => {
	if (is_wsl_default) return powerShellPathFromWsl();
	return `${process$1.env.SYSTEMROOT || process$1.env.windir || String.raw`C:\Windows`}\\System32\\WindowsPowerShell\\v1.0\\powershell.exe`;
};

//#endregion
//#region ../../node_modules/.pnpm/define-lazy-prop@3.0.0/node_modules/define-lazy-prop/index.js
function defineLazyProperty(object, propertyName, valueGetter) {
	const define = (value) => Object.defineProperty(object, propertyName, {
		value,
		enumerable: true,
		writable: true
	});
	Object.defineProperty(object, propertyName, {
		configurable: true,
		enumerable: true,
		get() {
			const result = valueGetter();
			define(result);
			return result;
		},
		set(value) {
			define(value);
		}
	});
	return object;
}

//#endregion
//#region ../../node_modules/.pnpm/default-browser-id@5.0.0/node_modules/default-browser-id/index.js
const execFileAsync$3 = promisify(execFile);
async function defaultBrowserId() {
	if (process$1.platform !== "darwin") throw new Error("macOS only");
	const { stdout } = await execFileAsync$3("defaults", [
		"read",
		"com.apple.LaunchServices/com.apple.launchservices.secure",
		"LSHandlers"
	]);
	return /LSHandlerRoleAll = "(?!-)(?<id>[^"]+?)";\s+?LSHandlerURLScheme = (?:http|https);/.exec(stdout)?.groups.id ?? "com.apple.Safari";
}

//#endregion
//#region ../../node_modules/.pnpm/run-applescript@7.1.0/node_modules/run-applescript/index.js
const execFileAsync$2 = promisify(execFile);
async function runAppleScript(script, { humanReadableOutput = true, signal } = {}) {
	if (process$1.platform !== "darwin") throw new Error("macOS only");
	const outputArguments = humanReadableOutput ? [] : ["-ss"];
	const execOptions = {};
	if (signal) execOptions.signal = signal;
	const { stdout } = await execFileAsync$2("osascript", [
		"-e",
		script,
		outputArguments
	], execOptions);
	return stdout.trim();
}

//#endregion
//#region ../../node_modules/.pnpm/bundle-name@4.1.0/node_modules/bundle-name/index.js
async function bundleName(bundleId) {
	return runAppleScript(`tell application "Finder" to set app_path to application file id "${bundleId}" as string\ntell application "System Events" to get value of property list item "CFBundleName" of property list file (app_path & ":Contents:Info.plist")`);
}

//#endregion
//#region ../../node_modules/.pnpm/default-browser@5.2.1/node_modules/default-browser/windows.js
const execFileAsync$1 = promisify(execFile);
const windowsBrowserProgIds = {
	AppXq0fevzme2pys62n3e0fbqa7peapykr8v: {
		name: "Edge",
		id: "com.microsoft.edge.old"
	},
	MSEdgeDHTML: {
		name: "Edge",
		id: "com.microsoft.edge"
	},
	MSEdgeHTM: {
		name: "Edge",
		id: "com.microsoft.edge"
	},
	"IE.HTTP": {
		name: "Internet Explorer",
		id: "com.microsoft.ie"
	},
	FirefoxURL: {
		name: "Firefox",
		id: "org.mozilla.firefox"
	},
	ChromeHTML: {
		name: "Chrome",
		id: "com.google.chrome"
	},
	BraveHTML: {
		name: "Brave",
		id: "com.brave.Browser"
	},
	BraveBHTML: {
		name: "Brave Beta",
		id: "com.brave.Browser.beta"
	},
	BraveSSHTM: {
		name: "Brave Nightly",
		id: "com.brave.Browser.nightly"
	}
};
var UnknownBrowserError = class extends Error {};
async function defaultBrowser$1(_execFileAsync = execFileAsync$1) {
	const { stdout } = await _execFileAsync("reg", [
		"QUERY",
		" HKEY_CURRENT_USER\\Software\\Microsoft\\Windows\\Shell\\Associations\\UrlAssociations\\http\\UserChoice",
		"/v",
		"ProgId"
	]);
	const match$1 = /ProgId\s*REG_SZ\s*(?<id>\S+)/.exec(stdout);
	if (!match$1) throw new UnknownBrowserError(`Cannot find Windows browser in stdout: ${JSON.stringify(stdout)}`);
	const { id } = match$1.groups;
	const browser = windowsBrowserProgIds[id];
	if (!browser) throw new UnknownBrowserError(`Unknown browser ID: ${id}`);
	return browser;
}

//#endregion
//#region ../../node_modules/.pnpm/default-browser@5.2.1/node_modules/default-browser/index.js
const execFileAsync = promisify(execFile);
const titleize = (string$2) => string$2.toLowerCase().replaceAll(/(?:^|\s|-)\S/g, (x) => x.toUpperCase());
async function defaultBrowser() {
	if (process$1.platform === "darwin") {
		const id = await defaultBrowserId();
		return {
			name: await bundleName(id),
			id
		};
	}
	if (process$1.platform === "linux") {
		const { stdout } = await execFileAsync("xdg-mime", [
			"query",
			"default",
			"x-scheme-handler/http"
		]);
		const id = stdout.trim();
		return {
			name: titleize(id.replace(/.desktop$/, "").replace("-", " ")),
			id
		};
	}
	if (process$1.platform === "win32") return defaultBrowser$1();
	throw new Error("Only macOS, Linux, and Windows are supported");
}

//#endregion
//#region ../../node_modules/.pnpm/open@10.2.0/node_modules/open/index.js
const execFile$1 = promisify(childProcess.execFile);
const __dirname = Path.dirname(fileURLToPath(import.meta.url));
const localXdgOpenPath = Path.join(__dirname, "xdg-open");
const { platform: platform$1, arch } = process$1;
/**
Get the default browser name in Windows from WSL.

@returns {Promise<string>} Browser name.
*/
async function getWindowsDefaultBrowserFromWsl() {
	const powershellPath = await powerShellPath();
	const rawCommand = String.raw`(Get-ItemProperty -Path "HKCU:\Software\Microsoft\Windows\Shell\Associations\UrlAssociations\http\UserChoice").ProgId`;
	const { stdout } = await execFile$1(powershellPath, [
		"-NoProfile",
		"-NonInteractive",
		"-ExecutionPolicy",
		"Bypass",
		"-EncodedCommand",
		Buffer$1.from(rawCommand, "utf16le").toString("base64")
	], { encoding: "utf8" });
	const progId = stdout.trim();
	const browserMap = {
		ChromeHTML: "com.google.chrome",
		BraveHTML: "com.brave.Browser",
		MSEdgeHTM: "com.microsoft.edge",
		FirefoxURL: "org.mozilla.firefox"
	};
	return browserMap[progId] ? { id: browserMap[progId] } : {};
}
const pTryEach = async (array$1, mapper) => {
	let latestError;
	for (const item of array$1) try {
		return await mapper(item);
	} catch (error) {
		latestError = error;
	}
	throw latestError;
};
const baseOpen = async (options) => {
	options = {
		wait: false,
		background: false,
		newInstance: false,
		allowNonzeroExitCode: false,
		...options
	};
	if (Array.isArray(options.app)) return pTryEach(options.app, (singleApp) => baseOpen({
		...options,
		app: singleApp
	}));
	let { name: app$1, arguments: appArguments = [] } = options.app ?? {};
	appArguments = [...appArguments];
	if (Array.isArray(app$1)) return pTryEach(app$1, (appName) => baseOpen({
		...options,
		app: {
			name: appName,
			arguments: appArguments
		}
	}));
	if (app$1 === "browser" || app$1 === "browserPrivate") {
		const ids = {
			"com.google.chrome": "chrome",
			"google-chrome.desktop": "chrome",
			"com.brave.Browser": "brave",
			"org.mozilla.firefox": "firefox",
			"firefox.desktop": "firefox",
			"com.microsoft.msedge": "edge",
			"com.microsoft.edge": "edge",
			"com.microsoft.edgemac": "edge",
			"microsoft-edge.desktop": "edge"
		};
		const flags = {
			chrome: "--incognito",
			brave: "--incognito",
			firefox: "--private-window",
			edge: "--inPrivate"
		};
		const browser = is_wsl_default ? await getWindowsDefaultBrowserFromWsl() : await defaultBrowser();
		if (browser.id in ids) {
			const browserName = ids[browser.id];
			if (app$1 === "browserPrivate") appArguments.push(flags[browserName]);
			return baseOpen({
				...options,
				app: {
					name: apps[browserName],
					arguments: appArguments
				}
			});
		}
		throw new Error(`${browser.name} is not supported as a default browser`);
	}
	let command$1;
	const cliArguments = [];
	const childProcessOptions = {};
	if (platform$1 === "darwin") {
		command$1 = "open";
		if (options.wait) cliArguments.push("--wait-apps");
		if (options.background) cliArguments.push("--background");
		if (options.newInstance) cliArguments.push("--new");
		if (app$1) cliArguments.push("-a", app$1);
	} else if (platform$1 === "win32" || is_wsl_default && !isInsideContainer() && !app$1) {
		command$1 = await powerShellPath();
		cliArguments.push("-NoProfile", "-NonInteractive", "-ExecutionPolicy", "Bypass", "-EncodedCommand");
		if (!is_wsl_default) childProcessOptions.windowsVerbatimArguments = true;
		const encodedArguments = ["Start"];
		if (options.wait) encodedArguments.push("-Wait");
		if (app$1) {
			encodedArguments.push(`"\`"${app$1}\`""`);
			if (options.target) appArguments.push(options.target);
		} else if (options.target) encodedArguments.push(`"${options.target}"`);
		if (appArguments.length > 0) {
			appArguments = appArguments.map((argument) => `"\`"${argument}\`""`);
			encodedArguments.push("-ArgumentList", appArguments.join(","));
		}
		options.target = Buffer$1.from(encodedArguments.join(" "), "utf16le").toString("base64");
	} else {
		if (app$1) command$1 = app$1;
		else {
			const isBundled = !__dirname || __dirname === "/";
			let exeLocalXdgOpen = false;
			try {
				await fs.access(localXdgOpenPath, constants.X_OK);
				exeLocalXdgOpen = true;
			} catch {}
			command$1 = process$1.versions.electron ?? (platform$1 === "android" || isBundled || !exeLocalXdgOpen) ? "xdg-open" : localXdgOpenPath;
		}
		if (appArguments.length > 0) cliArguments.push(...appArguments);
		if (!options.wait) {
			childProcessOptions.stdio = "ignore";
			childProcessOptions.detached = true;
		}
	}
	if (platform$1 === "darwin" && appArguments.length > 0) cliArguments.push("--args", ...appArguments);
	if (options.target) cliArguments.push(options.target);
	const subprocess = childProcess.spawn(command$1, cliArguments, childProcessOptions);
	if (options.wait) return new Promise((resolve, reject) => {
		subprocess.once("error", reject);
		subprocess.once("close", (exitCode) => {
			if (!options.allowNonzeroExitCode && exitCode > 0) {
				reject(/* @__PURE__ */ new Error(`Exited with code ${exitCode}`));
				return;
			}
			resolve(subprocess);
		});
	});
	subprocess.unref();
	return subprocess;
};
const open = (target, options) => {
	if (typeof target !== "string") throw new TypeError("Expected a `target`");
	return baseOpen({
		...options,
		target
	});
};
function detectArchBinary(binary$1) {
	if (typeof binary$1 === "string" || Array.isArray(binary$1)) return binary$1;
	const { [arch]: archBinary } = binary$1;
	if (!archBinary) throw new Error(`${arch} is not supported`);
	return archBinary;
}
function detectPlatformBinary({ [platform$1]: platformBinary }, { wsl }) {
	if (wsl && is_wsl_default) return detectArchBinary(wsl);
	if (!platformBinary) throw new Error(`${platform$1} is not supported`);
	return detectArchBinary(platformBinary);
}
const apps = {};
defineLazyProperty(apps, "chrome", () => detectPlatformBinary({
	darwin: "google chrome",
	win32: "chrome",
	linux: [
		"google-chrome",
		"google-chrome-stable",
		"chromium"
	]
}, { wsl: {
	ia32: "/mnt/c/Program Files (x86)/Google/Chrome/Application/chrome.exe",
	x64: ["/mnt/c/Program Files/Google/Chrome/Application/chrome.exe", "/mnt/c/Program Files (x86)/Google/Chrome/Application/chrome.exe"]
} }));
defineLazyProperty(apps, "brave", () => detectPlatformBinary({
	darwin: "brave browser",
	win32: "brave",
	linux: ["brave-browser", "brave"]
}, { wsl: {
	ia32: "/mnt/c/Program Files (x86)/BraveSoftware/Brave-Browser/Application/brave.exe",
	x64: ["/mnt/c/Program Files/BraveSoftware/Brave-Browser/Application/brave.exe", "/mnt/c/Program Files (x86)/BraveSoftware/Brave-Browser/Application/brave.exe"]
} }));
defineLazyProperty(apps, "firefox", () => detectPlatformBinary({
	darwin: "firefox",
	win32: String.raw`C:\Program Files\Mozilla Firefox\firefox.exe`,
	linux: "firefox"
}, { wsl: "/mnt/c/Program Files/Mozilla Firefox/firefox.exe" }));
defineLazyProperty(apps, "edge", () => detectPlatformBinary({
	darwin: "microsoft edge",
	win32: "msedge",
	linux: ["microsoft-edge", "microsoft-edge-dev"]
}, { wsl: "/mnt/c/Program Files (x86)/Microsoft/Edge/Application/msedge.exe" }));
defineLazyProperty(apps, "browser", () => "browser");
defineLazyProperty(apps, "browserPrivate", () => "browserPrivate");
var open_default = open;

//#endregion
//#region ../../node_modules/.pnpm/cli-spinners@2.9.2/node_modules/cli-spinners/spinners.json
var require_spinners = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cli-spinners@2.9.2/node_modules/cli-spinners/spinners.json": ((exports, module) => {
	module.exports = {
		"dots": {
			"interval": 80,
			"frames": [
				"⠋",
				"⠙",
				"⠹",
				"⠸",
				"⠼",
				"⠴",
				"⠦",
				"⠧",
				"⠇",
				"⠏"
			]
		},
		"dots2": {
			"interval": 80,
			"frames": [
				"⣾",
				"⣽",
				"⣻",
				"⢿",
				"⡿",
				"⣟",
				"⣯",
				"⣷"
			]
		},
		"dots3": {
			"interval": 80,
			"frames": [
				"⠋",
				"⠙",
				"⠚",
				"⠞",
				"⠖",
				"⠦",
				"⠴",
				"⠲",
				"⠳",
				"⠓"
			]
		},
		"dots4": {
			"interval": 80,
			"frames": [
				"⠄",
				"⠆",
				"⠇",
				"⠋",
				"⠙",
				"⠸",
				"⠰",
				"⠠",
				"⠰",
				"⠸",
				"⠙",
				"⠋",
				"⠇",
				"⠆"
			]
		},
		"dots5": {
			"interval": 80,
			"frames": [
				"⠋",
				"⠙",
				"⠚",
				"⠒",
				"⠂",
				"⠂",
				"⠒",
				"⠲",
				"⠴",
				"⠦",
				"⠖",
				"⠒",
				"⠐",
				"⠐",
				"⠒",
				"⠓",
				"⠋"
			]
		},
		"dots6": {
			"interval": 80,
			"frames": [
				"⠁",
				"⠉",
				"⠙",
				"⠚",
				"⠒",
				"⠂",
				"⠂",
				"⠒",
				"⠲",
				"⠴",
				"⠤",
				"⠄",
				"⠄",
				"⠤",
				"⠴",
				"⠲",
				"⠒",
				"⠂",
				"⠂",
				"⠒",
				"⠚",
				"⠙",
				"⠉",
				"⠁"
			]
		},
		"dots7": {
			"interval": 80,
			"frames": [
				"⠈",
				"⠉",
				"⠋",
				"⠓",
				"⠒",
				"⠐",
				"⠐",
				"⠒",
				"⠖",
				"⠦",
				"⠤",
				"⠠",
				"⠠",
				"⠤",
				"⠦",
				"⠖",
				"⠒",
				"⠐",
				"⠐",
				"⠒",
				"⠓",
				"⠋",
				"⠉",
				"⠈"
			]
		},
		"dots8": {
			"interval": 80,
			"frames": [
				"⠁",
				"⠁",
				"⠉",
				"⠙",
				"⠚",
				"⠒",
				"⠂",
				"⠂",
				"⠒",
				"⠲",
				"⠴",
				"⠤",
				"⠄",
				"⠄",
				"⠤",
				"⠠",
				"⠠",
				"⠤",
				"⠦",
				"⠖",
				"⠒",
				"⠐",
				"⠐",
				"⠒",
				"⠓",
				"⠋",
				"⠉",
				"⠈",
				"⠈"
			]
		},
		"dots9": {
			"interval": 80,
			"frames": [
				"⢹",
				"⢺",
				"⢼",
				"⣸",
				"⣇",
				"⡧",
				"⡗",
				"⡏"
			]
		},
		"dots10": {
			"interval": 80,
			"frames": [
				"⢄",
				"⢂",
				"⢁",
				"⡁",
				"⡈",
				"⡐",
				"⡠"
			]
		},
		"dots11": {
			"interval": 100,
			"frames": [
				"⠁",
				"⠂",
				"⠄",
				"⡀",
				"⢀",
				"⠠",
				"⠐",
				"⠈"
			]
		},
		"dots12": {
			"interval": 80,
			"frames": [
				"⢀⠀",
				"⡀⠀",
				"⠄⠀",
				"⢂⠀",
				"⡂⠀",
				"⠅⠀",
				"⢃⠀",
				"⡃⠀",
				"⠍⠀",
				"⢋⠀",
				"⡋⠀",
				"⠍⠁",
				"⢋⠁",
				"⡋⠁",
				"⠍⠉",
				"⠋⠉",
				"⠋⠉",
				"⠉⠙",
				"⠉⠙",
				"⠉⠩",
				"⠈⢙",
				"⠈⡙",
				"⢈⠩",
				"⡀⢙",
				"⠄⡙",
				"⢂⠩",
				"⡂⢘",
				"⠅⡘",
				"⢃⠨",
				"⡃⢐",
				"⠍⡐",
				"⢋⠠",
				"⡋⢀",
				"⠍⡁",
				"⢋⠁",
				"⡋⠁",
				"⠍⠉",
				"⠋⠉",
				"⠋⠉",
				"⠉⠙",
				"⠉⠙",
				"⠉⠩",
				"⠈⢙",
				"⠈⡙",
				"⠈⠩",
				"⠀⢙",
				"⠀⡙",
				"⠀⠩",
				"⠀⢘",
				"⠀⡘",
				"⠀⠨",
				"⠀⢐",
				"⠀⡐",
				"⠀⠠",
				"⠀⢀",
				"⠀⡀"
			]
		},
		"dots13": {
			"interval": 80,
			"frames": [
				"⣼",
				"⣹",
				"⢻",
				"⠿",
				"⡟",
				"⣏",
				"⣧",
				"⣶"
			]
		},
		"dots8Bit": {
			"interval": 80,
			"frames": [
				"⠀",
				"⠁",
				"⠂",
				"⠃",
				"⠄",
				"⠅",
				"⠆",
				"⠇",
				"⡀",
				"⡁",
				"⡂",
				"⡃",
				"⡄",
				"⡅",
				"⡆",
				"⡇",
				"⠈",
				"⠉",
				"⠊",
				"⠋",
				"⠌",
				"⠍",
				"⠎",
				"⠏",
				"⡈",
				"⡉",
				"⡊",
				"⡋",
				"⡌",
				"⡍",
				"⡎",
				"⡏",
				"⠐",
				"⠑",
				"⠒",
				"⠓",
				"⠔",
				"⠕",
				"⠖",
				"⠗",
				"⡐",
				"⡑",
				"⡒",
				"⡓",
				"⡔",
				"⡕",
				"⡖",
				"⡗",
				"⠘",
				"⠙",
				"⠚",
				"⠛",
				"⠜",
				"⠝",
				"⠞",
				"⠟",
				"⡘",
				"⡙",
				"⡚",
				"⡛",
				"⡜",
				"⡝",
				"⡞",
				"⡟",
				"⠠",
				"⠡",
				"⠢",
				"⠣",
				"⠤",
				"⠥",
				"⠦",
				"⠧",
				"⡠",
				"⡡",
				"⡢",
				"⡣",
				"⡤",
				"⡥",
				"⡦",
				"⡧",
				"⠨",
				"⠩",
				"⠪",
				"⠫",
				"⠬",
				"⠭",
				"⠮",
				"⠯",
				"⡨",
				"⡩",
				"⡪",
				"⡫",
				"⡬",
				"⡭",
				"⡮",
				"⡯",
				"⠰",
				"⠱",
				"⠲",
				"⠳",
				"⠴",
				"⠵",
				"⠶",
				"⠷",
				"⡰",
				"⡱",
				"⡲",
				"⡳",
				"⡴",
				"⡵",
				"⡶",
				"⡷",
				"⠸",
				"⠹",
				"⠺",
				"⠻",
				"⠼",
				"⠽",
				"⠾",
				"⠿",
				"⡸",
				"⡹",
				"⡺",
				"⡻",
				"⡼",
				"⡽",
				"⡾",
				"⡿",
				"⢀",
				"⢁",
				"⢂",
				"⢃",
				"⢄",
				"⢅",
				"⢆",
				"⢇",
				"⣀",
				"⣁",
				"⣂",
				"⣃",
				"⣄",
				"⣅",
				"⣆",
				"⣇",
				"⢈",
				"⢉",
				"⢊",
				"⢋",
				"⢌",
				"⢍",
				"⢎",
				"⢏",
				"⣈",
				"⣉",
				"⣊",
				"⣋",
				"⣌",
				"⣍",
				"⣎",
				"⣏",
				"⢐",
				"⢑",
				"⢒",
				"⢓",
				"⢔",
				"⢕",
				"⢖",
				"⢗",
				"⣐",
				"⣑",
				"⣒",
				"⣓",
				"⣔",
				"⣕",
				"⣖",
				"⣗",
				"⢘",
				"⢙",
				"⢚",
				"⢛",
				"⢜",
				"⢝",
				"⢞",
				"⢟",
				"⣘",
				"⣙",
				"⣚",
				"⣛",
				"⣜",
				"⣝",
				"⣞",
				"⣟",
				"⢠",
				"⢡",
				"⢢",
				"⢣",
				"⢤",
				"⢥",
				"⢦",
				"⢧",
				"⣠",
				"⣡",
				"⣢",
				"⣣",
				"⣤",
				"⣥",
				"⣦",
				"⣧",
				"⢨",
				"⢩",
				"⢪",
				"⢫",
				"⢬",
				"⢭",
				"⢮",
				"⢯",
				"⣨",
				"⣩",
				"⣪",
				"⣫",
				"⣬",
				"⣭",
				"⣮",
				"⣯",
				"⢰",
				"⢱",
				"⢲",
				"⢳",
				"⢴",
				"⢵",
				"⢶",
				"⢷",
				"⣰",
				"⣱",
				"⣲",
				"⣳",
				"⣴",
				"⣵",
				"⣶",
				"⣷",
				"⢸",
				"⢹",
				"⢺",
				"⢻",
				"⢼",
				"⢽",
				"⢾",
				"⢿",
				"⣸",
				"⣹",
				"⣺",
				"⣻",
				"⣼",
				"⣽",
				"⣾",
				"⣿"
			]
		},
		"sand": {
			"interval": 80,
			"frames": [
				"⠁",
				"⠂",
				"⠄",
				"⡀",
				"⡈",
				"⡐",
				"⡠",
				"⣀",
				"⣁",
				"⣂",
				"⣄",
				"⣌",
				"⣔",
				"⣤",
				"⣥",
				"⣦",
				"⣮",
				"⣶",
				"⣷",
				"⣿",
				"⡿",
				"⠿",
				"⢟",
				"⠟",
				"⡛",
				"⠛",
				"⠫",
				"⢋",
				"⠋",
				"⠍",
				"⡉",
				"⠉",
				"⠑",
				"⠡",
				"⢁"
			]
		},
		"line": {
			"interval": 130,
			"frames": [
				"-",
				"\\",
				"|",
				"/"
			]
		},
		"line2": {
			"interval": 100,
			"frames": [
				"⠂",
				"-",
				"–",
				"—",
				"–",
				"-"
			]
		},
		"pipe": {
			"interval": 100,
			"frames": [
				"┤",
				"┘",
				"┴",
				"└",
				"├",
				"┌",
				"┬",
				"┐"
			]
		},
		"simpleDots": {
			"interval": 400,
			"frames": [
				".  ",
				".. ",
				"...",
				"   "
			]
		},
		"simpleDotsScrolling": {
			"interval": 200,
			"frames": [
				".  ",
				".. ",
				"...",
				" ..",
				"  .",
				"   "
			]
		},
		"star": {
			"interval": 70,
			"frames": [
				"✶",
				"✸",
				"✹",
				"✺",
				"✹",
				"✷"
			]
		},
		"star2": {
			"interval": 80,
			"frames": [
				"+",
				"x",
				"*"
			]
		},
		"flip": {
			"interval": 70,
			"frames": [
				"_",
				"_",
				"_",
				"-",
				"`",
				"`",
				"'",
				"´",
				"-",
				"_",
				"_",
				"_"
			]
		},
		"hamburger": {
			"interval": 100,
			"frames": [
				"☱",
				"☲",
				"☴"
			]
		},
		"growVertical": {
			"interval": 120,
			"frames": [
				"▁",
				"▃",
				"▄",
				"▅",
				"▆",
				"▇",
				"▆",
				"▅",
				"▄",
				"▃"
			]
		},
		"growHorizontal": {
			"interval": 120,
			"frames": [
				"▏",
				"▎",
				"▍",
				"▌",
				"▋",
				"▊",
				"▉",
				"▊",
				"▋",
				"▌",
				"▍",
				"▎"
			]
		},
		"balloon": {
			"interval": 140,
			"frames": [
				" ",
				".",
				"o",
				"O",
				"@",
				"*",
				" "
			]
		},
		"balloon2": {
			"interval": 120,
			"frames": [
				".",
				"o",
				"O",
				"°",
				"O",
				"o",
				"."
			]
		},
		"noise": {
			"interval": 100,
			"frames": [
				"▓",
				"▒",
				"░"
			]
		},
		"bounce": {
			"interval": 120,
			"frames": [
				"⠁",
				"⠂",
				"⠄",
				"⠂"
			]
		},
		"boxBounce": {
			"interval": 120,
			"frames": [
				"▖",
				"▘",
				"▝",
				"▗"
			]
		},
		"boxBounce2": {
			"interval": 100,
			"frames": [
				"▌",
				"▀",
				"▐",
				"▄"
			]
		},
		"triangle": {
			"interval": 50,
			"frames": [
				"◢",
				"◣",
				"◤",
				"◥"
			]
		},
		"binary": {
			"interval": 80,
			"frames": [
				"010010",
				"001100",
				"100101",
				"111010",
				"111101",
				"010111",
				"101011",
				"111000",
				"110011",
				"110101"
			]
		},
		"arc": {
			"interval": 100,
			"frames": [
				"◜",
				"◠",
				"◝",
				"◞",
				"◡",
				"◟"
			]
		},
		"circle": {
			"interval": 120,
			"frames": [
				"◡",
				"⊙",
				"◠"
			]
		},
		"squareCorners": {
			"interval": 180,
			"frames": [
				"◰",
				"◳",
				"◲",
				"◱"
			]
		},
		"circleQuarters": {
			"interval": 120,
			"frames": [
				"◴",
				"◷",
				"◶",
				"◵"
			]
		},
		"circleHalves": {
			"interval": 50,
			"frames": [
				"◐",
				"◓",
				"◑",
				"◒"
			]
		},
		"squish": {
			"interval": 100,
			"frames": ["╫", "╪"]
		},
		"toggle": {
			"interval": 250,
			"frames": ["⊶", "⊷"]
		},
		"toggle2": {
			"interval": 80,
			"frames": ["▫", "▪"]
		},
		"toggle3": {
			"interval": 120,
			"frames": ["□", "■"]
		},
		"toggle4": {
			"interval": 100,
			"frames": [
				"■",
				"□",
				"▪",
				"▫"
			]
		},
		"toggle5": {
			"interval": 100,
			"frames": ["▮", "▯"]
		},
		"toggle6": {
			"interval": 300,
			"frames": ["ဝ", "၀"]
		},
		"toggle7": {
			"interval": 80,
			"frames": ["⦾", "⦿"]
		},
		"toggle8": {
			"interval": 100,
			"frames": ["◍", "◌"]
		},
		"toggle9": {
			"interval": 100,
			"frames": ["◉", "◎"]
		},
		"toggle10": {
			"interval": 100,
			"frames": [
				"㊂",
				"㊀",
				"㊁"
			]
		},
		"toggle11": {
			"interval": 50,
			"frames": ["⧇", "⧆"]
		},
		"toggle12": {
			"interval": 120,
			"frames": ["☗", "☖"]
		},
		"toggle13": {
			"interval": 80,
			"frames": [
				"=",
				"*",
				"-"
			]
		},
		"arrow": {
			"interval": 100,
			"frames": [
				"←",
				"↖",
				"↑",
				"↗",
				"→",
				"↘",
				"↓",
				"↙"
			]
		},
		"arrow2": {
			"interval": 80,
			"frames": [
				"⬆️ ",
				"↗️ ",
				"➡️ ",
				"↘️ ",
				"⬇️ ",
				"↙️ ",
				"⬅️ ",
				"↖️ "
			]
		},
		"arrow3": {
			"interval": 120,
			"frames": [
				"▹▹▹▹▹",
				"▸▹▹▹▹",
				"▹▸▹▹▹",
				"▹▹▸▹▹",
				"▹▹▹▸▹",
				"▹▹▹▹▸"
			]
		},
		"bouncingBar": {
			"interval": 80,
			"frames": [
				"[    ]",
				"[=   ]",
				"[==  ]",
				"[=== ]",
				"[====]",
				"[ ===]",
				"[  ==]",
				"[   =]",
				"[    ]",
				"[   =]",
				"[  ==]",
				"[ ===]",
				"[====]",
				"[=== ]",
				"[==  ]",
				"[=   ]"
			]
		},
		"bouncingBall": {
			"interval": 80,
			"frames": [
				"( ●    )",
				"(  ●   )",
				"(   ●  )",
				"(    ● )",
				"(     ●)",
				"(    ● )",
				"(   ●  )",
				"(  ●   )",
				"( ●    )",
				"(●     )"
			]
		},
		"smiley": {
			"interval": 200,
			"frames": ["😄 ", "😝 "]
		},
		"monkey": {
			"interval": 300,
			"frames": [
				"🙈 ",
				"🙈 ",
				"🙉 ",
				"🙊 "
			]
		},
		"hearts": {
			"interval": 100,
			"frames": [
				"💛 ",
				"💙 ",
				"💜 ",
				"💚 ",
				"❤️ "
			]
		},
		"clock": {
			"interval": 100,
			"frames": [
				"🕛 ",
				"🕐 ",
				"🕑 ",
				"🕒 ",
				"🕓 ",
				"🕔 ",
				"🕕 ",
				"🕖 ",
				"🕗 ",
				"🕘 ",
				"🕙 ",
				"🕚 "
			]
		},
		"earth": {
			"interval": 180,
			"frames": [
				"🌍 ",
				"🌎 ",
				"🌏 "
			]
		},
		"material": {
			"interval": 17,
			"frames": [
				"█▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁",
				"██▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁",
				"███▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁",
				"████▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁",
				"██████▁▁▁▁▁▁▁▁▁▁▁▁▁▁",
				"██████▁▁▁▁▁▁▁▁▁▁▁▁▁▁",
				"███████▁▁▁▁▁▁▁▁▁▁▁▁▁",
				"████████▁▁▁▁▁▁▁▁▁▁▁▁",
				"█████████▁▁▁▁▁▁▁▁▁▁▁",
				"█████████▁▁▁▁▁▁▁▁▁▁▁",
				"██████████▁▁▁▁▁▁▁▁▁▁",
				"███████████▁▁▁▁▁▁▁▁▁",
				"█████████████▁▁▁▁▁▁▁",
				"██████████████▁▁▁▁▁▁",
				"██████████████▁▁▁▁▁▁",
				"▁██████████████▁▁▁▁▁",
				"▁██████████████▁▁▁▁▁",
				"▁██████████████▁▁▁▁▁",
				"▁▁██████████████▁▁▁▁",
				"▁▁▁██████████████▁▁▁",
				"▁▁▁▁█████████████▁▁▁",
				"▁▁▁▁██████████████▁▁",
				"▁▁▁▁██████████████▁▁",
				"▁▁▁▁▁██████████████▁",
				"▁▁▁▁▁██████████████▁",
				"▁▁▁▁▁██████████████▁",
				"▁▁▁▁▁▁██████████████",
				"▁▁▁▁▁▁██████████████",
				"▁▁▁▁▁▁▁█████████████",
				"▁▁▁▁▁▁▁█████████████",
				"▁▁▁▁▁▁▁▁████████████",
				"▁▁▁▁▁▁▁▁████████████",
				"▁▁▁▁▁▁▁▁▁███████████",
				"▁▁▁▁▁▁▁▁▁███████████",
				"▁▁▁▁▁▁▁▁▁▁██████████",
				"▁▁▁▁▁▁▁▁▁▁██████████",
				"▁▁▁▁▁▁▁▁▁▁▁▁████████",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁███████",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁██████",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁█████",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁█████",
				"█▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁████",
				"██▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁███",
				"██▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁███",
				"███▁▁▁▁▁▁▁▁▁▁▁▁▁▁███",
				"████▁▁▁▁▁▁▁▁▁▁▁▁▁▁██",
				"█████▁▁▁▁▁▁▁▁▁▁▁▁▁▁█",
				"█████▁▁▁▁▁▁▁▁▁▁▁▁▁▁█",
				"██████▁▁▁▁▁▁▁▁▁▁▁▁▁█",
				"████████▁▁▁▁▁▁▁▁▁▁▁▁",
				"█████████▁▁▁▁▁▁▁▁▁▁▁",
				"█████████▁▁▁▁▁▁▁▁▁▁▁",
				"█████████▁▁▁▁▁▁▁▁▁▁▁",
				"█████████▁▁▁▁▁▁▁▁▁▁▁",
				"███████████▁▁▁▁▁▁▁▁▁",
				"████████████▁▁▁▁▁▁▁▁",
				"████████████▁▁▁▁▁▁▁▁",
				"██████████████▁▁▁▁▁▁",
				"██████████████▁▁▁▁▁▁",
				"▁██████████████▁▁▁▁▁",
				"▁██████████████▁▁▁▁▁",
				"▁▁▁█████████████▁▁▁▁",
				"▁▁▁▁▁████████████▁▁▁",
				"▁▁▁▁▁████████████▁▁▁",
				"▁▁▁▁▁▁███████████▁▁▁",
				"▁▁▁▁▁▁▁▁█████████▁▁▁",
				"▁▁▁▁▁▁▁▁█████████▁▁▁",
				"▁▁▁▁▁▁▁▁▁█████████▁▁",
				"▁▁▁▁▁▁▁▁▁█████████▁▁",
				"▁▁▁▁▁▁▁▁▁▁█████████▁",
				"▁▁▁▁▁▁▁▁▁▁▁████████▁",
				"▁▁▁▁▁▁▁▁▁▁▁████████▁",
				"▁▁▁▁▁▁▁▁▁▁▁▁███████▁",
				"▁▁▁▁▁▁▁▁▁▁▁▁███████▁",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁███████",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁███████",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁█████",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁████",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁████",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁████",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁███",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁███",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁██",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁██",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁██",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁█",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁█",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁█",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁",
				"▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁"
			]
		},
		"moon": {
			"interval": 80,
			"frames": [
				"🌑 ",
				"🌒 ",
				"🌓 ",
				"🌔 ",
				"🌕 ",
				"🌖 ",
				"🌗 ",
				"🌘 "
			]
		},
		"runner": {
			"interval": 140,
			"frames": ["🚶 ", "🏃 "]
		},
		"pong": {
			"interval": 80,
			"frames": [
				"▐⠂       ▌",
				"▐⠈       ▌",
				"▐ ⠂      ▌",
				"▐ ⠠      ▌",
				"▐  ⡀     ▌",
				"▐  ⠠     ▌",
				"▐   ⠂    ▌",
				"▐   ⠈    ▌",
				"▐    ⠂   ▌",
				"▐    ⠠   ▌",
				"▐     ⡀  ▌",
				"▐     ⠠  ▌",
				"▐      ⠂ ▌",
				"▐      ⠈ ▌",
				"▐       ⠂▌",
				"▐       ⠠▌",
				"▐       ⡀▌",
				"▐      ⠠ ▌",
				"▐      ⠂ ▌",
				"▐     ⠈  ▌",
				"▐     ⠂  ▌",
				"▐    ⠠   ▌",
				"▐    ⡀   ▌",
				"▐   ⠠    ▌",
				"▐   ⠂    ▌",
				"▐  ⠈     ▌",
				"▐  ⠂     ▌",
				"▐ ⠠      ▌",
				"▐ ⡀      ▌",
				"▐⠠       ▌"
			]
		},
		"shark": {
			"interval": 120,
			"frames": [
				"▐|\\____________▌",
				"▐_|\\___________▌",
				"▐__|\\__________▌",
				"▐___|\\_________▌",
				"▐____|\\________▌",
				"▐_____|\\_______▌",
				"▐______|\\______▌",
				"▐_______|\\_____▌",
				"▐________|\\____▌",
				"▐_________|\\___▌",
				"▐__________|\\__▌",
				"▐___________|\\_▌",
				"▐____________|\\▌",
				"▐____________/|▌",
				"▐___________/|_▌",
				"▐__________/|__▌",
				"▐_________/|___▌",
				"▐________/|____▌",
				"▐_______/|_____▌",
				"▐______/|______▌",
				"▐_____/|_______▌",
				"▐____/|________▌",
				"▐___/|_________▌",
				"▐__/|__________▌",
				"▐_/|___________▌",
				"▐/|____________▌"
			]
		},
		"dqpb": {
			"interval": 100,
			"frames": [
				"d",
				"q",
				"p",
				"b"
			]
		},
		"weather": {
			"interval": 100,
			"frames": [
				"☀️ ",
				"☀️ ",
				"☀️ ",
				"🌤 ",
				"⛅️ ",
				"🌥 ",
				"☁️ ",
				"🌧 ",
				"🌨 ",
				"🌧 ",
				"🌨 ",
				"🌧 ",
				"🌨 ",
				"⛈ ",
				"🌨 ",
				"🌧 ",
				"🌨 ",
				"☁️ ",
				"🌥 ",
				"⛅️ ",
				"🌤 ",
				"☀️ ",
				"☀️ "
			]
		},
		"christmas": {
			"interval": 400,
			"frames": ["🌲", "🎄"]
		},
		"grenade": {
			"interval": 80,
			"frames": [
				"،  ",
				"′  ",
				" ´ ",
				" ‾ ",
				"  ⸌",
				"  ⸊",
				"  |",
				"  ⁎",
				"  ⁕",
				" ෴ ",
				"  ⁓",
				"   ",
				"   ",
				"   "
			]
		},
		"point": {
			"interval": 125,
			"frames": [
				"∙∙∙",
				"●∙∙",
				"∙●∙",
				"∙∙●",
				"∙∙∙"
			]
		},
		"layer": {
			"interval": 150,
			"frames": [
				"-",
				"=",
				"≡"
			]
		},
		"betaWave": {
			"interval": 80,
			"frames": [
				"ρββββββ",
				"βρβββββ",
				"ββρββββ",
				"βββρβββ",
				"ββββρββ",
				"βββββρβ",
				"ββββββρ"
			]
		},
		"fingerDance": {
			"interval": 160,
			"frames": [
				"🤘 ",
				"🤟 ",
				"🖖 ",
				"✋ ",
				"🤚 ",
				"👆 "
			]
		},
		"fistBump": {
			"interval": 80,
			"frames": [
				"🤜　　　　🤛 ",
				"🤜　　　　🤛 ",
				"🤜　　　　🤛 ",
				"　🤜　　🤛　 ",
				"　　🤜🤛　　 ",
				"　🤜✨🤛　　 ",
				"🤜　✨　🤛　 "
			]
		},
		"soccerHeader": {
			"interval": 80,
			"frames": [
				" 🧑⚽️       🧑 ",
				"🧑  ⚽️      🧑 ",
				"🧑   ⚽️     🧑 ",
				"🧑    ⚽️    🧑 ",
				"🧑     ⚽️   🧑 ",
				"🧑      ⚽️  🧑 ",
				"🧑       ⚽️🧑  ",
				"🧑      ⚽️  🧑 ",
				"🧑     ⚽️   🧑 ",
				"🧑    ⚽️    🧑 ",
				"🧑   ⚽️     🧑 ",
				"🧑  ⚽️      🧑 "
			]
		},
		"mindblown": {
			"interval": 160,
			"frames": [
				"😐 ",
				"😐 ",
				"😮 ",
				"😮 ",
				"😦 ",
				"😦 ",
				"😧 ",
				"😧 ",
				"🤯 ",
				"💥 ",
				"✨ ",
				"　 ",
				"　 ",
				"　 "
			]
		},
		"speaker": {
			"interval": 160,
			"frames": [
				"🔈 ",
				"🔉 ",
				"🔊 ",
				"🔉 "
			]
		},
		"orangePulse": {
			"interval": 100,
			"frames": [
				"🔸 ",
				"🔶 ",
				"🟠 ",
				"🟠 ",
				"🔶 "
			]
		},
		"bluePulse": {
			"interval": 100,
			"frames": [
				"🔹 ",
				"🔷 ",
				"🔵 ",
				"🔵 ",
				"🔷 "
			]
		},
		"orangeBluePulse": {
			"interval": 100,
			"frames": [
				"🔸 ",
				"🔶 ",
				"🟠 ",
				"🟠 ",
				"🔶 ",
				"🔹 ",
				"🔷 ",
				"🔵 ",
				"🔵 ",
				"🔷 "
			]
		},
		"timeTravel": {
			"interval": 100,
			"frames": [
				"🕛 ",
				"🕚 ",
				"🕙 ",
				"🕘 ",
				"🕗 ",
				"🕖 ",
				"🕕 ",
				"🕔 ",
				"🕓 ",
				"🕒 ",
				"🕑 ",
				"🕐 "
			]
		},
		"aesthetic": {
			"interval": 80,
			"frames": [
				"▰▱▱▱▱▱▱",
				"▰▰▱▱▱▱▱",
				"▰▰▰▱▱▱▱",
				"▰▰▰▰▱▱▱",
				"▰▰▰▰▰▱▱",
				"▰▰▰▰▰▰▱",
				"▰▰▰▰▰▰▰",
				"▰▱▱▱▱▱▱"
			]
		},
		"dwarfFortress": {
			"interval": 80,
			"frames": [
				" ██████£££  ",
				"☺██████£££  ",
				"☺██████£££  ",
				"☺▓█████£££  ",
				"☺▓█████£££  ",
				"☺▒█████£££  ",
				"☺▒█████£££  ",
				"☺░█████£££  ",
				"☺░█████£££  ",
				"☺ █████£££  ",
				" ☺█████£££  ",
				" ☺█████£££  ",
				" ☺▓████£££  ",
				" ☺▓████£££  ",
				" ☺▒████£££  ",
				" ☺▒████£££  ",
				" ☺░████£££  ",
				" ☺░████£££  ",
				" ☺ ████£££  ",
				"  ☺████£££  ",
				"  ☺████£££  ",
				"  ☺▓███£££  ",
				"  ☺▓███£££  ",
				"  ☺▒███£££  ",
				"  ☺▒███£££  ",
				"  ☺░███£££  ",
				"  ☺░███£££  ",
				"  ☺ ███£££  ",
				"   ☺███£££  ",
				"   ☺███£££  ",
				"   ☺▓██£££  ",
				"   ☺▓██£££  ",
				"   ☺▒██£££  ",
				"   ☺▒██£££  ",
				"   ☺░██£££  ",
				"   ☺░██£££  ",
				"   ☺ ██£££  ",
				"    ☺██£££  ",
				"    ☺██£££  ",
				"    ☺▓█£££  ",
				"    ☺▓█£££  ",
				"    ☺▒█£££  ",
				"    ☺▒█£££  ",
				"    ☺░█£££  ",
				"    ☺░█£££  ",
				"    ☺ █£££  ",
				"     ☺█£££  ",
				"     ☺█£££  ",
				"     ☺▓£££  ",
				"     ☺▓£££  ",
				"     ☺▒£££  ",
				"     ☺▒£££  ",
				"     ☺░£££  ",
				"     ☺░£££  ",
				"     ☺ £££  ",
				"      ☺£££  ",
				"      ☺£££  ",
				"      ☺▓££  ",
				"      ☺▓££  ",
				"      ☺▒££  ",
				"      ☺▒££  ",
				"      ☺░££  ",
				"      ☺░££  ",
				"      ☺ ££  ",
				"       ☺££  ",
				"       ☺££  ",
				"       ☺▓£  ",
				"       ☺▓£  ",
				"       ☺▒£  ",
				"       ☺▒£  ",
				"       ☺░£  ",
				"       ☺░£  ",
				"       ☺ £  ",
				"        ☺£  ",
				"        ☺£  ",
				"        ☺▓  ",
				"        ☺▓  ",
				"        ☺▒  ",
				"        ☺▒  ",
				"        ☺░  ",
				"        ☺░  ",
				"        ☺   ",
				"        ☺  &",
				"        ☺ ☼&",
				"       ☺ ☼ &",
				"       ☺☼  &",
				"      ☺☼  & ",
				"      ‼   & ",
				"     ☺   &  ",
				"    ‼    &  ",
				"   ☺    &   ",
				"  ‼     &   ",
				" ☺     &    ",
				"‼      &    ",
				"      &     ",
				"      &     ",
				"     &   ░  ",
				"     &   ▒  ",
				"    &    ▓  ",
				"    &    £  ",
				"   &    ░£  ",
				"   &    ▒£  ",
				"  &     ▓£  ",
				"  &     ££  ",
				" &     ░££  ",
				" &     ▒££  ",
				"&      ▓££  ",
				"&      £££  ",
				"      ░£££  ",
				"      ▒£££  ",
				"      ▓£££  ",
				"      █£££  ",
				"     ░█£££  ",
				"     ▒█£££  ",
				"     ▓█£££  ",
				"     ██£££  ",
				"    ░██£££  ",
				"    ▒██£££  ",
				"    ▓██£££  ",
				"    ███£££  ",
				"   ░███£££  ",
				"   ▒███£££  ",
				"   ▓███£££  ",
				"   ████£££  ",
				"  ░████£££  ",
				"  ▒████£££  ",
				"  ▓████£££  ",
				"  █████£££  ",
				" ░█████£££  ",
				" ▒█████£££  ",
				" ▓█████£££  ",
				" ██████£££  ",
				" ██████£££  "
			]
		}
	};
}) });

//#endregion
//#region ../../node_modules/.pnpm/cli-spinners@2.9.2/node_modules/cli-spinners/index.js
var require_cli_spinners = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/cli-spinners@2.9.2/node_modules/cli-spinners/index.js": ((exports, module) => {
	const spinners$1 = Object.assign({}, require_spinners());
	const spinnersList = Object.keys(spinners$1);
	Object.defineProperty(spinners$1, "random", { get() {
		return spinners$1[spinnersList[Math.floor(Math.random() * spinnersList.length)]];
	} });
	module.exports = spinners$1;
}) });

//#endregion
//#region ../../node_modules/.pnpm/is-unicode-supported@1.3.0/node_modules/is-unicode-supported/index.js
function isUnicodeSupported$2() {
	if (process$1.platform !== "win32") return process$1.env.TERM !== "linux";
	return Boolean(process$1.env.CI) || Boolean(process$1.env.WT_SESSION) || Boolean(process$1.env.TERMINUS_SUBLIME) || process$1.env.ConEmuTask === "{cmd::Cmder}" || process$1.env.TERM_PROGRAM === "Terminus-Sublime" || process$1.env.TERM_PROGRAM === "vscode" || process$1.env.TERM === "xterm-256color" || process$1.env.TERM === "alacritty" || process$1.env.TERMINAL_EMULATOR === "JetBrains-JediTerm";
}

//#endregion
//#region ../../node_modules/.pnpm/log-symbols@6.0.0/node_modules/log-symbols/index.js
init_source();
const main = {
	info: source_default.blue("ℹ"),
	success: source_default.green("✔"),
	warning: source_default.yellow("⚠"),
	error: source_default.red("✖")
};
const fallback = {
	info: source_default.blue("i"),
	success: source_default.green("√"),
	warning: source_default.yellow("‼"),
	error: source_default.red("×")
};
const logSymbols = isUnicodeSupported$2() ? main : fallback;
var log_symbols_default = logSymbols;

//#endregion
//#region ../../node_modules/.pnpm/ansi-regex@6.2.0/node_modules/ansi-regex/index.js
function ansiRegex({ onlyFirst = false } = {}) {
	return new RegExp(`(?:\\u001B\\][\\s\\S]*?(?:\\u0007|\\u001B\\u005C|\\u009C))|[\\u001B\\u009B][[\\]()#;?]*(?:\\d{1,4}(?:[;:]\\d{0,4})*)?[\\dA-PR-TZcf-nq-uy=><~]`, onlyFirst ? void 0 : "g");
}

//#endregion
//#region ../../node_modules/.pnpm/strip-ansi@7.1.0/node_modules/strip-ansi/index.js
const regex = ansiRegex();
function stripAnsi(string$2) {
	if (typeof string$2 !== "string") throw new TypeError(`Expected a \`string\`, got \`${typeof string$2}\``);
	return string$2.replace(regex, "");
}

//#endregion
//#region ../../node_modules/.pnpm/is-interactive@2.0.0/node_modules/is-interactive/index.js
function isInteractive({ stream = process.stdout } = {}) {
	return Boolean(stream && stream.isTTY && process.env.TERM !== "dumb" && !("CI" in process.env));
}

//#endregion
//#region ../../node_modules/.pnpm/is-unicode-supported@2.1.0/node_modules/is-unicode-supported/index.js
function isUnicodeSupported$1() {
	const { env: env$2 } = process$1;
	const { TERM: TERM$1, TERM_PROGRAM } = env$2;
	if (process$1.platform !== "win32") return TERM$1 !== "linux";
	return Boolean(env$2.WT_SESSION) || Boolean(env$2.TERMINUS_SUBLIME) || env$2.ConEmuTask === "{cmd::Cmder}" || TERM_PROGRAM === "Terminus-Sublime" || TERM_PROGRAM === "vscode" || TERM$1 === "xterm-256color" || TERM$1 === "alacritty" || TERM$1 === "rxvt-unicode" || TERM$1 === "rxvt-unicode-256color" || env$2.TERMINAL_EMULATOR === "JetBrains-JediTerm";
}

//#endregion
//#region ../../node_modules/.pnpm/stdin-discarder@0.2.2/node_modules/stdin-discarder/index.js
const ASCII_ETX_CODE = 3;
var _activeCount = /* @__PURE__ */ new WeakMap();
var _StdinDiscarder_brand = /* @__PURE__ */ new WeakSet();
var StdinDiscarder = class {
	constructor() {
		_classPrivateMethodInitSpec(this, _StdinDiscarder_brand);
		_classPrivateFieldInitSpec(this, _activeCount, 0);
	}
	start() {
		var _this$activeCount;
		_classPrivateFieldSet2(_activeCount, this, (_this$activeCount = _classPrivateFieldGet2(_activeCount, this), _this$activeCount++, _this$activeCount));
		if (_classPrivateFieldGet2(_activeCount, this) === 1) _assertClassBrand(_StdinDiscarder_brand, this, _realStart).call(this);
	}
	stop() {
		var _this$activeCount3;
		if (_classPrivateFieldGet2(_activeCount, this) <= 0) throw new Error("`stop` called more times than `start`");
		_classPrivateFieldSet2(_activeCount, this, (_this$activeCount3 = _classPrivateFieldGet2(_activeCount, this), _this$activeCount3--, _this$activeCount3));
		if (_classPrivateFieldGet2(_activeCount, this) === 0) _assertClassBrand(_StdinDiscarder_brand, this, _realStop).call(this);
	}
};
function _realStart() {
	if (process$1.platform === "win32" || !process$1.stdin.isTTY) return;
	process$1.stdin.setRawMode(true);
	process$1.stdin.on("data", _assertClassBrand(_StdinDiscarder_brand, this, _handleInput));
	process$1.stdin.resume();
}
function _realStop() {
	if (!process$1.stdin.isTTY) return;
	process$1.stdin.off("data", _assertClassBrand(_StdinDiscarder_brand, this, _handleInput));
	process$1.stdin.pause();
	process$1.stdin.setRawMode(false);
}
function _handleInput(chunk) {
	if (chunk[0] === ASCII_ETX_CODE) process$1.emit("SIGINT");
}
const stdinDiscarder = new StdinDiscarder();
var stdin_discarder_default = stdinDiscarder;

//#endregion
//#region ../../node_modules/.pnpm/ora@8.2.0/node_modules/ora/index.js
init_source();
var import_cli_spinners = /* @__PURE__ */ __toESM(require_cli_spinners(), 1);
var import_cli_spinners$1 = /* @__PURE__ */ __toESM(require_cli_spinners(), 1);
var _linesToClear = /* @__PURE__ */ new WeakMap();
var _isDiscardingStdin = /* @__PURE__ */ new WeakMap();
var _lineCount = /* @__PURE__ */ new WeakMap();
var _frameIndex = /* @__PURE__ */ new WeakMap();
var _lastSpinnerFrameTime = /* @__PURE__ */ new WeakMap();
var _options = /* @__PURE__ */ new WeakMap();
var _spinner = /* @__PURE__ */ new WeakMap();
var _stream = /* @__PURE__ */ new WeakMap();
var _id = /* @__PURE__ */ new WeakMap();
var _initialInterval = /* @__PURE__ */ new WeakMap();
var _isEnabled = /* @__PURE__ */ new WeakMap();
var _isSilent = /* @__PURE__ */ new WeakMap();
var _indent = /* @__PURE__ */ new WeakMap();
var _text = /* @__PURE__ */ new WeakMap();
var _prefixText = /* @__PURE__ */ new WeakMap();
var _suffixText = /* @__PURE__ */ new WeakMap();
var _Ora_brand = /* @__PURE__ */ new WeakSet();
var Ora = class {
	constructor(options) {
		_classPrivateMethodInitSpec(this, _Ora_brand);
		_classPrivateFieldInitSpec(this, _linesToClear, 0);
		_classPrivateFieldInitSpec(this, _isDiscardingStdin, false);
		_classPrivateFieldInitSpec(this, _lineCount, 0);
		_classPrivateFieldInitSpec(this, _frameIndex, -1);
		_classPrivateFieldInitSpec(this, _lastSpinnerFrameTime, 0);
		_classPrivateFieldInitSpec(this, _options, void 0);
		_classPrivateFieldInitSpec(this, _spinner, void 0);
		_classPrivateFieldInitSpec(this, _stream, void 0);
		_classPrivateFieldInitSpec(this, _id, void 0);
		_classPrivateFieldInitSpec(this, _initialInterval, void 0);
		_classPrivateFieldInitSpec(this, _isEnabled, void 0);
		_classPrivateFieldInitSpec(this, _isSilent, void 0);
		_classPrivateFieldInitSpec(this, _indent, void 0);
		_classPrivateFieldInitSpec(this, _text, void 0);
		_classPrivateFieldInitSpec(this, _prefixText, void 0);
		_classPrivateFieldInitSpec(this, _suffixText, void 0);
		if (typeof options === "string") options = { text: options };
		_classPrivateFieldSet2(_options, this, {
			color: "cyan",
			stream: process$1.stderr,
			discardStdin: true,
			hideCursor: true,
			...options
		});
		this.color = _classPrivateFieldGet2(_options, this).color;
		this.spinner = _classPrivateFieldGet2(_options, this).spinner;
		_classPrivateFieldSet2(_initialInterval, this, _classPrivateFieldGet2(_options, this).interval);
		_classPrivateFieldSet2(_stream, this, _classPrivateFieldGet2(_options, this).stream);
		_classPrivateFieldSet2(_isEnabled, this, typeof _classPrivateFieldGet2(_options, this).isEnabled === "boolean" ? _classPrivateFieldGet2(_options, this).isEnabled : isInteractive({ stream: _classPrivateFieldGet2(_stream, this) }));
		_classPrivateFieldSet2(_isSilent, this, typeof _classPrivateFieldGet2(_options, this).isSilent === "boolean" ? _classPrivateFieldGet2(_options, this).isSilent : false);
		this.text = _classPrivateFieldGet2(_options, this).text;
		this.prefixText = _classPrivateFieldGet2(_options, this).prefixText;
		this.suffixText = _classPrivateFieldGet2(_options, this).suffixText;
		this.indent = _classPrivateFieldGet2(_options, this).indent;
		if (process$1.env.NODE_ENV === "test") {
			this._stream = _classPrivateFieldGet2(_stream, this);
			this._isEnabled = _classPrivateFieldGet2(_isEnabled, this);
			Object.defineProperty(this, "_linesToClear", {
				get() {
					return _classPrivateFieldGet2(_linesToClear, this);
				},
				set(newValue) {
					_classPrivateFieldSet2(_linesToClear, this, newValue);
				}
			});
			Object.defineProperty(this, "_frameIndex", { get() {
				return _classPrivateFieldGet2(_frameIndex, this);
			} });
			Object.defineProperty(this, "_lineCount", { get() {
				return _classPrivateFieldGet2(_lineCount, this);
			} });
		}
	}
	get indent() {
		return _classPrivateFieldGet2(_indent, this);
	}
	set indent(indent$1 = 0) {
		if (!(indent$1 >= 0 && Number.isInteger(indent$1))) throw new Error("The `indent` option must be an integer from 0 and up");
		_classPrivateFieldSet2(_indent, this, indent$1);
		_assertClassBrand(_Ora_brand, this, _updateLineCount).call(this);
	}
	get interval() {
		return _classPrivateFieldGet2(_initialInterval, this) ?? _classPrivateFieldGet2(_spinner, this).interval ?? 100;
	}
	get spinner() {
		return _classPrivateFieldGet2(_spinner, this);
	}
	set spinner(spinner) {
		_classPrivateFieldSet2(_frameIndex, this, -1);
		_classPrivateFieldSet2(_initialInterval, this, void 0);
		if (typeof spinner === "object") {
			if (spinner.frames === void 0) throw new Error("The given spinner must have a `frames` property");
			_classPrivateFieldSet2(_spinner, this, spinner);
		} else if (!isUnicodeSupported$1()) _classPrivateFieldSet2(_spinner, this, import_cli_spinners.default.line);
		else if (spinner === void 0) _classPrivateFieldSet2(_spinner, this, import_cli_spinners.default.dots);
		else if (spinner !== "default" && import_cli_spinners.default[spinner]) _classPrivateFieldSet2(_spinner, this, import_cli_spinners.default[spinner]);
		else throw new Error(`There is no built-in spinner named '${spinner}'. See https://github.com/sindresorhus/cli-spinners/blob/main/spinners.json for a full list.`);
	}
	get text() {
		return _classPrivateFieldGet2(_text, this);
	}
	set text(value = "") {
		_classPrivateFieldSet2(_text, this, value);
		_assertClassBrand(_Ora_brand, this, _updateLineCount).call(this);
	}
	get prefixText() {
		return _classPrivateFieldGet2(_prefixText, this);
	}
	set prefixText(value = "") {
		_classPrivateFieldSet2(_prefixText, this, value);
		_assertClassBrand(_Ora_brand, this, _updateLineCount).call(this);
	}
	get suffixText() {
		return _classPrivateFieldGet2(_suffixText, this);
	}
	set suffixText(value = "") {
		_classPrivateFieldSet2(_suffixText, this, value);
		_assertClassBrand(_Ora_brand, this, _updateLineCount).call(this);
	}
	get isSpinning() {
		return _classPrivateFieldGet2(_id, this) !== void 0;
	}
	get isEnabled() {
		return _classPrivateFieldGet2(_isEnabled, this) && !_classPrivateFieldGet2(_isSilent, this);
	}
	set isEnabled(value) {
		if (typeof value !== "boolean") throw new TypeError("The `isEnabled` option must be a boolean");
		_classPrivateFieldSet2(_isEnabled, this, value);
	}
	get isSilent() {
		return _classPrivateFieldGet2(_isSilent, this);
	}
	set isSilent(value) {
		if (typeof value !== "boolean") throw new TypeError("The `isSilent` option must be a boolean");
		_classPrivateFieldSet2(_isSilent, this, value);
	}
	frame() {
		const now = Date.now();
		if (_classPrivateFieldGet2(_frameIndex, this) === -1 || now - _classPrivateFieldGet2(_lastSpinnerFrameTime, this) >= this.interval) {
			var _this$frameIndex;
			_classPrivateFieldSet2(_frameIndex, this, _classPrivateFieldSet2(_frameIndex, this, (_this$frameIndex = _classPrivateFieldGet2(_frameIndex, this), ++_this$frameIndex)) % _classPrivateFieldGet2(_spinner, this).frames.length);
			_classPrivateFieldSet2(_lastSpinnerFrameTime, this, now);
		}
		const { frames } = _classPrivateFieldGet2(_spinner, this);
		let frame = frames[_classPrivateFieldGet2(_frameIndex, this)];
		if (this.color) frame = source_default[this.color](frame);
		const fullPrefixText = typeof _classPrivateFieldGet2(_prefixText, this) === "string" && _classPrivateFieldGet2(_prefixText, this) !== "" ? _classPrivateFieldGet2(_prefixText, this) + " " : "";
		const fullText = typeof this.text === "string" ? " " + this.text : "";
		const fullSuffixText = typeof _classPrivateFieldGet2(_suffixText, this) === "string" && _classPrivateFieldGet2(_suffixText, this) !== "" ? " " + _classPrivateFieldGet2(_suffixText, this) : "";
		return fullPrefixText + frame + fullText + fullSuffixText;
	}
	clear() {
		if (!_classPrivateFieldGet2(_isEnabled, this) || !_classPrivateFieldGet2(_stream, this).isTTY) return this;
		_classPrivateFieldGet2(_stream, this).cursorTo(0);
		for (let index = 0; index < _classPrivateFieldGet2(_linesToClear, this); index++) {
			if (index > 0) _classPrivateFieldGet2(_stream, this).moveCursor(0, -1);
			_classPrivateFieldGet2(_stream, this).clearLine(1);
		}
		if (_classPrivateFieldGet2(_indent, this) || this.lastIndent !== _classPrivateFieldGet2(_indent, this)) _classPrivateFieldGet2(_stream, this).cursorTo(_classPrivateFieldGet2(_indent, this));
		this.lastIndent = _classPrivateFieldGet2(_indent, this);
		_classPrivateFieldSet2(_linesToClear, this, 0);
		return this;
	}
	render() {
		if (_classPrivateFieldGet2(_isSilent, this)) return this;
		this.clear();
		_classPrivateFieldGet2(_stream, this).write(this.frame());
		_classPrivateFieldSet2(_linesToClear, this, _classPrivateFieldGet2(_lineCount, this));
		return this;
	}
	start(text) {
		if (text) this.text = text;
		if (_classPrivateFieldGet2(_isSilent, this)) return this;
		if (!_classPrivateFieldGet2(_isEnabled, this)) {
			if (this.text) _classPrivateFieldGet2(_stream, this).write(`- ${this.text}\n`);
			return this;
		}
		if (this.isSpinning) return this;
		if (_classPrivateFieldGet2(_options, this).hideCursor) cli_cursor_default.hide(_classPrivateFieldGet2(_stream, this));
		if (_classPrivateFieldGet2(_options, this).discardStdin && process$1.stdin.isTTY) {
			_classPrivateFieldSet2(_isDiscardingStdin, this, true);
			stdin_discarder_default.start();
		}
		this.render();
		_classPrivateFieldSet2(_id, this, setInterval(this.render.bind(this), this.interval));
		return this;
	}
	stop() {
		if (!_classPrivateFieldGet2(_isEnabled, this)) return this;
		clearInterval(_classPrivateFieldGet2(_id, this));
		_classPrivateFieldSet2(_id, this, void 0);
		_classPrivateFieldSet2(_frameIndex, this, 0);
		this.clear();
		if (_classPrivateFieldGet2(_options, this).hideCursor) cli_cursor_default.show(_classPrivateFieldGet2(_stream, this));
		if (_classPrivateFieldGet2(_options, this).discardStdin && process$1.stdin.isTTY && _classPrivateFieldGet2(_isDiscardingStdin, this)) {
			stdin_discarder_default.stop();
			_classPrivateFieldSet2(_isDiscardingStdin, this, false);
		}
		return this;
	}
	succeed(text) {
		return this.stopAndPersist({
			symbol: log_symbols_default.success,
			text
		});
	}
	fail(text) {
		return this.stopAndPersist({
			symbol: log_symbols_default.error,
			text
		});
	}
	warn(text) {
		return this.stopAndPersist({
			symbol: log_symbols_default.warning,
			text
		});
	}
	info(text) {
		return this.stopAndPersist({
			symbol: log_symbols_default.info,
			text
		});
	}
	stopAndPersist(options = {}) {
		if (_classPrivateFieldGet2(_isSilent, this)) return this;
		const prefixText = options.prefixText ?? _classPrivateFieldGet2(_prefixText, this);
		const fullPrefixText = _assertClassBrand(_Ora_brand, this, _getFullPrefixText).call(this, prefixText, " ");
		const symbolText = options.symbol ?? " ";
		const text = options.text ?? this.text;
		const fullText = typeof text === "string" ? (symbolText ? " " : "") + text : "";
		const suffixText = options.suffixText ?? _classPrivateFieldGet2(_suffixText, this);
		const fullSuffixText = _assertClassBrand(_Ora_brand, this, _getFullSuffixText).call(this, suffixText, " ");
		const textToWrite = fullPrefixText + symbolText + fullText + fullSuffixText + "\n";
		this.stop();
		_classPrivateFieldGet2(_stream, this).write(textToWrite);
		return this;
	}
};
function _getFullPrefixText(prefixText = _classPrivateFieldGet2(_prefixText, this), postfix = " ") {
	if (typeof prefixText === "string" && prefixText !== "") return prefixText + postfix;
	if (typeof prefixText === "function") return prefixText() + postfix;
	return "";
}
function _getFullSuffixText(suffixText = _classPrivateFieldGet2(_suffixText, this), prefix$1 = " ") {
	if (typeof suffixText === "string" && suffixText !== "") return prefix$1 + suffixText;
	if (typeof suffixText === "function") return prefix$1 + suffixText();
	return "";
}
function _updateLineCount() {
	const columns = _classPrivateFieldGet2(_stream, this).columns ?? 80;
	const fullPrefixText = _assertClassBrand(_Ora_brand, this, _getFullPrefixText).call(this, _classPrivateFieldGet2(_prefixText, this), "-");
	const fullSuffixText = _assertClassBrand(_Ora_brand, this, _getFullSuffixText).call(this, _classPrivateFieldGet2(_suffixText, this), "-");
	const fullText = " ".repeat(_classPrivateFieldGet2(_indent, this)) + fullPrefixText + "--" + _classPrivateFieldGet2(_text, this) + "--" + fullSuffixText;
	_classPrivateFieldSet2(_lineCount, this, 0);
	for (const line of stripAnsi(fullText).split("\n")) _classPrivateFieldSet2(_lineCount, this, _classPrivateFieldGet2(_lineCount, this) + Math.max(1, Math.ceil(stringWidth(line, { countAnsiEscapeCodes: true }) / columns)));
}
function ora(options) {
	return new Ora(options);
}

//#endregion
//#region src/util/abort-controller.ts
/**
* When working with AbortSignals, it's common to encounter errors that are a result of the signal being aborted.
* This utility function helps to ignore those specific errors, allowing the program to continue its execution without being interrupted by them.
*
* @example
* const controller = new AbortController();
* fetch(URL, { signal: controller.signal }).catch(
*   ignoreAbortErrors(controller.signal) // we don't care if it errored as it was aborted
* )
*/
function ignoreAbortErrors(signal) {
	return (err$1) => {
		if (signal.aborted) return;
		throw err$1;
	};
}

//#endregion
//#region src/util/disposables.ts
/**
* Utility function that allows to acquire a resource
* colocated with its release logic
*
* @example
* using resource = acquireRelease(
*   () => new AbortController(),
*   (c) => c.abort(),
* );
*/
function acquireRelease(fn, release) {
	const value = fn();
	return Object.assign(value, { [Symbol.dispose]: () => release(value) });
}
/**
* Utility function that allows to create a disposable
* with a custom dispose logic like Golang's `defer`
* which allows us to avoid `try/finally` blocks.
*/
function defer(fn) {
	return { [Symbol.dispose]: fn };
}
/**
* Creates an AbortController that is also disposable,
* which aborts when disposed with a given reason.
*/
function createAbortController(reason) {
	return acquireRelease(() => {
		const controller = new AbortController();
		return {
			abort: (newReason) => controller.abort(newReason ?? reason),
			signal: controller.signal,
			ignoreInterruptions: ignoreAbortErrors(controller.signal)
		};
	}, (c$1) => c$1.abort(reason));
}

//#endregion
//#region \0@oxc-project+runtime@0.98.0/helpers/usingCtx.js
function _usingCtx() {
	var r = "function" == typeof SuppressedError ? SuppressedError : function(r$1, e$1) {
		var n$1 = Error();
		return n$1.name = "SuppressedError", n$1.error = r$1, n$1.suppressed = e$1, n$1;
	}, e = {}, n = [];
	function using(r$1, e$1) {
		if (null != e$1) {
			if (Object(e$1) !== e$1) throw new TypeError("using declarations can only be used with objects, functions, null, or undefined.");
			if (r$1) var o = e$1[Symbol.asyncDispose || Symbol["for"]("Symbol.asyncDispose")];
			if (void 0 === o && (o = e$1[Symbol.dispose || Symbol["for"]("Symbol.dispose")], r$1)) var t = o;
			if ("function" != typeof o) throw new TypeError("Object is not disposable.");
			t && (o = function o$1() {
				try {
					t.call(e$1);
				} catch (r$2) {
					return Promise.reject(r$2);
				}
			}), n.push({
				v: e$1,
				d: o,
				a: r$1
			});
		} else r$1 && n.push({
			d: e$1,
			a: r$1
		});
		return e$1;
	}
	return {
		e,
		u: using.bind(null, !1),
		a: using.bind(null, !0),
		d: function d$1() {
			var o, t = this.e, s$1 = 0;
			function next() {
				for (; o = n.pop();) try {
					if (!o.a && 1 === s$1) return s$1 = 0, n.push(o), Promise.resolve().then(next);
					if (o.d) {
						var r$1 = o.d.call(o.v);
						if (o.a) return s$1 |= 2, Promise.resolve(r$1).then(next, err$1);
					} else s$1 |= 1;
				} catch (r$2) {
					return err$1(r$2);
				}
				if (1 === s$1) return t !== e ? Promise.reject(t) : Promise.resolve();
				if (t !== e) throw t;
			}
			function err$1(n$1) {
				return t = t !== e ? new r(n$1, t) : n$1, next();
			}
			return next();
		}
	};
}

//#endregion
//#region src/commands/login.ts
var import_cjs$26 = /* @__PURE__ */ __toESM(require_cjs());
init_source();
const debug$8 = createDebugger("sandbox:login");
const login = import_cjs$26.command({
	name: "login",
	description: "Log in to the Sandbox CLI",
	args: {},
	async handler() {
		try {
			var _usingCtx$1 = _usingCtx();
			const spinner = _usingCtx$1.u(acquireRelease(() => ora("Creating device authorization request...").start(), (s$1) => s$1.stop()));
			const oauth = await OAuth();
			const request = await oauth.deviceAuthorizationRequest();
			const { user_code, verification_uri, verification_uri_complete } = request;
			spinner.text = "Waiting for you to authenticate...";
			const subscribeToStdin = process.stdin.isTTY;
			const _returnListener = _usingCtx$1.u(acquireRelease(() => {
				const callback = async (char) => {
					if (char.toString() === "\r") await open_default(verification_uri_complete);
				};
				const beforeExit = () => {
					if (spinner.isSpinning) spinner.fail("Authentication cancelled.");
					process.off("SIGINT", beforeExit);
					process.emit("SIGINT");
				};
				if (subscribeToStdin) {
					process.stdin.on("data", callback);
					process.on("SIGINT", beforeExit);
				}
				return () => {
					process.stdin.off("data", callback);
					process.off("SIGINT", beforeExit);
				};
			}, (unsubscribe) => unsubscribe()));
			const verificationLink = output.link(verification_uri.replace("https://", ""), verification_uri_complete);
			let text = `Visit ${source_default.bold(verificationLink)} and enter ${source_default.bold(user_code)}`;
			if (subscribeToStdin) text += source_default.grey("\nℹ️ Press [ENTER] to open the browser");
			spinner.text = `Waiting for authentication...\n${text}`;
			let error;
			for await (const event of pollForToken({
				request,
				oauth
			})) switch (event._tag) {
				case "Timeout":
					debug$8(`Connection timeout. Slowing down, polling every ${event.newInterval / 1e3}s...`);
					break;
				case "SlowDown":
					debug$8(`Authorization server requests to slow down. Polling every ${event.newInterval / 1e3}s...`);
					break;
				case "Response":
					debug$8("Device Access Token response:", await event.response.text());
					break;
				case "Error":
					error = event.error;
					break;
				default: output.error(source_default.yellow([
					`${source_default.bold("warn:")} unexpected polling event ignored.`,
					`├▶ event: ${JSON.stringify(event)}`,
					`│  This indicates a version mismatch between CLI and SDK.`,
					`╰▶ ${source_default.bold("help:")} update @vercel/sandbox with \`npm update @vercel/sandbox\`.`,
					`   If this persists, report at https://github.com/vercel/vercel/issues`,
					`   with the above event details.`
				].join("\n")));
			}
			if (error) {
				spinner.fail(`${source_default.red("error:")} ${error.message}`);
				process.exitCode = 1;
			} else {
				spinner.succeed(`${source_default.cyan("Congratulations!")} You are now signed in.`);
				const auth = getAuth();
				if (auth?.token) try {
					const { teamId, teamSlug, projectId, projectSlug } = await inferScope({ token: auth.token });
					process.stderr.write(source_default.dim("   │ ") + "team: " + source_default.cyan(teamSlug ?? teamId) + "\n");
					process.stderr.write(source_default.dim("   ╰ ") + "project: " + source_default.cyan(projectSlug ?? projectId) + "\n");
				} catch {}
			}
		} catch (_) {
			_usingCtx$1.e = _;
		} finally {
			_usingCtx$1.d();
		}
	}
});

//#endregion
//#region ../../node_modules/.pnpm/@vercel+oidc@3.2.0/node_modules/@vercel/oidc/dist/get-context.js
var require_get_context = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/@vercel+oidc@3.2.0/node_modules/@vercel/oidc/dist/get-context.js": ((exports, module) => {
	var __defProp$2 = Object.defineProperty;
	var __getOwnPropDesc$2 = Object.getOwnPropertyDescriptor;
	var __getOwnPropNames$2 = Object.getOwnPropertyNames;
	var __hasOwnProp$2 = Object.prototype.hasOwnProperty;
	var __export$2 = (target, all) => {
		for (var name in all) __defProp$2(target, name, {
			get: all[name],
			enumerable: true
		});
	};
	var __copyProps$2 = (to, from, except, desc) => {
		if (from && typeof from === "object" || typeof from === "function") {
			for (let key of __getOwnPropNames$2(from)) if (!__hasOwnProp$2.call(to, key) && key !== except) __defProp$2(to, key, {
				get: () => from[key],
				enumerable: !(desc = __getOwnPropDesc$2(from, key)) || desc.enumerable
			});
		}
		return to;
	};
	var __toCommonJS$2 = (mod) => __copyProps$2(__defProp$2({}, "__esModule", { value: true }), mod);
	var get_context_exports = {};
	__export$2(get_context_exports, {
		SYMBOL_FOR_REQ_CONTEXT: () => SYMBOL_FOR_REQ_CONTEXT,
		getContext: () => getContext
	});
	module.exports = __toCommonJS$2(get_context_exports);
	const SYMBOL_FOR_REQ_CONTEXT = Symbol.for("@vercel/request-context");
	function getContext() {
		return globalThis[SYMBOL_FOR_REQ_CONTEXT]?.get?.() ?? {};
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/@vercel+oidc@3.2.0/node_modules/@vercel/oidc/dist/get-vercel-oidc-token.js
var require_get_vercel_oidc_token = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/@vercel+oidc@3.2.0/node_modules/@vercel/oidc/dist/get-vercel-oidc-token.js": ((exports, module) => {
	var __defProp$1 = Object.defineProperty;
	var __getOwnPropDesc$1 = Object.getOwnPropertyDescriptor;
	var __getOwnPropNames$1 = Object.getOwnPropertyNames;
	var __hasOwnProp$1 = Object.prototype.hasOwnProperty;
	var __export$1 = (target, all) => {
		for (var name in all) __defProp$1(target, name, {
			get: all[name],
			enumerable: true
		});
	};
	var __copyProps$1 = (to, from, except, desc) => {
		if (from && typeof from === "object" || typeof from === "function") {
			for (let key of __getOwnPropNames$1(from)) if (!__hasOwnProp$1.call(to, key) && key !== except) __defProp$1(to, key, {
				get: () => from[key],
				enumerable: !(desc = __getOwnPropDesc$1(from, key)) || desc.enumerable
			});
		}
		return to;
	};
	var __toCommonJS$1 = (mod) => __copyProps$1(__defProp$1({}, "__esModule", { value: true }), mod);
	var get_vercel_oidc_token_exports = {};
	__export$1(get_vercel_oidc_token_exports, {
		getVercelOidcToken: () => getVercelOidcToken$1,
		getVercelOidcTokenSync: () => getVercelOidcTokenSync
	});
	module.exports = __toCommonJS$1(get_vercel_oidc_token_exports);
	var import_get_context$1 = require_get_context();
	var import_token_error = require_token_error();
	async function getVercelOidcToken$1(options) {
		let token$1 = "";
		let err$1;
		try {
			token$1 = getVercelOidcTokenSync();
		} catch (error) {
			err$1 = error;
		}
		try {
			const [{ getTokenPayload, isExpired }, { refreshToken }] = await Promise.all([await import("./token-util-B2dwBZRL.mjs").then(__toDynamicImportESM()), await import("./token-CFLlmxRf.mjs").then(__toDynamicImportESM())]);
			if (!token$1 || isExpired(getTokenPayload(token$1), options?.expirationBufferMs)) {
				await refreshToken(options);
				token$1 = getVercelOidcTokenSync();
			}
		} catch (error) {
			let message = err$1 instanceof Error ? err$1.message : "";
			if (error instanceof Error) message = `${message}
${error.message}`;
			if (message) throw new import_token_error.VercelOidcTokenError(message);
			throw error;
		}
		return token$1;
	}
	function getVercelOidcTokenSync() {
		const token$1 = (0, import_get_context$1.getContext)().headers?.["x-vercel-oidc-token"] ?? process.env.VERCEL_OIDC_TOKEN;
		if (!token$1) throw new Error(`The 'x-vercel-oidc-token' header is missing from the request. Do you have the OIDC option enabled in the Vercel project settings?`);
		return token$1;
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/@vercel+oidc@3.2.0/node_modules/@vercel/oidc/dist/index.js
var require_dist = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/@vercel+oidc@3.2.0/node_modules/@vercel/oidc/dist/index.js": ((exports, module) => {
	var __defProp = Object.defineProperty;
	var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
	var __getOwnPropNames = Object.getOwnPropertyNames;
	var __hasOwnProp = Object.prototype.hasOwnProperty;
	var __export = (target, all) => {
		for (var name in all) __defProp(target, name, {
			get: all[name],
			enumerable: true
		});
	};
	var __copyProps = (to, from, except, desc) => {
		if (from && typeof from === "object" || typeof from === "function") {
			for (let key of __getOwnPropNames(from)) if (!__hasOwnProp.call(to, key) && key !== except) __defProp(to, key, {
				get: () => from[key],
				enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
			});
		}
		return to;
	};
	var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
	var src_exports = {};
	__export(src_exports, {
		AccessTokenMissingError: () => import_auth_errors.AccessTokenMissingError,
		RefreshAccessTokenFailedError: () => import_auth_errors.RefreshAccessTokenFailedError,
		getContext: () => import_get_context.getContext,
		getVercelOidcToken: () => import_get_vercel_oidc_token.getVercelOidcToken,
		getVercelOidcTokenSync: () => import_get_vercel_oidc_token.getVercelOidcTokenSync,
		getVercelToken: () => import_token_util.getVercelToken
	});
	module.exports = __toCommonJS(src_exports);
	var import_get_vercel_oidc_token = require_get_vercel_oidc_token();
	var import_get_context = require_get_context();
	var import_auth_errors = require_auth_errors();
	var import_token_util = require_token_util();
}) });

//#endregion
//#region src/args/auth.ts
var import_cjs$25 = /* @__PURE__ */ __toESM(require_cjs());
init_source();
var import_dist = require_dist();
const debug$7 = createDebugger("sandbox:args:auth");
/**
* Timestamp (ms epoch) of the most recent auto-login. Used to identify
* tokens so that the first 401/403 against a freshly-issued token can be
* retried instead of surfaced to the user.
*/
let freshTokenAcquiredAt;
const FRESH_TOKEN_WINDOW_MS = 1e4;
function isTokenFresh() {
	return freshTokenAcquiredAt !== void 0 && Date.now() - freshTokenAcquiredAt < FRESH_TOKEN_WINDOW_MS;
}
function markTokenAsFresh() {
	freshTokenAcquiredAt = Date.now();
}
const token = import_cjs$25.option({
	long: "token",
	description: "A Vercel authentication token. If not provided, will use the token stored in your system from `VERCEL_AUTH_TOKEN` or will start a log in process.",
	type: {
		...import_cjs$25.string,
		displayName: "pat_or_oidc",
		defaultValueIsSerializable: false,
		onMissing: async () => {
			if (process.env.VERCEL_AUTH_TOKEN) return process.env.VERCEL_AUTH_TOKEN;
			if (process.env.VERCEL_OIDC_TOKEN) try {
				return await (0, import_dist.getVercelOidcToken)();
			} catch (cause) {
				debug$7(`Failed to get or refresh OIDC token: ${getMessage(cause)}`);
				console.warn(source_default.yellow(`${source_default.bold("warn:")} failed to get or refresh OIDC token, using personal token authentication.`));
			}
			try {
				return await (0, import_dist.getVercelToken)();
			} catch (error) {
				if (error instanceof import_dist.AccessTokenMissingError || error instanceof import_dist.RefreshAccessTokenFailedError) {
					debug$7(`CLI token unavailable (${error.name}), prompting for login...`);
					console.warn(source_default.yellow(`${source_default.bold("notice:")} Your session has expired. Please log in again.`));
					await login.handler({});
					try {
						const refreshed = await (0, import_dist.getVercelToken)();
						markTokenAsFresh();
						return refreshed;
					} catch (retryError) {
						throw new Error([
							`Failed to retrieve authentication token.`,
							`${source_default.bold("hint:")} Try logging in again with \`sandbox login\`.`,
							"╰▶ Docs: https://vercel.com/docs/vercel-sandbox/cli-reference#authentication"
						].join("\n"));
					}
				}
				throw error;
			}
		}
	}
});
function getMessage(error) {
	if (!(error instanceof Error)) return String(error);
	let message = error.message;
	if (error.cause) message += `\nCaused by: ${getMessage(error.cause)}`;
	return message;
}

//#endregion
//#region src/util/zod.ts
/**
* A Zod codec that serializes and deserializes JSON strings.
*/
const jsonCodec = (schema) => z$1.codec(z$1.string(), schema, {
	encode: (value) => JSON.stringify(value, null, 2),
	decode: (jsonString, ctx) => {
		try {
			return JSON.parse(jsonString);
		} catch (err$1) {
			ctx.issues.push({
				code: "invalid_format",
				format: "json",
				input: jsonString,
				message: err$1.message
			});
			return z$1.NEVER;
		}
	}
});

//#endregion
//#region src/util/project-configuration.ts
const ProjectConfiguration = jsonCodec(z.object({
	orgId: z.string(),
	projectId: z.string()
}));
function readProjectConfiguration(cwd) {
	try {
		const pathname = Path.join(cwd, ".vercel", "project.json");
		const string$2 = fs$1.readFileSync(pathname, "utf8");
		return ProjectConfiguration.decode(string$2);
	} catch {
		return null;
	}
}

//#endregion
//#region src/util/infer-scope.ts
const debug$6 = createDebugger("sandbox:scope");
async function inferScope$1({ token: token$1, team: team$1 }) {
	const jwt = z.jwt().safeParse(token$1);
	if (jwt.success) {
		debug$6("trying to infer scope from OIDC JWT");
		const data = await inferFromJwt(jwt.data);
		debug$6("Using scope from OIDC JWT", data);
		return data;
	}
	const projectJson = readProjectConfiguration(process.cwd());
	if (projectJson) {
		debug$6("Using scope from project configuration", {
			owner: projectJson.orgId,
			project: projectJson.projectId
		});
		return {
			owner: projectJson.orgId,
			project: projectJson.projectId
		};
	}
	debug$6("trying to infer scope from API token", {
		token: token$1,
		team: team$1
	});
	const fromToken = await inferFromToken(token$1, team$1);
	debug$6("Using scope from API token", fromToken);
	return fromToken;
}
const JwtSchema = z.object({
	project_id: z.string(),
	owner_id: z.string(),
	project: z.string().optional(),
	owner: z.string().optional()
}).transform((data) => {
	return {
		project: data.project_id,
		owner: data.owner_id,
		projectSlug: data.project,
		ownerSlug: data.owner
	};
});
async function inferFromJwt(jwt) {
	const body = JSON.parse(Buffer.from(jwt.split(".")[1], "base64").toString());
	return {
		_tag: "OIDC",
		...JwtSchema.parse(body)
	};
}
async function inferFromToken(token$1, requestedTeam) {
	const { teamId, teamSlug, projectId, projectSlug } = await Auth.inferScope({
		token: token$1,
		teamId: requestedTeam
	});
	return {
		owner: teamId,
		project: projectId,
		ownerSlug: teamSlug ?? teamId,
		projectSlug: projectSlug ?? projectId
	};
}

//#endregion
//#region src/util/fresh-auth-retry.ts
const debug$5 = createDebugger("sandbox:fresh-auth-retry");
/**
* Run an async operation, transparently retrying on 401/403 when the auth
* token was just acquired via auto-login.
*/
async function withFreshAuthRetry(factory) {
	return retry(async (bail, attempt) => {
		try {
			return await factory();
		} catch (error) {
			const status = getAuthFailureStatus(error);
			if (status !== void 0 && isTokenFresh()) {
				debug$5(`fresh-auth retry attempt ${attempt} (status ${status})`);
				throw error;
			}
			bail(error);
			return;
		}
	}, {
		retries: 3,
		minTimeout: 250,
		factor: 2,
		maxRetryTime: 3e3
	});
}
/**
* Returns the HTTP status if the error represents a 401 or 403.
* Returns undefined for any other error.
*/
function getAuthFailureStatus(error) {
	let status;
	if (error instanceof APIError) status = error.response.status;
	else if (error instanceof NotOk) status = error.response.statusCode;
	return status === 401 || status === 403 ? status : void 0;
}

//#endregion
//#region src/args/scope.ts
var import_cjs$24 = /* @__PURE__ */ __toESM(require_cjs());
init_source();
const debug$4 = createDebugger("sandbox:scope");
const project = import_cjs$24.option({
	long: "project",
	type: {
		...import_cjs$24.optional(import_cjs$24.string),
		displayName: "my-project"
	},
	description: "The project name or ID to associate with the command. Can be inferred from VERCEL_OIDC_TOKEN."
});
/** Parser for the `--team` option. */
const teamParser = import_cjs$24.option({
	long: "team",
	type: {
		...import_cjs$24.optional(import_cjs$24.string),
		displayName: "my-team"
	}
});
/** Parser for the `--scope` option. */
const scopeParser = import_cjs$24.option({
	long: "scope",
	type: {
		...import_cjs$24.optional(import_cjs$24.string),
		displayName: "my-team"
	},
	description: "The scope/team to associate with the command. Can be inferred from VERCEL_OIDC_TOKEN. [alias: --team]"
});
/** Parser for the `--scope` option that falls back to `--team` if not provided. */
const team = {
	...scopeParser,
	register(opts) {
		scopeParser.register?.(opts);
		teamParser.register?.(opts);
	},
	async parse(context) {
		const [scope$1, team$1] = await Promise.all([scopeParser.parse(context), teamParser.parse(context)]);
		if (scope$1._tag !== "error" && typeof scope$1.value === "undefined") {
			if (team$1._tag !== "error" && team$1.value) return team$1;
			else if (team$1._tag === "error") return team$1;
		}
		return scope$1;
	}
};
/**
* Combining multiple argument parsing into a single "scope" argument
* that includes token, project, and team.
* If project or team are not provided, they will be inferred.
*/
const scope = {
	register(ctx) {
		token.register?.(ctx);
		project.register?.(ctx);
		team.register?.(ctx);
	},
	async parse(context) {
		const t = await token.parse(context);
		if (t._tag === "error") return t;
		const visited = new Set(context.visitedNodes);
		let [projectId, teamId] = await Promise.all([project.parse(context), team.parse(context)]);
		const nodes = Array.from(context.visitedNodes).filter((x) => !visited.has(x));
		if (projectId._tag === "error") return projectId;
		if (teamId._tag === "error") return teamId;
		let projectSlug;
		let teamSlug;
		if (typeof projectId.value === "undefined" || typeof teamId.value === "undefined") try {
			const scope$1 = await withFreshAuthRetry(() => inferScope$1({
				token: t.value,
				team: teamId.value
			}));
			projectId.value ?? (projectId.value = scope$1.project);
			teamId.value ?? (teamId.value = scope$1.owner);
			projectSlug = scope$1.projectSlug;
			teamSlug = scope$1.ownerSlug;
		} catch (err$1) {
			debug$4("scope inference failed: %O", err$1);
			return {
				_tag: "error",
				error: { errors: [{
					nodes,
					message: [
						`Could not determine team/project scope.${err$1 instanceof NotOk ? ` ${err$1.response.responseText}` : ""}`,
						`${source_default.bold("hint:")} Specify explicitly with --scope TEAM_SLUG --project PROJECT_NAME, or set VERCEL_OIDC_TOKEN.`,
						"╰▶ Docs: https://vercel.com/docs/sandbox"
					].join("\n")
				}] }
			};
		}
		return {
			_tag: "ok",
			value: {
				token: t.value,
				project: projectId.value,
				team: teamId.value,
				projectSlug,
				teamSlug
			}
		};
	},
	helpTopics() {
		return [
			...token.helpTopics(),
			...project.helpTopics(),
			...team.helpTopics()
		].map((x) => ({
			...x,
			category: "Auth & Scope"
		}));
	}
};

//#endregion
//#region package.json
var version = "3.4.0";

//#endregion
//#region src/error.ts
init_source();
var StyledError = class extends Error {
	constructor(message, cause) {
		super(source_default.red(message), { cause });
		this.name = "StyledError";
	}
};

//#endregion
//#region src/util/format-error.ts
init_source();
const debug$3 = createDebugger("sandbox:errors");
const ApiErrorResponse = z$1.object({ error: z$1.object({ message: z$1.string() }) });
/**
* Formats an {@link APIError} into a {@link StyledError} with the pretty
* multi-line layout used across the CLI.
*/
async function formatApiError(error) {
	const tmpPath = await writeResponseToTemp(error);
	const status = error.response.status;
	return new StyledError([
		ApiErrorResponse.safeParse(error.json).data?.error.message ?? getErrorMessage(status),
		`├▶ requested url: ${error.response.url}`,
		`├▶ status code: ${status} ${error.response.statusText}`,
		`╰▶ ${source_default.bold("hint:")} the full response buffer is stored in ${source_default.italic(tmpPath)}`
	].join("\n"), error);
}
function getErrorMessage(status) {
	if (status === 400) return "Sandbox API request failed: the request was invalid (400). Check the command arguments and try again.";
	if (status === 401 || status === 403) return "Sandbox API request failed due to authentication. Check your token or run `sandbox login`.";
	if (status === 404) return "Sandbox API request failed: resource not found.";
	if (status === 429) return "Sandbox API rate limit exceeded (429). Please wait and try again.";
	if (status >= 500) return "Sandbox API responded with a server error. Please try again.";
	return "Sandbox API request failed.";
}
async function writeResponseToTemp({ response, text }) {
	const unique = [process.pid, process.hrtime.bigint()].map((x) => x.toString(36)).join("");
	const tmpPath = Path.join(tmpdir(), `sandbox-cli-response-${unique}.http`);
	const buffers = [];
	buffers.push(Buffer.from(`${response.url}\r\n`));
	buffers.push(Buffer.from(`${response.status} ${response.statusText}\r\n`));
	for (const [key, value] of response.headers) buffers.push(Buffer.from(`${key}: ${value}\r\n`));
	buffers.push(Buffer.from(`\r\n`));
	if (text) buffers.push(Buffer.from(text));
	await writeFile(tmpPath, Buffer.concat(buffers));
	return tmpPath;
}
/**
* Detects network-level timeouts when talking to the Sandbox API.
*/
function isApiTimeoutError(error) {
	if (!error || typeof error !== "object") return false;
	const e = error;
	if (e.name === "TimeoutError" || e.name === "AbortError") return true;
	if (e.code === "ETIMEDOUT") return true;
	const causeCode = e.cause?.code;
	if (causeCode === "UND_ERR_CONNECT_TIMEOUT" || causeCode === "UND_ERR_HEADERS_TIMEOUT") return true;
	if (e.name === "TypeError" && e.message === "fetch failed") return isApiTimeoutError(e.cause);
	return false;
}
function formatApiTimeoutError(error) {
	return new StyledError(["The request to the Sandbox API timed out.", `╰▶ ${source_default.bold("hint:")} check your network connection and try again.`].join("\n"), error);
}
function formatStreamError(error) {
	return new StyledError([
		"The sandbox stream was interrupted.",
		`├▶ code: ${error.code}`,
		`├▶ session: ${error.sessionId}`,
		`╰▶ ${source_default.bold("hint:")} the sandbox may have stopped. Resume or recreate it and try again.`
	].join("\n"), error);
}
/**
* Single funnel for every error that reaches the CLI's top-level catch.
*/
async function printTopLevelError(e) {
	const styled = await toStyledError(e);
	console.error();
	if (styled) console.error(styled.message);
	else {
		console.error(source_default.red(e instanceof Error ? e.message : String(e)));
		if (debug$3.enabled) console.error(e);
	}
}
async function toStyledError(e) {
	if (e instanceof StyledError) return e;
	if (e instanceof APIError) return formatApiError(e);
	if (e instanceof StreamError) return formatStreamError(e);
	if (isApiTimeoutError(e)) return formatApiTimeoutError(e);
	return null;
}

//#endregion
//#region src/client.ts
/**
* A {@link Sandbox} wrapper that adds user-agent headers and error handling.
*/
const sandboxClient = {
	get: (params) => withErrorHandling(() => Sandbox.get({
		fetch: fetchWithUserAgent,
		resume: false,
		...params
	})),
	create: (params) => withErrorHandling(() => Sandbox.create({
		fetch: fetchWithUserAgent,
		...params
	})),
	fork: (params) => withErrorHandling(() => Sandbox.fork({
		fetch: fetchWithUserAgent,
		...params
	})),
	list: (params) => withErrorHandling(() => Sandbox.list({
		fetch: fetchWithUserAgent,
		...params
	}))
};
const snapshotClient = {
	list: (params) => withErrorHandling(() => Snapshot.list({
		fetch: fetchWithUserAgent,
		...params
	})),
	get: (params) => withErrorHandling(() => Snapshot.get({ ...params })),
	tree: (params) => withErrorHandling(() => Snapshot.tree({
		fetch: fetchWithUserAgent,
		...params
	}))
};
const fetchWithUserAgent = (input, init$1) => {
	const headers = new Headers(init$1?.headers ?? (input && typeof input === "object" && "headers" in input ? input?.headers : {}));
	let agent = `vercel/sandbox-cli/${version}`;
	const existingAgent = headers.get("user-agent");
	if (existingAgent) agent += ` ${existingAgent}`;
	headers.set("user-agent", agent);
	return fetch(input, {
		...init$1,
		headers
	});
};
async function withErrorHandling(factory) {
	try {
		return await withFreshAuthRetry(factory);
	} catch (error) {
		if (error instanceof APIError) throw await formatApiError(error);
		throw error;
	}
}

//#endregion
//#region src/args/snapshot-id.ts
var import_cjs$23 = /* @__PURE__ */ __toESM(require_cjs());
init_source();
const snapshotId = import_cjs$23.extendType(import_cjs$23.string, {
	displayName: "snapshot_id",
	description: "The ID of the snapshot",
	async from(s$1) {
		if (!s$1.startsWith("snap_")) throw new Error([`Malformed snapshot ID: "${s$1}".`, `${source_default.bold("hint:")} Snapshot IDs must start with 'snap_' (e.g., snap_abc123def456).`].join("\n"));
		return s$1;
	}
});

//#endregion
//#region src/args/ports.ts
var import_cjs$22 = /* @__PURE__ */ __toESM(require_cjs());
init_source();
const publishPorts = import_cjs$22.multioption({
	long: "publish-port",
	short: "p",
	description: "Publish sandbox port(s) to DOMAIN.vercel.run",
	type: import_cjs$22.array(import_cjs$22.extendType(import_cjs$22.number, {
		displayName: "PORT",
		async from(number) {
			if (!Number.isInteger(number) || number < 1024 || number > 65535) throw new Error([
				`Invalid port: ${number}.`,
				`${source_default.bold("hint:")} Ports must be integers between 1024-65535 (privileged ports 0-1023 are reserved).`,
				"Examples: 3000, 8443"
			].join("\n"));
			return number;
		}
	}))
});

//#endregion
//#region src/types/snapshot-expiration.ts
var import_cjs$21 = require_cjs();
const SnapshotExpiration = (0, import_cjs$21.extendType)(import_cjs$21.string, {
	displayName: "DURATION|none",
	description: "A duration (e.g. 7d, 30d) or \"none\" for no expiration",
	async from(value) {
		if (value === "none") return "0";
		return Duration.from(value);
	}
});

//#endregion
//#region src/args/snapshot-retention.ts
var import_cjs$20 = /* @__PURE__ */ __toESM(require_cjs());
const snapshotExpiration = import_cjs$20.option({
	long: "snapshot-expiration",
	type: import_cjs$20.optional(SnapshotExpiration),
	description: "Default snapshot expiration. Use \"none\" or 0 for no expiration. Example: 7d, 30d"
});
const keepLastSnapshots = import_cjs$20.option({
	long: "keep-last-snapshots",
	type: import_cjs$20.optional(import_cjs$20.extendType(import_cjs$20.number, {
		displayName: "COUNT",
		async from(n) {
			if (!Number.isInteger(n) || n < 1 || n > 10) throw new Error(`Invalid --keep-last-snapshots value: ${n}. Must be an integer between 1 and 10.`);
			return n;
		}
	})),
	description: "Keep only the N most recent snapshots of this sandbox (1-10)."
});
const keepLastSnapshotsFor = import_cjs$20.option({
	long: "keep-last-snapshots-for",
	type: import_cjs$20.optional(SnapshotExpiration),
	description: "Expiration applied to kept snapshots. Use \"none\" or 0 for no expiration. Example: 7d, 30d"
});
const deleteEvictedSnapshots = import_cjs$20.option({
	long: "delete-evicted-snapshots",
	type: import_cjs$20.optional({
		...import_cjs$20.oneOf(["true", "false"]),
		displayName: "true|false"
	}),
	description: "When \"true\" (the default), evicted snapshots are deleted immediately; when \"false\", they keep the default expiration."
});
const snapshotRetentionArgs = {
	snapshotExpiration,
	keepLastSnapshots,
	keepLastSnapshotsFor,
	deleteEvictedSnapshots
};

//#endregion
//#region src/args/sandbox-name.ts
var import_cjs$19 = /* @__PURE__ */ __toESM(require_cjs());
init_source();
const sandboxName = import_cjs$19.extendType(import_cjs$19.string, {
	displayName: "name",
	description: "The name of the sandbox",
	async from(s$1) {
		if (!s$1 || s$1.trim().length === 0) throw new Error([
			`Sandbox name cannot be empty.`,
			`${source_default.bold("hint:")} Provide a sandbox name.`,
			"╰▶ run `sandbox list` to see available sandboxes."
		].join("\n"));
		return s$1;
	}
});

//#endregion
//#region src/util/print-command.ts
init_source();
function printCommand(command$1, args$5) {
	return source_default.gray(source_default.dim("$ ") + [command$1, ...args$5].join(" "));
}

//#endregion
//#region src/interactive-shell/extend-sandbox-timeout.ts
var import_ms$6 = /* @__PURE__ */ __toESM(require_ms());
const debug$2 = createDebugger("sandbox:timeout");
const BUFFER = (0, import_ms$6.default)("10 seconds");
async function extendSandboxTimeoutPeriodically(sandbox, signal) {
	const session = sandbox.currentSession();
	const timeout$1 = session.timeout;
	if (timeout$1 == null) return;
	const nextTick = session.createdAt.getTime() + timeout$1;
	debug$2(`next tick: ${new Date(nextTick).toISOString()}`);
	while (!signal.aborted) {
		const currentTimeout = session.timeout;
		if (currentTimeout == null) return;
		const sleepMs = session.createdAt.getTime() + currentTimeout - Date.now() - BUFFER;
		if (sleepMs > 2e3) {
			debug$2(`sleeping for ${sleepMs}ms until next timeout extension`);
			await setTimeout$1(sleepMs, null, { signal });
		}
		await sandbox.extendTimeout((0, import_ms$6.default)("5 minutes"));
		const updatedTimeout = session.timeout;
		if (updatedTimeout == null) return;
		const nextTick$1 = session.createdAt.getTime() + updatedTimeout;
		debug$2(`extended sandbox timeout by 5 minutes. next tick: ${new Date(nextTick$1).toISOString()}`);
	}
}

//#endregion
//#region src/interactive-shell/interactive-shell.ts
init_source();
const debug$1 = createDebugger("sandbox:interactive-shell");
/**
* A default TERM value so applications like `vim` and `nano` work properly.
*/
const TERM = "xterm-256color";
/**
* A custom prompt so interactive sessions show the Vercel triangle and the
* working directory (e.g. `▲ /vercel/sandbox/ `) instead of the shell's
* default prompt. The server passes this through to the shell verbatim.
*
* This is built from primitives every POSIX shell honors so it renders the
* same regardless of which shell a customer image ships as `/bin/sh`.
*
* The color escapes are wrapped in the raw readline "ignore" markers
* `\x01` (RL_PROMPT_START_IGNORE) and `\x02` (RL_PROMPT_END_IGNORE) — the
* same bytes that bash's `\[` / `\]` decode into. This lets bash's readline
* exclude the (zero-width) escape sequences from its prompt-width calculation
* so cursor positioning stays correct on long/wrapping lines and during line
* editing. In non-bash shells these are C0 control bytes that terminals treat
* as non-printing, so they don't affect rendering there.
*/
const PS1 = `▲ \x01\x1b[2m\x02$PWD/\x01\x1b[0m\x02 `;
/**
* Starts an interactive shell session with a sandbox. The API hands us a
* WebSocket URL and token, and we tunnel stdin/stdout over it.
*/
async function startInteractiveShell(options) {
	try {
		var _usingCtx$1 = _usingCtx();
		let cleaned = false;
		const cleanup = () => {
			if (cleaned) return;
			process.stdin.removeAllListeners();
			try {
				process.stdin.setRawMode(false);
				process.stdin.pause();
				process.stdin.unref();
			} catch {}
			cleaned = true;
		};
		process.once("beforeExit", cleanup);
		const _cleanup = _usingCtx$1.u(defer(cleanup));
		const progress = _usingCtx$1.u(acquireRelease(() => ora({ discardStdin: false }).start(), (s$1) => s$1.stop()));
		progress.text = "Opening interactive session...";
		const { url, token: token$1 } = await options.sandbox.openInteractive();
		const [command$1, ...args$5] = options.execution;
		const execution = options.sudo ? [
			"sudo",
			command$1,
			...args$5
		] : [command$1, ...args$5];
		progress.text = "Connecting...";
		const client = new WebSocket(`${url}?token=${encodeURIComponent(token$1)}`);
		const _client = _usingCtx$1.u(defer(() => {
			try {
				client.close();
			} catch {}
		}));
		await new Promise((resolve, reject) => {
			client.once("open", () => resolve());
			client.once("error", (err$1) => reject(err$1));
		});
		debug$1("connected to %s", url);
		client.send(JSON.stringify({
			type: "start",
			command: execution[0],
			args: execution.slice(1),
			env: toEnvArray({
				TERM,
				PS1,
				...options.envVars
			}),
			cwd: options.cwd ?? options.sandbox.cwd,
			cols: process.stdout.columns,
			rows: process.stdout.rows
		}));
		progress.stop();
		const extension = _usingCtx$1.u(createAbortController("stopped extensions"));
		if (!options.skipExtendingTimeout) extendSandboxTimeoutPeriodically(options.sandbox, extension.signal).catch(extension.ignoreInterruptions);
		client.on("message", (data, isBinary) => {
			if (isBinary) {
				process.stdout.write(data);
				return;
			}
			try {
				const msg = JSON.parse(data.toString());
				if (msg.type === "exit") process.exitCode = typeof msg.code === "number" ? msg.code : void 0;
			} catch {
				process.stdout.write(data);
			}
		});
		if (process.stdin.isTTY) process.stdin.setRawMode(true);
		process.stdin.resume();
		const onStdin = (chunk) => {
			if (client.readyState === WebSocket.OPEN) client.send(chunk);
		};
		process.stdin.on("data", onStdin);
		const onResize = () => {
			if (client.readyState !== WebSocket.OPEN) return;
			client.send(JSON.stringify({
				type: "resize",
				cols: process.stdout.columns,
				rows: process.stdout.rows
			}));
		};
		process.on("SIGWINCH", onResize);
		console.error(printCommand(options.execution[0], options.execution.slice(1)));
		await new Promise((resolve, reject) => {
			client.once("close", () => resolve());
			client.once("error", (err$1) => reject(err$1));
		});
		extension.abort("client disconnected");
		process.removeListener("SIGWINCH", onResize);
		process.stdin.removeListener("data", onStdin);
		console.error(source_default.dim(`\n╰▶ connection to ▲ ${options.sandbox.name} closed.`));
	} catch (_) {
		_usingCtx$1.e = _;
	} finally {
		_usingCtx$1.d();
	}
}
function toEnvArray(env$2) {
	return Object.entries(env$2).map(([key, value]) => `${key}=${value}`);
}

//#endregion
//#region src/args/key-value-pair.ts
var import_cjs$18 = /* @__PURE__ */ __toESM(require_cjs());
init_source();
const KeyValuePair = import_cjs$18.extendType(import_cjs$18.string, {
	displayName: "key=value",
	async from(input) {
		if (!input.includes("=")) return {
			key: input,
			value: process.env[input]
		};
		const [key, ...rest$1] = input.split("=");
		return {
			key,
			value: rest$1.join("=")
		};
	}
});
const ObjectFromKeyValue = import_cjs$18.extendType(import_cjs$18.array(KeyValuePair), { async from(input) {
	const obj = Object.create(null);
	const missingVars = [];
	for (const { key, value } of input) if (value === void 0) missingVars.push(key);
	else obj[key] = value;
	if (missingVars.length > 0) {
		const plural$1 = missingVars.length > 1;
		console.error(source_default.yellow([`${source_default.bold("warn:")} env var${plural$1 ? "s were" : " was"} not defined and ${plural$1 ? "were" : "was"} not passed: ${missingVars.join(", ")}`, `╰▶ ${source_default.bold("hint:")} --env VAR is equivalent to --env VAR=$VAR`].join("\n")));
	}
	return obj;
} });

//#endregion
//#region src/commands/exec.ts
var import_cjs$17 = /* @__PURE__ */ __toESM(require_cjs());
var import_ms$5 = /* @__PURE__ */ __toESM(require_ms());
init_source();
const args$4 = {
	sandbox: import_cjs$17.positional({ type: sandboxName }),
	command: import_cjs$17.positional({
		displayName: "command",
		description: "The executable to invoke"
	}),
	args: import_cjs$17.rest({
		displayName: "args",
		description: "arguments to pass to the command"
	}),
	asSudo: import_cjs$17.flag({
		long: "sudo",
		description: "Give extended privileges to the command."
	}),
	interactive: import_cjs$17.flag({
		long: "interactive",
		short: "i",
		description: "Run the command in a secure interactive shell",
		type: import_cjs$17.extendType(import_cjs$17.boolean, {
			defaultValue() {
				return false;
			},
			async from(input) {
				if (input && !isatty(1)) throw new Error([`The --interactive flag requires a terminal (TTY).`, `${source_default.bold("hint:")} Run this command in an interactive terminal, or remove --interactive to run non-interactively.`].join("\n"));
				return input;
			}
		})
	}),
	skipExtendingTimeout: import_cjs$17.flag({
		long: "no-extend-timeout",
		description: "Do not extend the sandbox timeout while running an interactive command. Only affects interactive executions."
	}),
	tty: import_cjs$17.flag({
		long: "tty",
		short: "t",
		description: "Allocate a tty for an interactive command. This is a no-op."
	}),
	cwd: import_cjs$17.option({
		long: "workdir",
		short: "w",
		description: "The working directory to run the command in",
		type: import_cjs$17.optional(import_cjs$17.string)
	}),
	envVars: import_cjs$17.multioption({
		long: "env",
		short: "e",
		type: ObjectFromKeyValue,
		description: "Environment variables to set for the command"
	}),
	timeout: import_cjs$17.option({
		long: "timeout",
		type: import_cjs$17.optional(Duration),
		description: "Maximum duration to wait for the command (e.g. 30s, 5m). On expiry the process is killed with SIGKILL. Cannot be combined with --interactive."
	}),
	scope
};
const exec = import_cjs$17.command({
	name: "exec",
	description: "Execute a command in an existing sandbox",
	args: args$4,
	async handler({ command: command$1, cwd, args: args$5, asSudo, sandbox: sandboxName$1, scope: { token: token$1, team: team$1, project: project$1 }, interactive, envVars, skipExtendingTimeout, timeout: timeout$1 }) {
		if (interactive && timeout$1) throw new Error(["--timeout cannot be combined with --interactive.", `${source_default.bold("hint:")} Remove one of the two flags. Interactive sessions do not enforce a command timeout.`].join("\n"));
		const sandbox = typeof sandboxName$1 !== "string" ? sandboxName$1 : await sandboxClient.get({
			name: sandboxName$1,
			projectId: project$1,
			teamId: team$1,
			token: token$1,
			resume: true,
			__includeSystemRoutes: true
		});
		if (!interactive) {
			console.error(printCommand(command$1, args$5));
			const result = await sandbox.runCommand({
				cmd: command$1,
				args: args$5,
				stderr: process.stderr,
				stdout: process.stdout,
				sudo: asSudo,
				cwd,
				env: envVars,
				timeoutMs: timeout$1 ? (0, import_ms$5.default)(timeout$1) : void 0
			});
			if (timeout$1 && result.exitCode === 137) console.error(`${source_default.yellow("Command was killed (SIGKILL, exit code 137)")}.`);
			process.exitCode = result.exitCode;
		} else await startInteractiveShell({
			sandbox,
			cwd,
			execution: [command$1, ...args$5],
			envVars,
			sudo: asSudo,
			skipExtendingTimeout
		});
	}
});

//#endregion
//#region src/args/network-policy.ts
var import_cjs$16 = /* @__PURE__ */ __toESM(require_cjs());
init_source();
const networkPolicyMode = import_cjs$16.extendType(import_cjs$16.string, {
	displayName: "MODE",
	async from(value) {
		const validModes = ["allow-all", "deny-all"];
		if (!validModes.includes(value)) throw new Error([`Invalid network policy mode: ${value}.`, `${source_default.bold("hint:")} Valid modes are: ${validModes.join(", ")}`].join("\n"));
		return value;
	}
});
const networkPolicy = import_cjs$16.option({
	long: "network-policy",
	description: `Network policy mode: "allow-all" or "deny-all"
      - allow-all: sandbox can access any website/domain
      - deny-all: sandbox has no network access
    Omit this option and use --allowed-domain / --allowed-cidr / --denied-cidr for custom policies.`,
	type: import_cjs$16.optional(networkPolicyMode)
});
const allowedDomains = import_cjs$16.multioption({
	long: "allowed-domain",
	description: `Domain to allow traffic to (creates a custom network policy). Supports "*" for wildcards for a segment (e.g. '*.vercel.com', 'www.*.com'). If used as the first segment, will match any subdomain.`,
	type: import_cjs$16.array(import_cjs$16.string)
});
const allowedCIDRs = import_cjs$16.multioption({
	long: "allowed-cidr",
	description: `CIDR to allow traffic to (creates a custom network policy). Takes precedence over 'allowed-domain'.`,
	type: import_cjs$16.array(import_cjs$16.string)
});
const deniedCIDRs = import_cjs$16.multioption({
	long: "denied-cidr",
	description: `CIDR to deny traffic to (creates a custom network policy). Takes precedence over allowed domains/CIDRs.`,
	type: import_cjs$16.array(import_cjs$16.string)
});
const networkPolicyArgs = {
	networkPolicy,
	allowedDomains,
	allowedCIDRs,
	deniedCIDRs
};

//#endregion
//#region src/util/network-policy.ts
init_source();
/**
* Resolves the network policy mode from --network-policy and --mode flags.
* Errors if both are provided with conflicting values.
*/
function resolveMode(networkPolicy$1, mode) {
	if (networkPolicy$1 && mode && networkPolicy$1 !== mode) throw new Error([`Conflicting network policy modes: --network-policy=${networkPolicy$1} and --mode=${mode}.`, `${source_default.bold("hint:")} Use only one of --network-policy or --mode.`].join("\n"));
	return networkPolicy$1 ?? mode;
}
/**
* Builds a NetworkPolicy from CLI arguments.
*/
function buildNetworkPolicy(args$5) {
	const { networkPolicy: networkPolicy$1, allowedDomains: allowedDomains$1, allowedCIDRs: allowedCIDRs$1, deniedCIDRs: deniedCIDRs$1 } = args$5;
	const hasListOptions = allowedDomains$1.length > 0 || allowedCIDRs$1.length > 0 || deniedCIDRs$1.length > 0;
	if (networkPolicy$1 && hasListOptions) throw new Error([`Cannot combine --network-policy=${networkPolicy$1} with --allowed-domain, --allowed-cidr, or --denied-cidr.`, `${source_default.bold("hint:")} Use --allowed-domain / --allowed-cidr / --denied-cidr without --network-policy for custom policies.`].join("\n"));
	if (hasListOptions) return {
		...allowedDomains$1.length > 0 && { allow: allowedDomains$1 },
		...(allowedCIDRs$1.length > 0 || deniedCIDRs$1.length > 0) && { subnets: {
			...allowedCIDRs$1.length > 0 && { allow: allowedCIDRs$1 },
			...deniedCIDRs$1.length > 0 && { deny: deniedCIDRs$1 }
		} }
	};
	return networkPolicy$1 ?? "allow-all";
}

//#endregion
//#region src/util/keep-last-snapshots.ts
var import_ms$4 = /* @__PURE__ */ __toESM(require_ms());
init_source();
/**
* Validates the `--keep-last-snapshots*` flag combination and builds the
* payload object that the SDK expects, or returns `undefined` when no
* retention policy was configured.
*
* Throws when `--keep-last-snapshots-for` or `--delete-evicted-snapshots` are
* passed without `--keep-last-snapshots`.
*/
function buildKeepLastSnapshotsPayload(input) {
	const { keepLastSnapshots: keepLastSnapshots$1, keepLastSnapshotsFor: keepLastSnapshotsFor$1, deleteEvictedSnapshots: deleteEvictedSnapshots$1 } = input;
	if (keepLastSnapshots$1 === void 0 && (keepLastSnapshotsFor$1 !== void 0 || deleteEvictedSnapshots$1 !== void 0)) throw new Error(["--keep-last-snapshots-for and --delete-evicted-snapshots require --keep-last-snapshots.", `${source_default.bold("hint:")} Pass --keep-last-snapshots <count> to enable the retention policy.`].join("\n"));
	if (keepLastSnapshots$1 === void 0) return;
	return {
		count: keepLastSnapshots$1,
		expiration: keepLastSnapshotsFor$1 !== void 0 ? (0, import_ms$4.default)(keepLastSnapshotsFor$1) : void 0,
		deleteEvicted: deleteEvictedSnapshots$1 !== void 0 ? deleteEvictedSnapshots$1 === "true" : void 0
	};
}

//#endregion
//#region src/util/print-sandbox-summary.ts
init_source();
/**
* Print the "✅ Sandbox <name> <action>." summary that the create and fork
* commands emit after a successful creation, including project/team and any
* published port routes.
*
* - The sandbox name is written to stdout, everything else to stderr — so
*   `sandbox create | xargs -I {} sandbox exec {} -- ...` keeps working.
* - Pass `action: "created"` for create, or `action: \`forked from ${chalk.cyan(source)}\``
*   for fork.
*/
function printSandboxSummary(opts) {
	const { sandbox, scope: scope$1, action } = opts;
	const teamDisplay = scope$1.teamSlug ?? scope$1.team;
	const projectDisplay = scope$1.projectSlug ?? scope$1.project;
	const routes = sandbox.routes.filter((x) => x.port !== sandbox.interactivePort);
	const hasPorts = routes.length > 0;
	process.stderr.write("✅ Sandbox ");
	process.stdout.write(source_default.cyan(sandbox.name));
	process.stderr.write(" " + action + ".\n");
	process.stderr.write(source_default.dim("   │ ") + "team: " + source_default.cyan(teamDisplay) + "\n");
	if (hasPorts) {
		process.stderr.write(source_default.dim("   │ ") + "project: " + source_default.cyan(projectDisplay) + "\n");
		process.stderr.write(source_default.dim("   │ ") + "ports:\n");
		for (let i = 0; i < routes.length; i++) {
			const route = routes[i];
			const prefix$1 = i === routes.length - 1 ? source_default.dim("   ╰ ") : source_default.dim("   │ ");
			process.stderr.write(prefix$1 + "• " + route.port + " -> " + source_default.cyan(route.url) + "\n");
		}
	} else process.stderr.write(source_default.dim("   ╰ ") + "project: " + source_default.cyan(projectDisplay) + "\n");
}

//#endregion
//#region src/commands/create.ts
var import_cjs$15 = /* @__PURE__ */ __toESM(require_cjs());
var import_ms$3 = /* @__PURE__ */ __toESM(require_ms());
init_source();
const args$3 = {
	name: import_cjs$15.option({
		long: "name",
		description: "A user-chosen name for the sandbox. It must be unique per project.",
		type: import_cjs$15.optional(import_cjs$15.string)
	}),
	nonPersistent: import_cjs$15.flag({
		long: "non-persistent",
		description: "Disable automatic restore of the filesystem between sessions."
	}),
	runtime,
	image: import_cjs$15.option({
		long: "image",
		description: "A Vercel Container Registry (VCR) image name and optional tag or sha to start the sandbox from (e.g. my-repo, my-repo:v1).",
		type: import_cjs$15.optional(import_cjs$15.string)
	}),
	timeout,
	vcpus,
	ports: publishPorts,
	silent: import_cjs$15.flag({
		long: "silent",
		description: "Don't write sandbox name to stdout"
	}),
	snapshot: import_cjs$15.option({
		long: "snapshot",
		short: "s",
		description: "Start the sandbox from a snapshot ID",
		type: import_cjs$15.optional(snapshotId)
	}),
	connect: import_cjs$15.flag({
		long: "connect",
		description: "Start an interactive shell session after creating the sandbox"
	}),
	envVars: import_cjs$15.multioption({
		long: "env",
		short: "e",
		type: ObjectFromKeyValue,
		description: "Default environment variables for sandbox commands"
	}),
	tags: import_cjs$15.multioption({
		long: "tag",
		short: "t",
		type: ObjectFromKeyValue,
		description: "Key-value tags to associate with the sandbox (e.g. --tag env=staging)"
	}),
	...snapshotRetentionArgs,
	...networkPolicyArgs,
	scope
};
const create = import_cjs$15.command({
	name: "create",
	description: "Create a sandbox in the specified account and project.",
	args: args$3,
	examples: [{
		description: "Create and connect to a sandbox without a network access",
		command: `sandbox run --network-policy=none --connect`
	}],
	async handler({ name, nonPersistent, ports, scope: scope$1, runtime: runtime$1, image, timeout: timeout$1, vcpus: vcpus$1, silent, snapshot: snapshot$1, connect: connect$1, envVars, tags, snapshotExpiration: snapshotExpiration$1, keepLastSnapshots: keepLastSnapshots$1, keepLastSnapshotsFor: keepLastSnapshotsFor$1, deleteEvictedSnapshots: deleteEvictedSnapshots$1, networkPolicy: networkPolicyMode$1, allowedDomains: allowedDomains$1, allowedCIDRs: allowedCIDRs$1, deniedCIDRs: deniedCIDRs$1 }) {
		if (image && runtime$1) throw new Error("--image and --runtime cannot be used together.");
		const networkPolicy$1 = buildNetworkPolicy({
			networkPolicy: networkPolicyMode$1,
			allowedDomains: allowedDomains$1,
			allowedCIDRs: allowedCIDRs$1,
			deniedCIDRs: deniedCIDRs$1
		});
		const keepLastSnapshotsPayload = buildKeepLastSnapshotsPayload({
			keepLastSnapshots: keepLastSnapshots$1,
			keepLastSnapshotsFor: keepLastSnapshotsFor$1,
			deleteEvictedSnapshots: deleteEvictedSnapshots$1
		});
		const persistent = !nonPersistent;
		const resources = vcpus$1 ? { vcpus: vcpus$1 } : void 0;
		const tagsObj = Object.keys(tags).length > 0 ? tags : void 0;
		const spinner = silent ? void 0 : ora("Creating sandbox...").start();
		const sandbox = snapshot$1 ? await sandboxClient.create({
			name,
			source: {
				type: "snapshot",
				snapshotId: snapshot$1
			},
			teamId: scope$1.team,
			projectId: scope$1.project,
			token: scope$1.token,
			ports,
			timeout: (0, import_ms$3.default)(timeout$1),
			resources,
			networkPolicy: networkPolicy$1,
			env: envVars,
			tags: tagsObj,
			persistent,
			snapshotExpiration: snapshotExpiration$1 ? (0, import_ms$3.default)(snapshotExpiration$1) : void 0,
			keepLastSnapshots: keepLastSnapshotsPayload,
			__interactive: true
		}) : await sandboxClient.create({
			name,
			teamId: scope$1.team,
			projectId: scope$1.project,
			token: scope$1.token,
			ports,
			...image ? { image } : { runtime: runtime$1 ?? "node24" },
			timeout: (0, import_ms$3.default)(timeout$1),
			resources,
			networkPolicy: networkPolicy$1,
			env: envVars,
			tags: tagsObj,
			persistent,
			snapshotExpiration: snapshotExpiration$1 ? (0, import_ms$3.default)(snapshotExpiration$1) : void 0,
			keepLastSnapshots: keepLastSnapshotsPayload,
			__interactive: true
		});
		spinner?.stop();
		if (!sandbox.interactivePort) throw new Error([
			`Sandbox created but interactive port is missing.`,
			`${source_default.bold("hint:")} This is an internal error. Please try again.`,
			"╰▶ Report this issue: https://github.com/vercel/sandbox/issues"
		].join("\n"));
		if (!silent) printSandboxSummary({
			sandbox,
			scope: scope$1,
			action: "created"
		});
		if (connect$1) await exec.handler({
			scope: scope$1,
			asSudo: false,
			args: [],
			cwd: void 0,
			skipExtendingTimeout: false,
			envVars: {},
			command: "sh",
			interactive: true,
			tty: true,
			sandbox,
			timeout: void 0
		});
		return sandbox;
	}
});

//#endregion
//#region src/commands/fork.ts
var import_cjs$14 = /* @__PURE__ */ __toESM(require_cjs());
var import_ms$2 = /* @__PURE__ */ __toESM(require_ms());
init_source();
const args$2 = {
	source: import_cjs$14.positional({
		displayName: "source",
		description: "Name of the source sandbox to fork from.",
		type: sandboxName
	}),
	name: import_cjs$14.option({
		long: "name",
		description: "A user-chosen name for the forked sandbox. Must be unique per project.",
		type: import_cjs$14.optional(import_cjs$14.string)
	}),
	nonPersistent: import_cjs$14.flag({
		long: "non-persistent",
		description: "Disable automatic restore of the filesystem between sessions."
	}),
	timeout: import_cjs$14.option({
		long: "timeout",
		type: import_cjs$14.optional(Duration),
		description: "Override the maximum sandbox runtime (inherited from source if omitted). Example: 5m, 30m"
	}),
	vcpus,
	ports: publishPorts,
	silent: import_cjs$14.flag({
		long: "silent",
		description: "Don't write sandbox name to stdout"
	}),
	connect: import_cjs$14.flag({
		long: "connect",
		description: "Start an interactive shell session after creating the forked sandbox"
	}),
	envVars: import_cjs$14.multioption({
		long: "env",
		short: "e",
		type: ObjectFromKeyValue,
		description: "Environment variables to set on the fork. Env vars from the source sandbox are not copied (encrypted server-side)."
	}),
	tags: import_cjs$14.multioption({
		long: "tag",
		short: "t",
		type: ObjectFromKeyValue,
		description: "Key-value tags to associate with the fork. When provided, fully replaces the tags copied from the source (no per-key merge)."
	}),
	...snapshotRetentionArgs,
	...networkPolicyArgs,
	scope
};
const fork = import_cjs$14.command({
	name: "fork",
	description: "Fork an existing sandbox into a new one. Copies config (cpu, timeout, network policy, tags, etc.) from the source sandbox; env vars are NOT copied and must be re-supplied via --env.",
	args: args$2,
	examples: [{
		description: "Fork a sandbox with all config copied from the source",
		command: `sandbox fork my-source`
	}, {
		description: "Fork with a specific name and overridden vcpus",
		command: `sandbox fork my-source --name my-forked-sandbox --vcpus 4`
	}],
	async handler({ source, name, nonPersistent, ports, scope: scope$1, timeout: timeout$1, vcpus: vcpus$1, silent, connect: connect$1, envVars, tags, snapshotExpiration: snapshotExpiration$1, keepLastSnapshots: keepLastSnapshots$1, keepLastSnapshotsFor: keepLastSnapshotsFor$1, deleteEvictedSnapshots: deleteEvictedSnapshots$1, networkPolicy: networkPolicyMode$1, allowedDomains: allowedDomains$1, allowedCIDRs: allowedCIDRs$1, deniedCIDRs: deniedCIDRs$1 }) {
		const networkPolicy$1 = networkPolicyMode$1 !== void 0 || allowedDomains$1.length > 0 || allowedCIDRs$1.length > 0 || deniedCIDRs$1.length > 0 ? buildNetworkPolicy({
			networkPolicy: networkPolicyMode$1,
			allowedDomains: allowedDomains$1,
			allowedCIDRs: allowedCIDRs$1,
			deniedCIDRs: deniedCIDRs$1
		}) : void 0;
		const keepLastSnapshotsPayload = buildKeepLastSnapshotsPayload({
			keepLastSnapshots: keepLastSnapshots$1,
			keepLastSnapshotsFor: keepLastSnapshotsFor$1,
			deleteEvictedSnapshots: deleteEvictedSnapshots$1
		});
		const tagsObj = Object.keys(tags).length > 0 ? tags : void 0;
		const envObj = Object.keys(envVars).length > 0 ? envVars : void 0;
		const spinner = silent ? void 0 : ora(`Forking sandbox ${source_default.cyan(source)}...`).start();
		const sandbox = await sandboxClient.fork({
			sourceSandbox: source,
			teamId: scope$1.team,
			projectId: scope$1.project,
			token: scope$1.token,
			...name !== void 0 && { name },
			...ports.length > 0 && { ports },
			...timeout$1 !== void 0 && { timeout: (0, import_ms$2.default)(timeout$1) },
			...vcpus$1 !== void 0 && { resources: { vcpus: vcpus$1 } },
			...networkPolicy$1 !== void 0 && { networkPolicy: networkPolicy$1 },
			...envObj !== void 0 && { env: envObj },
			...tagsObj !== void 0 && { tags: tagsObj },
			...nonPersistent && { persistent: false },
			...snapshotExpiration$1 !== void 0 && { snapshotExpiration: (0, import_ms$2.default)(snapshotExpiration$1) },
			...keepLastSnapshotsPayload !== void 0 && { keepLastSnapshots: keepLastSnapshotsPayload },
			__interactive: true
		});
		spinner?.stop();
		if (!sandbox.interactivePort) throw new Error([
			`Sandbox forked but interactive port is missing.`,
			`${source_default.bold("hint:")} This is an internal error. Please try again.`,
			"╰▶ Report this issue: https://github.com/vercel/sandbox/issues"
		].join("\n"));
		if (!silent) printSandboxSummary({
			sandbox,
			scope: scope$1,
			action: `forked from ${source_default.cyan(source)}`
		});
		if (connect$1) await exec.handler({
			scope: scope$1,
			asSudo: false,
			args: [],
			cwd: void 0,
			skipExtendingTimeout: false,
			envVars: {},
			command: "sh",
			interactive: true,
			tty: true,
			sandbox,
			timeout: void 0
		});
		return sandbox;
	}
});

//#endregion
//#region src/util/omit.ts
function omit(obj, ...keys) {
	const result = { ...obj };
	for (const key of keys) delete result[key];
	return result;
}

//#endregion
//#region src/commands/run.ts
var import_cjs$13 = /* @__PURE__ */ __toESM(require_cjs());
const args$1 = {
	...args$3,
	...omit(args$4, "sandbox", "timeout"),
	removeAfterUse: import_cjs$13.flag({
		long: "rm",
		description: "Automatically remove the sandbox when the command exits."
	}),
	stopAfterUse: import_cjs$13.flag({
		long: "stop",
		description: "Stop the sandbox when the command exits."
	})
};
const run = import_cjs$13.command({
	name: "run",
	description: "Create and run a command in a sandbox",
	args: args$1,
	async handler({ removeAfterUse, stopAfterUse, ...rest$1 }) {
		if (removeAfterUse && stopAfterUse) throw new Error("--rm and --stop are mutually exclusive.");
		let sandbox;
		if (rest$1.name) try {
			sandbox = await sandboxClient.get({
				name: rest$1.name,
				projectId: rest$1.scope.project,
				teamId: rest$1.scope.team,
				token: rest$1.scope.token,
				resume: true,
				__includeSystemRoutes: true
			});
		} catch (error) {
			if (error instanceof StyledError && error.cause instanceof APIError && error.cause.response.status === 404) sandbox = await create.handler({
				...rest$1,
				nonPersistent: rest$1.nonPersistent || removeAfterUse
			});
			else throw error;
		}
		else sandbox = await create.handler({
			...rest$1,
			nonPersistent: rest$1.nonPersistent || removeAfterUse
		});
		try {
			await exec.handler({
				...rest$1,
				sandbox,
				timeout: void 0
			});
		} finally {
			if (removeAfterUse) await sandbox.delete();
			if (stopAfterUse) await sandbox.stop();
		}
	}
});

//#endregion
//#region src/commands/list.ts
var import_cjs$12 = /* @__PURE__ */ __toESM(require_cjs());
init_source();
const list = import_cjs$12.command({
	name: "list",
	aliases: ["ls"],
	description: "List all sandboxes for the specified account and project.",
	args: {
		all: import_cjs$12.flag({
			long: "all",
			short: "a",
			description: "Show all sandboxes (default shows just running)"
		}),
		namePrefix: import_cjs$12.option({
			long: "name-prefix",
			description: "Filter sandboxes by name prefix",
			type: import_cjs$12.optional(import_cjs$12.string)
		}),
		sortBy: import_cjs$12.option({
			long: "sort-by",
			description: "Sort sandboxes by field. Options: createdAt (default), name, statusUpdatedAt",
			type: import_cjs$12.optional(import_cjs$12.oneOf([
				"createdAt",
				"name",
				"statusUpdatedAt"
			]))
		}),
		sortOrder: import_cjs$12.option({
			long: "sort-order",
			description: "Sort order. Options: asc, desc (default)",
			type: import_cjs$12.optional(import_cjs$12.oneOf(["asc", "desc"]))
		}),
		tags: import_cjs$12.multioption({
			long: "tag",
			description: "Filter sandboxes by tag. Format: \"key=value\"",
			type: ObjectFromKeyValue
		}),
		limit: import_cjs$12.option({
			long: "limit",
			description: "Maximum number of sandboxes per page (default 50).",
			type: import_cjs$12.optional(import_cjs$12.number)
		}),
		cursor: import_cjs$12.option({
			long: "cursor",
			description: "Pagination cursor from a previous 'More results' hint.",
			type: import_cjs$12.optional(import_cjs$12.string)
		}),
		scope
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, all, namePrefix, sortBy, sortOrder, tags, limit, cursor }) {
		if (namePrefix) {
			if (sortBy && sortBy !== "name") {
				console.error(source_default.red("Error: --sort-by must be 'name' when using --name-prefix"));
				return;
			}
			sortBy = "name";
		}
		const { sandboxes, pagination } = await (async () => {
			try {
				var _usingCtx$1 = _usingCtx();
				const _spinner$1 = _usingCtx$1.u(acquireRelease(() => ora("Fetching sandboxes...").start(), (s$1) => s$1.stop()));
				return sandboxClient.list({
					token: token$1,
					teamId: team$1,
					projectId: project$1,
					limit: limit ?? 50,
					...cursor && { cursor },
					...namePrefix && { namePrefix },
					...sortBy && { sortBy },
					...sortOrder && { sortOrder },
					...Object.keys(tags).length > 0 && { tags }
				});
			} catch (_) {
				_usingCtx$1.e = _;
			} finally {
				_usingCtx$1.d();
			}
		})();
		const displayedSandboxes = all ? sandboxes : sandboxes.filter((x) => x.status === "running");
		const memoryFormatter = new Intl.NumberFormat(void 0, {
			style: "unit",
			unit: "megabyte"
		});
		const columns = {
			NAME: { value: (s$1) => s$1.name },
			STATUS: {
				value: (s$1) => s$1.status,
				color: (s$1) => SandboxStatusColor[s$1.status] ?? source_default.reset
			},
			CREATED: { value: (s$1) => timeAgo(s$1.createdAt) },
			MEMORY: { value: (s$1) => s$1.memory != null ? memoryFormatter.format(s$1.memory) : "-" },
			VCPUS: { value: (s$1) => s$1.vcpus ?? "-" },
			RUNTIME: { value: (s$1) => s$1.runtime ?? "-" },
			TIMEOUT: { value: (s$1) => {
				if (s$1.expiresAt != null) return timeAgo(s$1.expiresAt);
				if (s$1.timeout != null) return timeAgo(s$1.createdAt + s$1.timeout);
				return "-";
			} },
			SNAPSHOT: { value: (s$1) => s$1.currentSnapshotId ?? "-" },
			TAGS: { value: (s$1) => s$1.tags && Object.keys(s$1.tags).length > 0 ? Object.entries(s$1.tags).map(([k, v]) => `${k}:${v}`).join(", ") : "-" }
		};
		if (all) {
			columns.CPU = { value: (s$1) => s$1.totalActiveCpuDurationMs ? formatRunDuration(s$1.totalActiveCpuDurationMs) : "-" };
			columns["NETWORK (OUT/IN)"] = { value: (s$1) => s$1.totalEgressBytes || s$1.totalIngressBytes ? `${formatBytes(s$1.totalEgressBytes ?? 0)} / ${formatBytes(s$1.totalIngressBytes ?? 0)}` : "- / -" };
		}
		console.log(table({
			rows: displayedSandboxes,
			columns
		}));
		if (pagination.next !== null) console.log(formatNextCursorHint(pagination.next));
	}
});
const SandboxStatusColor = {
	running: source_default.cyan,
	failed: source_default.red,
	stopped: source_default.gray.dim,
	stopping: source_default.gray,
	pending: source_default.magenta,
	snapshotting: source_default.blue,
	aborted: source_default.gray.dim
};

//#endregion
//#region src/commands/connect.ts
var import_cjs$11 = /* @__PURE__ */ __toESM(require_cjs());
const connect = import_cjs$11.command({
	name: "connect",
	aliases: ["ssh", "shell"],
	description: "Start an interactive shell in an existing sandbox",
	args: omit(args$4, "command", "args", "interactive", "tty"),
	async handler(args$5) {
		return exec.handler({
			command: "sh",
			args: [],
			interactive: true,
			tty: true,
			...args$5
		});
	}
});

//#endregion
//#region src/commands/stop.ts
var import_cjs$10 = /* @__PURE__ */ __toESM(require_cjs());
init_source();
function c(label, value) {
	return {
		label,
		value
	};
}
/** Visible width of a cell (label + value, no ANSI). */
function cellWidth(cell) {
	return cell ? cell.label.length + cell.value.length : 0;
}
/** Print rows as a tree with column-aligned label: value pairs. */
function printTree(rows) {
	const widths = [];
	for (const row of rows) for (let i = 0; i < row.length; i++) widths[i] = Math.max(widths[i] ?? 0, cellWidth(row[i]));
	for (let r = 0; r < rows.length; r++) {
		const prefix$1 = r === rows.length - 1 ? source_default.dim("   ╰ ") : source_default.dim("   │ ");
		const line = rows[r].map((cell, i) => {
			if (!cell) return " ".repeat(widths[i]);
			const pad = widths[i] - cell.label.length - cell.value.length;
			return cell.label + source_default.cyan(cell.value) + " ".repeat(Math.max(0, pad));
		}).join("  ").trimEnd();
		process.stderr.write(prefix$1 + line + "\n");
	}
}
function printStopResult(name, sandbox, sessionSnapshot) {
	process.stderr.write(source_default.green("✔") + " Sandbox stopped.\n");
	const snapshot$1 = sessionSnapshot.snapshot;
	printTree([
		[
			c("sandbox: ", name),
			sandbox.totalActiveCpuDurationMs != null ? c("active cpu: ", formatRunDuration(sandbox.totalActiveCpuDurationMs)) : null,
			sandbox.memory != null ? c("mem: ", `${sandbox.memory} MB`) : null,
			sandbox.totalDurationMs != null ? c("duration: ", formatRunDuration(sandbox.totalDurationMs)) : null,
			sandbox.totalIngressBytes != null ? c("ingress: ", formatBytes(sandbox.totalIngressBytes)) : null,
			sandbox.totalEgressBytes != null ? c("egress: ", formatBytes(sandbox.totalEgressBytes)) : null
		],
		[
			c("session: ", sessionSnapshot.id),
			sessionSnapshot.activeCpuDurationMs != null ? c("active cpu: ", formatRunDuration(sessionSnapshot.activeCpuDurationMs)) : null,
			c("mem: ", `${sessionSnapshot.memory} MB`),
			sessionSnapshot.duration != null ? c("duration: ", formatRunDuration(sessionSnapshot.duration)) : null,
			sessionSnapshot.networkTransfer ? c("ingress: ", formatBytes(sessionSnapshot.networkTransfer.ingress)) : null,
			sessionSnapshot.networkTransfer ? c("egress: ", formatBytes(sessionSnapshot.networkTransfer.egress)) : null
		],
		...snapshot$1 ? [[
			c("snapshot: ", snapshot$1.id),
			c("size: ", formatBytes(snapshot$1.sizeBytes)),
			c("expires: ", snapshot$1.expiresAt ? timeAgo(snapshot$1.expiresAt) : "never")
		]] : []
	]);
}
const stop = import_cjs$10.command({
	name: "stop",
	description: "Stop the current session of one or more sandboxes",
	args: {
		sandboxName: import_cjs$10.positional({
			type: sandboxName,
			description: "A sandbox name to stop"
		}),
		sandboxNames: import_cjs$10.restPositionals({
			type: sandboxName,
			description: "More sandboxes to stop"
		}),
		scope
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, sandboxName: sandboxName$1, sandboxNames }) {
		const names = Array.from(new Set([sandboxName$1, ...sandboxNames]));
		const spinner = ora({
			text: names.length === 1 ? `Stopping ${names[0]}` : `Stopping ${names.length} sandboxes`,
			stream: process.stderr
		}).start();
		const results = await Promise.allSettled(names.map(async (name) => {
			const sandbox = await sandboxClient.get({
				token: token$1,
				teamId: team$1,
				projectId: project$1,
				name
			});
			return {
				name,
				sandbox,
				sessionSnapshot: await sandbox.stop()
			};
		}));
		spinner.stop();
		for (const result of results) if (result.status === "fulfilled") {
			const { name, sandbox, sessionSnapshot } = result.value;
			printStopResult(name, sandbox, sessionSnapshot);
		} else {
			const error = result.reason;
			process.stderr.write(source_default.red("✖") + ` ${error.message ?? error}\n`);
			process.exitCode = 1;
		}
	}
});

//#endregion
//#region ../../node_modules/.pnpm/eventemitter3@5.0.1/node_modules/eventemitter3/index.js
var require_eventemitter3 = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/eventemitter3@5.0.1/node_modules/eventemitter3/index.js": ((exports, module) => {
	var has = Object.prototype.hasOwnProperty, prefix = "~";
	/**
	* Constructor to create a storage for our `EE` objects.
	* An `Events` instance is a plain object whose properties are event names.
	*
	* @constructor
	* @private
	*/
	function Events() {}
	if (Object.create) {
		Events.prototype = Object.create(null);
		if (!new Events().__proto__) prefix = false;
	}
	/**
	* Representation of a single event listener.
	*
	* @param {Function} fn The listener function.
	* @param {*} context The context to invoke the listener with.
	* @param {Boolean} [once=false] Specify if the listener is a one-time listener.
	* @constructor
	* @private
	*/
	function EE(fn, context, once) {
		this.fn = fn;
		this.context = context;
		this.once = once || false;
	}
	/**
	* Add a listener for a given event.
	*
	* @param {EventEmitter} emitter Reference to the `EventEmitter` instance.
	* @param {(String|Symbol)} event The event name.
	* @param {Function} fn The listener function.
	* @param {*} context The context to invoke the listener with.
	* @param {Boolean} once Specify if the listener is a one-time listener.
	* @returns {EventEmitter}
	* @private
	*/
	function addListener(emitter, event, fn, context, once) {
		if (typeof fn !== "function") throw new TypeError("The listener must be a function");
		var listener = new EE(fn, context || emitter, once), evt = prefix ? prefix + event : event;
		if (!emitter._events[evt]) emitter._events[evt] = listener, emitter._eventsCount++;
		else if (!emitter._events[evt].fn) emitter._events[evt].push(listener);
		else emitter._events[evt] = [emitter._events[evt], listener];
		return emitter;
	}
	/**
	* Clear event by name.
	*
	* @param {EventEmitter} emitter Reference to the `EventEmitter` instance.
	* @param {(String|Symbol)} evt The Event name.
	* @private
	*/
	function clearEvent(emitter, evt) {
		if (--emitter._eventsCount === 0) emitter._events = new Events();
		else delete emitter._events[evt];
	}
	/**
	* Minimal `EventEmitter` interface that is molded against the Node.js
	* `EventEmitter` interface.
	*
	* @constructor
	* @public
	*/
	function EventEmitter$1() {
		this._events = new Events();
		this._eventsCount = 0;
	}
	/**
	* Return an array listing the events for which the emitter has registered
	* listeners.
	*
	* @returns {Array}
	* @public
	*/
	EventEmitter$1.prototype.eventNames = function eventNames() {
		var names = [], events, name;
		if (this._eventsCount === 0) return names;
		for (name in events = this._events) if (has.call(events, name)) names.push(prefix ? name.slice(1) : name);
		if (Object.getOwnPropertySymbols) return names.concat(Object.getOwnPropertySymbols(events));
		return names;
	};
	/**
	* Return the listeners registered for a given event.
	*
	* @param {(String|Symbol)} event The event name.
	* @returns {Array} The registered listeners.
	* @public
	*/
	EventEmitter$1.prototype.listeners = function listeners(event) {
		var evt = prefix ? prefix + event : event, handlers = this._events[evt];
		if (!handlers) return [];
		if (handlers.fn) return [handlers.fn];
		for (var i = 0, l = handlers.length, ee = new Array(l); i < l; i++) ee[i] = handlers[i].fn;
		return ee;
	};
	/**
	* Return the number of listeners listening to a given event.
	*
	* @param {(String|Symbol)} event The event name.
	* @returns {Number} The number of listeners.
	* @public
	*/
	EventEmitter$1.prototype.listenerCount = function listenerCount(event) {
		var evt = prefix ? prefix + event : event, listeners = this._events[evt];
		if (!listeners) return 0;
		if (listeners.fn) return 1;
		return listeners.length;
	};
	/**
	* Calls each of the listeners registered for a given event.
	*
	* @param {(String|Symbol)} event The event name.
	* @returns {Boolean} `true` if the event had listeners, else `false`.
	* @public
	*/
	EventEmitter$1.prototype.emit = function emit(event, a1, a2, a3, a4, a5) {
		var evt = prefix ? prefix + event : event;
		if (!this._events[evt]) return false;
		var listeners = this._events[evt], len = arguments.length, args$5, i;
		if (listeners.fn) {
			if (listeners.once) this.removeListener(event, listeners.fn, void 0, true);
			switch (len) {
				case 1: return listeners.fn.call(listeners.context), true;
				case 2: return listeners.fn.call(listeners.context, a1), true;
				case 3: return listeners.fn.call(listeners.context, a1, a2), true;
				case 4: return listeners.fn.call(listeners.context, a1, a2, a3), true;
				case 5: return listeners.fn.call(listeners.context, a1, a2, a3, a4), true;
				case 6: return listeners.fn.call(listeners.context, a1, a2, a3, a4, a5), true;
			}
			for (i = 1, args$5 = new Array(len - 1); i < len; i++) args$5[i - 1] = arguments[i];
			listeners.fn.apply(listeners.context, args$5);
		} else {
			var length = listeners.length, j;
			for (i = 0; i < length; i++) {
				if (listeners[i].once) this.removeListener(event, listeners[i].fn, void 0, true);
				switch (len) {
					case 1:
						listeners[i].fn.call(listeners[i].context);
						break;
					case 2:
						listeners[i].fn.call(listeners[i].context, a1);
						break;
					case 3:
						listeners[i].fn.call(listeners[i].context, a1, a2);
						break;
					case 4:
						listeners[i].fn.call(listeners[i].context, a1, a2, a3);
						break;
					default:
						if (!args$5) for (j = 1, args$5 = new Array(len - 1); j < len; j++) args$5[j - 1] = arguments[j];
						listeners[i].fn.apply(listeners[i].context, args$5);
				}
			}
		}
		return true;
	};
	/**
	* Add a listener for a given event.
	*
	* @param {(String|Symbol)} event The event name.
	* @param {Function} fn The listener function.
	* @param {*} [context=this] The context to invoke the listener with.
	* @returns {EventEmitter} `this`.
	* @public
	*/
	EventEmitter$1.prototype.on = function on(event, fn, context) {
		return addListener(this, event, fn, context, false);
	};
	/**
	* Add a one-time listener for a given event.
	*
	* @param {(String|Symbol)} event The event name.
	* @param {Function} fn The listener function.
	* @param {*} [context=this] The context to invoke the listener with.
	* @returns {EventEmitter} `this`.
	* @public
	*/
	EventEmitter$1.prototype.once = function once(event, fn, context) {
		return addListener(this, event, fn, context, true);
	};
	/**
	* Remove the listeners of a given event.
	*
	* @param {(String|Symbol)} event The event name.
	* @param {Function} fn Only remove the listeners that match this function.
	* @param {*} context Only remove the listeners that have this context.
	* @param {Boolean} once Only remove one-time listeners.
	* @returns {EventEmitter} `this`.
	* @public
	*/
	EventEmitter$1.prototype.removeListener = function removeListener(event, fn, context, once) {
		var evt = prefix ? prefix + event : event;
		if (!this._events[evt]) return this;
		if (!fn) {
			clearEvent(this, evt);
			return this;
		}
		var listeners = this._events[evt];
		if (listeners.fn) {
			if (listeners.fn === fn && (!once || listeners.once) && (!context || listeners.context === context)) clearEvent(this, evt);
		} else {
			for (var i = 0, events = [], length = listeners.length; i < length; i++) if (listeners[i].fn !== fn || once && !listeners[i].once || context && listeners[i].context !== context) events.push(listeners[i]);
			if (events.length) this._events[evt] = events.length === 1 ? events[0] : events;
			else clearEvent(this, evt);
		}
		return this;
	};
	/**
	* Remove all listeners, or those of the specified event.
	*
	* @param {(String|Symbol)} [event] The event name.
	* @returns {EventEmitter} `this`.
	* @public
	*/
	EventEmitter$1.prototype.removeAllListeners = function removeAllListeners(event) {
		var evt;
		if (event) {
			evt = prefix ? prefix + event : event;
			if (this._events[evt]) clearEvent(this, evt);
		} else {
			this._events = new Events();
			this._eventsCount = 0;
		}
		return this;
	};
	EventEmitter$1.prototype.off = EventEmitter$1.prototype.removeListener;
	EventEmitter$1.prototype.addListener = EventEmitter$1.prototype.on;
	EventEmitter$1.prefixed = prefix;
	EventEmitter$1.EventEmitter = EventEmitter$1;
	if ("undefined" !== typeof module) module.exports = EventEmitter$1;
}) });

//#endregion
//#region ../../node_modules/.pnpm/eventemitter3@5.0.1/node_modules/eventemitter3/index.mjs
var import_eventemitter3 = /* @__PURE__ */ __toESM(require_eventemitter3(), 1);
var eventemitter3_default = import_eventemitter3.default;

//#endregion
//#region ../../node_modules/.pnpm/colorette@2.0.20/node_modules/colorette/index.js
const { env = {}, argv = [], platform = "" } = typeof process === "undefined" ? {} : process;
const isDisabled = "NO_COLOR" in env || argv.includes("--no-color");
const isForced = "FORCE_COLOR" in env || argv.includes("--color");
const isWindows = platform === "win32";
const isDumbTerminal = env.TERM === "dumb";
const isCompatibleTerminal = tty$1 && tty$1.isatty && tty$1.isatty(1) && env.TERM && !isDumbTerminal;
const isCI = "CI" in env && ("GITHUB_ACTIONS" in env || "GITLAB_CI" in env || "CIRCLECI" in env);
const isColorSupported = !isDisabled && (isForced || isWindows && !isDumbTerminal || isCompatibleTerminal || isCI);
const replaceClose = (index, string$2, close, replace, head = string$2.substring(0, index) + replace, tail = string$2.substring(index + close.length), next = tail.indexOf(close)) => head + (next < 0 ? tail : replaceClose(next, tail, close, replace));
const clearBleed = (index, string$2, open$1, close, replace) => index < 0 ? open$1 + string$2 + close : open$1 + replaceClose(index, string$2, close, replace) + close;
const filterEmpty = (open$1, close, replace = open$1, at = open$1.length + 1) => (string$2) => string$2 || !(string$2 === "" || string$2 === void 0) ? clearBleed(("" + string$2).indexOf(close, at), string$2, open$1, close, replace) : "";
const init = (open$1, close, replace) => filterEmpty(`\x1b[${open$1}m`, `\x1b[${close}m`, replace);
const colors = {
	reset: init(0, 0),
	bold: init(1, 22, "\x1B[22m\x1B[1m"),
	dim: init(2, 22, "\x1B[22m\x1B[2m"),
	italic: init(3, 23),
	underline: init(4, 24),
	inverse: init(7, 27),
	hidden: init(8, 28),
	strikethrough: init(9, 29),
	black: init(30, 39),
	red: init(31, 39),
	green: init(32, 39),
	yellow: init(33, 39),
	blue: init(34, 39),
	magenta: init(35, 39),
	cyan: init(36, 39),
	white: init(37, 39),
	gray: init(90, 39),
	bgBlack: init(40, 49),
	bgRed: init(41, 49),
	bgGreen: init(42, 49),
	bgYellow: init(43, 49),
	bgBlue: init(44, 49),
	bgMagenta: init(45, 49),
	bgCyan: init(46, 49),
	bgWhite: init(47, 49),
	blackBright: init(90, 39),
	redBright: init(91, 39),
	greenBright: init(92, 39),
	yellowBright: init(93, 39),
	blueBright: init(94, 39),
	magentaBright: init(95, 39),
	cyanBright: init(96, 39),
	whiteBright: init(97, 39),
	bgBlackBright: init(100, 49),
	bgRedBright: init(101, 49),
	bgGreenBright: init(102, 49),
	bgYellowBright: init(103, 49),
	bgBlueBright: init(104, 49),
	bgMagentaBright: init(105, 49),
	bgCyanBright: init(106, 49),
	bgWhiteBright: init(107, 49)
};
const createColors = ({ useColor = isColorSupported } = {}) => useColor ? colors : Object.keys(colors).reduce((colors$1, key) => ({
	...colors$1,
	[key]: String
}), {});
const { reset, bold, dim, italic, underline, inverse, hidden, strikethrough, black, red, green, yellow, blue, magenta, cyan, white, gray, bgBlack, bgRed, bgGreen, bgYellow, bgBlue, bgMagenta, bgCyan, bgWhite, blackBright, redBright, greenBright, yellowBright, blueBright, magentaBright, cyanBright, whiteBright, bgBlackBright, bgRedBright, bgGreenBright, bgYellowBright, bgBlueBright, bgMagentaBright, bgCyanBright, bgWhiteBright } = createColors();

//#endregion
//#region ../../node_modules/.pnpm/rfdc@1.4.1/node_modules/rfdc/index.js
var require_rfdc = /* @__PURE__ */ __commonJS({ "../../node_modules/.pnpm/rfdc@1.4.1/node_modules/rfdc/index.js": ((exports, module) => {
	module.exports = rfdc$1;
	function copyBuffer(cur) {
		if (cur instanceof Buffer) return Buffer.from(cur);
		return new cur.constructor(cur.buffer.slice(), cur.byteOffset, cur.length);
	}
	function rfdc$1(opts) {
		opts = opts || {};
		if (opts.circles) return rfdcCircles(opts);
		const constructorHandlers = /* @__PURE__ */ new Map();
		constructorHandlers.set(Date, (o) => new Date(o));
		constructorHandlers.set(Map, (o, fn) => new Map(cloneArray(Array.from(o), fn)));
		constructorHandlers.set(Set, (o, fn) => new Set(cloneArray(Array.from(o), fn)));
		if (opts.constructorHandlers) for (const handler$1 of opts.constructorHandlers) constructorHandlers.set(handler$1[0], handler$1[1]);
		let handler = null;
		return opts.proto ? cloneProto : clone$1;
		function cloneArray(a, fn) {
			const keys = Object.keys(a);
			const a2 = new Array(keys.length);
			for (let i = 0; i < keys.length; i++) {
				const k = keys[i];
				const cur = a[k];
				if (typeof cur !== "object" || cur === null) a2[k] = cur;
				else if (cur.constructor !== Object && (handler = constructorHandlers.get(cur.constructor))) a2[k] = handler(cur, fn);
				else if (ArrayBuffer.isView(cur)) a2[k] = copyBuffer(cur);
				else a2[k] = fn(cur);
			}
			return a2;
		}
		function clone$1(o) {
			if (typeof o !== "object" || o === null) return o;
			if (Array.isArray(o)) return cloneArray(o, clone$1);
			if (o.constructor !== Object && (handler = constructorHandlers.get(o.constructor))) return handler(o, clone$1);
			const o2 = {};
			for (const k in o) {
				if (Object.hasOwnProperty.call(o, k) === false) continue;
				const cur = o[k];
				if (typeof cur !== "object" || cur === null) o2[k] = cur;
				else if (cur.constructor !== Object && (handler = constructorHandlers.get(cur.constructor))) o2[k] = handler(cur, clone$1);
				else if (ArrayBuffer.isView(cur)) o2[k] = copyBuffer(cur);
				else o2[k] = clone$1(cur);
			}
			return o2;
		}
		function cloneProto(o) {
			if (typeof o !== "object" || o === null) return o;
			if (Array.isArray(o)) return cloneArray(o, cloneProto);
			if (o.constructor !== Object && (handler = constructorHandlers.get(o.constructor))) return handler(o, cloneProto);
			const o2 = {};
			for (const k in o) {
				const cur = o[k];
				if (typeof cur !== "object" || cur === null) o2[k] = cur;
				else if (cur.constructor !== Object && (handler = constructorHandlers.get(cur.constructor))) o2[k] = handler(cur, cloneProto);
				else if (ArrayBuffer.isView(cur)) o2[k] = copyBuffer(cur);
				else o2[k] = cloneProto(cur);
			}
			return o2;
		}
	}
	function rfdcCircles(opts) {
		const refs = [];
		const refsNew = [];
		const constructorHandlers = /* @__PURE__ */ new Map();
		constructorHandlers.set(Date, (o) => new Date(o));
		constructorHandlers.set(Map, (o, fn) => new Map(cloneArray(Array.from(o), fn)));
		constructorHandlers.set(Set, (o, fn) => new Set(cloneArray(Array.from(o), fn)));
		if (opts.constructorHandlers) for (const handler$1 of opts.constructorHandlers) constructorHandlers.set(handler$1[0], handler$1[1]);
		let handler = null;
		return opts.proto ? cloneProto : clone$1;
		function cloneArray(a, fn) {
			const keys = Object.keys(a);
			const a2 = new Array(keys.length);
			for (let i = 0; i < keys.length; i++) {
				const k = keys[i];
				const cur = a[k];
				if (typeof cur !== "object" || cur === null) a2[k] = cur;
				else if (cur.constructor !== Object && (handler = constructorHandlers.get(cur.constructor))) a2[k] = handler(cur, fn);
				else if (ArrayBuffer.isView(cur)) a2[k] = copyBuffer(cur);
				else {
					const index = refs.indexOf(cur);
					if (index !== -1) a2[k] = refsNew[index];
					else a2[k] = fn(cur);
				}
			}
			return a2;
		}
		function clone$1(o) {
			if (typeof o !== "object" || o === null) return o;
			if (Array.isArray(o)) return cloneArray(o, clone$1);
			if (o.constructor !== Object && (handler = constructorHandlers.get(o.constructor))) return handler(o, clone$1);
			const o2 = {};
			refs.push(o);
			refsNew.push(o2);
			for (const k in o) {
				if (Object.hasOwnProperty.call(o, k) === false) continue;
				const cur = o[k];
				if (typeof cur !== "object" || cur === null) o2[k] = cur;
				else if (cur.constructor !== Object && (handler = constructorHandlers.get(cur.constructor))) o2[k] = handler(cur, clone$1);
				else if (ArrayBuffer.isView(cur)) o2[k] = copyBuffer(cur);
				else {
					const i = refs.indexOf(cur);
					if (i !== -1) o2[k] = refsNew[i];
					else o2[k] = clone$1(cur);
				}
			}
			refs.pop();
			refsNew.pop();
			return o2;
		}
		function cloneProto(o) {
			if (typeof o !== "object" || o === null) return o;
			if (Array.isArray(o)) return cloneArray(o, cloneProto);
			if (o.constructor !== Object && (handler = constructorHandlers.get(o.constructor))) return handler(o, cloneProto);
			const o2 = {};
			refs.push(o);
			refsNew.push(o2);
			for (const k in o) {
				const cur = o[k];
				if (typeof cur !== "object" || cur === null) o2[k] = cur;
				else if (cur.constructor !== Object && (handler = constructorHandlers.get(cur.constructor))) o2[k] = handler(cur, cloneProto);
				else if (ArrayBuffer.isView(cur)) o2[k] = copyBuffer(cur);
				else {
					const i = refs.indexOf(cur);
					if (i !== -1) o2[k] = refsNew[i];
					else o2[k] = cloneProto(cur);
				}
			}
			refs.pop();
			refsNew.pop();
			return o2;
		}
	}
}) });

//#endregion
//#region ../../node_modules/.pnpm/listr2@9.0.3/node_modules/listr2/dist/index.js
var import_rfdc = /* @__PURE__ */ __toESM(require_rfdc(), 1);
var _DefaultRenderer, _Class, _SimpleRenderer, _TestRenderer, _VerboseRenderer;
/**
* Indicates an UNICODE characters is coming up.
*/
const ANSI_ESCAPE = "\x1B[";
/**
* Generic ANSI escape characters for terminal based operations.
*/
const ANSI_ESCAPE_CODES = {
	CURSOR_HIDE: ANSI_ESCAPE + "?25l",
	CURSOR_SHOW: ANSI_ESCAPE + "?25h"
};
/**
* Environment variables for Listr.
*/
let ListrEnvironmentVariables = /* @__PURE__ */ function(ListrEnvironmentVariables$1) {
	ListrEnvironmentVariables$1["FORCE_UNICODE"] = "LISTR_FORCE_UNICODE";
	ListrEnvironmentVariables$1["FORCE_TTY"] = "LISTR_FORCE_TTY";
	ListrEnvironmentVariables$1["DISABLE_COLOR"] = "NO_COLOR";
	ListrEnvironmentVariables$1["FORCE_COLOR"] = "FORCE_COLOR";
	return ListrEnvironmentVariables$1;
}({});
/**
* The actual error type that is collected and to help identify where the error is triggered from.
*/
let ListrErrorTypes = /* @__PURE__ */ function(ListrErrorTypes$1) {
	/** Task has failed and will try to retry. */
	ListrErrorTypes$1["WILL_RETRY"] = "WILL_RETRY";
	/** Task has failed and will try to rollback. */
	ListrErrorTypes$1["WILL_ROLLBACK"] = "WILL_ROLLBACK";
	/** Task has failed, ran the rollback action but the rollback action itself has failed. */
	ListrErrorTypes$1["HAS_FAILED_TO_ROLLBACK"] = "HAS_FAILED_TO_ROLLBACK";
	/** Task has failed. */
	ListrErrorTypes$1["HAS_FAILED"] = "HAS_FAILED";
	/** Task has failed, but exitOnError is set to false, so will ignore this error. */
	ListrErrorTypes$1["HAS_FAILED_WITHOUT_ERROR"] = "HAS_FAILED_WITHOUT_ERROR";
	return ListrErrorTypes$1;
}({});
/**
* Events that are triggered by Listr.
*
* These are stateful and singleton events by being attached to the main Listr class and propagating to the subtasks.
*
* @see {@link https://listr2.kilic.dev/listr/events.html}
*/
let ListrEventType = /* @__PURE__ */ function(ListrEventType$1) {
	/** Indicates that underlying renderer should refresh the current render. */
	ListrEventType$1["SHOULD_REFRESH_RENDER"] = "SHOUD_REFRESH_RENDER";
	return ListrEventType$1;
}({});
let ListrRendererSelection = /* @__PURE__ */ function(ListrRendererSelection$1) {
	ListrRendererSelection$1["PRIMARY"] = "PRIMARY";
	ListrRendererSelection$1["SECONDARY"] = "SECONDARY";
	ListrRendererSelection$1["SILENT"] = "SILENT";
	return ListrRendererSelection$1;
}({});
/**
* Internal events that are fired from the Task.
*
* @see {@link https://listr2.kilic.dev/task/events.html}
*/
let ListrTaskEventType = /* @__PURE__ */ function(ListrTaskEventType$1) {
	/** Title has changed for the current Task. */
	ListrTaskEventType$1["TITLE"] = "TITLE";
	/**
	* State has changed for the current Task.
	*
	* @see {@link module:listr2.ListrTaskState}
	*/
	ListrTaskEventType$1["STATE"] = "STATE";
	/** The current Task has been marked as enabled. */
	ListrTaskEventType$1["ENABLED"] = "ENABLED";
	/** The current Task is currently processing subtasks. */
	ListrTaskEventType$1["SUBTASK"] = "SUBTASK";
	/** The current Task is now processing a prompt. */
	ListrTaskEventType$1["PROMPT"] = "PROMPT";
	/** The current Task is now dumping output. */
	ListrTaskEventType$1["OUTPUT"] = "OUTPUT";
	/**
	* The current Task is now dumping a message.
	*
	* @see {module:Listr2.ListrTaskMessage}
	*/
	ListrTaskEventType$1["MESSAGE"] = "MESSAGE";
	/** The current Task is closed and no further action in expected. */
	ListrTaskEventType$1["CLOSED"] = "CLOSED";
	return ListrTaskEventType$1;
}({});
/**
* Tasks can be in various states during the execution.
*
* Whenever a state change occurs, the task will emit a {@link module:listr2.ListrTaskEventType.STATE} with the appropriate state.
*/
let ListrTaskState = /* @__PURE__ */ function(ListrTaskState$1) {
	/** Task has not started yet, waiting for pick-up. */
	ListrTaskState$1["WAITING"] = "WAITING";
	/** Task has started. */
	ListrTaskState$1["STARTED"] = "STARTED";
	/** Task has been completed. */
	ListrTaskState$1["COMPLETED"] = "COMPLETED";
	/** Task has failed. */
	ListrTaskState$1["FAILED"] = "FAILED";
	/** Task has been skipped. */
	ListrTaskState$1["SKIPPED"] = "SKIPPED";
	/** Task is currently trying to rollback. */
	ListrTaskState$1["ROLLING_BACK"] = "ROLLING_BACK";
	/** Task has rolledback successfully after failing. */
	ListrTaskState$1["ROLLED_BACK"] = "ROLLED_BACK";
	/** Task is currently retrying. */
	ListrTaskState$1["RETRY"] = "RETRY";
	/** Task is currently paused. */
	ListrTaskState$1["PAUSED"] = "PAUSED";
	/** Task is currently trying to process a prompt. */
	ListrTaskState$1["PROMPT"] = "PROMPT";
	/** Task has successfully processed the prompt. */
	ListrTaskState$1["PROMPT_COMPLETED"] = "PROMPT_COMPLETED";
	/** Task has failed to process the prompt. */
	ListrTaskState$1["PROMPT_FAILED"] = "PROMPT_FAILED";
	return ListrTaskState$1;
}({});
var EventManager = class {
	constructor() {
		this.emitter = new eventemitter3_default();
	}
	emit(dispatch, args$5) {
		this.emitter.emit(dispatch, args$5);
	}
	on(dispatch, handler) {
		this.emitter.addListener(dispatch, handler);
	}
	once(dispatch, handler) {
		this.emitter.once(dispatch, handler);
	}
	off(dispatch, handler) {
		this.emitter.off(dispatch, handler);
	}
	complete() {
		this.emitter.removeAllListeners();
	}
};
/**
* Tests to see if the object is an RxJS {@link Observable}
* @param obj the object to test
*/
function isObservable(obj) {
	return !!obj && typeof obj === "object" && typeof obj.subscribe === "function";
}
/**
* Tests to see if the object is an Readable or NodeJS.ReadableStream {@link Readable, NodeJS.ReadableStream}
* @param obj the object to test
*/
function isReadable(obj) {
	return !!obj && typeof obj === "object" && obj.readable === true && typeof obj.read === "function" && typeof obj.on === "function";
}
function isUnicodeSupported() {
	/* istanbul ignore next */
	return !!process.env[ListrEnvironmentVariables.FORCE_UNICODE] || process.platform !== "win32" || !!process.env.CI || !!process.env.WT_SESSION || process.env.TERM_PROGRAM === "vscode" || process.env.TERM === "xterm-256color" || process.env.TERM === "alacritty";
}
const CLEAR_LINE_REGEX = "(?:\\u001b|\\u009b)\\[[\\=><~/#&.:=?%@~_-]*[0-9]*[\\a-ln-tqyz=><~/#&.:=?%@~_-]+";
const BELL_REGEX = /\u0007/;
function cleanseAnsi(chunk) {
	return String(chunk).replace(new RegExp(CLEAR_LINE_REGEX, "gmi"), "").replace(new RegExp(BELL_REGEX, "gmi"), "").trim();
}
/**
* Creates color palette through underlying dependency of `colorette`.
*
* @see {@link https://www.npmjs.com/package/colorette}
*/
const color = createColors();
function indent(string$2, count) {
	return string$2.replace(/^(?!\s*$)/gm, " ".repeat(count));
}
const FIGURES_MAIN = {
	warning: "⚠",
	cross: "✖",
	arrowDown: "↓",
	tick: "✔",
	arrowRight: "→",
	pointer: "❯",
	checkboxOn: "☒",
	arrowLeft: "←",
	squareSmallFilled: "◼",
	pointerSmall: "›"
};
const FIGURES_FALLBACK = {
	...FIGURES_MAIN,
	warning: "‼",
	cross: "×",
	tick: "√",
	pointer: ">",
	checkboxOn: "[×]",
	squareSmallFilled: "■"
};
const figures = isUnicodeSupported() ? FIGURES_MAIN : FIGURES_FALLBACK;
function splat(message, ...splat$1) {
	return format(String(message), ...splat$1);
}
/** Default ListrLogLevels for the logger */
let ListrLogLevels = /* @__PURE__ */ function(ListrLogLevels$1) {
	ListrLogLevels$1["STARTED"] = "STARTED";
	ListrLogLevels$1["COMPLETED"] = "COMPLETED";
	ListrLogLevels$1["FAILED"] = "FAILED";
	ListrLogLevels$1["SKIPPED"] = "SKIPPED";
	ListrLogLevels$1["OUTPUT"] = "OUTPUT";
	ListrLogLevels$1["TITLE"] = "TITLE";
	ListrLogLevels$1["ROLLBACK"] = "ROLLBACK";
	ListrLogLevels$1["RETRY"] = "RETRY";
	ListrLogLevels$1["PROMPT"] = "PROMPT";
	ListrLogLevels$1["PAUSED"] = "PAUSED";
	return ListrLogLevels$1;
}({});
const LISTR_LOGGER_STYLE = {
	icon: {
		[ListrLogLevels.STARTED]: figures.pointer,
		[ListrLogLevels.FAILED]: figures.cross,
		[ListrLogLevels.SKIPPED]: figures.arrowDown,
		[ListrLogLevels.COMPLETED]: figures.tick,
		[ListrLogLevels.OUTPUT]: figures.pointerSmall,
		[ListrLogLevels.TITLE]: figures.arrowRight,
		[ListrLogLevels.RETRY]: figures.warning,
		[ListrLogLevels.ROLLBACK]: figures.arrowLeft,
		[ListrLogLevels.PAUSED]: figures.squareSmallFilled
	},
	color: {
		[ListrLogLevels.STARTED]: color.yellow,
		[ListrLogLevels.FAILED]: color.red,
		[ListrLogLevels.SKIPPED]: color.yellow,
		[ListrLogLevels.COMPLETED]: color.green,
		[ListrLogLevels.RETRY]: color.yellowBright,
		[ListrLogLevels.ROLLBACK]: color.redBright,
		[ListrLogLevels.PAUSED]: color.yellowBright
	}
};
const LISTR_LOGGER_STDERR_LEVELS = [
	ListrLogLevels.RETRY,
	ListrLogLevels.ROLLBACK,
	ListrLogLevels.FAILED
];
/**
* Creates a new Listr2 logger.
*
* This logger is used throughout the renderers for consistency.
*
* @see {@link https://listr2.kilic.dev/renderer/logger.html}
*/
var ListrLogger = class {
	constructor(options) {
		var _this$options, _this$options$fields, _this$options$fields2;
		this.options = options;
		this.options = {
			useIcons: true,
			toStderr: [],
			...options ?? {}
		};
		(_this$options = this.options).fields ?? (_this$options.fields = {});
		(_this$options$fields = this.options.fields).prefix ?? (_this$options$fields.prefix = []);
		(_this$options$fields2 = this.options.fields).suffix ?? (_this$options$fields2.suffix = []);
		this.process = this.options.processOutput ?? new ProcessOutput();
	}
	log(level, message, options) {
		const output$1 = this.format(level, message, options);
		if (this.options.toStderr.includes(level)) {
			this.process.toStderr(output$1);
			return;
		}
		this.process.toStdout(output$1);
	}
	toStdout(message, options, eol = true) {
		this.process.toStdout(this.format(null, message, options), eol);
	}
	toStderr(message, options, eol = true) {
		this.process.toStderr(this.format(null, message, options), eol);
	}
	wrap(message, options) {
		if (!message) return message;
		return this.applyFormat(`[${message}]`, options);
	}
	splat(...args$5) {
		const message = args$5.shift() ?? "";
		return args$5.length === 0 ? message : splat(message, args$5);
	}
	suffix(message, ...suffixes) {
		suffixes.filter(Boolean).forEach((suffix) => {
			message += this.spacing(message);
			if (typeof suffix === "string") message += this.wrap(suffix);
			else if (typeof suffix === "object") {
				suffix.args ?? (suffix.args = []);
				if (typeof suffix.condition === "function" ? !suffix.condition(...suffix.args) : !(suffix.condition ?? true)) return message;
				message += this.wrap(typeof suffix.field === "function" ? suffix.field(...suffix.args) : suffix.field, { format: suffix?.format(...suffix.args) });
			}
		});
		return message;
	}
	prefix(message, ...prefixes) {
		prefixes.filter(Boolean).forEach((prefix$1) => {
			message = this.spacing(message) + message;
			if (typeof prefix$1 === "string") message = this.wrap(prefix$1) + message;
			else if (typeof prefix$1 === "object") {
				prefix$1.args ?? (prefix$1.args = []);
				if (typeof prefix$1.condition === "function" ? !prefix$1.condition(...prefix$1.args) : !(prefix$1.condition ?? true)) return message;
				message = this.wrap(typeof prefix$1.field === "function" ? prefix$1.field(...prefix$1.args) : prefix$1.field, { format: prefix$1?.format() }) + message;
			}
		});
		return message;
	}
	fields(message, options) {
		if (this.options?.fields?.prefix) message = this.prefix(message, ...this.options.fields.prefix);
		if (options?.prefix) message = this.prefix(message, ...options.prefix);
		if (options?.suffix) message = this.suffix(message, ...options.suffix);
		if (this.options?.fields?.suffix) message = this.suffix(message, ...this.options.fields.suffix);
		return message;
	}
	icon(level, icon) {
		if (!level) return null;
		if (!icon) {
			const i = this.options.icon?.[level];
			icon = typeof i === "function" ? i() : i;
		}
		const coloring = this.options.color?.[level];
		if (icon && coloring) icon = coloring(icon);
		return icon;
	}
	format(level, message, options) {
		if (!Array.isArray(message)) message = [message];
		message = this.splat(message.shift(), ...message).toString().split(EOL).filter((m$1) => !m$1 || m$1.trim() !== "").map((m$1) => {
			return this.style(level, this.fields(m$1, {
				prefix: Array.isArray(options?.prefix) ? options.prefix : [options?.prefix],
				suffix: Array.isArray(options?.suffix) ? options.suffix : [options?.suffix]
			}));
		}).join(EOL);
		return message;
	}
	style(level, message) {
		if (!level || !message) return message;
		const icon = this.icon(level, !this.options.useIcons && this.wrap(level));
		if (icon) message = icon + " " + message;
		return message;
	}
	applyFormat(message, options) {
		if (options?.format) return options.format(message);
		return message;
	}
	spacing(message) {
		return typeof message === "undefined" || message.trim() === "" ? "" : " ";
	}
};
var ProcessOutputBuffer = class {
	constructor(options) {
		this.buffer = [];
		this.decoder = new StringDecoder();
		this.options = options;
	}
	get all() {
		return this.buffer;
	}
	get last() {
		return this.buffer.at(-1);
	}
	get length() {
		return this.buffer.length;
	}
	write(data, ...args$5) {
		const callback = args$5[args$5.length - 1];
		this.buffer.push({
			time: Date.now(),
			stream: this.options?.stream,
			entry: this.decoder.write(typeof data === "string" ? Buffer.from(data, typeof args$5[0] === "string" ? args$5[0] : void 0) : Buffer.from(data))
		});
		if (this.options?.limit) this.buffer = this.buffer.slice(-this.options.limit);
		if (typeof callback === "function") callback();
		return true;
	}
	reset() {
		this.buffer = [];
	}
};
var ProcessOutputStream = class {
	constructor(stream) {
		this.stream = stream;
		this.method = stream.write;
		this.buffer = new ProcessOutputBuffer({ stream });
	}
	get out() {
		return Object.assign({}, this.stream, { write: this.write.bind(this) });
	}
	hijack() {
		this.stream.write = this.buffer.write.bind(this.buffer);
	}
	release() {
		this.stream.write = this.method;
		const buffer = [...this.buffer.all];
		this.buffer.reset();
		return buffer;
	}
	write(...args$5) {
		return this.method.apply(this.stream, args$5);
	}
};
/**
* Creates a new Listr2 process-output controller.
*
* This is used to control the flow to `process.stdout` and `process.stderr` for all renderers.
*
* @see {@link https://listr2.kilic.dev/renderer/process-output.html}
*/
var ProcessOutput = class {
	constructor(stdout, stderr, options) {
		this.options = options;
		this.stream = {
			stdout: new ProcessOutputStream(stdout ?? process.stdout),
			stderr: new ProcessOutputStream(stderr ?? process.stderr)
		};
		this.options = {
			dump: ["stdout", "stderr"],
			leaveEmptyLine: true,
			...options
		};
	}
	get stdout() {
		return this.stream.stdout.out;
	}
	get stderr() {
		return this.stream.stderr.out;
	}
	hijack() {
		if (this.active) throw new Error("ProcessOutput has been already hijacked!");
		this.stream.stdout.write(ANSI_ESCAPE_CODES.CURSOR_HIDE);
		Object.values(this.stream).forEach((stream) => stream.hijack());
		this.active = true;
	}
	release() {
		const output$1 = Object.entries(this.stream).map(([name, stream]) => ({
			name,
			buffer: stream.release()
		})).filter((output$1$1) => this.options.dump.includes(output$1$1.name)).flatMap((output$1$1) => output$1$1.buffer).sort((a, b) => a.time - b.time).map((message) => {
			return {
				...message,
				entry: cleanseAnsi(message.entry)
			};
		}).filter((message) => message.entry);
		if (output$1.length > 0) {
			if (this.options.leaveEmptyLine) this.stdout.write(EOL);
			output$1.forEach((message) => {
				(message.stream ?? this.stdout).write(message.entry + EOL);
			});
		}
		this.stream.stdout.write(ANSI_ESCAPE_CODES.CURSOR_SHOW);
		this.active = false;
	}
	toStdout(buffer, eol = true) {
		if (eol) buffer = buffer + EOL;
		return this.stream.stdout.write(buffer);
	}
	toStderr(buffer, eol = true) {
		if (eol) buffer = buffer + EOL;
		return this.stream.stderr.write(buffer);
	}
};
/* istanbul ignore next */
function createWritable(cb) {
	const writable = new Writable();
	writable.rows = Infinity;
	writable.columns = Infinity;
	writable.write = (chunk) => {
		cb(chunk.toString());
		return true;
	};
	return writable;
}
/* istanbul ignore next */
var Spinner = class {
	constructor() {
		this.spinner = !isUnicodeSupported() ? [
			"-",
			"\\",
			"|",
			"/"
		] : [
			"⠋",
			"⠙",
			"⠹",
			"⠸",
			"⠼",
			"⠴",
			"⠦",
			"⠧",
			"⠇",
			"⠏"
		];
		this.spinnerPosition = 0;
	}
	spin() {
		this.spinnerPosition = ++this.spinnerPosition % this.spinner.length;
	}
	fetch() {
		return this.spinner[this.spinnerPosition];
	}
	isRunning() {
		return !!this.id;
	}
	start(cb, interval = 100) {
		this.id = setInterval(() => {
			this.spin();
			if (cb) cb();
		}, interval);
	}
	stop() {
		clearInterval(this.id);
	}
};
let ListrDefaultRendererLogLevels = /* @__PURE__ */ function(ListrDefaultRendererLogLevels$1) {
	ListrDefaultRendererLogLevels$1["SKIPPED_WITH_COLLAPSE"] = "SKIPPED_WITH_COLLAPSE";
	ListrDefaultRendererLogLevels$1["SKIPPED_WITHOUT_COLLAPSE"] = "SKIPPED_WITHOUT_COLLAPSE";
	ListrDefaultRendererLogLevels$1["OUTPUT"] = "OUTPUT";
	ListrDefaultRendererLogLevels$1["OUTPUT_WITH_BOTTOMBAR"] = "OUTPUT_WITH_BOTTOMBAR";
	ListrDefaultRendererLogLevels$1["PENDING"] = "PENDING";
	ListrDefaultRendererLogLevels$1["COMPLETED"] = "COMPLETED";
	ListrDefaultRendererLogLevels$1["COMPLETED_WITH_FAILED_SUBTASKS"] = "COMPLETED_WITH_FAILED_SUBTASKS";
	ListrDefaultRendererLogLevels$1["COMPLETED_WITH_FAILED_SISTER_TASKS"] = "COMPLETED_WITH_SISTER_TASKS_FAILED";
	ListrDefaultRendererLogLevels$1["RETRY"] = "RETRY";
	ListrDefaultRendererLogLevels$1["ROLLING_BACK"] = "ROLLING_BACK";
	ListrDefaultRendererLogLevels$1["ROLLED_BACK"] = "ROLLED_BACK";
	ListrDefaultRendererLogLevels$1["FAILED"] = "FAILED";
	ListrDefaultRendererLogLevels$1["FAILED_WITH_FAILED_SUBTASKS"] = "FAILED_WITH_SUBTASKS";
	ListrDefaultRendererLogLevels$1["WAITING"] = "WAITING";
	ListrDefaultRendererLogLevels$1["PAUSED"] = "PAUSED";
	return ListrDefaultRendererLogLevels$1;
}({});
const LISTR_DEFAULT_RENDERER_STYLE = {
	icon: {
		[ListrDefaultRendererLogLevels.SKIPPED_WITH_COLLAPSE]: figures.arrowDown,
		[ListrDefaultRendererLogLevels.SKIPPED_WITHOUT_COLLAPSE]: figures.warning,
		[ListrDefaultRendererLogLevels.OUTPUT]: figures.pointerSmall,
		[ListrDefaultRendererLogLevels.OUTPUT_WITH_BOTTOMBAR]: figures.pointerSmall,
		[ListrDefaultRendererLogLevels.PENDING]: figures.pointer,
		[ListrDefaultRendererLogLevels.COMPLETED]: figures.tick,
		[ListrDefaultRendererLogLevels.COMPLETED_WITH_FAILED_SUBTASKS]: figures.warning,
		[ListrDefaultRendererLogLevels.COMPLETED_WITH_FAILED_SISTER_TASKS]: figures.squareSmallFilled,
		[ListrDefaultRendererLogLevels.RETRY]: figures.warning,
		[ListrDefaultRendererLogLevels.ROLLING_BACK]: figures.warning,
		[ListrDefaultRendererLogLevels.ROLLED_BACK]: figures.arrowLeft,
		[ListrDefaultRendererLogLevels.FAILED]: figures.cross,
		[ListrDefaultRendererLogLevels.FAILED_WITH_FAILED_SUBTASKS]: figures.pointer,
		[ListrDefaultRendererLogLevels.WAITING]: figures.squareSmallFilled,
		[ListrDefaultRendererLogLevels.PAUSED]: figures.squareSmallFilled
	},
	color: {
		[ListrDefaultRendererLogLevels.SKIPPED_WITH_COLLAPSE]: color.yellow,
		[ListrDefaultRendererLogLevels.SKIPPED_WITHOUT_COLLAPSE]: color.yellow,
		[ListrDefaultRendererLogLevels.PENDING]: color.yellow,
		[ListrDefaultRendererLogLevels.COMPLETED]: color.green,
		[ListrDefaultRendererLogLevels.COMPLETED_WITH_FAILED_SUBTASKS]: color.yellow,
		[ListrDefaultRendererLogLevels.COMPLETED_WITH_FAILED_SISTER_TASKS]: color.red,
		[ListrDefaultRendererLogLevels.RETRY]: color.yellowBright,
		[ListrDefaultRendererLogLevels.ROLLING_BACK]: color.redBright,
		[ListrDefaultRendererLogLevels.ROLLED_BACK]: color.redBright,
		[ListrDefaultRendererLogLevels.FAILED]: color.red,
		[ListrDefaultRendererLogLevels.FAILED_WITH_FAILED_SUBTASKS]: color.red,
		[ListrDefaultRendererLogLevels.WAITING]: color.dim,
		[ListrDefaultRendererLogLevels.PAUSED]: color.yellowBright
	}
};
/**
* A basic function to parse minutes and tasks passed given a duration.
* Useful for renderers to show the task time.
*/
/* istanbul ignore next */
function parseTimer(duration) {
	const seconds = Math.floor(duration / 1e3);
	const minutes = Math.floor(seconds / 60);
	let parsedTime;
	if (seconds === 0 && minutes === 0) parsedTime = `0.${Math.floor(duration / 100)}s`;
	if (seconds > 0) parsedTime = `${seconds % 60}s`;
	if (minutes > 0) parsedTime = `${minutes}m${parsedTime}`;
	return parsedTime;
}
/* istanbul ignore next */
const PRESET_TIMER = {
	condition: true,
	field: parseTimer,
	format: () => color.dim
};
var DefaultRenderer = (_DefaultRenderer = class DefaultRenderer$1 {
	constructor(tasks, options, events) {
		this.buffer = {
			output: /* @__PURE__ */ new Map(),
			bottom: /* @__PURE__ */ new Map()
		};
		this.cache = {
			render: /* @__PURE__ */ new Map(),
			rendererOptions: /* @__PURE__ */ new Map(),
			rendererTaskOptions: /* @__PURE__ */ new Map()
		};
		this.tasks = tasks;
		this.options = options;
		this.events = events;
		this.options = {
			...DefaultRenderer$1.rendererOptions,
			...this.options,
			icon: {
				...LISTR_DEFAULT_RENDERER_STYLE.icon,
				...options?.icon ?? {}
			},
			color: {
				...LISTR_DEFAULT_RENDERER_STYLE.color,
				...options?.color ?? {}
			}
		};
		this.spinner = this.options.spinner ?? new Spinner();
		this.logger = this.options.logger ?? new ListrLogger({
			useIcons: true,
			toStderr: []
		});
		this.logger.options.icon = this.options.icon;
		this.logger.options.color = this.options.color;
	}
	async render() {
		const { createLogUpdate } = await import("./log-update-i5LRJCHc.mjs");
		const { default: truncate } = await import("./cli-truncate-BM8ZM2fH.mjs");
		const { default: wrap } = await import("./wrap-ansi-BEIieZPu.mjs");
		this.updater = createLogUpdate(this.logger.process.stdout);
		this.truncate = truncate;
		this.wrap = wrap;
		this.logger.process.hijack();
		/* istanbul ignore if */
		if (!this.options?.lazy) this.spinner.start(() => {
			this.update();
		});
		this.events.on(ListrEventType.SHOULD_REFRESH_RENDER, () => {
			this.update();
		});
	}
	update() {
		this.updater(this.create());
	}
	end() {
		this.spinner.stop();
		this.updater.clear();
		this.updater.done();
		if (!this.options.clearOutput) this.logger.process.toStdout(this.create({ prompt: false }));
		this.logger.process.release();
	}
	create(options) {
		options = {
			tasks: true,
			bottomBar: true,
			prompt: true,
			...options
		};
		const render = [];
		const renderTasks = this.renderer(this.tasks);
		const renderBottomBar = this.renderBottomBar();
		const renderPrompt = this.renderPrompt();
		if (options.tasks && renderTasks.length > 0) render.push(...renderTasks);
		if (options.bottomBar && renderBottomBar.length > 0) {
			if (render.length > 0) render.push("");
			render.push(...renderBottomBar);
		}
		if (options.prompt && renderPrompt.length > 0) {
			if (render.length > 0) render.push("");
			render.push(...renderPrompt);
		}
		return render.join(EOL);
	}
	style(task, output$1 = false) {
		const rendererOptions = this.cache.rendererOptions.get(task.id);
		if (task.isSkipped()) {
			if (output$1 || rendererOptions.collapseSkips) return this.logger.icon(ListrDefaultRendererLogLevels.SKIPPED_WITH_COLLAPSE);
			else if (rendererOptions.collapseSkips === false) return this.logger.icon(ListrDefaultRendererLogLevels.SKIPPED_WITHOUT_COLLAPSE);
		}
		if (output$1) {
			if (this.shouldOutputToBottomBar(task)) return this.logger.icon(ListrDefaultRendererLogLevels.OUTPUT_WITH_BOTTOMBAR);
			return this.logger.icon(ListrDefaultRendererLogLevels.OUTPUT);
		}
		if (task.hasSubtasks()) {
			if (task.isStarted() || task.isPrompt() && rendererOptions.showSubtasks !== false && !task.subtasks.every((subtask) => !subtask.hasTitle())) return this.logger.icon(ListrDefaultRendererLogLevels.PENDING);
			else if (task.isCompleted() && task.subtasks.some((subtask) => subtask.hasFailed())) return this.logger.icon(ListrDefaultRendererLogLevels.COMPLETED_WITH_FAILED_SUBTASKS);
			else if (task.hasFailed()) return this.logger.icon(ListrDefaultRendererLogLevels.FAILED_WITH_FAILED_SUBTASKS);
		}
		if (task.isStarted() || task.isPrompt()) return this.logger.icon(ListrDefaultRendererLogLevels.PENDING, !this.options?.lazy && this.spinner.fetch());
		else if (task.isCompleted()) return this.logger.icon(ListrDefaultRendererLogLevels.COMPLETED);
		else if (task.isRetrying()) return this.logger.icon(ListrDefaultRendererLogLevels.RETRY, !this.options?.lazy && this.spinner.fetch());
		else if (task.isRollingBack()) return this.logger.icon(ListrDefaultRendererLogLevels.ROLLING_BACK, !this.options?.lazy && this.spinner.fetch());
		else if (task.hasRolledBack()) return this.logger.icon(ListrDefaultRendererLogLevels.ROLLED_BACK);
		else if (task.hasFailed()) return this.logger.icon(ListrDefaultRendererLogLevels.FAILED);
		else if (task.isPaused()) return this.logger.icon(ListrDefaultRendererLogLevels.PAUSED);
		return this.logger.icon(ListrDefaultRendererLogLevels.WAITING);
	}
	format(message, icon, level) {
		if (message.trim() === "") return [];
		if (icon) message = icon + " " + message;
		let parsed;
		const columns = (process.stdout.columns ?? 80) - level * this.options.indentation - 2;
		switch (this.options.formatOutput) {
			case "truncate":
				parsed = message.split(EOL).map((s$1, i) => {
					return this.truncate(this.indent(s$1, i), columns);
				});
				break;
			case "wrap":
				parsed = this.wrap(message, columns, {
					hard: true,
					trim: false
				}).split(EOL).map((s$1, i) => this.indent(s$1, i));
				break;
			default: throw new ListrRendererError("Format option for the renderer is wrong.");
		}
		if (this.options.removeEmptyLines) parsed = parsed.filter(Boolean);
		return parsed.map((str) => indent(str, level * this.options.indentation));
	}
	shouldOutputToOutputBar(task) {
		const outputBar = this.cache.rendererTaskOptions.get(task.id).outputBar;
		return typeof outputBar === "number" && outputBar !== 0 || typeof outputBar === "boolean" && outputBar !== false;
	}
	shouldOutputToBottomBar(task) {
		const bottomBar = this.cache.rendererTaskOptions.get(task.id).bottomBar;
		return typeof bottomBar === "number" && bottomBar !== 0 || typeof bottomBar === "boolean" && bottomBar !== false || !task.hasTitle();
	}
	renderer(tasks, level = 0) {
		return tasks.flatMap((task) => {
			if (!task.isEnabled()) return [];
			if (this.cache.render.has(task.id)) return this.cache.render.get(task.id);
			this.calculate(task);
			this.setupBuffer(task);
			const rendererOptions = this.cache.rendererOptions.get(task.id);
			const rendererTaskOptions = this.cache.rendererTaskOptions.get(task.id);
			const output$1 = [];
			if (task.isPrompt()) {
				if (this.activePrompt && this.activePrompt !== task.id) throw new ListrRendererError("Only one prompt can be active at the given time, please re-evaluate your task design.");
				else if (!this.activePrompt) {
					task.on(ListrTaskEventType.PROMPT, (prompt) => {
						const cleansed = cleanseAnsi(prompt);
						if (cleansed) this.prompt = cleansed;
					});
					task.on(ListrTaskEventType.STATE, (state) => {
						if (state === ListrTaskState.PROMPT_COMPLETED || task.hasFinalized() || task.hasReset()) {
							this.prompt = null;
							this.activePrompt = null;
							task.off(ListrTaskEventType.PROMPT);
						}
					});
					this.activePrompt = task.id;
				}
			}
			if (task.hasTitle()) if (!(tasks.some((task$1) => task$1.hasFailed()) && !task.hasFailed() && task.options.exitOnError !== false && !(task.isCompleted() || task.isSkipped()))) if (task.hasFailed() && rendererOptions.collapseErrors) output$1.push(...this.format(!task.hasSubtasks() && task.message.error && rendererOptions.showErrorMessage ? task.message.error : task.title, this.style(task), level));
			else if (task.isSkipped() && rendererOptions.collapseSkips) output$1.push(...this.format(this.logger.suffix(task.message.skip && rendererOptions.showSkipMessage ? task.message.skip : task.title, {
				field: ListrLogLevels.SKIPPED,
				condition: rendererOptions.suffixSkips,
				format: () => color.dim
			}), this.style(task), level));
			else if (task.isRetrying()) output$1.push(...this.format(this.logger.suffix(task.title, {
				field: `${ListrLogLevels.RETRY}:${task.message.retry.count}`,
				format: () => color.yellow,
				condition: rendererOptions.suffixRetries
			}), this.style(task), level));
			else if (task.isCompleted() && task.hasTitle() && assertFunctionOrSelf(rendererTaskOptions.timer?.condition, task.message.duration)) output$1.push(...this.format(this.logger.suffix(task?.title, {
				...rendererTaskOptions.timer,
				args: [task.message.duration]
			}), this.style(task), level));
			else if (task.isPaused()) output$1.push(...this.format(this.logger.suffix(task.title, {
				...rendererOptions.pausedTimer,
				args: [task.message.paused - Date.now()]
			}), this.style(task), level));
			else output$1.push(...this.format(task.title, this.style(task), level));
			else output$1.push(...this.format(task.title, this.logger.icon(ListrDefaultRendererLogLevels.COMPLETED_WITH_FAILED_SISTER_TASKS), level));
			if (!task.hasSubtasks() || !rendererOptions.showSubtasks) {
				if (task.hasFailed() && rendererOptions.collapseErrors === false && (rendererOptions.showErrorMessage || !rendererOptions.showSubtasks)) output$1.push(...this.dump(task, level, ListrLogLevels.FAILED));
				else if (task.isSkipped() && rendererOptions.collapseSkips === false && (rendererOptions.showSkipMessage || !rendererOptions.showSubtasks)) output$1.push(...this.dump(task, level, ListrLogLevels.SKIPPED));
			}
			if (task.isPending() || rendererTaskOptions.persistentOutput) output$1.push(...this.renderOutputBar(task, level));
			if (rendererOptions.showSubtasks !== false && task.hasSubtasks() && (task.isPending() || task.hasFinalized() && !task.hasTitle() || task.isCompleted() && rendererOptions.collapseSubtasks === false && !task.subtasks.some((subtask) => this.cache.rendererOptions.get(subtask.id)?.collapseSubtasks === true) || task.subtasks.some((subtask) => this.cache.rendererOptions.get(subtask.id)?.collapseSubtasks === false) || task.subtasks.some((subtask) => subtask.hasFailed()) || task.subtasks.some((subtask) => subtask.hasRolledBack()))) {
				const subtaskLevel = !task.hasTitle() ? level : level + 1;
				const subtaskRender = this.renderer(task.subtasks, subtaskLevel);
				output$1.push(...subtaskRender);
			}
			if (task.hasFinalized()) {
				if (!rendererTaskOptions.persistentOutput) {
					this.buffer.bottom.delete(task.id);
					this.buffer.output.delete(task.id);
				}
			}
			if (task.isClosed()) {
				this.cache.render.set(task.id, output$1);
				this.reset(task);
			}
			return output$1;
		});
	}
	renderOutputBar(task, level) {
		const output$1 = this.buffer.output.get(task.id);
		if (!output$1) return [];
		return output$1.all.flatMap((o) => this.dump(task, level, ListrLogLevels.OUTPUT, o.entry));
	}
	renderBottomBar() {
		if (this.buffer.bottom.size === 0) return [];
		return Array.from(this.buffer.bottom.values()).flatMap((output$1) => output$1.all).sort((a, b) => a.time - b.time).map((output$1) => output$1.entry);
	}
	renderPrompt() {
		if (!this.prompt) return [];
		return [this.prompt];
	}
	calculate(task) {
		if (this.cache.rendererOptions.has(task.id) && this.cache.rendererTaskOptions.has(task.id)) return;
		const rendererOptions = {
			...this.options,
			...task.rendererOptions
		};
		this.cache.rendererOptions.set(task.id, rendererOptions);
		this.cache.rendererTaskOptions.set(task.id, {
			...DefaultRenderer$1.rendererTaskOptions,
			timer: rendererOptions.timer,
			...task.rendererTaskOptions
		});
	}
	setupBuffer(task) {
		if (this.buffer.bottom.has(task.id) || this.buffer.output.has(task.id)) return;
		const rendererTaskOptions = this.cache.rendererTaskOptions.get(task.id);
		if (this.shouldOutputToBottomBar(task) && !this.buffer.bottom.has(task.id)) {
			this.buffer.bottom.set(task.id, new ProcessOutputBuffer({ limit: typeof rendererTaskOptions.bottomBar === "number" ? rendererTaskOptions.bottomBar : 1 }));
			task.on(ListrTaskEventType.OUTPUT, (output$1) => {
				const data = this.dump(task, -1, ListrLogLevels.OUTPUT, output$1);
				this.buffer.bottom.get(task.id).write(data.join(EOL));
			});
			task.on(ListrTaskEventType.STATE, (state) => {
				switch (state) {
					case ListrTaskState.RETRY || ListrTaskState.ROLLING_BACK:
						this.buffer.bottom.delete(task.id);
						break;
				}
			});
		} else if (this.shouldOutputToOutputBar(task) && !this.buffer.output.has(task.id)) {
			this.buffer.output.set(task.id, new ProcessOutputBuffer({ limit: typeof rendererTaskOptions.outputBar === "number" ? rendererTaskOptions.outputBar : 1 }));
			task.on(ListrTaskEventType.OUTPUT, (output$1) => {
				this.buffer.output.get(task.id).write(output$1);
			});
			task.on(ListrTaskEventType.STATE, (state) => {
				switch (state) {
					case ListrTaskState.RETRY || ListrTaskState.ROLLING_BACK:
						this.buffer.output.delete(task.id);
						break;
				}
			});
		}
	}
	reset(task) {
		this.cache.rendererOptions.delete(task.id);
		this.cache.rendererTaskOptions.delete(task.id);
		this.buffer.output.delete(task.id);
	}
	dump(task, level, source = ListrLogLevels.OUTPUT, data) {
		if (!data) switch (source) {
			case ListrLogLevels.OUTPUT:
				data = task.output;
				break;
			case ListrLogLevels.SKIPPED:
				data = task.message.skip;
				break;
			case ListrLogLevels.FAILED:
				data = task.message.error;
				break;
		}
		if (task.hasTitle() && source === ListrLogLevels.FAILED && data === task.title || typeof data !== "string") return [];
		if (source === ListrLogLevels.OUTPUT) data = cleanseAnsi(data);
		return this.format(data, this.style(task, true), level + 1);
	}
	indent(str, i) {
		return i > 0 ? indent(str.trimEnd(), this.options.indentation) : str.trimEnd();
	}
}, _DefaultRenderer.nonTTY = false, _DefaultRenderer.rendererOptions = {
	indentation: 2,
	clearOutput: false,
	showSubtasks: true,
	collapseSubtasks: true,
	collapseSkips: true,
	showSkipMessage: true,
	suffixSkips: false,
	collapseErrors: true,
	showErrorMessage: true,
	suffixRetries: true,
	lazy: false,
	removeEmptyLines: true,
	formatOutput: "wrap",
	pausedTimer: {
		...PRESET_TIMER,
		format: () => color.yellowBright
	}
}, _DefaultRenderer.rendererTaskOptions = { outputBar: true }, _DefaultRenderer);
var SilentRenderer = (_Class = class {
	constructor(tasks, options) {
		this.tasks = tasks;
		this.options = options;
	}
	render() {}
	end() {}
}, _Class.nonTTY = true, _Class);
var SimpleRenderer = (_SimpleRenderer = class SimpleRenderer$1 {
	constructor(tasks, options) {
		this.cache = {
			rendererOptions: /* @__PURE__ */ new Map(),
			rendererTaskOptions: /* @__PURE__ */ new Map()
		};
		this.tasks = tasks;
		this.options = options;
		this.options = {
			...SimpleRenderer$1.rendererOptions,
			...options,
			icon: {
				...LISTR_LOGGER_STYLE.icon,
				...options?.icon ?? {}
			},
			color: {
				...LISTR_LOGGER_STYLE.color,
				...options?.color ?? {}
			}
		};
		this.logger = this.options.logger ?? new ListrLogger({
			useIcons: true,
			toStderr: LISTR_LOGGER_STDERR_LEVELS
		});
		this.logger.options.icon = this.options.icon;
		this.logger.options.color = this.options.color;
		if (this.options.timestamp) this.logger.options.fields.prefix.unshift(this.options.timestamp);
	}
	end() {}
	render() {
		this.renderer(this.tasks);
	}
	renderer(tasks) {
		tasks.forEach((task) => {
			this.calculate(task);
			task.once(ListrTaskEventType.CLOSED, () => {
				this.reset(task);
			});
			const rendererOptions = this.cache.rendererOptions.get(task.id);
			const rendererTaskOptions = this.cache.rendererTaskOptions.get(task.id);
			task.on(ListrTaskEventType.SUBTASK, (subtasks) => {
				this.renderer(subtasks);
			});
			task.on(ListrTaskEventType.STATE, (state) => {
				if (!task.hasTitle()) return;
				if (state === ListrTaskState.STARTED) this.logger.log(ListrLogLevels.STARTED, task.title);
				else if (state === ListrTaskState.COMPLETED) {
					const timer = rendererTaskOptions?.timer;
					this.logger.log(ListrLogLevels.COMPLETED, task.title, timer && { suffix: {
						...timer,
						condition: !!task.message?.duration && timer.condition,
						args: [task.message.duration]
					} });
				} else if (state === ListrTaskState.PROMPT) {
					this.logger.process.hijack();
					task.on(ListrTaskEventType.PROMPT, (prompt) => {
						this.logger.process.toStderr(prompt, false);
					});
				} else if (state === ListrTaskState.PROMPT_COMPLETED) {
					task.off(ListrTaskEventType.PROMPT);
					this.logger.process.release();
				}
			});
			task.on(ListrTaskEventType.OUTPUT, (output$1) => {
				this.logger.log(ListrLogLevels.OUTPUT, output$1);
			});
			task.on(ListrTaskEventType.MESSAGE, (message) => {
				if (message.error) this.logger.log(ListrLogLevels.FAILED, task.title, { suffix: {
					field: `${ListrLogLevels.FAILED}: ${message.error}`,
					format: () => color.red
				} });
				else if (message.skip) this.logger.log(ListrLogLevels.SKIPPED, task.title, { suffix: {
					field: `${ListrLogLevels.SKIPPED}: ${message.skip}`,
					format: () => color.yellow
				} });
				else if (message.rollback) this.logger.log(ListrLogLevels.ROLLBACK, task.title, { suffix: {
					field: `${ListrLogLevels.ROLLBACK}: ${message.rollback}`,
					format: () => color.red
				} });
				else if (message.retry) this.logger.log(ListrLogLevels.RETRY, task.title, { suffix: {
					field: `${ListrLogLevels.RETRY}:${message.retry.count}`,
					format: () => color.red
				} });
				else if (message.paused) {
					const timer = rendererOptions?.pausedTimer;
					this.logger.log(ListrLogLevels.PAUSED, task.title, timer && { suffix: {
						...timer,
						condition: !!message?.paused && timer.condition,
						args: [message.paused - Date.now()]
					} });
				}
			});
		});
	}
	calculate(task) {
		if (this.cache.rendererOptions.has(task.id) && this.cache.rendererTaskOptions.has(task.id)) return;
		const rendererOptions = {
			...this.options,
			...task.rendererOptions
		};
		this.cache.rendererOptions.set(task.id, rendererOptions);
		this.cache.rendererTaskOptions.set(task.id, {
			...SimpleRenderer$1.rendererTaskOptions,
			timer: rendererOptions.timer,
			...task.rendererTaskOptions
		});
	}
	reset(task) {
		this.cache.rendererOptions.delete(task.id);
		this.cache.rendererTaskOptions.delete(task.id);
	}
}, _SimpleRenderer.nonTTY = true, _SimpleRenderer.rendererOptions = { pausedTimer: {
	...PRESET_TIMER,
	field: (time) => `${ListrLogLevels.PAUSED}:${time}`,
	format: () => color.yellowBright
} }, _SimpleRenderer.rendererTaskOptions = {}, _SimpleRenderer);
var TestRendererSerializer = class {
	constructor(options) {
		this.options = options;
	}
	serialize(event, data, task) {
		return JSON.stringify(this.generate(event, data, task));
	}
	generate(event, data, task) {
		const output$1 = {
			event,
			data
		};
		if (typeof this.options?.task !== "boolean") {
			const t = Object.fromEntries(this.options.task.map((entity) => {
				const property = task[entity];
				if (typeof property === "function") return [entity, property.call(task)];
				return [entity, property];
			}));
			if (Object.keys(task).length > 0) output$1.task = t;
		}
		return output$1;
	}
};
var TestRenderer = (_TestRenderer = class TestRenderer$1 {
	constructor(tasks, options) {
		this.tasks = tasks;
		this.options = options;
		this.options = {
			...TestRenderer$1.rendererOptions,
			...this.options
		};
		this.logger = this.options.logger ?? new ListrLogger({ useIcons: false });
		this.serializer = new TestRendererSerializer(this.options);
	}
	render() {
		this.renderer(this.tasks);
	}
	end() {}
	renderer(tasks) {
		tasks.forEach((task) => {
			if (this.options.subtasks) task.on(ListrTaskEventType.SUBTASK, (subtasks) => {
				this.renderer(subtasks);
			});
			if (this.options.state) task.on(ListrTaskEventType.STATE, (state) => {
				this.logger.toStdout(this.serializer.serialize(ListrTaskEventType.STATE, state, task));
			});
			if (this.options.output) task.on(ListrTaskEventType.OUTPUT, (data) => {
				this.logger.toStdout(this.serializer.serialize(ListrTaskEventType.OUTPUT, data, task));
			});
			if (this.options.prompt) task.on(ListrTaskEventType.PROMPT, (prompt) => {
				this.logger.toStdout(this.serializer.serialize(ListrTaskEventType.PROMPT, prompt, task));
			});
			if (this.options.title) task.on(ListrTaskEventType.TITLE, (title) => {
				this.logger.toStdout(this.serializer.serialize(ListrTaskEventType.TITLE, title, task));
			});
			task.on(ListrTaskEventType.MESSAGE, (message) => {
				const parsed = Object.fromEntries(Object.entries(message).map(([key, value]) => {
					if (this.options.messages.includes(key)) return [key, value];
				}).filter(Boolean));
				if (Object.keys(parsed).length > 0) {
					const output$1 = this.serializer.serialize(ListrTaskEventType.MESSAGE, parsed, task);
					if (this.options.messagesToStderr.some((state) => Object.keys(parsed).includes(state))) this.logger.toStderr(output$1);
					else this.logger.toStdout(output$1);
				}
			});
		});
	}
}, _TestRenderer.nonTTY = true, _TestRenderer.rendererOptions = {
	subtasks: true,
	state: Object.values(ListrTaskState),
	output: true,
	prompt: true,
	title: true,
	messages: [
		"skip",
		"error",
		"retry",
		"rollback",
		"paused"
	],
	messagesToStderr: [
		"error",
		"rollback",
		"retry"
	],
	task: [
		"hasRolledBack",
		"isRollingBack",
		"isCompleted",
		"isSkipped",
		"hasFinalized",
		"hasSubtasks",
		"title",
		"hasReset",
		"hasTitle",
		"isPrompt",
		"isPaused",
		"isPending",
		"isSkipped",
		"isStarted",
		"hasFailed",
		"isEnabled",
		"isRetrying",
		"path"
	]
}, _TestRenderer);
var VerboseRenderer = (_VerboseRenderer = class VerboseRenderer$1 {
	constructor(tasks, options) {
		this.cache = {
			rendererOptions: /* @__PURE__ */ new Map(),
			rendererTaskOptions: /* @__PURE__ */ new Map()
		};
		this.tasks = tasks;
		this.options = options;
		this.options = {
			...VerboseRenderer$1.rendererOptions,
			...this.options,
			icon: {
				...LISTR_LOGGER_STYLE.icon,
				...options?.icon ?? {}
			},
			color: {
				...LISTR_LOGGER_STYLE.color,
				...options?.color ?? {}
			}
		};
		this.logger = this.options.logger ?? new ListrLogger({
			useIcons: false,
			toStderr: LISTR_LOGGER_STDERR_LEVELS
		});
		this.logger.options.icon = this.options.icon;
		this.logger.options.color = this.options.color;
		if (this.options.timestamp) this.logger.options.fields.prefix.unshift(this.options.timestamp);
	}
	render() {
		this.renderer(this.tasks);
	}
	end() {}
	renderer(tasks) {
		tasks.forEach((task) => {
			this.calculate(task);
			task.once(ListrTaskEventType.CLOSED, () => {
				this.reset(task);
			});
			const rendererOptions = this.cache.rendererOptions.get(task.id);
			const rendererTaskOptions = this.cache.rendererTaskOptions.get(task.id);
			task.on(ListrTaskEventType.SUBTASK, (subtasks) => {
				this.renderer(subtasks);
			});
			task.on(ListrTaskEventType.STATE, (state) => {
				if (!task.hasTitle()) return;
				if (state === ListrTaskState.STARTED) this.logger.log(ListrLogLevels.STARTED, task.title);
				else if (state === ListrTaskState.COMPLETED) {
					const timer = rendererTaskOptions.timer;
					this.logger.log(ListrLogLevels.COMPLETED, task.title, timer && { suffix: {
						...timer,
						condition: !!task.message?.duration && timer.condition,
						args: [task.message.duration]
					} });
				}
			});
			task.on(ListrTaskEventType.OUTPUT, (data) => {
				this.logger.log(ListrLogLevels.OUTPUT, data);
			});
			task.on(ListrTaskEventType.PROMPT, (prompt) => {
				const cleansed = cleanseAnsi(prompt);
				if (cleansed) this.logger.log(ListrLogLevels.PROMPT, cleansed);
			});
			if (this.options?.logTitleChange !== false) task.on(ListrTaskEventType.TITLE, (title) => {
				this.logger.log(ListrLogLevels.TITLE, title);
			});
			task.on(ListrTaskEventType.MESSAGE, (message) => {
				if (message?.error) this.logger.log(ListrLogLevels.FAILED, message.error);
				else if (message?.skip) this.logger.log(ListrLogLevels.SKIPPED, message.skip);
				else if (message?.rollback) this.logger.log(ListrLogLevels.ROLLBACK, message.rollback);
				else if (message?.retry) this.logger.log(ListrLogLevels.RETRY, task.title, { suffix: message.retry.count.toString() });
				else if (message?.paused) {
					const timer = rendererOptions?.pausedTimer;
					this.logger.log(ListrLogLevels.PAUSED, task.title, timer && { suffix: {
						...timer,
						condition: !!message?.paused && timer.condition,
						args: [message.paused - Date.now()]
					} });
				}
			});
		});
	}
	calculate(task) {
		if (this.cache.rendererOptions.has(task.id) && this.cache.rendererTaskOptions.has(task.id)) return;
		const rendererOptions = {
			...this.options,
			...task.rendererOptions
		};
		this.cache.rendererOptions.set(task.id, rendererOptions);
		this.cache.rendererTaskOptions.set(task.id, {
			...VerboseRenderer$1.rendererTaskOptions,
			timer: rendererOptions.timer,
			...task.rendererTaskOptions
		});
	}
	reset(task) {
		this.cache.rendererOptions.delete(task.id);
		this.cache.rendererTaskOptions.delete(task.id);
	}
}, _VerboseRenderer.nonTTY = true, _VerboseRenderer.rendererOptions = {
	logTitleChange: false,
	pausedTimer: {
		...PRESET_TIMER,
		format: () => color.yellowBright
	}
}, _VerboseRenderer);
const RENDERERS = {
	default: DefaultRenderer,
	simple: SimpleRenderer,
	verbose: VerboseRenderer,
	test: TestRenderer,
	silent: SilentRenderer
};
function isRendererSupported(renderer) {
	return process.stdout.isTTY === true || renderer.nonTTY === true;
}
function getRendererClass(renderer) {
	if (typeof renderer === "string") return RENDERERS[renderer] ?? RENDERERS.default;
	return typeof renderer === "function" ? renderer : RENDERERS.default;
}
function getRenderer(options) {
	if (assertFunctionOrSelf(options?.silentRendererCondition)) return {
		renderer: getRendererClass("silent"),
		selection: ListrRendererSelection.SILENT
	};
	const r = {
		renderer: getRendererClass(options.renderer),
		options: options.rendererOptions,
		selection: ListrRendererSelection.PRIMARY
	};
	if (!isRendererSupported(r.renderer) || assertFunctionOrSelf(options?.fallbackRendererCondition)) return {
		renderer: getRendererClass(options.fallbackRenderer),
		options: options.fallbackRendererOptions,
		selection: ListrRendererSelection.SECONDARY
	};
	return r;
}
/**
* This function asserts the given value as a function or itself.
* If the value itself is a function it will evaluate it with the passed in arguments,
* elsewise it will directly return itself.
*/
function assertFunctionOrSelf(functionOrSelf, ...args$5) {
	if (typeof functionOrSelf === "function") return functionOrSelf(...args$5);
	else return functionOrSelf;
}
const clone = (0, import_rfdc.default)({ circles: true });
/**
* Deep clones a object in the easiest manner.
*/
function cloneObject(obj) {
	return clone(obj);
}
var Concurrency = class {
	constructor(options) {
		this.concurrency = options.concurrency;
		this.count = 0;
		this.queue = /* @__PURE__ */ new Set();
	}
	add(fn) {
		if (this.count < this.concurrency) return this.run(fn);
		return new Promise((resolve) => {
			const callback = () => resolve(this.run(fn));
			this.queue.add(callback);
		});
	}
	flush() {
		for (const callback of this.queue) {
			if (this.count >= this.concurrency) break;
			this.queue.delete(callback);
			callback();
		}
	}
	run(fn) {
		this.count++;
		const promise = fn();
		const cleanup = () => {
			this.count--;
			this.flush();
		};
		promise.then(cleanup, () => {
			this.queue.clear();
		});
		return promise;
	}
};
function delay(time) {
	return new Promise((resolve) => {
		setTimeout(resolve, time);
	});
}
/**
* Internal error handling mechanism for Listr collects the errors and details for a failed task.
*
* @see {@link https://listr2.kilic.dev/task/error-handling.html}
*/
var ListrError = class extends Error {
	constructor(error, type, task) {
		super(error.message);
		this.error = error;
		this.type = type;
		this.task = task;
		this.name = "ListrError";
		this.path = task.path;
		if (task?.options.collectErrors === "full") {
			this.task = cloneObject(task);
			this.ctx = cloneObject(task.listr.ctx);
		}
		this.stack = error?.stack;
	}
};
/**
* Internal error coming from renderer.
*/
var ListrRendererError = class extends Error {};
/**
* Internal error handling mechanism for Listr prompts to identify the failing cause is coming from a prompt.
*
* @see {@link https://listr2.kilic.dev/task/prompts.html}
*/
var PromptError = class extends Error {};
/**
* The original Task that is defined by the user is wrapped with the TaskWrapper to provide additional functionality.
*
* @see {@link https://listr2.kilic.dev/task/task.html}
*/
var TaskWrapper = class {
	constructor(task) {
		this.task = task;
	}
	/* istanbul ignore next */
	get title() {
		return this.task.title;
	}
	/**
	* Title of the current task.
	*
	* @see {@link https://listr2.kilic.dev/task/title.html}
	*/
	set title(title) {
		title = Array.isArray(title) ? title : [title];
		this.task.title$ = splat(title.shift(), ...title);
	}
	/* istanbul ignore next */
	get output() {
		return this.task.output;
	}
	/* istanbul ignore next */
	/**
	* Send output from the current task to the renderer.
	*
	* @see {@link https://listr2.kilic.dev/task/output.html}
	*/
	set output(output$1) {
		output$1 = Array.isArray(output$1) ? output$1 : [output$1];
		this.task.output$ = splat(output$1.shift(), ...output$1);
	}
	/* istanbul ignore next */
	/** Send an output to the output channel as prompt. */
	set promptOutput(output$1) {
		this.task.promptOutput$ = output$1;
	}
	/**
	* Creates a new set of Listr subtasks.
	*
	* @see {@link https://listr2.kilic.dev/task/subtasks.html}
	*/
	newListr(task, options) {
		let tasks;
		if (typeof task === "function") tasks = task(this);
		else tasks = task;
		return new Listr(tasks, options, this.task);
	}
	/**
	* Report an error that has to be collected and handled.
	*
	* @see {@link https://listr2.kilic.dev/task/error-handling.html}
	*/
	report(error, type) {
		if (this.task.options.collectErrors !== false) this.task.listr.errors.push(new ListrError(error, type, this.task));
		this.task.message$ = { error: error.message ?? this.task?.title };
	}
	/**
	* Skip the current task.
	*
	* @see {@link https://listr2.kilic.dev/task/skip.html}
	*/
	skip(message, ...metadata) {
		this.task.state$ = ListrTaskState.SKIPPED;
		if (message) this.task.message$ = { skip: message ? splat(message, ...metadata) : this.task?.title };
	}
	/**
	* Check whether this task is currently in a retry state.
	*
	* @see {@link https://listr2.kilic.dev/task/retry.html}
	*/
	isRetrying() {
		return this.task.isRetrying() ? this.task.retry : { count: 0 };
	}
	/* istanbul ignore next */
	/**
	* Create a new prompt for getting user input through the prompt adapter.
	* This will create a new prompt through the adapter if the task is not currently rendering a prompt or will return the active instance.
	*
	* This part of the application requires optional peer dependencies, please refer to documentation.
	*
	* @see {@link https://listr2.kilic.dev/task/prompt.html}
	*/
	prompt(adapter) {
		if (this.task.prompt) return this.task.prompt;
		return new adapter(this.task, this);
	}
	/* istanbul ignore next */
	/**
	* Generates a fake stdout for your use case, where it will be tunnelled through Listr to handle the rendering process.
	*
	* @see {@link https://listr2.kilic.dev/renderer/process-output.html}
	*/
	stdout(type) {
		return createWritable((chunk) => {
			switch (type) {
				case ListrTaskEventType.PROMPT:
					this.promptOutput = chunk;
					break;
				default: this.output = chunk;
			}
		});
	}
	/** Run this task. */
	run(ctx) {
		return this.task.run(ctx, this);
	}
};
var ListrTaskEventManager = class extends EventManager {};
/**
* Creates and handles a runnable instance of the Task.
*/
var Task = class extends ListrTaskEventManager {
	constructor(listr, task, options, rendererOptions, rendererTaskOptions) {
		super();
		this.id = randomUUID();
		this.state = ListrTaskState.WAITING;
		this.message = {};
		this.listr = listr;
		this.task = task;
		this.options = options;
		this.rendererOptions = rendererOptions;
		this.rendererTaskOptions = rendererTaskOptions;
		if (task.title) {
			const title = Array.isArray(task?.title) ? task.title : [task.title];
			this.title = splat(title.shift(), ...title);
			this.initialTitle = this.title;
		}
		this.taskFn = task.task;
		this.parent = listr.parentTask;
	}
	/**
	* Update the current state of the Task and emit the neccassary events.
	*/
	set state$(state) {
		this.state = state;
		this.emit(ListrTaskEventType.STATE, state);
		if (this.hasSubtasks() && this.hasFailed()) {
			for (const subtask of this.subtasks) if (subtask.state === ListrTaskState.STARTED) subtask.state$ = ListrTaskState.FAILED;
		}
		this.listr.events.emit(ListrEventType.SHOULD_REFRESH_RENDER);
	}
	/**
	* Update the current output of the Task and emit the neccassary events.
	*/
	set output$(data) {
		this.output = data;
		this.emit(ListrTaskEventType.OUTPUT, data);
		this.listr.events.emit(ListrEventType.SHOULD_REFRESH_RENDER);
	}
	/**
	* Update the current prompt output of the Task and emit the neccassary events.
	*/
	set promptOutput$(data) {
		this.emit(ListrTaskEventType.PROMPT, data);
		if (cleanseAnsi(data)) this.listr.events.emit(ListrEventType.SHOULD_REFRESH_RENDER);
	}
	/**
	* Update or extend the current message of the Task and emit the neccassary events.
	*/
	set message$(data) {
		this.message = {
			...this.message,
			...data
		};
		this.emit(ListrTaskEventType.MESSAGE, data);
		this.listr.events.emit(ListrEventType.SHOULD_REFRESH_RENDER);
	}
	/**
	* Update the current title of the Task and emit the neccassary events.
	*/
	set title$(title) {
		this.title = title;
		this.emit(ListrTaskEventType.TITLE, title);
		this.listr.events.emit(ListrEventType.SHOULD_REFRESH_RENDER);
	}
	/**
	* Current task path in the hierarchy.
	*/
	get path() {
		return [...this.listr.path, this.initialTitle];
	}
	/**
	* Checks whether the current task with the given context should be set as enabled.
	*/
	async check(ctx) {
		if (this.state === ListrTaskState.WAITING) {
			this.enabled = await assertFunctionOrSelf(this.task?.enabled ?? true, ctx);
			this.emit(ListrTaskEventType.ENABLED, this.enabled);
			this.listr.events.emit(ListrEventType.SHOULD_REFRESH_RENDER);
		}
		return this.enabled;
	}
	/** Returns whether this task has subtasks. */
	hasSubtasks() {
		return this.subtasks?.length > 0;
	}
	/** Returns whether this task is finalized in someform. */
	hasFinalized() {
		return this.isCompleted() || this.hasFailed() || this.isSkipped() || this.hasRolledBack();
	}
	/** Returns whether this task is in progress. */
	isPending() {
		return this.isStarted() || this.isPrompt() || this.hasReset();
	}
	/** Returns whether this task has started. */
	isStarted() {
		return this.state === ListrTaskState.STARTED;
	}
	/** Returns whether this task is skipped. */
	isSkipped() {
		return this.state === ListrTaskState.SKIPPED;
	}
	/** Returns whether this task has been completed. */
	isCompleted() {
		return this.state === ListrTaskState.COMPLETED;
	}
	/** Returns whether this task has been failed. */
	hasFailed() {
		return this.state === ListrTaskState.FAILED;
	}
	/** Returns whether this task has an active rollback task going on. */
	isRollingBack() {
		return this.state === ListrTaskState.ROLLING_BACK;
	}
	/** Returns whether the rollback action was successful. */
	hasRolledBack() {
		return this.state === ListrTaskState.ROLLED_BACK;
	}
	/** Returns whether this task has an actively retrying task going on. */
	isRetrying() {
		return this.state === ListrTaskState.RETRY;
	}
	/** Returns whether this task has some kind of reset like retry and rollback going on. */
	hasReset() {
		return this.state === ListrTaskState.RETRY || this.state === ListrTaskState.ROLLING_BACK;
	}
	/** Returns whether enabled function resolves to true. */
	isEnabled() {
		return this.enabled;
	}
	/** Returns whether this task actually has a title. */
	hasTitle() {
		return typeof this?.title === "string";
	}
	/** Returns whether this task has a prompt inside. */
	isPrompt() {
		return this.state === ListrTaskState.PROMPT;
	}
	/** Returns whether this task is currently paused. */
	isPaused() {
		return this.state === ListrTaskState.PAUSED;
	}
	/** Returns whether this task is closed. */
	isClosed() {
		return this.closed;
	}
	/** Pause the given task for certain time. */
	async pause(time) {
		const state = this.state;
		this.state$ = ListrTaskState.PAUSED;
		this.message$ = { paused: Date.now() + time };
		await delay(time);
		this.state$ = state;
		this.message$ = { paused: null };
	}
	/** Run the current task. */
	async run(context, wrapper) {
		const handleResult = (result) => {
			if (result instanceof Listr) {
				result.options = {
					...this.options,
					...result.options
				};
				result.rendererClass = getRendererClass("silent");
				this.subtasks = result.tasks;
				result.errors = this.listr.errors;
				this.emit(ListrTaskEventType.SUBTASK, this.subtasks);
				result = result.run(context);
			} else if (result instanceof Promise) result = result.then(handleResult);
			else if (isReadable(result)) result = new Promise((resolve, reject) => {
				result.on("data", (data) => {
					this.output$ = data.toString();
				});
				result.on("error", (error) => reject(error));
				result.on("end", () => resolve(null));
			});
			else if (isObservable(result)) result = new Promise((resolve, reject) => {
				result.subscribe({
					next: (data) => {
						this.output$ = data;
					},
					error: reject,
					complete: resolve
				});
			});
			return result;
		};
		const startTime = Date.now();
		this.state$ = ListrTaskState.STARTED;
		const skipped = await assertFunctionOrSelf(this.task?.skip ?? false, context);
		if (skipped) {
			if (typeof skipped === "string") this.message$ = { skip: skipped };
			else if (this.hasTitle()) this.message$ = { skip: this.title };
			else this.message$ = { skip: "Skipped task without a title." };
			this.state$ = ListrTaskState.SKIPPED;
			return;
		}
		try {
			const retryCount = typeof this.task?.retry === "number" && this.task.retry > 0 ? this.task.retry + 1 : typeof this.task?.retry === "object" && this.task.retry.tries > 0 ? this.task.retry.tries + 1 : 1;
			const retryDelay = typeof this.task.retry === "object" && this.task.retry.delay;
			for (let retries = 1; retries <= retryCount; retries++) try {
				await handleResult(this.taskFn(context, wrapper));
				break;
			} catch (err$1) {
				if (retries !== retryCount) {
					this.retry = {
						count: retries,
						error: err$1
					};
					this.message$ = { retry: this.retry };
					this.title$ = this.initialTitle;
					this.output = void 0;
					wrapper.report(err$1, ListrErrorTypes.WILL_RETRY);
					this.state$ = ListrTaskState.RETRY;
					if (retryDelay) await this.pause(retryDelay);
				} else throw err$1;
			}
			if (this.isStarted() || this.isRetrying()) {
				this.message$ = { duration: Date.now() - startTime };
				this.state$ = ListrTaskState.COMPLETED;
			}
		} catch (error) {
			if (this.prompt instanceof PromptError) error = this.prompt;
			if (this.task?.rollback) {
				wrapper.report(error, ListrErrorTypes.WILL_ROLLBACK);
				try {
					this.state$ = ListrTaskState.ROLLING_BACK;
					await this.task.rollback(context, wrapper);
					this.message$ = { rollback: this.title };
					this.state$ = ListrTaskState.ROLLED_BACK;
				} catch (err$1) {
					this.state$ = ListrTaskState.FAILED;
					wrapper.report(err$1, ListrErrorTypes.HAS_FAILED_TO_ROLLBACK);
					this.close();
					throw err$1;
				}
				if (this.listr.options?.exitAfterRollback !== false) {
					this.close();
					throw error;
				}
			} else {
				this.state$ = ListrTaskState.FAILED;
				if (this.listr.options.exitOnError !== false && await assertFunctionOrSelf(this.task?.exitOnError, context) !== false) {
					wrapper.report(error, ListrErrorTypes.HAS_FAILED);
					this.close();
					throw error;
				} else if (!this.hasSubtasks()) wrapper.report(error, ListrErrorTypes.HAS_FAILED_WITHOUT_ERROR);
			}
		} finally {
			this.close();
		}
	}
	close() {
		this.emit(ListrTaskEventType.CLOSED);
		this.listr.events.emit(ListrEventType.SHOULD_REFRESH_RENDER);
		this.complete();
	}
};
var ListrEventManager = class extends EventManager {};
/**
* Create a new task list with Listr.
*
* @see {@link https://listr2.kilic.dev/listr/listr.html}
*/
var Listr = class {
	constructor(task, options, parentTask) {
		this.tasks = [];
		this.errors = [];
		this.path = [];
		this.task = task;
		this.options = options;
		this.parentTask = parentTask;
		this.options = {
			concurrent: false,
			renderer: "default",
			fallbackRenderer: "simple",
			exitOnError: true,
			exitAfterRollback: true,
			collectErrors: false,
			registerSignalListeners: true,
			...this.parentTask?.options ?? {},
			...options
		};
		if (this.options.concurrent === true) this.options.concurrent = Infinity;
		else if (typeof this.options.concurrent !== "number") this.options.concurrent = 1;
		this.concurrency = new Concurrency({ concurrency: this.options.concurrent });
		if (parentTask) {
			this.path = [...parentTask.listr.path, parentTask.title];
			this.errors = parentTask.listr.errors;
		}
		if (this.parentTask?.listr.events instanceof ListrEventManager) this.events = this.parentTask.listr.events;
		else this.events = new ListrEventManager();
		/* istanbul ignore if */
		if (this.options?.forceTTY || process.env[ListrEnvironmentVariables.FORCE_TTY]) {
			process.stdout.isTTY = true;
			process.stderr.isTTY = true;
		}
		/* istanbul ignore if */
		if (this.options?.forceUnicode) process.env[ListrEnvironmentVariables.FORCE_UNICODE] = "1";
		const renderer = getRenderer({
			renderer: this.options.renderer,
			rendererOptions: this.options.rendererOptions,
			fallbackRenderer: this.options.fallbackRenderer,
			fallbackRendererOptions: this.options.fallbackRendererOptions,
			fallbackRendererCondition: this.options?.fallbackRendererCondition,
			silentRendererCondition: this.options?.silentRendererCondition
		});
		this.rendererClass = renderer.renderer;
		this.rendererClassOptions = renderer.options;
		this.rendererSelection = renderer.selection;
		/* istanbul ignore next */
		this.add(task ?? []);
		/* istanbul ignore if */
		if (this.options.registerSignalListeners) {
			this.boundSignalHandler = this.signalHandler.bind(this);
			process.once("SIGINT", this.boundSignalHandler).setMaxListeners(0);
		}
	}
	/**
	* Whether this is the root task.
	*/
	isRoot() {
		return !this.parentTask;
	}
	/**
	* Whether this is a subtask of another task list.
	*/
	isSubtask() {
		return !!this.parentTask;
	}
	/**
	* Add tasks to current task list.
	*
	* @see {@link https://listr2.kilic.dev/task/task.html}
	*/
	add(tasks) {
		this.tasks.push(...this.generate(tasks));
	}
	/**
	* Run the task list.
	*
	* @see {@link https://listr2.kilic.dev/listr/listr.html#run-the-generated-task-list}
	*/
	async run(context) {
		if (!this.renderer) this.renderer = new this.rendererClass(this.tasks, this.rendererClassOptions, this.events);
		await this.renderer.render();
		this.ctx = this.options?.ctx ?? context ?? {};
		await Promise.all(this.tasks.map((task) => task.check(this.ctx)));
		try {
			await Promise.all(this.tasks.map((task) => this.concurrency.add(() => this.runTask(task))));
			this.renderer.end();
			this.removeSignalHandler();
		} catch (err$1) {
			if (this.options.exitOnError !== false) {
				this.renderer.end(err$1);
				this.removeSignalHandler();
				throw err$1;
			}
		}
		return this.ctx;
	}
	generate(tasks) {
		tasks = Array.isArray(tasks) ? tasks : [tasks];
		return tasks.map((task) => {
			let rendererTaskOptions;
			if (this.rendererSelection === ListrRendererSelection.PRIMARY) rendererTaskOptions = task.rendererOptions;
			else if (this.rendererSelection === ListrRendererSelection.SECONDARY) rendererTaskOptions = task.fallbackRendererOptions;
			return new Task(this, task, this.options, this.rendererClassOptions, rendererTaskOptions);
		});
	}
	async runTask(task) {
		if (!await task.check(this.ctx)) return;
		return new TaskWrapper(task).run(this.ctx);
	}
	signalHandler() {
		this.tasks?.forEach(async (task) => {
			if (task.isPending()) task.state$ = ListrTaskState.FAILED;
		});
		if (this.isRoot()) {
			this.renderer?.end(/* @__PURE__ */ new Error("Interrupted."));
			process.exit(127);
		}
	}
	removeSignalHandler() {
		if (this.boundSignalHandler) process.removeListener("SIGINT", this.boundSignalHandler);
	}
};

//#endregion
//#region src/commands/remove.ts
var import_cjs$9 = /* @__PURE__ */ __toESM(require_cjs());
const remove = import_cjs$9.command({
	name: "remove",
	aliases: ["rm"],
	description: "Permanently remove one or more sandboxes",
	args: {
		sandboxName: import_cjs$9.positional({
			type: sandboxName,
			description: "a sandbox name to remove"
		}),
		sandboxNames: import_cjs$9.restPositionals({
			type: sandboxName,
			description: "more sandboxes to remove"
		}),
		scope
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, sandboxName: sandboxName$1, sandboxNames }) {
		const tasks = Array.from(new Set([sandboxName$1, ...sandboxNames]), (name) => ({
			title: `Removing sandbox ${name}`,
			async task() {
				await (await sandboxClient.get({
					token: token$1,
					teamId: team$1,
					projectId: project$1,
					name
				})).delete();
			}
		}));
		try {
			await new Listr(tasks, { concurrent: true }).run();
		} catch {
			process.exitCode = 1;
		}
	}
});

//#endregion
//#region src/commands/cp.ts
var import_cjs$8 = /* @__PURE__ */ __toESM(require_cjs());
init_source();
const parseLocalOrRemotePath = async (input) => {
	const parts = input.split(":");
	if (parts.length === 2) {
		const [id, path] = parts;
		if (!id || !path) throw new Error([
			`Invalid copy path format: "${input}".`,
			`${source_default.bold("hint:")} Expected format: SANDBOX_NAME:PATH (e.g., my-sandbox:/home/user/file.txt).`,
			"╰▶ Local paths should not contain colons."
		].join("\n"));
		return {
			type: "remote",
			sandboxName: await sandboxName.from(id),
			path
		};
	}
	return {
		type: "local",
		path: input
	};
};
const localOrRemote = import_cjs$8.extendType(import_cjs$8.string, { from: parseLocalOrRemotePath });
const cp = import_cjs$8.command({
	name: "copy",
	description: "Copy files between your local filesystem and a remote sandbox",
	aliases: ["cp"],
	args: {
		source: import_cjs$8.positional({
			displayName: `src`,
			description: `The source file to copy from local file system, or a sandbox_name:path from a remote sandbox`,
			type: localOrRemote
		}),
		dest: import_cjs$8.positional({
			displayName: `dst`,
			description: `The destination file to copy to local file system, or a sandbox_name:path to a remote sandbox`,
			type: localOrRemote
		}),
		scope
	},
	async handler({ scope: scope$1, source, dest }) {
		const spinner = ora({ text: `Reading source file (${source.path})...` }).start();
		let sourceFile = null;
		if (source.type === "local") sourceFile = await fs.readFile(source.path).catch((err$1) => {
			if (err$1.code === "ENOENT") return null;
			throw err$1;
		});
		else {
			const file = await (await sandboxClient.get({
				name: source.sandboxName,
				teamId: scope$1.team,
				token: scope$1.token,
				projectId: scope$1.project
			})).readFile({ path: source.path });
			if (file) sourceFile = await consume.buffer(file);
		}
		if (!sourceFile) {
			if (source.type === "remote") {
				const dir = Path.dirname(source.path);
				spinner.fail([`File not found: ${source.path} in sandbox ${source.sandboxName}.`, `${source_default.bold("hint:")} Verify the file path exists using \`sandbox exec ${source.sandboxName} ls ${dir}\`.`].join("\n"));
			} else spinner.fail(`Source file (${source.path}) not found.`);
			return;
		}
		spinner.text = `Writing to destination file (${dest.path})...`;
		if (dest.type === "local") await fs.writeFile(dest.path, sourceFile);
		else await (await sandboxClient.get({
			name: dest.sandboxName,
			teamId: scope$1.team,
			projectId: scope$1.project,
			token: scope$1.token
		})).writeFiles([{
			path: dest.path,
			content: sourceFile
		}]);
		spinner.succeed(`Copied ${source.path} to ${dest.path} successfully!`);
	}
});

//#endregion
//#region src/commands/logout.ts
var import_cjs$7 = /* @__PURE__ */ __toESM(require_cjs());
init_source();
const debug = createDebugger("sandbox:logout");
const logout = import_cjs$7.command({
	name: "logout",
	description: "Log out of the Sandbox CLI",
	args: {},
	async handler() {
		const auth = getAuth();
		if (!auth?.token) {
			output.print([`No active session found. You are not currently logged in.`, `${source_default.bold("hint:")} To log in, run: \`sandbox login\``].join("\n"));
			return;
		}
		await (await OAuth()).revokeToken(auth.token);
		updateAuthConfig({});
		debug("Configuration has been deleted");
		output.print("Logged out!");
	}
});

//#endregion
//#region src/commands/snapshot.ts
var import_cjs$6 = /* @__PURE__ */ __toESM(require_cjs());
init_source();
var import_ms$1 = /* @__PURE__ */ __toESM(require_ms());
const args = {
	stop: import_cjs$6.flag({
		long: "stop",
		description: "Confirm that the sandbox will be stopped when snapshotting"
	}),
	silent: import_cjs$6.flag({
		long: "silent",
		description: "Don't write snapshot ID to stdout"
	}),
	expiration: import_cjs$6.option({
		long: "expiration",
		type: import_cjs$6.optional(Duration),
		description: "The expiration time of the snapshot. Use 0 for no expiration."
	}),
	sandbox: import_cjs$6.positional({ type: sandboxName }),
	scope
};
const snapshot = import_cjs$6.command({
	name: "snapshot",
	description: "Take a snapshot of the filesystem of a sandbox",
	args,
	async handler({ sandbox: sandboxName$1, stop: stop$1, scope: { token: token$1, team: team$1, project: project$1 }, silent, expiration }) {
		if (!stop$1) {
			console.error(["Snapshotting will stop the current session of this sandbox.", `${source_default.bold("hint:")} Confirm with --stop to continue.`].join("\n"));
			process.exitCode = 1;
			return;
		}
		const sandbox = typeof sandboxName$1 !== "string" ? sandboxName$1 : await sandboxClient.get({
			name: sandboxName$1,
			projectId: project$1,
			teamId: team$1,
			token: token$1
		});
		if (!["running"].includes(sandbox.status)) {
			console.error([
				`Sandbox ${sandbox.name} is not available (status: ${sandbox.status}).`,
				`${source_default.bold("hint:")} Only 'running' sandboxes can be snapshotted.`,
				"├▶ Use `sandbox list` to check sandbox status.",
				"╰▶ Use `sandbox create` to create a new sandbox."
			].join("\n"));
			process.exitCode = 1;
			return;
		}
		const spinner = silent ? void 0 : ora("Creating snapshot...").start();
		const snapshot$1 = await sandbox.snapshot({ expiration: expiration === void 0 ? void 0 : (0, import_ms$1.default)(expiration) });
		spinner?.succeed(`Snapshot ${snapshot$1.snapshotId} created.`);
	}
});

//#endregion
//#region src/util/snapshot-tree.ts
init_source();
function formatExpires(expiresAt) {
	if (expiresAt === void 0) return source_default.gray("never");
	const ms$7 = expiresAt - Date.now();
	if (ms$7 <= 0) return source_default.red("expired");
	const formatted = timeAgo(expiresAt);
	return ms$7 <= 3600 * 1e3 ? source_default.red(formatted) : source_default.green(formatted);
}
function renderNode(id, expiresAt, isCurrent) {
	const bullet = isCurrent ? source_default.magenta.bold("●") : source_default.magenta("●");
	const suffix = isCurrent ? `  ${source_default.green("◂ current")}` : "";
	return `${bullet} ${source_default.yellow(id)}   expires: ${formatExpires(expiresAt)}${suffix}`;
}
function renderSiblings(siblings, count) {
	const lines = [];
	const shown = siblings.slice(0, 5);
	const totalCount = parseInt(count, 10);
	const hasPlus = count.endsWith("+");
	const remaining = totalCount - 1 - shown.length;
	for (let i = 0; i < shown.length; i++) {
		const connector = i === shown.length - 1 && remaining <= 0 ? "╰──" : "├──";
		lines.push(`│   ${connector} ${source_default.gray(shown[i].id)}`);
	}
	if (remaining > 0) {
		const suffix = hasPlus ? "+" : "";
		lines.push(`│   ╰── ${source_default.gray(`+${remaining}${suffix} more snapshots`)}`);
	}
	return lines;
}
function renderSnapshotTree(params) {
	const { ancestors, descendants } = params;
	const hideCurrent = params.hideCurrent === true;
	const currentSnapshotId = hideCurrent ? void 0 : params.currentSnapshotId;
	const lines = [];
	const pushNode = (node, isCurrent) => {
		lines.push("│");
		lines.push(renderNode(node.snapshot.id, node.snapshot.expiresAt, isCurrent));
		if (node.siblings.length > 0) lines.push(...renderSiblings(node.siblings, node.count));
	};
	const descendantNodes = descendants.snapshots.filter((n) => n.snapshot.id !== currentSnapshotId);
	if (descendantNodes.length > 0) for (const node of [...descendantNodes].reverse()) pushNode(node, false);
	if (!hideCurrent) {
		const { currentSnapshotId: id, currentSnapshotExpiresAt } = params;
		pushNode(params.current ?? ancestors.snapshots.find((n) => n.snapshot.id === id) ?? descendants.snapshots.find((n) => n.snapshot.id === id) ?? {
			snapshot: {
				id,
				sourceSessionId: "",
				expiresAt: currentSnapshotExpiresAt
			},
			siblings: [],
			count: "1"
		}, true);
	}
	const ancestorNodes = ancestors.snapshots.filter((n) => n.snapshot.id !== currentSnapshotId);
	for (const node of ancestorNodes) pushNode(node, false);
	if (!hideCurrent) {
		const lastAncestor = ancestorNodes[ancestorNodes.length - 1];
		if (ancestors.pagination.next === null) {
			if (lastAncestor && !lastAncestor.snapshot.parentId || ancestorNodes.length === 0) {
				lines.push("│");
				lines.push(`${source_default.white("○")} ${source_default.gray("(root)")}`);
			}
		}
	}
	return lines.join("\n");
}

//#endregion
//#region src/commands/snapshots.ts
var import_cjs$4 = /* @__PURE__ */ __toESM(require_cjs());
var import_cjs$5 = require_cjs();
init_source();
const list$2 = import_cjs$4.command({
	name: "list",
	aliases: ["ls"],
	description: "List snapshots for the specified account and project.",
	args: {
		scope,
		name: import_cjs$4.option({
			type: import_cjs$4.optional(sandboxName),
			long: "name",
			description: "Filter snapshots by sandbox."
		}),
		sortOrder: import_cjs$4.option({
			long: "sort-order",
			description: "Sort order. Options: asc, desc (default)",
			type: import_cjs$4.optional(import_cjs$4.oneOf(["asc", "desc"]))
		}),
		limit: import_cjs$4.option({
			long: "limit",
			description: "Maximum number of snapshots per page (default 50).",
			type: import_cjs$4.optional(import_cjs$4.number)
		}),
		cursor: import_cjs$4.option({
			long: "cursor",
			description: "Pagination cursor from a previous 'More results' hint.",
			type: import_cjs$4.optional(import_cjs$4.string)
		})
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, name, sortOrder, limit, cursor }) {
		const { snapshots: snapshots$1, pagination } = await (async () => {
			try {
				var _usingCtx$1 = _usingCtx();
				const _spinner$1 = _usingCtx$1.u(acquireRelease(() => ora("Fetching snapshots...").start(), (s$1) => s$1.stop()));
				return snapshotClient.list({
					token: token$1,
					teamId: team$1,
					projectId: project$1,
					name,
					limit: limit ?? 50,
					...cursor && { cursor },
					...sortOrder && { sortOrder }
				});
			} catch (_) {
				_usingCtx$1.e = _;
			} finally {
				_usingCtx$1.d();
			}
		})();
		console.log(table({
			rows: snapshots$1,
			columns: {
				ID: { value: (s$1) => s$1.id },
				STATUS: {
					value: (s$1) => s$1.status,
					color: (s$1) => SnapshotStatusColor.get(s$1.status) ?? source_default.reset
				},
				CREATED: { value: (s$1) => timeAgo(s$1.createdAt) },
				EXPIRATION: { value: (s$1) => s$1.status === "deleted" ? source_default.gray.dim("deleted") : timeAgo(s$1.expiresAt) },
				SIZE: { value: (s$1) => formatBytes(s$1.sizeBytes) },
				["SOURCE SESSION"]: { value: (s$1) => s$1.sourceSessionId }
			}
		}));
		if (pagination.next !== null) console.log(formatNextCursorHint(pagination.next));
	}
});
const get = import_cjs$4.command({
	name: "get",
	description: "Get details of a snapshot.",
	args: {
		scope,
		snapshotId: import_cjs$4.positional({
			type: snapshotId,
			description: "Snapshot ID to retrieve"
		})
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, snapshotId: id }) {
		const snapshot$1 = await (async () => {
			try {
				var _usingCtx3 = _usingCtx();
				const _spinner$1 = _usingCtx3.u(acquireRelease(() => ora("Fetching snapshot...").start(), (s$1) => s$1.stop()));
				return snapshotClient.get({
					token: token$1,
					teamId: team$1,
					projectId: project$1,
					snapshotId: id
				});
			} catch (_) {
				_usingCtx3.e = _;
			} finally {
				_usingCtx3.d();
			}
		})();
		console.log(table({
			rows: [snapshot$1],
			columns: {
				ID: { value: (s$1) => s$1.snapshotId },
				STATUS: {
					value: (s$1) => s$1.status,
					color: (s$1) => SnapshotStatusColor.get(s$1.status) ?? source_default.reset
				},
				CREATED: { value: (s$1) => timeAgo(s$1.createdAt) },
				EXPIRATION: { value: (s$1) => s$1.status === "deleted" ? source_default.gray.dim("deleted") : timeAgo(s$1.expiresAt) },
				SIZE: { value: (s$1) => formatBytes(s$1.sizeBytes) },
				["SOURCE SESSION"]: { value: (s$1) => s$1.sourceSessionId }
			}
		}));
	}
});
const remove$1 = import_cjs$4.command({
	name: "delete",
	aliases: ["rm", "remove"],
	description: "Delete one or more snapshots.",
	args: {
		snapshotId: import_cjs$4.positional({
			type: snapshotId,
			description: "Snapshot ID to delete"
		}),
		snapshotIds: import_cjs$4.restPositionals({
			type: snapshotId,
			description: "More snapshots IDs to delete"
		}),
		scope
	},
	async handler({ scope: { team: team$1, token: token$1, project: project$1 }, snapshotId: snapshotId$1, snapshotIds }) {
		const tasks = Array.from(new Set([snapshotId$1, ...snapshotIds]), (snapshotId$2) => {
			return {
				title: `Deleting snapshot ${snapshotId$2}`,
				async task() {
					const snapshot$1 = await snapshotClient.get({
						token: token$1,
						teamId: team$1,
						projectId: project$1,
						snapshotId: snapshotId$2
					});
					if (snapshot$1.status !== "created") throw new Error(`Snapshot ${snapshotId$2} is in status "${snapshot$1.status}" and cannot be deleted.`);
					await snapshot$1.delete();
				}
			};
		});
		try {
			await new Listr(tasks, { concurrent: true }).run();
		} catch {
			process.exitCode = 1;
		}
	}
});
const tree = import_cjs$4.command({
	name: "tree",
	description: "Show the snapshot ancestry tree for a sandbox.",
	args: {
		scope,
		sandboxName: import_cjs$4.positional({
			type: sandboxName,
			description: "Sandbox name"
		}),
		sortOrder: import_cjs$4.option({
			long: "sort-order",
			description: "Sort order. Options: asc, desc (default). 'desc' walks ancestors, 'asc' walks descendants. Requires --cursor.",
			type: import_cjs$4.optional(import_cjs$4.oneOf(["asc", "desc"]))
		}),
		limit: import_cjs$4.option({
			long: "limit",
			description: "Maximum number of snapshots per page (1–10, default 10).",
			type: import_cjs$4.optional(import_cjs$4.number)
		}),
		cursor: import_cjs$4.option({
			long: "cursor",
			description: "Pagination cursor from a previous 'More ancestors' or 'More descendants' hint.",
			type: import_cjs$4.optional(import_cjs$4.string)
		})
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, sandboxName: name, sortOrder, limit, cursor }) {
		if (limit !== void 0 && (limit < 1 || limit > 10)) {
			console.error(source_default.red("Error: --limit must be between 1 and 10."));
			process.exitCode = 1;
			return;
		}
		if (sortOrder !== void 0 && !cursor) {
			console.error(source_default.red("Error: --sort-order requires --cursor."));
			process.exitCode = 1;
			return;
		}
		const pageLimit = limit ?? 10;
		if (cursor) {
			const effectiveSortOrder = sortOrder ?? "desc";
			const page = await (async () => {
				try {
					var _usingCtx4 = _usingCtx();
					const _spinner$1 = _usingCtx4.u(acquireRelease(() => ora("Fetching snapshot tree...").start(), (s$1) => s$1.stop()));
					return snapshotClient.tree({
						snapshotId: cursor,
						sortOrder: effectiveSortOrder,
						limit: pageLimit,
						token: token$1,
						teamId: team$1,
						projectId: project$1
					});
				} catch (_) {
					_usingCtx4.e = _;
				} finally {
					_usingCtx4.d();
				}
			})();
			const ancestors = effectiveSortOrder === "desc" ? page : {
				snapshots: [],
				pagination: {
					count: 0,
					next: null
				}
			};
			const descendants = effectiveSortOrder === "asc" ? page : {
				snapshots: [],
				pagination: {
					count: 0,
					next: null
				}
			};
			console.log(renderSnapshotTree({
				hideCurrent: true,
				ancestors,
				descendants
			}));
			if (page.pagination.next !== null) console.log(formatNextCursorHint(page.pagination.next));
			return;
		}
		const result = await (async () => {
			try {
				var _usingCtx5 = _usingCtx();
				const _spinner$1 = _usingCtx5.u(acquireRelease(() => ora("Fetching snapshot tree...").start(), (s$1) => s$1.stop()));
				const currentSnapshotId = (await sandboxClient.get({
					name,
					token: token$1,
					teamId: team$1,
					projectId: project$1
				})).currentSnapshotId;
				if (!currentSnapshotId) return null;
				const [currentSnap, ancestors, descendants] = await Promise.all([
					snapshotClient.get({
						snapshotId: currentSnapshotId,
						token: token$1,
						teamId: team$1,
						projectId: project$1
					}),
					snapshotClient.tree({
						snapshotId: currentSnapshotId,
						sortOrder: "desc",
						limit: pageLimit,
						token: token$1,
						teamId: team$1,
						projectId: project$1
					}),
					snapshotClient.tree({
						snapshotId: currentSnapshotId,
						sortOrder: "asc",
						limit: pageLimit,
						token: token$1,
						teamId: team$1,
						projectId: project$1
					})
				]);
				return {
					currentSnap,
					currentSnapshotId,
					ancestors,
					descendants,
					current: ancestors.anchor ?? descendants.anchor
				};
			} catch (_) {
				_usingCtx5.e = _;
			} finally {
				_usingCtx5.d();
			}
		})();
		if (!result) {
			console.log(source_default.yellow("No snapshots found for this sandbox."));
			return;
		}
		console.log(renderSnapshotTree({
			currentSnapshotId: result.currentSnapshotId,
			currentSnapshotExpiresAt: result.currentSnap.expiresAt?.getTime(),
			ancestors: result.ancestors,
			descendants: result.descendants,
			current: result.current
		}));
		const limitArg = limit !== void 0 ? ` --limit ${limit}` : "";
		if (result.ancestors.pagination.next !== null) console.log(`\nMore ancestors: sandbox snapshots tree ${name} --sort-order desc${limitArg} --cursor ${result.ancestors.pagination.next}`);
		if (result.descendants.pagination.next !== null) console.log(`More descendants: sandbox snapshots tree ${name} --sort-order asc${limitArg} --cursor ${result.descendants.pagination.next}`);
	}
});
const snapshots = (0, import_cjs$5.subcommands)({
	name: "snapshots",
	description: "Manage sandbox snapshots",
	cmds: {
		list: list$2,
		get,
		tree,
		delete: remove$1
	}
});
const SnapshotStatusColor = new Map([
	["created", source_default.cyan],
	["deleted", source_default.gray.dim],
	["failed", source_default.red]
]);

//#endregion
//#region src/commands/sessions.ts
var import_cjs$2 = /* @__PURE__ */ __toESM(require_cjs());
var import_cjs$3 = require_cjs();
init_source();
const list$1 = import_cjs$2.command({
	name: "list",
	aliases: ["ls"],
	description: "List sessions from a sandbox",
	args: {
		all: import_cjs$2.flag({
			long: "all",
			short: "a",
			description: "Show all sessions (default shows just running)"
		}),
		sandbox: import_cjs$2.positional({
			type: sandboxName,
			description: "Sandbox name to list sessions for"
		}),
		sortOrder: import_cjs$2.option({
			long: "sort-order",
			description: "Sort order. Options: asc, desc (default)",
			type: import_cjs$2.optional(import_cjs$2.oneOf(["asc", "desc"]))
		}),
		limit: import_cjs$2.option({
			long: "limit",
			description: "Maximum number of sessions per page (default 50).",
			type: import_cjs$2.optional(import_cjs$2.number)
		}),
		cursor: import_cjs$2.option({
			long: "cursor",
			description: "Pagination cursor from a previous 'More results' hint.",
			type: import_cjs$2.optional(import_cjs$2.string)
		}),
		scope
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, all, sandbox: name, sortOrder, limit, cursor }) {
		const sandbox = await sandboxClient.get({
			name,
			projectId: project$1,
			teamId: team$1,
			token: token$1
		});
		const { sessions: sessions$1, pagination } = await (async () => {
			try {
				var _usingCtx$1 = _usingCtx();
				const _spinner$1 = _usingCtx$1.u(acquireRelease(() => ora("Fetching sessions...").start(), (s$1) => s$1.stop()));
				return sandbox.listSessions({
					limit: limit ?? 50,
					...cursor && { cursor },
					...sortOrder && { sortOrder }
				});
			} catch (_) {
				_usingCtx$1.e = _;
			} finally {
				_usingCtx$1.d();
			}
		})();
		const displayedSessions = all ? sessions$1 : sessions$1.filter((x) => x.status === "running");
		const memoryFormatter = new Intl.NumberFormat(void 0, {
			style: "unit",
			unit: "megabyte"
		});
		const columns = {
			ID: { value: (s$1) => s$1.id },
			STATUS: {
				value: (s$1) => s$1.status,
				color: (s$1) => SessionStatusColor[s$1.status] ?? source_default.reset
			},
			CREATED: { value: (s$1) => timeAgo(s$1.createdAt) },
			MEMORY: { value: (s$1) => memoryFormatter.format(s$1.memory) },
			VCPUS: { value: (s$1) => s$1.vcpus },
			RUNTIME: { value: (s$1) => s$1.runtime },
			TIMEOUT: { value: (s$1) => timeAgo(s$1.createdAt + s$1.timeout) },
			DURATION: { value: (s$1) => s$1.duration ? formatRunDuration(s$1.duration) : "-" },
			SNAPSHOT: { value: (s$1) => s$1.sourceSnapshotId ?? "-" }
		};
		if (all) {
			columns.CPU = { value: (s$1) => s$1.activeCpuDurationMs ? formatRunDuration(s$1.activeCpuDurationMs) : "-" };
			columns["NETWORK (OUT/IN)"] = { value: (s$1) => s$1.networkTransfer?.egress || s$1.networkTransfer?.ingress ? `${formatBytes(s$1.networkTransfer?.egress ?? 0)} / ${formatBytes(s$1.networkTransfer?.ingress ?? 0)}` : "- / -" };
		}
		console.log(table({
			rows: displayedSessions,
			columns
		}));
		if (pagination.next !== null) console.log(formatNextCursorHint(pagination.next));
	}
});
const sessions = (0, import_cjs$3.subcommands)({
	name: "sessions",
	description: "Manage sandbox sessions",
	cmds: { list: list$1 }
});
const SessionStatusColor = {
	running: source_default.cyan,
	failed: source_default.red,
	stopped: source_default.gray.dim,
	stopping: source_default.gray,
	pending: source_default.magenta,
	snapshotting: source_default.blue,
	aborted: source_default.gray.dim
};

//#endregion
//#region src/commands/config.ts
var import_cjs$1 = /* @__PURE__ */ __toESM(require_cjs());
init_source();
var import_ms = /* @__PURE__ */ __toESM(require_ms());
const vcpusCommand = import_cjs$1.command({
	name: "vcpus",
	description: "Update the vCPU count of a sandbox",
	args: {
		sandbox: import_cjs$1.positional({
			type: sandboxName,
			description: "Sandbox name to update"
		}),
		count: import_cjs$1.positional({
			type: vcpusType,
			description: "Number of vCPUs to allocate (each vCPU includes 2048 MB of memory)"
		}),
		scope
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, sandbox: name, count }) {
		const sandbox = await sandboxClient.get({
			name,
			projectId: project$1,
			teamId: team$1,
			token: token$1
		});
		const spinner = ora("Updating sandbox configuration...").start();
		try {
			await sandbox.update({ resources: { vcpus: count } });
			spinner.stop();
			process.stderr.write("✅ Configuration updated for sandbox " + source_default.cyan(name) + "\n");
			process.stderr.write(source_default.dim("   ╰ ") + "vcpus: " + source_default.cyan(count) + "\n");
		} catch (error) {
			spinner.stop();
			throw error;
		}
	}
});
const timeoutCommand = import_cjs$1.command({
	name: "timeout",
	description: "Update the timeout of a sandbox (will be applied to all new sessions)",
	args: {
		sandbox: import_cjs$1.positional({
			type: sandboxName,
			description: "Sandbox name to update"
		}),
		duration: import_cjs$1.positional({
			type: Duration,
			description: "The maximum duration a sandbox can run for. Example: 5m, 1h"
		}),
		scope
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, sandbox: name, duration }) {
		const sandbox = await sandboxClient.get({
			name,
			projectId: project$1,
			teamId: team$1,
			token: token$1
		});
		const spinner = ora("Updating sandbox configuration...").start();
		try {
			await sandbox.update({ timeout: (0, import_ms.default)(duration) });
			spinner.stop();
			process.stderr.write("✅ Configuration updated for sandbox " + source_default.cyan(name) + "\n");
			process.stderr.write(source_default.dim("   ╰ ") + "timeout: " + source_default.cyan(duration) + "\n");
		} catch (error) {
			spinner.stop();
			throw error;
		}
	}
});
const persistentCommand = import_cjs$1.command({
	name: "persistent",
	description: "Enable or disable automatic restore of the filesystem between sessions",
	args: {
		sandbox: import_cjs$1.positional({
			type: sandboxName,
			description: "Sandbox name to update"
		}),
		value: import_cjs$1.positional({
			type: {
				...import_cjs$1.oneOf(["true", "false"]),
				displayName: "true|false"
			},
			description: "Enable or disable automatic restore of the filesystem between sessions"
		}),
		scope
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, sandbox: name, value }) {
		const sandbox = await sandboxClient.get({
			name,
			projectId: project$1,
			teamId: team$1,
			token: token$1
		});
		const spinner = ora("Updating sandbox configuration...").start();
		try {
			await sandbox.update({ persistent: value === "true" });
			spinner.stop();
			process.stderr.write("✅ Configuration updated for sandbox " + source_default.cyan(name) + "\n");
			process.stderr.write(source_default.dim("   ╰ ") + "persistent: " + source_default.cyan(value) + "\n");
		} catch (error) {
			spinner.stop();
			throw error;
		}
	}
});
const snapshotExpirationCommand = import_cjs$1.command({
	name: "snapshot-expiration",
	description: "Update the default snapshot expiration of a sandbox",
	args: {
		sandbox: import_cjs$1.positional({
			type: sandboxName,
			description: "Sandbox name to update"
		}),
		duration: import_cjs$1.positional({
			type: SnapshotExpiration,
			description: "Snapshot expiration duration (e.g. 7d, 30d) or \"none\" for no expiration"
		}),
		scope
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, sandbox: name, duration }) {
		const sandbox = await sandboxClient.get({
			name,
			projectId: project$1,
			teamId: team$1,
			token: token$1
		});
		const spinner = ora("Updating sandbox configuration...").start();
		try {
			await sandbox.update({ snapshotExpiration: (0, import_ms.default)(duration) });
			spinner.stop();
			const display = (0, import_ms.default)(duration) === 0 ? "none" : duration;
			process.stderr.write("✅ Configuration updated for sandbox " + source_default.cyan(name) + "\n");
			process.stderr.write(source_default.dim("   ╰ ") + "snapshot-expiration: " + source_default.cyan(display) + "\n");
		} catch (error) {
			spinner.stop();
			throw error;
		}
	}
});
const keepLastCountType = import_cjs$1.extendType(import_cjs$1.number, {
	displayName: "COUNT",
	async from(n) {
		if (!Number.isInteger(n) || n < 0 || n > 10) throw new Error(`Invalid count: ${n}. Must be an integer between 0 and 10 (0 removes the policy).`);
		return n;
	}
});
const keepLastSnapshotsCommand = import_cjs$1.command({
	name: "keep-last-snapshots",
	description: "Update the snapshot retention policy (keep only the N most recent snapshots) of a sandbox",
	args: {
		sandbox: import_cjs$1.positional({
			type: sandboxName,
			description: "Sandbox name to update"
		}),
		count: import_cjs$1.positional({
			type: keepLastCountType,
			description: "Number of most recent snapshots to keep (1-10). Pass 0 to remove the policy."
		}),
		scope
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, sandbox: name, count }) {
		const sandbox = await sandboxClient.get({
			name,
			projectId: project$1,
			teamId: team$1,
			token: token$1
		});
		const spinner = ora("Updating sandbox configuration...").start();
		try {
			if (count === 0) await sandbox.update({ keepLastSnapshots: null });
			else {
				const current = sandbox.keepLastSnapshots;
				await sandbox.update({ keepLastSnapshots: {
					count,
					expiration: current?.expiration,
					deleteEvicted: current?.deleteEvicted
				} });
			}
			spinner.stop();
			process.stderr.write("✅ Configuration updated for sandbox " + source_default.cyan(name) + "\n");
			process.stderr.write(source_default.dim("   ╰ ") + "keep-last-snapshots: " + source_default.cyan(count === 0 ? "cleared" : `count=${count}`) + "\n");
		} catch (error) {
			spinner.stop();
			throw error;
		}
	}
});
const keepLastSnapshotsForCommand = import_cjs$1.command({
	name: "keep-last-snapshots-for",
	description: "Update the expiration applied to snapshots kept by the retention policy",
	args: {
		sandbox: import_cjs$1.positional({
			type: sandboxName,
			description: "Sandbox name to update"
		}),
		duration: import_cjs$1.positional({
			type: SnapshotExpiration,
			description: "Expiration for kept snapshots. Use \"none\" or 0 for no expiration. Example: 7d, 30d"
		}),
		scope
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, sandbox: name, duration }) {
		const sandbox = await sandboxClient.get({
			name,
			projectId: project$1,
			teamId: team$1,
			token: token$1
		});
		const current = sandbox.keepLastSnapshots;
		if (!current) throw new Error(["No keep-last-snapshots policy is set on this sandbox.", `${source_default.bold("hint:")} Set a count first with: sandbox config keep-last-snapshots ${name} <count>`].join("\n"));
		const spinner = ora("Updating sandbox configuration...").start();
		try {
			await sandbox.update({ keepLastSnapshots: {
				count: current.count,
				expiration: (0, import_ms.default)(duration),
				deleteEvicted: current.deleteEvicted
			} });
			spinner.stop();
			const display = (0, import_ms.default)(duration) === 0 ? "none" : duration;
			process.stderr.write("✅ Configuration updated for sandbox " + source_default.cyan(name) + "\n");
			process.stderr.write(source_default.dim("   ╰ ") + "keep-last-snapshots-for: " + source_default.cyan(display) + "\n");
		} catch (error) {
			spinner.stop();
			throw error;
		}
	}
});
const deleteEvictedSnapshotsCommand = import_cjs$1.command({
	name: "delete-evicted-snapshots",
	description: "When \"true\" (the default), snapshots evicted by the keep-last-snapshots policy are deleted immediately; when \"false\", they keep the default expiration.",
	args: {
		sandbox: import_cjs$1.positional({
			type: sandboxName,
			description: "Sandbox name to update"
		}),
		value: import_cjs$1.positional({
			type: {
				...import_cjs$1.oneOf(["true", "false"]),
				displayName: "true|false"
			},
			description: "Whether to delete evicted snapshots immediately (\"true\") or let them keep the default expiration (\"false\")."
		}),
		scope
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, sandbox: name, value }) {
		const sandbox = await sandboxClient.get({
			name,
			projectId: project$1,
			teamId: team$1,
			token: token$1
		});
		const current = sandbox.keepLastSnapshots;
		if (!current) throw new Error(["No keep-last-snapshots policy is set on this sandbox.", `${source_default.bold("hint:")} Set a count first with: sandbox config keep-last-snapshots ${name} <count>`].join("\n"));
		const spinner = ora("Updating sandbox configuration...").start();
		try {
			await sandbox.update({ keepLastSnapshots: {
				count: current.count,
				expiration: current.expiration,
				deleteEvicted: value === "true"
			} });
			spinner.stop();
			process.stderr.write("✅ Configuration updated for sandbox " + source_default.cyan(name) + "\n");
			process.stderr.write(source_default.dim("   ╰ ") + "delete-evicted-snapshots: " + source_default.cyan(value) + "\n");
		} catch (error) {
			spinner.stop();
			throw error;
		}
	}
});
const currentSnapshotCommand = import_cjs$1.command({
	name: "current-snapshot",
	description: "Update the current snapshot of a sandbox",
	args: {
		sandbox: import_cjs$1.positional({
			type: sandboxName,
			description: "Sandbox name to update"
		}),
		snapshotId: import_cjs$1.positional({
			type: snapshotId,
			description: "Snapshot ID to set as the current snapshot"
		}),
		scope
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, sandbox: name, snapshotId: snapshotId$1 }) {
		const sandbox = await sandboxClient.get({
			name,
			projectId: project$1,
			teamId: team$1,
			token: token$1
		});
		const spinner = ora("Updating sandbox configuration...").start();
		try {
			await sandbox.update({ currentSnapshotId: snapshotId$1 });
			spinner.stop();
			process.stderr.write("✅ Configuration updated for sandbox " + source_default.cyan(name) + "\n");
			process.stderr.write(source_default.dim("   ╰ ") + "current-snapshot: " + source_default.cyan(snapshotId$1) + "\n");
		} catch (error) {
			spinner.stop();
			if (error instanceof APIError && error.response.status === 404) throw new StyledError(`Snapshot '${snapshotId$1}' was not found or does not belong to this project.`, error);
			throw error;
		}
	}
});
const portsCommand = import_cjs$1.command({
	name: "ports",
	description: "Update the published ports of a sandbox. Replaces all existing published ports.",
	args: {
		sandbox: import_cjs$1.positional({
			type: sandboxName,
			description: "Sandbox name to update"
		}),
		ports: publishPorts,
		scope
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, sandbox: name, ports }) {
		const sandbox = await sandboxClient.get({
			name,
			projectId: project$1,
			teamId: team$1,
			token: token$1
		});
		const spinner = ora("Updating sandbox ports...").start();
		try {
			await sandbox.update({ ports });
			spinner.stop();
			process.stderr.write("✅ Ports updated for sandbox " + source_default.cyan(name) + "\n");
			if (ports.length === 0) process.stderr.write(source_default.dim("   ╰ ") + "all ports unpublished\n");
			else {
				const routes = getPublishedRoutes(sandbox);
				process.stderr.write(source_default.dim("   │ ") + "ports:\n");
				for (let i = 0; i < ports.length; i++) {
					const port = ports[i];
					const route = routes?.find((route$1) => route$1.port === port);
					const prefix$1 = i === ports.length - 1 ? source_default.dim("   ╰ ") : source_default.dim("   │ ");
					const url = route ? " -> " + source_default.cyan(route.url) : "";
					process.stderr.write(prefix$1 + "• " + port + url + "\n");
				}
			}
		} catch (error) {
			spinner.stop();
			throw error;
		}
	}
});
const listCommand = import_cjs$1.command({
	name: "list",
	description: "Display the current configuration of a sandbox",
	args: {
		sandbox: import_cjs$1.positional({
			type: sandboxName,
			description: "Sandbox name to inspect"
		}),
		scope
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, sandbox: name }) {
		const sandbox = await (async () => {
			try {
				var _usingCtx$1 = _usingCtx();
				const _spinner$1 = _usingCtx$1.u(acquireRelease(() => ora("Fetching sandbox configuration...").start(), (s$1) => s$1.stop()));
				return sandboxClient.get({
					name,
					projectId: project$1,
					teamId: team$1,
					token: token$1
				});
			} catch (_) {
				_usingCtx$1.e = _;
			} finally {
				_usingCtx$1.d();
			}
		})();
		const networkPolicy$1 = typeof sandbox.networkPolicy === "string" ? sandbox.networkPolicy : "restricted";
		const tagsDisplay = sandbox.tags && Object.keys(sandbox.tags).length > 0 ? Object.entries(sandbox.tags).map(([k, v]) => `${k}=${v}`).join(", ") : "-";
		const rows = [
			{
				field: "vCPUs",
				value: String(sandbox.vcpus ?? "-")
			},
			{
				field: "Timeout",
				value: sandbox.timeout != null ? (0, import_ms.default)(sandbox.timeout, { long: true }) : "-"
			},
			{
				field: "Persistent",
				value: String(sandbox.persistent)
			},
			{
				field: "Network policy",
				value: String(networkPolicy$1)
			},
			{
				field: "Ports",
				value: formatPorts(sandbox)
			},
			{
				field: "Snapshot expiration",
				value: sandbox.snapshotExpiration != null && sandbox.snapshotExpiration > 0 ? (0, import_ms.default)(sandbox.snapshotExpiration, { long: true }) : sandbox.snapshotExpiration === 0 ? "none" : "-"
			},
			{
				field: "Keep last snapshots",
				value: formatKeepLastSnapshots(sandbox.keepLastSnapshots)
			},
			{
				field: "Current snapshot",
				value: sandbox.currentSnapshotId ?? "-"
			},
			{
				field: "Tags",
				value: tagsDisplay
			}
		];
		console.log(table({
			rows,
			columns: {
				FIELD: {
					value: (r) => r.field,
					color: () => source_default.bold
				},
				VALUE: { value: (r) => r.value }
			}
		}));
	}
});
const networkPolicyCommand = import_cjs$1.command({
	name: "network-policy",
	description: `Update the network policy of a sandbox`,
	args: {
		sandbox: import_cjs$1.positional({ type: sandboxName }),
		...networkPolicyArgs,
		mode: import_cjs$1.option({
			long: "mode",
			description: `Alias for --network-policy.`,
			type: import_cjs$1.optional(networkPolicyMode)
		}),
		scope
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, sandbox: sandboxName$1, networkPolicy: networkPolicyFlag, mode: modeFlag, allowedDomains: allowedDomains$1, allowedCIDRs: allowedCIDRs$1, deniedCIDRs: deniedCIDRs$1 }) {
		const networkPolicyMode$1 = resolveMode(networkPolicyFlag, modeFlag);
		if (networkPolicyMode$1 === void 0 && allowedDomains$1.length === 0 && allowedCIDRs$1.length === 0 && deniedCIDRs$1.length === 0) throw new Error(`Network policy mode or custom rules must be set.`);
		const networkPolicy$1 = buildNetworkPolicy({
			networkPolicy: networkPolicyMode$1,
			allowedDomains: allowedDomains$1,
			allowedCIDRs: allowedCIDRs$1,
			deniedCIDRs: deniedCIDRs$1
		});
		const sandbox = typeof sandboxName$1 !== "string" ? sandboxName$1 : await sandboxClient.get({
			name: sandboxName$1,
			projectId: project$1,
			teamId: team$1,
			token: token$1
		});
		const spinner = ora("Updating network policy...").start();
		try {
			await sandbox.update({ networkPolicy: networkPolicy$1 });
			spinner.stop();
			process.stderr.write("✅ Network policy updated for sandbox " + source_default.cyan(sandbox.name) + "\n");
			const mode = typeof networkPolicy$1 === "string" ? networkPolicy$1 : "restricted";
			process.stderr.write(source_default.dim("   ╰ ") + "mode: " + source_default.cyan(mode) + "\n");
		} catch (error) {
			spinner.stop();
			throw error;
		}
	}
});
const tagsCommand = import_cjs$1.command({
	name: "tags",
	description: "Update the tags of a sandbox. Replaces all existing tags with the provided tags.",
	args: {
		sandbox: import_cjs$1.positional({
			type: sandboxName,
			description: "Sandbox name to update"
		}),
		tags: import_cjs$1.multioption({
			long: "tag",
			short: "t",
			type: ObjectFromKeyValue,
			description: "Key-value tags to set (e.g. --tag env=staging). Omit to clear all tags."
		}),
		scope
	},
	async handler({ scope: { token: token$1, team: team$1, project: project$1 }, sandbox: name, tags }) {
		const sandbox = await sandboxClient.get({
			name,
			projectId: project$1,
			teamId: team$1,
			token: token$1
		});
		const tagsObj = Object.keys(tags).length > 0 ? tags : {};
		const spinner = ora("Updating sandbox tags...").start();
		try {
			await sandbox.update({ tags: tagsObj });
			spinner.stop();
			process.stderr.write("✅ Tags updated for sandbox " + source_default.cyan(name) + "\n");
			const entries$1 = Object.entries(tagsObj);
			if (entries$1.length === 0) process.stderr.write(source_default.dim("   ╰ ") + "all tags cleared\n");
			else for (let i = 0; i < entries$1.length; i++) {
				const [k, v] = entries$1[i];
				const prefix$1 = i === entries$1.length - 1 ? source_default.dim("   ╰ ") : source_default.dim("   │ ");
				process.stderr.write(prefix$1 + source_default.cyan(k) + "=" + source_default.cyan(v) + "\n");
			}
		} catch (error) {
			spinner.stop();
			throw error;
		}
	}
});
function formatKeepLastSnapshots(keepLastSnapshots$1) {
	if (!keepLastSnapshots$1) return "-";
	const parts = [`count=${keepLastSnapshots$1.count}`];
	if (keepLastSnapshots$1.expiration !== void 0) parts.push(`for=${keepLastSnapshots$1.expiration === 0 ? "none" : (0, import_ms.default)(keepLastSnapshots$1.expiration, { long: true })}`);
	if (keepLastSnapshots$1.deleteEvicted !== void 0) parts.push(`delete-evicted-snapshots=${keepLastSnapshots$1.deleteEvicted}`);
	return parts.join(", ");
}
function formatPorts(sandbox) {
	const ports = getPublishedRoutes(sandbox)?.map((route) => route.port) ?? [];
	if (ports.length === 0) return "-";
	return ports.join(", ");
}
function getPublishedRoutes(sandbox) {
	return sandbox.routes.filter((route) => route.port !== sandbox.interactivePort);
}
const config = import_cjs$1.subcommands({
	name: "config",
	description: "View and update sandbox configuration",
	cmds: {
		list: listCommand,
		vcpus: vcpusCommand,
		timeout: timeoutCommand,
		persistent: persistentCommand,
		"network-policy": networkPolicyCommand,
		"snapshot-expiration": snapshotExpirationCommand,
		"keep-last-snapshots": keepLastSnapshotsCommand,
		"keep-last-snapshots-for": keepLastSnapshotsForCommand,
		"delete-evicted-snapshots": deleteEvictedSnapshotsCommand,
		"current-snapshot": currentSnapshotCommand,
		ports: portsCommand,
		tags: tagsCommand
	}
});

//#endregion
//#region src/app.ts
var import_cjs = require_cjs();
const app = (opts) => (0, import_cjs.subcommands)({
	name: opts?.appName ?? "sandbox",
	description: "Interfacing with Vercel Sandbox",
	version,
	cmds: {
		list,
		create,
		fork,
		config,
		copy: cp,
		exec,
		connect,
		stop,
		remove,
		run,
		snapshot,
		snapshots,
		sessions,
		...!opts?.withoutAuth && {
			login,
			logout
		}
	},
	examples: [
		{
			description: "Create a sandbox and start a shell",
			command: "sandbox create --connect"
		},
		{
			description: "Run a command in a new sandbox",
			command: `sandbox run -- node -e "console.log('hello')"`
		},
		{
			description: "Execute command in an existing sandbox",
			command: `sandbox exec <name> -- npm test`
		}
	]
});

//#endregion
export { source_exports as a, init_source as i, printTopLevelError as n, require_cjs as r, app as t };
//# sourceMappingURL=app-CsBNEDNb.mjs.map